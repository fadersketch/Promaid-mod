# Promaid 发布指南

发布所需文件都已准备好,放在本仓库目录里。最终发布 jar:
`你的本机 `maidmods\patched\promaid-1.0.0.jar`(SHA256 `3c39b2b9...`)。

副本核对清单(每个文件已就绪):
- `README.md` —— GitHub 仓库首页 + Modrinth 项目正文
- `CHANGELOG.md` —— 242 条更新日志(发布说明可引用)
- `LICENSE` —— MIT(也已打进 jar 根目录)
- `promaid_src/` —— 完整可编译源码(含便携构建脚本)

发布前建议把 `promaid-1.0.0.jar` 复制一份到本仓库目录,并重命名为
`Promaid-1.0.0.jar`(与游戏 mods 里的文件名一致),方便上传时找。

---

## 一、GitHub 发布

### 1. 创建仓库(网页操作)
1. 登录 https://github.com → 右上角 **+** → **New repository**。
2. 填:
   - **Repository name**:`Promaid-mod`(必须小写,与 mods.toml 的 displayURL 一致)
   - **Description**:`更智能的车万女仆——TLM 1.20.1 Forge 扩展模组:自主建造 / LLM AI / 长期记忆 / 战斗自保 / 自动装备`
   - **Public**(公开,开源必须)
   - **Add a README**:不要勾选(我会给你填好内容的 README)
   - **.gitignore**:选 **None**(我已写好 .gitignore)
   - **License**:选 **MIT License**(不要勾选,我会给你填好带署名的 LICENSE)
3. 点 **Create repository**。

### 2. 上传文件(网页方式,最简单)
创建仓库后的空仓库页,有两种方式任选:
- **A. 直接拖拽上传**:点 **uploading an existing file** 链接,把以下文件拖进页面:
  - `README.md`、`CHANGELOG.md`、`.gitignore`、`LICENSE`、`promaid-1.0.0.jar`(发布用的最终 jar)
  - `promaid_src` 文件夹:网页上传不支持整文件夹,需要先把它压缩成 zip,上传后网页上解压再删除 zip(麻烦)
- **B. 用 Git 命令推(推荐,能一次带上整个源码树)**:
  在电脑上操作(需要已安装 Git)。打开命令行,执行:

  ```
  cd /d C:\Users\Sketch\.zcode\workspace\default\promaid-mod
  git init
  git add .
  git commit -m "Promaid v1.0.0 初始发布"
  git branch -M main
  git remote add origin https://github.com/fadersketch/Promaid-mod.git
  git push -u origin main
  ```

  推之前,记得把发布用 jar `promaid-1.0.0.jar` 复制进 `promaid-mod` 目录。推送时若提示登录,网页登录即可(会缓存凭据)。
  `.gitignore` 已经排除了 `*.jar`,所以要在推送后单独上传 jar(release 附件),或临时改 .gitignore 放开 jar。

### 3. 发布 v1.0.0 Release(附带下载链接)
1. 在仓库首页点 **Releases** → **Create a new release**。
2. 填:
   - **Choose a tag**:输入 `v1.0.0` → 点 **Create new tag on publish**(如果已 push tag 也可选)
   - **Target**:`main`
   - **Release title**:`v1.0.0`
   - **内容**(可粘贴):
     ```
     Promaid v1.0.0 —— 更智能的车万女仆(基于 TLM 1.5.3 / Forge 1.20.1 / Minecraft 1.20.1)

     主要功能:自主建造(40+ 内置蓝图 / 工头 / 全局进度 / 区块冻结)、LLM AI 对话(20+ 工具 / 主动对话 / 长期记忆)、战斗自保(自动回血 / 搭方块避险 / 水桶垫水 / 岩浆逃生 / 自动装备武器盾牌 / 主人死亡传送)、任务(挖矿 / 整理 / 烹饪 / 酿造 / 钓鱼 / 喂食)。

     前置:须先安装 Touhou Little Maid 1.5.3。开源 MIT。详见 README。
     ```
   - 上传 jar:`promaid-1.0.0.jar`
3. 点 **Publish release**。

---

## 二、Modrinth 发布

Modrinth 是 AI 生成内容政策最为宽松的主要模组平台之一,可发布 MIT 开源 + 仅发布 jar、源码指向 GitHub。

### 1. 创建项目
1. 注册/登录 https://modrinth.com(可用 qwertyuiopqaz_0930@qq.com 注册)。
2. 右上点头像 → **Dashboard** → **Create project**。
3. 填:
   - **Title**:`Promaid (更智能的车万女仆)`
   - **Slug**:`promaid`
   - **Description**(一句话,显示在卡片上):
     `车万女仆 TLM 的 Forge 扩展:自主建造 / LLM AI 对话 / 长期记忆 / 战斗自保 / 自动装备。基于 TLM 1.5.3,Forge 1.20.1。`
   - **License**:选 **MIT** → 填作者 `fadersketch` + 填 LICENSE URL(`https://github.com/fadersketch/Promaid-mod/blob/main/LICENSE`)
   - **Client / Server**:都勾上(SIDE: both)
   - **Loaders**:勾 **Forge**
   - **Versions / game versions**:勾 **1.20.1**
9. **Categories**:勾 `Library`、`Technology`、`Vanilla-based`、`Miscellaneous`(按需)
   - **Source link**:`https://github.com/fadersketch/Promaid-mod`

### 2. 填正文(复制 README.md 内容)
把 `README.md` 全文粘贴进项目描述区。README 里的标题和分组标题会正常显示。

### 3. 上传版本
项目创建后进入 **Versions** → **Create new version**:
- **Version number**:`1.0.0`
- **Game versions**:勾 `1.20.1`
- **Loaders**:勾 `Forge`
- **File**:上传 `promaid-1.0.0.jar`
  - **File name**:`promaid-1.0.0.jar`(与文件内 mods.toml 的 version 一致)
  - **Release type**:`release`
- **Dependencies**:添加 **Touhou Little Maid (TLM)** 作为 **required**(需要时,搜索 `touhou little maid` 或按 slug `touhou-little-maid`,选择 required 依赖)
- **Changelog**:粘贴简短说明(可引用 CHANGELOG.md 顶部内容)
- **Publish new version**

---

## 三、发布清单(最后核对)

- [ ] 游戏已关闭,jar 已更新为 v1.0.0(SHA256 `3c39b2b9...`)
- [ ] `promaid-1.0.0.jar` 内含 LICENSE、mods.toml 署名 Khragg / 链接 fadersketch
- [ ] GitHub 仓库 `Promaid-mod`(public)已建,push 源码 + LICENSE + README + CHANGELOG
- [ ] GitHub Release `v1.0.0` 已发,jar 已上传
- [ ] Modrinth 项目 `promaid`(Forge / 1.20.1 / MIT)已建,jar 已上传,TLM 已设为 required
- [ ] README 正文已在 Modrinth 描述区

## 备注:关于 mods.toml 里的署名
- `authors="Khragg"` = 模组作者署名(假名/网名),由你在游戏内显示。
- `displayURL` / `issueTrackerURL` 指向你的 GitHub 账号 `fadersketch`,用于玩家反馈。
- 两个名字可不同,互不影响,均已配置正确。
  若想统一,把 mods.toml 的 `authors` 与 README/LICENSE 的版权名改成同一个即可(改后需重新打包)。
