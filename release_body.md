# Promaid v1.2.3（1.20.1 Forge · 加急更新）

开发者不玩多人 / 没做服务端优化 / 请理解。

- **Minecraft 1.20.1 · Forge 47.4.x**
- **前置**：车万女仆 Touhou Little Maid ≥ 1.5.0（实测 1.5.3）——**纯独立前置，不对任何其他模组做专属联动**
- **附件**：`promaid-1.2.3.jar`（直接放入 mods 文件夹）
- **升级提示**：安装前请**删除 mods 目录里的旧 `promaid-1.2.2.jar` / `promaid-1.2.1.jar` / `promaid-1.2.0.jar` / `promaid-1.1.x.jar`**——两个 jar 的 modId 相同，同时加载会导致启动失败
- 旧存档兼容：物品 / 任务 / 蓝图 / 记忆 / 排班等持久化数据沿用 `maid_smart` 命名空间，v1.0.4 / v1.1.x / v1.2.x 存档无缝升级
- **其他模组武器天然可用**：武器判定基于原版攻击力属性，tacz / 卓越前线 / 拔刀剑 / 史诗战斗 / 万法皆通等模组武器照常参与自动装备与伤害计算，无需专门适配

---

## ⚠ 首先声明：这是一次加急更新

**B 站用户 lawte 反馈**：安装本模组后，女仆**由好感度升阶带来的血量上限加成**、以及**身上穿着的装备**，会因为**收放魂符被卡掉**。同一个问题在 GitHub **issue #18** 里 **P1aneZ** 也报了（「魂符放出后从女仆背包/装备栏取物会丢失」，并附了逐条复现与字节码证据）。

这是会**吃掉玩家存档里实打实物品**的严重 bug，所以这次不按原来的节奏攒版本，**单独发一次加急更新**；同时把**本来准备上线的一批新功能一起带上**（见下文第二节）。

**两个症状是同一个出处，而且不在 TLM、也不在服务端**——她存档里一直是好的（TLM 收放魂符就是「拍快照 → 放出来时读回」，血量上限与装备都无损）。坏在**本模组自己的「入世界自动补包」**（v1.2.2 里为了治「客户端看不到她」而加的）：

1. **血量上限掉回 20**：那个补包收到「生成实体」包后会 new 出一只**全新的客户端实体**，而它那一串补包**偏偏没补属性表**（原版只在「开始追踪」那一枪下发属性表）。于是客户端算出来的血量上限永远是默认的 20；等好感度升阶让服务端属性变脏，原版又把属性表补回来 → 就是你看到的「**80 和 20 反复横跳**」。
2. **拖装备被吞**：**收放魂符的「放出」本身就是一次入世界**，于是每收放一次，客户端那只女仆就被**销毁重建**一次；而 TLM 的容器槽位绑的是**建容器那一刻那只实体**的句柄——删了再生成，你正开着的物品栏/装备栏还指着**已经被删掉的那只**，拖进去的东西就落进了虚空。

**修复**（实测五百九十五 / 五百九十六，两版本同步）：补包改成**按实体网络 id 分流**——魂符放出的**新实体不再补包**（原版追踪本来就会发全套：生成 + 数据 + 属性 + 装备），只有**同一只女仆离场又回来**（区块卸载重载这类、id 没变）才补；补包那一支同时补发一次饰品全量同步；另外新增一条诊断日志：她的装备槽**从有东西变空**时会写一行（`logs/promaid.log` 搜「装备离开」，含槽位、丢了什么、她背包实时快照与 8 帧调用栈）——以后真再遇到「谁把她的装备拿走了」，一行日志就能指认。

> **为什么要单独发一版**：这两条修复在 **09-20 就已经提交**（实测五百九十五 / 五百九十六），但 **v1.2.2 的 release 附件上传于 09-20 01:20 UTC，比它们早约 12 小时**——也就是说**从 release 页面下到 v1.2.2 的玩家仍然会踩到这个 bug**（issue #18 的作者用字节码核对过：那份附件里连 `MaidGiveBack` 类都没有）。所以这一版的第一优先级，就是把它真正交到玩家手里。

**顺带修掉的同类问题**：**issue #17**（女仆着火时会把背包里的金苹果**连着吃到背包空**——那条冷却对「会被消耗的普通食物」从来没生效过）。现在金苹果/附魔金苹果吃完会按**它自己给的效果时长**记一次冷却（普通 = 吸收 2 分钟，附魔 = 抗性/抗火 5 分钟），一次一颗、吸收耗尽才会再吃；低血回血那两条路共用同一条冷却。

---

## 这一版和上一版（v1.2.2）的区别，一句话

**「先把会吃玩家东西的 bug 发出去，再把攒下的新功能一起上线」**：收放魂符不再丢血量上限与装备、着火不再连吃金苹果；同时带来**战斗模式分类表**（逐任务点名近战/远程/不参与）、**压缩盒**（5 格 × 每格 114514，放进她背包就是她背包的延伸）、**空袭轰炸**（末地水晶 / 重生锚 / 床 / 投掷 TNT）、**飞行跟随**（主人飞走了她背鞘翅追过来）四块新玩法，以及**散步速度终于调得动**、**索敌不再隔着方块记仇**、**战斗中不再被工作范围传送回来**等等一批打磨。

---

## 一、加急修复（本版重点，详见开头）

| 问题 | 现象 | 修复批次 |
|---|---|---|
| **收放魂符后血量上限掉回 20** | 好感升阶给的 80 血上限在收放魂符后来回横跳 | 实测五百九十五 |
| **收放魂符后拖装备被吞** | 从她背包/装备栏往外拖，第一件直接消失 | 实测五百九十六 |
| **着火连吃金苹果**（issue #17） | 一着火就把一整组金苹果几秒内吃光 | 实测六百一十九 |

另外新增**「装备离开」诊断日志**（她的装备槽从有东西变空时记一行，含调用栈），以后同类问题可直接定位。

## 二、新功能（本版一起上线）

- **战斗模式分类表**（新配置 `combat.taskModes`）：入战选任务与战中换战术原本三条规则全是写死的——谁能进候选池、**算近战还是远程靠猜**（UID 关键词与命名空间，未知模组任务一律当近战兜底）、池里有模组专属任务时**原版五件套整体让位**。现在可以**逐任务点名**：一行一条 `任务UID=近战/远程/不参与`（认中文写法与逗号分隔）——写 `近战`/`远程` = **分类以表为准，且不再被"模组优先让位"挤掉**；写 `不参与` = **入战选任务与战中换战术两条路都永不选它**；**留空 = 与旧版一字不差**。不想用「模组优先」就关掉新开关 `combat.vanillaYieldToMod`（默认开 = 旧行为）。查 UID：只读命令 **`/maid_smart combat modes`** 会把当前所有攻击类任务列成一张表（内置算什么 ｜ 现在算什么 ｜ 表里写了什么 ｜ 参不参与）。面板位置「战斗与自保 → 主动参战」；
- **压缩盒**（新道具）：**只有 5 格，但每格能堆 114514 个**，合成 = 4 个樱花木板放四角 + 4 个钻石放四边中点 + 1 个粉色染料放正中。**放进女仆背包就等于是她背包的延伸**——她会从盒子里取材料（挖矿/建造/烧制的料），**也会吃盒子里的食物**；界面是**箱子式鼠标取放**（左键拿放、右键拿一半、Shift 快速移动），背包格照原版口径显示数量与耐久条，带**常驻附魔光效**。**禁入清单**：压缩盒本身、**带附魔的物品**（附魔书按物品类型判——1.20.1 与 1.21.1 的附魔存储不一样）、以及你在配置里自己加的清单；一个判据**四条路共用**（界面点击 / 服务端搬运 / 女仆背包插入 / 溢出回退），拒绝时**会给话**，而且在这个界面里**鼠标也选不中压缩盒**。自检与诊断：`/maid_smart box check [女仆]`、`/maid_smart box <女仆>`；
- **空袭轰炸**：近战空袭打完那一记、远程空袭开火之后**顺手放炸弹**——四条链路各走各的，材料齐的几段在同一次攻击里都放：**末地水晶**（黑曜石底座 + 水晶）、**重生锚**（要 1 颗**萤石**方块，不是萤石粉）、**床**（只在原版会爆炸的维度）、**投掷 TNT**（挂在攻击链路最末段，**所有有战斗标签的模式**都会扔；TNT 判据很宽：注册名里带 tnt 的都算、方块**继承原版 `TntBlock`** 的也算，放出去的就是**它自己那一枚**；点火料 = 打火石（按类认、走耐久）或烈焰弹）。**默认全部保护**：不破坏方块（**模组 TNT 也归这条管**）、不伤主人与友军、爆炸火焰改**粉色火焰**（不蔓延、会自己灭、**玩家与女仆都不被它点燃掉血**）；她做这些事时**副手会举起正在用的那一件**（萤石 / 打火石）。目标悬空时走「后门」照样放得下；带着材料却没放，日志搜「轰炸跳过」会写原因；
- **飞行跟随**（**默认关**，观赏玩法）：你自己飞走了，她也能**背上鞘翅追过来**——触发与「搭路」同一档，**起手 25 格 / 收手 5 格是两个不同的球**（3~128 可调，中间 20 格迟滞），进到收手半径会**解除推进矢量**再交回普通跟随；燃料可以是**烟花 / 孔雀羽扇 / 能上天的位移类法术**（法术优先、不烧燃料）；两个空袭任务**未接敌**时同样适用；`/maid_smart flyfollow` 是调试入口，`elytra_goto <x> <y> <z>` 能让她飞去指定坐标（这一件取自粉丝 **Roderick32** 的「鞘翅赶路」分支）；
- **散步速度终于调得动**：默认倍率 **0.7 → 0.4**（老存档会自动迁移一次），可调下限 **0.3 → 0.05**。实测对照（专用服务器量的，玩家走路 4.32 格/秒、跑步 5.61）：0.3 → 1.9、**0.4 → 3.3**、0.5 → 4.9、0.7 → 8、1.0 → 14；注意 0.2 以下实测几乎不走，好用的慢档是 0.3~0.4。现场量速与调档：**`/maid_smart stroll check`**（打她的基础移速、实测对照表与散步门禁）、**`/maid_smart stroll go`**（用当前倍率走一次 24 格直线）、**`/maid_smart stroll speed <值>`**（当场改并落盘）；
- **索敌两处调整**：① **隔着方块的怪不再算威胁**——以前她会"感知"到地下空洞里、墙那头的怪并写进攻击目标，找不到入口就**原地打转**（根因是原版对"已经是攻击目标"的实体**跳过视线判定**，下游所有链路都当它看得见）；现在威胁候选加了一道视线门，"隔墙挥刀"也从源头断了。② **战斗时工作范围临时扩圈**——home 模式（排班/在家干活）下她接战后追怪，一跑出工作范围就会被 TLM **直接传送回工位**，"追出去 → 传回来"打不完；现在接战期间范围临时扩大（`combatWorkRange`，默认 15 格，0 = 关闭），**战斗结束自动落回常态**，且取的是"常态与战斗值里更大的那个"，不会把你调大的活动半径改小；
- **副手动作表现**（推广到全链路）：放置 / 充能 / 投掷 / 起爆，以及搭路 / 插火把 / 建造 / 种植 / 酿造 / 喂主人时，副手短暂举起她正在用的那一件（方块 / 萤石 / 打火石 / 树苗 / 酿造材料 / 食物）——看得出她在干什么。

## 三、其它修复与调整

- **法术自身冷却被多乘了 20 倍**（issue #16）：ISS 的 `getSpellCooldown()` 返回的本来就是 tick，旧版又乘了一次 20（烈焰冲锋真实 10 秒被写成了 200 秒）——现在原样返回；
- **图纸相关三条**（issue #12 / #14 / #15）：墙上告示牌/火把永远报缺料、流体保留判据可配置、缺料同类宽松按「建筑严格 / 机器放宽」分档；
- **回收方块"整体还她"**：萤石不再变成萤石粉；
- **「明明拿着孔雀羽扇却报缺飞行道具」的假提示**：本模组自己的副手动作把那件借走了，判定没算上；
- **「坐下还在搭路」**：坐姿（被玩家叫坐下/坐在椅子上）时不再启动搭路，搭到一半被坐下也会立即让位；
- **无主女仆全面降级**：不是你的女仆，promaid 一概不碰（整合包自己的仇恨/农田规则接管）；
- **面板的「字符串输入行」一直没画标签与说明**（用户截图反馈）：渲染链漏了一条分支，字符串类配置行只画一个空黑框——新加的「模式分类表」因此整页只剩一个黑框；顺带把既有的六条字符串行（API 地址/密钥/模型、导入路径、法术表、危险方块表、禁入清单）标签一起修好；
- **空袭俯冲段冲刺加速** 与 **俯冲段烟花力度 ×1.4**、**粉色火焰的"会自己灭"两层兜底**、**重生锚不再被末地水晶吞**、**爆炸特效对齐原版**等一批打磨。

## 四、本版的调整（行为变化，装之前看一眼）

- **散步默认速度变慢**：空闲散步的倍率默认从 0.7 改成 **0.4**（老存档只在"还停在旧默认 0.7"时自动迁移一次，你写回 0.7 随你）——如果你习惯了她快步走，改「空闲与流畅 → 散步速度倍率」；
- **排班表「全员在家」整条移除**：列表页那个批量按钮（一键把全队留在家不跟随）按玩家裁定删掉，它的计数逻辑与对应的网络包一起移除。**单只女仆的在家开关照旧**（详情页），「自动在家模式」也不受影响；
- **压缩盒默认不许放"带附魔的物品"**（附魔书/附魔武器等），也**不许把压缩盒放进压缩盒**——想放开附魔那条可以关「系统与杂项 → 压缩盒 → 禁入带附魔的物品」；
- **轰炸默认不动地形、不伤主人与友军**（两个开关都在「空袭数值 → ⑦ 空袭轰炸」），想按原版炸坑就打开；
- **战斗模式分类表留空 = 与旧版一字不差**；「模组任务优先让位」默认仍然开着（关掉才是"原版与模组同池纯按权重随机"）；
- **接战时工作范围会临时扩圈**（默认 15 格，与默认工作范围 12 格同量级）——不想让她跑出去追就把 `combatWorkRange` 设成 0。

## 五、手册与 README 也一起更新了

- **内置手册**：「这是什么」章的版本表述改成**两版本对齐**，并补上 **v1.2.2 起 / v1.2.3 起两段新增功能速览**；「战斗与自保」章新增一条 **【收放魂符后血量上限掉回 20 / 拖她的装备被吞？】**（症状 / 根因 / 改法 / 诊断入口）；「专业术语」章补 7 条新词；
- **仓库 README**：新增「v1.2.3 加急修复」一节（含 issue 链接）与**空袭轰炸、压缩盒、飞行跟随、战斗模式分类表、散步速度**等内容，章节数与面板小类数一并校准。

## 主要功能一览

**空袭（近战/远程两种飞行攻击模式 + 空袭轰炸）** · **战斗模式分类表** · **压缩盒** · **排班表**（全女仆日程调度：早/晚/全天班次、6 段任务编排、当前时段高亮、一键集合、自动在家模式） · **建造系统**（81 套内置蓝图 + 外部图纸/LLM 生成/AI 设计器、工地线框投影与 Z 键旋转、工头模式、进度持久化、缺料自动替代、区块内箱子取料、机械蓝图） · **挖矿**（矿表白名单、连锁采集、透视感知、发呆看门狗） · **伐木**（模组原木自动识别、随手补种、骨粉催熟） · **烧制 / 酿造**（什么能烧就烧、多女仆多炉、药剂手册批量/定向酿造） · **农场**（连锁收获/批量种植、耕地标记制、锄地索敌、作物骨粉） · **宰杀任务** · **战斗与自保**（回血阶梯、逃生手段、保命道具优先、致死自动回魂符、女仆自动复活、环境伤害软处理、主动切换战斗+自动还原） · **激流三叉戟旋转冲击** · **主人免伤三层防护** · **被动技能**（落地水/落地雪、岩浆放水、主人死亡传送、搭路、**飞行跟随**） · **单兵战术** · **贴身辅助**（自动投喂/治疗/盾牌，喂食喂水走物品自己的路径） · **武器与工具自动切换**（DPS 评分，兼容其他模组武器） · **指标石** · **AI 记忆**（段落/关系/画像/工作笔记 + 日/3日/周/月记忆日记 + 人格种子 + 睡一觉整理） · **主动对话 / 工作播报 / 自主决策 / 回复反馈学习** · **感知与情绪**（PAD 模型） · **21 个 AI 工具**（对话直接下命令） · **TTS 语音**（内置日语语音包 / 系统朗读 / 缓存 / 音量） · **冷却可视化 HUD** · **床铺互通** · **交互**（摸摸头/抱抱、蛋糕互动、女仆互助、手持光源、宠物保护） · **女仆安全与区块保载** · **运行日志** · 内置 **33 章手册** · 全部功能游戏内配置面板逐项开关（**7 大类 / 31 小类**）

## 测试环境声明

| 模组 | 关系 | 测试通过的版本 |
|---|---|---|
| Touhou Little Maid（车万女仆） | 必装前置 | 1.5.3（Forge 1.20.1） |
| 车万女仆：万法皆通（touhou_little_maid_spell） | 软联动（法术兼容，非必需） | 1.8.2（Forge 1.20.1） |

> 其他版本（含更新/更旧的 TLM）未必兼容，请以实际测试为准。除上述前置与法术软联动外，不对其他模组声明联动兼容性。

## 反馈

问题与建议请到 GitHub Issues：https://github.com/fadersketch/Promaid-mod/issues
（收放魂符那条对应 issue #18，着火连吃金苹果对应 issue #17，两个 issue 里的复现步骤都很完整，这次一并修掉了。）

## 许可证

MIT © 2026 —— 开源地址：https://github.com/fadersketch/Promaid-mod
