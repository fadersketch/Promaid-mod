# Promaid v1.1.0（1.20.1 Forge 正式版）

- **Minecraft 1.20.1 · Forge 47.4.x**
- **前置**：车万女仆 Touhou Little Maid ≥ 1.5.0（实测 1.5.3）——**纯独立前置，不再依赖任何其他模组**
- **附件**：`promaid-1.1.0.jar`（直接放入 mods 文件夹）
- 旧存档兼容：物品 / 任务 / 蓝图 / 记忆等持久化数据沿用 maid_smart 命名空间，v1.0.4 存档无缝升级

## 本次更新

### 🛡️ 女仆绝不伤害主人（三层防护，重点修复）
粉丝反馈：带女仆到雪地，空闲模式打雪仗后女仆会对主人跳劈，脱甲约 4 心，主手武器越强越疼。
- **根因**：TLM 原版雪地打雪仗行为会把**主人**写进女仆 brain 的 `ATTACK_TARGET`（雪球目标），promaid 的单兵战术行为读到目标就接管走位+跳劈，打出真实近战暴击伤害；
- **修复**：① 伤害事件总闸——女仆造成的主人/友方伤害在攻击/受伤/结算三处直接取消；② 全部攻击注入点过滤（战术跳劈、近战横扫、中立威胁反击、自保反击含弹幕/射箭、LLM 攻击工具）；③ 定期清除仇恨——每 2 秒把主人/友方从 `ATTACK_TARGET`、实体目标、复仇目标里清掉；
- 结果：雪地打雪仗只会扔雪球，**绝不会对主人挥刀**；同主人的女仆之间也互不误伤。

### 🧹 彻底移除第三方软联动（纯 TLM 独立版）
按作者口径，把对特定第三方模组的**专属联动彻底删干净**（保留按攻击力属性识别的通用武器逻辑）：
- **爱憎分明**：删除饥饿门控 mixin、4 个联动配置项与配置面板「爱憎分明模组调试」整页、自保极端饥饿反射保命、相关语言键；
- **心契誓约（maidmarriage / Heartfelt）**：删除拥抱/子女交互与调试面板 mixin、关系标签反射、纪念日情绪脉冲与联动配置项、建造心契交互锁；
- **超越维度 + 精妙储存**：删除整个 storage 包（绑定表/取物 handler）、`storage_bind_card`/`storage_unbind_card` 两个物品及全部调用点（建造材料统计与取物、酿药取物回退），含模型/贴图/配方/语言；
- **配置项清理**：`MEMORY_RELATIONSHIP_ADAPTER` / `MEMORY_RELATION_SCAN` / `MEMORY_TRUST_DELTA` / `MEMORY_HEARTFELT_ANNIVERSARY` 及界面行、`callresponse` 语言键全部移除；
- **保留**：按攻击力属性的通用武器判定/自动装备（tacz/卓越前线/拔刀剑/万法皆通等模组武器天然参与，无需专属代码）。

> 移除后：本模组为**纯 TLM 独立版**，只要求车万女仆前置；此前装的第三方联动模组与本模组不再有任何代码交互，对应联动功能（爱憎饥饿保命、心契交互锁、终端绑定卡、关系记忆标签等）随之下线。

### ✨ v1.1.0 正式版内容（相对 v1.0.4）
- **女仆自动回魂符**（全新）：受到致命伤害瞬间自动收符，主人从魂符重新放出即可；释放后冷却从释放时刻起算（默认 60 秒可调），收符提示带当前 CD 秒数；
- **排班贴身气泡**（全新）：靠近排班中的女仆随机冒出 50 条贴身对话（含打工人毒鸡汤），30 秒/只冷却，战斗/自保/骑乘豁免；
- **自保机制大改**：环境伤害改为软处理（脱离危险源后原地回血，不再满地图乱跑），卡方块先挣脱、实在不行才传送且有冷却；
- **搭路改为被动技能**：弓兵/近战模式周围无敌人时主人拉远也会铺路靠近，一旦接战立即停搭转入战斗；
- **排班表·整页任务选择器 + 当前时段整行高亮**；
- **专属服务器兼容修复 ×2**（粉丝反馈）：主类内联配置界面 lambda 触发 `RuntimeDistCleaner` 拦截 → 移入客户端专类；主动对话调用 TLM 客户端专用方法 `ChatClientInfo.fromMaid` → 改为服务端安全构造。

### 🐛 修复的 Bug
- 收魂符冷却卡死 / 回魂符误触发（低血也收符）/ 收符后冷却残留；
- 排班表任务选择面板遮挡、需狂点切换、搭路方块回收散落；
- 施肥吞工具、种苗长不成、烧制吞附魔物品、排班表改名不显示、排班区块持久化加载等。

## 主要功能一览

排班表 · 自主战斗/自保 · 搭路 · 伐木 · 挖矿/农场增强 · 烧制 · 宰杀 · 药剂手册 · 蓝图建造（手册/工头/HUD/缺料替代/机械蓝图）· LLM 对话与长期记忆 · 主动对话/情绪 · 回魂符 · 摸摸头/抱抱 · TTS 语音 · 内置 25 章手册 · 全部功能游戏内配置面板逐项开关

完整逐条更新日志见仓库 `CHANGELOG.md`。

## 测试环境声明

| 模组 | 关系 | 测试通过的版本 |
|---|---|---|
| Touhou Little Maid（车万女仆） | 必装前置 | 1.5.3（Forge 1.20.1） |

> 其他版本（含更新/更旧的 TLM）未必兼容，请以实际测试为准。第三方软联动已移除，不再声明联动模组兼容性。

## 反馈

问题与建议请到 GitHub Issues：https://github.com/fadersketch/Promaid-mod/issues

## 许可证

MIT © 2026 —— 开源地址：https://github.com/fadersketch/Promaid-mod
