# Promaid v1.2.1（1.20.1 Forge 正式版）

- **Minecraft 1.20.1 · Forge 47.4.x**
- **前置**：车万女仆 Touhou Little Maid ≥ 1.5.0（实测 1.5.3）——**纯独立前置，不对任何其他模组做专属联动**
- **附件**：`promaid-1.2.1.jar`（直接放入 mods 文件夹）
- **升级提示**：安装前请**删除 mods 目录里的旧 `promaid-1.2.0.jar` / `promaid-1.1.x.jar`**——两个 jar 的 modId 相同，同时加载会导致启动失败
- 旧存档兼容：物品 / 任务 / 蓝图 / 记忆等持久化数据沿用 `maid_smart` 命名空间，v1.0.4 / v1.1.x 存档无缝升级
- **其他模组武器天然可用**：武器判定基于原版攻击力属性，tacz / 卓越前线 / 拔刀剑 / 史诗战斗 / 万法皆通等模组武器照常参与自动装备与伤害计算，无需专门适配

## 本轮补丁（v1.2.1 覆盖更新 · 第十三批：羽扇高度 / 冲刺方向 / 误报修复 / 水质分级）

- **羽扇高度回调**：竖直冲量 0.85 → 1.05（按反馈取"玩家体感的 1.25 倍"）；
- **冲刺法术方向修正**：盘旋时她的视线沿切线，冲刺法术沿视线冲 → 先摆正到"朝目标 + 略抬"再放，
  不再横着飞出去；（起飞时看到的旋转动作是**烈焰冲锋自己的施法动画**，不是激流三叉戟链路——
  那条只认主手三叉戟 + 激流附魔，日志搜「激流突进」可确认）
- **刚放出不再误报缺件**：入世界后 0.5 秒静默 + 位移法术书缓存负结果缩短 + 入世界清缓存
  （法术数据要一拍才初始化，早了会误报一次）；
- **喂水水质分级**：口渴模组四档水质（肮脏/有点脏/可接受的/纯净），新增「喂水最低水质（0-3）」
  默认 2=可接受的——只对装水容器生效；喂水白名单子页里每杯水都会标出它的水质。

## 本轮补丁（v1.2.1 覆盖更新 · 第十二批：1.20.1 法术层修复 / 排班表归位 / 面板溢出 / 羽扇调平）

- **【1.20.1 关键】位移法术终于生效**：法术插件 1.20.1 版与 1.21.1 版有两个方法签名不同，
  旧探针写死 1.21.1 签名 → 探测失败 → 整层（顺带施法 + 位移法术）被关掉。现在两种签名都认，
  可选能力缺失不再拖垮整层——**带烈焰冲锋的书放背包/饰品栏即可平地起飞**；
- **手册空袭传送说明纠错**：让位的是"自动传送"且让位有界（滞空超 15 秒强制拉回）；
  **手动「一键集合」与主人死亡传送照常生效**；
- **「排班表」配置项归位**到「生产与工作」（此前面板挂在移动与行为，与手册不一致）；
- **喂食 / 喂水子页窄窗口溢出修复**（悬停信息收进屏幕、标题与统计行缩短）；
- **羽扇调平**：竖直冲量夹到 0.85 格/tick（旧值低速挥扇可达 2.2，明显比玩家高），并补上挥扇动作。

## 本轮补丁（v1.2.1 覆盖更新 · 第十一批：空袭缺件文案 / 法术进起飞判定 / 手册法术实战指南）

- **缺件提示改了**：不再点名烟花/羽扇，直接说「没有可以飞行的道具」，并尾注三种手段
  （烟花火箭 / 孔雀羽扇 / 位移类法术任一）；
- **飞行法术进入可起飞判定**：以前"能不能起飞"只认烟花/羽扇，法术只是兜底——
  现在统一成"烟花/羽扇可用 或 会提供高度的位移法术"，与模式激活、缺件提示口径三处对齐
  （有烟花仍先走烟花链路，手感不变）；
- **手册新增一节**「空袭状态下的法术怎么用：配装 + 节奏 + 排查」——两类法术各司其职、
  她什么时候放/什么时候刻意不放、不耗法力、举例（只带【升腾】不带烟花也能起飞）、日志怎么对；
- 手册三件套小节、常见问题与 README 同步改成「可以飞行的道具（三选一）」。

## 本轮补丁（v1.2.1 覆盖更新 · 第十批：羽扇修名 / 风爆不再震主人 / 喂水勾选面板 / 罗盘让位 / 魂符模型修复）

- **羽扇终于认了**：注册名少写一个词（`peacock_fan` → `peacock_feather_fan`），此前放哪都报缺羽扇——
  现在背包里放一把就算燃料；
- **主人不再被重锤风爆震飞**：原版风爆的爆炸没有来源实体，专项守卫归因不到女仆，击退仍会发给
  主人的客户端——已补"当前 tick 实体"兜底归因（只震不伤的这条漏网路径彻底堵上）；
- **喂水面板照搬喂食**：改为「打开 →」图形化勾选子页——搜索、图标网格（✔可喂 / ✖不喂，点击切换）、
  翻页、手动填注册名、底名单一键移出；候选按口渴模组自己的口径扫描（含其他模组装水容器）；
- **中键标记与河童的罗盘不再打架**：两者写的是同一份排班锚点（谁后写谁生效）；手持罗盘时中键
  自动让位，面板新增「中键工位标记」开关；
- **【1.20.1】魂符放出后模型不再变默认**：客户端实体重同步误用 packDirty（通常一个值都不发）→
  复活的客户端女仆保持默认模型；已换回 getNonDefaultValues（与 1.21.1 一致）。

## 本轮补丁（v1.2.1 覆盖更新 · 第九批：位移类法术当推进器（PR #11，@rodericksthescriptkid 社区贡献））

- **平地起飞**：没有烟花/羽扇也能上天——带「升腾」等提供高度的位移法术即可（施法前她抬头 62°，
  并清掉法术模组的施法目标以免朝向被掰平）；只带「烈焰冲锋」也顶得起来（沿视线冲刺、垂直分量保留）；
- **空中补高**：爬升相位缺高度时先用位移法术、再走烟花（不消耗燃料、也不占烟花冷却）；
- **飞行加速**：滑翔途中对着目标用「烈焰冲锋」冲刺续速（默认尊重法术自身冷却：烈焰冲锋 10 秒一记，
  她不会比玩家更频繁）；
- **指定法术瞬发**：序列照抄法术模组自己的 /maidspell iron_cast，按她书里铭刻的等级放（等级影响冲量）；
- **配置六件**：起飞/补高开关、飞行加速开关、两张法术表（面板里可自己填 id）、间隔（默认 40 tick）、
  「飞行加速·尊重法术冷却」开关；手册第 ⑧ 段与中英语言文件同步；
- **两树同步**：1.21.1 为粉丝 PR 原实现，1.20.1 由作者按 SRG 移植并双端编译验证。
- 感谢 @rodericksthescriptkid 的贡献！

## 本轮补丁（v1.2.1 覆盖更新 · 第八批：女仆喂水（软联动「口渴」Thirst Was Taken））

- **喂水**：主人口渴值低于阈值（默认 15）时，女仆从自己背包/手持选一杯喂你——结构照搬喂食链，
  判定位点换成口渴值；
- **效果与玩家自己喝完全一致**：代喂走物品自己的"喝完"路径——口渴恢复/纯度结算、玻璃瓶/陶碗等
  容器返还全部原版行为，喝完的容器回到女仆背包；其他 mod 的装水容器（水袋/水壶等）按注册名加进
  白名单即可喂；
- **喂水白名单**：默认水瓶 + 陶碗水；minecraft:potion 只认纯净水、其它药水永不喂；留空 = 不喂；
- **条件注册**：没装「口渴」时，配置面板与 toml 里都不会出现喂水相关项目，零开销。

## 本轮补丁（v1.2.1 覆盖更新 · 第七批：近战空袭起手挥臂 + 起飞前地面补跳）

- **近战空袭有攻击动作了**：挥臂提前到收翅俯冲起手的那一刻——旧版唯一的挥臂在高速俯冲的最末尾，
  视觉上根本捕捉不到；现在整段俯冲带着攻击动作（命中结算那一挥保留，三叉戟自转不受影响）；
- **起飞更稳**：起跳滑翔的跳起窗口内，离地前的每一 tick 都补一跳——旧版第一跳被 AI 走位/贴墙
  吃掉后只会干等窗口耗尽，烟花点不着再从头来，观感就是"有时候起飞困难"；离地后原流程不变。

## 本轮补丁（v1.2.1 覆盖更新 · 第六批：攻击动画补全）

粉丝反馈：重锤+风弹下落攻击"没有攻击的动作，只有效果"；远程空袭动画看不清、不确定有没有。

- **重锤猛击命中瞬间补挥臂**（1.21.1）：旧版猛击全程直接结算伤害，只有砸击粒子与音效、没有出手
  动作——现在命中瞬间挥臂（与地面普通近战同一动画）；
- **远程空袭补齐弓/弩箭/弩烟花开火挥臂**（两树）：旧版只有三叉戟（自转）、御币（弹幕）、
  TACZ 枪械（模组自己的开火动画）有动作，弓和弩全程只见弹道——现在开火瞬间挥臂；
- 排查确认：空袭近战猛击、御币、三叉戟、枪械的动画本来就有，未改动。

## 本轮补丁（v1.2.1 覆盖更新 · 第五批：TACZ 弹药箱兼容）

粉丝反馈："远程空战模式不识别 TACZ 的弹药箱，会提示没有弹药按照普通模式来，但普通模式不会开枪
（因为觉得没有弹药），能做一下兼容吗"。

- **弹药箱也算「有子弹」**：空袭的弹药门禁、自主战斗切枪械的弹药闸、挑枪偏好三处都认 TACZ 弹药箱
  （tacz:ammo_box）——给女仆一箱弹药 + 一把枪，空袭照常激活、普通枪械模式照常开枪；
- **消耗侧零改动**：TACZ 自己的换弹本来就支持弹药箱（对箱认弹、扣箱内计数、打空清空），扫描覆盖
  女仆背包+双手+护甲+饰品——这次只是把我们这边拦在门外的判定放行；
- **判据从严**：箱内弹药型号必须对得上枪（9mm 箱喂不了步枪）、创造弹药箱无限、打空的箱不算，
  不会出现「判定有弹却装填不上」；
- 没装 TACZ 时零开销、行为与旧版完全一致。

## 本轮补丁（v1.2.1 覆盖更新 · 第四批：合入社区 PR ×3）

合入 @rodericksthescriptkid 的三个 PR（全部已同步移植到两个加载器）：

- **PR #9 修 Sable 同装互斥**：装 Sable（机械动力：航空学物理引擎）时，本模组的"全世界范围 AABB 扫描"会被它拒查并静默返回空（1 小时 19 万条报错日志），农场锄地、指标石、自主战斗还原、home 巡逻、险境脱离等一大批功能静默失效——全部改用 `getAllEntities()` 遍历，顺带绕开早期的实体桶 bug（另补了 PR 漏掉的蓝图背包检查一处）；
- **PR #10 修"空袭女仆回不来"**：空中让位（重锤跃起/飞行作战）从无界改为有界——持续空中超过 15 秒且确实该被召回（跨维度/距离超线）就清状态强拉，不再出现"45 分钟每 60 秒一条不传"的永久禁传；
- **PR #10 新增 `/maid_smart resync`**：修"服务端活着、客户端永远没有她"的实体失配（Sable 改追踪位置 + 法术模组离场只删不补的组合链）。OP 命令 + 女仆重新入世界/离场时自动给主人补一次"删+生成+数据+装备"包（纯补包，不改服务端状态）；
- **PR #8 新增 空袭·法术层**（需装《车万女仆：魔法》）：近战/远程空袭用默认武器打的同时，按配置节奏向当前目标顺带施法——法术书放背包或饰品栏即可（不看主手、不占武器位、三件套口径不变）；只挑"本来就该面向目标"的两个相位发起（吟唱会抢朝向），爬升段与收翅俯冲刻意不施法；开关默认开、间隔 20 tick、距离 24 格，没装法术模组时零效果。

社区条目的实测号段已重排（五百六十四~五百六十七）。

## 本轮补丁（v1.2.1 覆盖更新 · 第三批：空袭兼容暮色森林孔雀羽扇）

- **空袭新增一种"飞行燃料"：暮色森林孔雀羽扇**——背包里放一把（不用手持），空袭三件套照常激活；
  **有扇先用扇，没扇才烧烟花**；缺件提示同步为「缺烟花火箭/羽扇」；
- 推进、扇风、扣耐久、使用节奏**全部照搬扇子自己的原版机制**（反编译两版 PeacockFanItem 实证）：
  每秒一挥（原版 getUseDuration=20）、每挥按"扇飞数+1"扣耐久（耐久附魔生效）、
  身前 3 格内的怪物/掉落物/箭矢会被扇开、TF 原版 item.fan.whoosh 音效与云朵粒子；
- 纯软兼容：扇子按注册名识别，不装暮色森林零开销、行为与旧版完全一致。

## 本轮补丁（v1.2.1 覆盖更新 · 第二批：工作区域回归 + 工位标记 + 修不跟随钓鱼）

- **不跟随（home/排班）女仆的"工作区域"回归原版口径**：挖矿/伐木/宰杀的任务选点、散步、巡逻全部
  钳制在工位锚点的限制圈内（与 TLM 原生任务同口径），home 模式下挖矿/伐木的"锚点迁移"停用——
  她不再顺着目标越走越远、收工也不再被远距离传送闪回；干活期间限制圈照常刷新，只免掉出圈硬拽；
- **新增 工位标记（潜行 + 鼠标中键）**：潜行 + 中键一个方块，把身边 32 格内自家在家模式女仆的工作区域
  标到那里（工位/休闲锚点立即生效）——想让她们守农场，潜行+中键农场中心即可；不潜行的中键照旧是
  原版取方块；
- **「排班活动半径」默认 32 → 12**（回归 TLM 原版量级；旧档一次性自动迁移，手动设过 32 的不动），
  想要大范围随时调回；
- **修：不跟随状态不会钓鱼**——home 模式下自动坐垫只在"工作区域内"找水坐，圈外不再生成走不到的
  远水坐垫（改为提示主人把工位标到水边）；跟随模式照旧；
- **修：跟随模式空闲散步整段失效**；巡逻/工作驱动锚点不再"跟着人漂移"。

## 本轮补丁（v1.2.1 覆盖更新，附件已替换为最新构建）

> **已经下载过 1.2.1 的请重新下载附件**（版本号仍是 1.2.1，内容更新了）。本轮加了三条，
> 其中第一条是必须更新的：它会让配置面板一保存就卡死游戏。

- **【务必更新】点「保存并返回」卡死 / 下次进游戏卡在 mod 加载界面**：1.2.0 起在配置事件处理里
  多做了一次写盘（当时为了让默认值迁移落盘），而这个加载器用**文件监听器**盯着 `config/*.toml`：

  ```
  我们写盘 → 文件变了 → 监听器触发 → 再发一次配置重载事件 → 我们又写盘 → ……
  ```

  变成停不下来的写盘风暴。实测（专用服务器，外部把配置改一次）：**20 秒内该文件被重写 38 次**，
  还留下半截的 `promaid-common.new.tmp.toml`。玩家侧表现就是反馈里的两句话——点一次
  「保存并返回」= 一次写盘 → 风暴起来 → 客户端卡死/崩；下次进游戏时加载器要修正旧键、
  同样要写盘 → **卡在 mod 加载界面**，每次重开都重演。

  现在配置事件里**一个字节都不写**（只记录引用），默认值迁移改到服务端启动时跑一次、
  且**只在真的改过值时**才落盘一次；修后同一套复现脚本 20 秒内重写次数 **38 → 0**。
  （1.20.1 侧同一口径收紧，防以后再犯。）

- **友军风免（新开关，默认开）**：女仆放出的**风暴 / 火球 / 风弹**不再把你和同主女仆震开。
  伤害其实早就免了（本模组的免伤总闸 + 法术附属自己的盟友判定），漏的是**击退**——
  原版爆炸的击退、铁魔法「呼啸之风」这类效果都是**直接改速度、根本不走伤害事件**，
  所以旧版会出现"血不掉、人还是飞了"，从高空打下来时主人还会被炸上天。
  现在只对**主人与同主女仆**生效、且只拦**明显的外力位移**（走路/重力/挤开都不算），
  女仆自己的**烟花推进、风弹自起跳**一字不改。
  开关：配置面板「战斗与自保 → 单兵战术 → 友军风免」（`combat.friendlyWindImmune`）。

- **《万法皆通》法术模式联动收紧**：那个附属的「近战法术 / 远程法术」任务，武器判据是
  "什么都能当武器"（源码如此）——以前会把背包里的剑/斧塞进法术模式，也会因为"背包里有任意
  一件模组物品"就参与主动参战。现在**必须她真的随身带着法术装备**（法术书/法器在手上、背包
  或饰品栏）才开放自动换武器与自主参战，且**换也只换法术装备**；没带法术装备时主手一次都不动。


## 本版是 v1.2.0 的修复版（没有新功能：修问题 + 四处行为调整）

v1.2.0 的功能总览（空袭 / 三叉戟 / 指标石 / 投喂 / 排班表 / 建造…）请看上一个版本的说明页；
这一版把它上线后暴露的问题修完了，并回退了建造那两项实测下来不好用的改造（详见下方「本版的调整」）。

## 修掉的重要 bug

- **排班表和游戏时间差 6 小时（"深夜在加班、白天在睡觉"）**：原版 `dayTime = 0` 是**日出 06:00**
  而不是午夜（`/time set midnight` = 18000），而排班表的换算直接把 dayTime 0 当成了 00:00
  → **整张表比游戏挂钟早 6 小时**：按挂钟填的"12:00~16:00"实际跑在傍晚、"06:00~14:00 睡觉"
  实际睡到晚上。现在时钟按**游戏挂钟**重算（与 TLM 显示的游戏时间、F3 一致），班次窗口一并重锚：
  早班 **06:00~18:00**、晚班 **18:00~06:00**（跨午夜，下半段 0:00~6:00）、全天 00:00~24:00，
  正好与 TLM 自家 DAY/NIGHT 作息重合；手册的"当前时段高亮"与调度器共用同一份实现。

- **建造完成后左上角进度 HUD 不消失**：HUD 是客户端拿服务端每秒推的快照画的，而服务端
  在没有进行中计划时旧版**一个包都不发** → 计划一完成/取消，最后一帧（实测卡在 99%）就永久
  挂在左上角。现在：① 计划清空时**补发一次空快照**；② 客户端加 **5 秒看门狗**（超时无推送
  就本地清屏，丢包/服务端异常/切世界都不会再留残影）；
- **进度卡在 99%**：蓝图盖在已有地形上时，"开工那一刻就已是目标方块"的格子旧版只前进游标、
  不计数，于是她把该放的都放完了进度仍差最后一格（截图里那一格）。现在这类格子单独计数并计入
  HUD 与手册进度条（真放上去时从该计数里摘掉，两套计数互斥、不会算两遍）；**完成判定与缺口
  补建口径不变**。

- **模组里"内置鞘翅的装备"（鞘翅胸甲这类）女仆不认**：三件套的鞘翅位旧判据写死成
  `instanceof ElytraItem`（只认原版鞘翅物品），而模组里这类装备是"护甲 + 重写滑翔钩子"，
  于是被判成"没有鞘翅" → 空袭模式激活不了、还一直播报缺鞘翅，**并且会把这件胸甲换下来**
  塞回背包（等于把护甲扒了）。现在判据改为 `ItemStack.canElytraFly(实体)`——与原版滑翔闸门
  `LivingEntity.updateFallFlying` 用的是**同一个方法**（反编译实证：那段字节码里没有任何
  原版物品判定），所以"对玩家能滑翔的装备"女仆一律认得，且胸甲槽已有这类装备时原地不动。
  渲染层仍只给原版鞘翅画我们的翅膀（模组装备自带模型，避免穿模）。缺鞘翅时运行日志会写一行
  「空袭装备 · 缺鞘翅（鞘翅判定: 胸甲=… 原版鞘翅=false，canElytraFly=true/false …）」，
  一眼分清是"我们没认"还是"那个物品没实现滑翔钩子"（后者对玩家同样滑不起来，得找那个模组）。
- 另外两条同源的：**取用优先级**——背包里同时有普通鞘翅和"鞘翅胸甲"这类装备时，优先穿带护甲的
  那件（胸甲槽已穿着任意一件时不换）；**飞坏了会自己换**——鞘翅耐久耗尽消失后，只要背包里还有
  备用鞘翅/这类装备，她下一 tick 就自己补上（空袭任务下"补齐三件套"每 tick 都在跑），
  包里没有备用的才会滑翔落地；每次换装写一行运行日志「空袭装备 · 胸甲槽换装：<新>（原 <旧>）」。

- **被围殴时她不再搭方块自保**——两处原因，都修了：
  ① **"悬空禁搭"把"她自己一跳"也判成了悬空**。v1.2.0 里这道闸口的口径被收紧成"只要正在下落就禁"，
  于是她战斗中的小跳（跳劈、走位跨台阶、被击退）落地前那几 tick 也被判成悬空 → 取料直接被拦 →
  搭高一块都放不下；而这个场景又特意不播报"没有材料"（静默失败），看起来就成了"她就是不搭、也不吭声"。
  现在"悬空"按字面判：**脚下 3 格内没有可站立面**才算悬空——滑翔/飞行空中与深坠落照旧禁，
  浅跳、被击飞、以及搭高自身"放块 → 被顶起 → 落回"全部放行；
  ② **"包里有没有回血资源"判定太窄**：原先只认药水 / 金苹果 / 一小撮硬编码原版食物（熟肉、面包、苹果），
  **金胡萝卜、各类模组料理一律不认** → 包里有食物却被判成"没有" → 被围殴时一块不搭
  （搭高只在她有回血资源时才做，这条门槛保留不动：没得回血爬上去没意义）。
  现在回血资源判定与**投喂系统同一个口径**——TLM 自己的 `isHealMeal` 口径（有食物属性且不在投喂黑名单里），
  任何能吃的东西都算；金苹果保留单独判定。
  另外自保每次决策都会往**运行日志**写一行「自保诊断」——血、距威胁、贴身线、
  认到的回血资源物品名、有没有可搭方块，这类"她怎么不搭"的问题现在一眼就能看出卡在哪。

- **1.21.1：四个配方全都加载不了**（指标石 / 蓝图书 / 药剂手册 / 排班表合不出来）：1.21 把数据包目录名
  统一改成了单数（`recipes`→`recipe`、`tags/items`→`tags/item`），移植时漏改，整个目录在 1.21.1 上不
  会被读取；另三个老配方还在用 1.20.1 的 `result.item` / `result.nbt` 字段（1.21 是 `result.id` /
  `result.components`）。现已按 1.21 规范改齐，1.20.1 侧保持原有的正确写法；
- **手册把「指标石」提为独立章节**（顺带在「功能总览」加了跳转链接）：原来它的用法压在建造章节最后一段，
  没有独立标题也不在总览里，等于藏起来了；现在手册共 **32 章**；

- **装了「是，史蒂夫模型」(YSM) 时黑屏进不去游戏**：TLM 装有 YSM 时会把女仆交给 YSM 的渲染器，并把已有图层逐个复制过去；而我们的鞘翅/旋转特效图层把泛型参数写死成 `GeckoEntityMaidRenderer`，复制时抛 `ClassCastException` → 实体渲染器整批建不起来 → 资源重载被中止 = 黑屏。现在两个图层的参数放宽为公共上界 `IGeoEntityRenderer`，并在非 Gecko 渲染器（即 YSM 时）**不绘制**这两个图层（YSM 模型没有 Gecko 骨骼表，硬画只会把翅膀堆在原点）。**1.20.1 与 1.21.1 同步修复**——装 YSM 的话，鞘翅与激流旋转特效在 YSM 模型上不显示，其余功能不受影响；

- **【最严重】"摔死 → 复活 → 又摔死"的死循环**：算安全落点的函数把**维度总高度（384）与最低建筑高度（-64）写反了**，上下扫描都扫不到地面，兜底落到 `y≈386` 的**天上**（日志坐实：受击类型 `fall`、伤害 86~113、落点 y≈386）。修复后落点**贴地**。同时：复活落点改走原版逻辑（床/重生锚）与你复活时站的那一格对齐；复活后给 2 秒抗性提升 V + 抗火并清负面效果，避免复活瞬间被秒；
- **几个开关"关了又自己打开"**：AI 记忆开关 / LLM 开关 / 建造暂停状态。根因是读布尔值时代码写成了读**字节标签的类型 ID**（恒等于 1），`!= 0` 恒为真——等于这几个开关从来没被真正关掉过；
- **投喂相关**：女仆"背包没吃的"疯狂播报；喂食 / 种树苗不再"先放后扣"——生存模式不能靠搭火把、种树苗白拿方块了；
- **开局赠礼重复给予**：以前每次登录都重发一本手册 + 一本排班表；改用原版进度系统做"只发一次"（老存档静默记账、不补发）；
- **排班表血量百分比公式取反**：满血恰好 100% 所以一直没被发现，掉血后反而显示 133% / 200% / 2000%；
- **自动复活不再"卡住"**：重生点不可用（床被拆 / 重生锚没电 / 维度不允许 / 从没设过）时直接在**主人所在位置**复活（强制生效、不看地形）；原先那个"复活后很快又死就把下次复活推迟 1 分钟起、逐次翻倍"的退避机制**整段删除**——它的副作用比它防的问题更严重。提示语也按实际落点分成两句；
- **空袭装备不齐的提示**只在遇到敌人时才报 → 改为**切到任务就检查**；
- **支援语音"没声"**：不是缺语音包，而是被 8 秒播报间隔吞掉（内置语音最小间隔默认 8 秒 → 5 秒）；
- **1.21.1 资源包版本号**写成了 1.20.1 的值（15 → 34），客户端不再报"该资源包为其它版本制作"；
- **配置迁移钩子长期静默失效**：Forge 把 MOD 总线事件注册到了主总线，导致此前累积的配置迁移全都没生效（穿透预算、看门狗时长、搭路距离等）；已修好并会把旧默认值自动迁移、写回磁盘。

### 空袭相关的那些坑（都修好了）

| 现象 | 根因 |
|---|---|
| 女仆**飞一半掉下来**、卡住狂烧烟花还受伤 | 起飞没冷却、单次起飞枚数无上限；助推烟花带了爆炸星会炸到自己（改为不带爆炸星，仍消耗 1 枚真实烟花） |
| 鞘翅**倒挂 / 挂在脑袋上 / 很小 / 空中不张开** | Gecko 图层没做坐标空间换算（Y 没翻所以倒挂）、挂点选错；改为按 `ElytraLocator → Elytra → UpperBody → Root` 优先级找骨骼，28 个模型全覆盖 |
| 鞘翅**只有 winefox 家族能显示** | 7 个没有专用挂点的模型掉进兜底逻辑、翅膀被顶到脚下/身体内部；已按 17 个样本统计出平均偏移补齐 |
| 鞘翅要**先起飞一次**才出现 | 唯一写胸甲槽的函数只在"有敌人"的行为里调用；改为切到任务就穿（一穿上就渲染） |
| **1.20.1 只会水平飞**、不爬升 | 1.20.1 的 `LookControl` 每 tick 把俯仰角清零，抬头角下一 tick 就被抹掉，烟花推力只剩水平分量（1.21.1 早有的对策没同步过来） |
| 打怪时**贴脸了却打不中** | 俯冲速度 1~2 格/tick，只比"当帧中心距"会整段掠过目标；新增**位移线段扫掠判定** + 判定距离放宽到 4 格 |
| **远程空袭锁死副手 → 女仆不能吃东西回血** | 旧版把烟花常驻副手，盾牌/食物都装不进去；而烟花**根本不需要拿在手上**（发射时自己造弹体）——现在副手完全让出给盾牌/食物 |
| 空袭**落地后一直重复放水/雪**、飞不起来 | 落地缓冲标志被清零后条件再次满足，无限循环；新增 2 秒独立冷却切断 |
| 女仆**对着墙疯狂空挥** | 本模组四条自造攻击链路绕过了 TLM 原版的"视线"门控（其中一条甚至能隔墙真打到）；六处出手点统一补上视线判定，且**不罚站**——看不见就走过去绕到能看见再打 |
| **岩浆里垫方块自救失效** | 禁搭判定的放行名单里三个方法名其实**全是"水"**，真正的 `isInLava` 从未出现（上一版收紧禁搭引入的回归） |
| **悬空 / 滑翔时仍在搭方块** | 禁搭判定显式放行了滑翔，且阈值对滑翔根本不可能触发；改为单独判滑翔位 + "只要在降就禁" |
| 弩**射不出攻击性烟花火箭** | 武器分流里压根没有"弩"这一路 |
| **御币被误判"缺弹药"** / 扇形范围攻击在空袭时丢失 | 御币是弹射武器的子类、被"数箭"那一路吃掉；空袭里写死了"单发"，TLM 的分档规则整张表被丢掉 |
| 飞行攻击**不吃附魔** | 1.20.1 造箭后直接射出，原版写在弓上的附魔段从未被调用（力量/冲击/火矢/无限全部补上）；击退附魔也被写死成固定值，已按原版算式修正 |
| 空袭**不适配拔刀剑** | TLM 的拔刀斩被硬编码门控在它自己的"地面攻击任务"上；改为在我们自己的挥刀点复刻同一段调用（含伤害倍率） |
| **1.21.1 很多女仆模型被隐藏** | 两个模型包放在同一个 `assets/geckolib/` 下导致**同 domain 撞名**，被 TLM 的排序逻辑整包丢弃 |

### 空袭的手感打磨

- **鞘翅外观定稿**：尺寸与玩家**等大**、位置上移 0.7 格，顶端正好落在肩线上；附魔光泽改回"只有真附魔过才亮"；并**跟随 TLM 女仆配置页的「显示背部物品」**（在那里关掉，鞘翅就隐藏）；
- **放烟花的瞬间副手会"亮一下"烟花模型**（约 0.5 秒）——显示的就是包里被真正消耗掉的那一枚，随后自动换回原来的盾牌/食物；
- **远程空袭会稳住高度**：不再慢慢往下沉，烟花消耗也明显变少。

## 界面与手册

- **手册新增整章「空袭与飞行作战（会飞的女仆）」**（约 5 页）：两个任务的区别 / 三件套 / 保姆式首飞举例 / 近战与远程的完整链路 / 索敌机制 / 开关位置与常见问题；
- 手册补齐了此前**一个字都没写**的功能：投喂食物勾选、人格种子、双 agent 提取、蛋糕可食用、悬空禁搭方块、手持光源发实光、TNT 点火保护期、红石机器专属搭建、树冠清理、穿透预算；
- 修掉手册里一个**失效的跳转链接**与一处**会误导读者的章号引用**；
- **配置面板**：「排班表」从「移动与行为」挪到「**生产与工作**」；空袭相关开关说明清楚它为什么横跨两个小类；新增「空袭牵引绳（格）」。

## 主要功能一览

**空袭（近战/远程两种飞行攻击模式）** · **排班表**（全女仆日程调度：早/晚/全天班次、6 段任务编排、当前时段高亮、一键集合、自动在家模式） · **建造系统**（81 套内置蓝图 + 外部图纸/LLM 生成/AI 设计器、工地线框投影与 Z 键旋转、工头模式、进度持久化、缺料自动替代、机械蓝图） · **挖矿**（矿表白名单、连锁采集、透视感知、发呆看门狗） · **伐木**（模组原木自动识别、随手补种、骨粉催熟） · **烧制 / 酿造**（什么能烧就烧、多女仆多炉、药剂手册批量/定向酿造） · **农场**（连锁收获/批量种植、耕地标记制、锄地索敌、作物骨粉） · **宰杀任务** · **战斗与自保**（回血阶梯、逃生手段、保命道具优先、致死自动回魂符、**女仆自动复活**、环境伤害软处理、主动切换战斗+自动还原） · **激流三叉戟旋转冲击**（面向敌人的旋转突进，地面与空袭收翅俯冲均可触发） · **主人免伤三层防护** · **被动技能**（落地水/落地雪、岩浆放水、主人死亡传送、搭路） · **单兵战术**（走位/风筝/举盾/跳劈暴击/枪械战术） · **贴身辅助**（自动投喂/治疗/盾牌） · **武器与工具自动切换**（DPS 评分，兼容其他模组武器） · **指标石**（随手铺路搭墙） · **AI 记忆**（段落/关系/画像/工作笔记 + 日/3日/周/月记忆日记 + 人格种子 + 睡一觉整理） · **主动对话 / 工作播报 / 自主决策 / 回复反馈学习** · **感知与情绪**（PAD 模型） · **17 个 AI 工具**（对话直接下命令） · **TTS 语音**（内置日语语音包 / 系统朗读 / 缓存 / 音量） · **冷却可视化 HUD** · **床铺互通** · **交互**（摸摸头/抱抱、蛋糕互动、女仆互助、手持光源、宠物保护） · **女仆安全与区块保载** · **运行日志** · 内置 **26 章手册** · 全部功能游戏内配置面板逐项开关

完整逐条更新日志见仓库 `CHANGELOG.md`。

## 本版的调整（行为变化，装之前看一眼）

- **建造默认速度改回 ×1**：以前默认是"极速"（放置冷却归零、吃满服务器上限，实测 1400+ 块/秒），
  一上来就看不出建造过程也白烧性能。现在 `build.speedTier` 默认 **×1**、`build.turbo`（极速）默认**关**；
  旧档做**一次性迁移**（跑过一次就不再碰），之后你想开极速随时开，不会被改回去；
- **建造瞬移限频减半**：放置前"瞬移到目标格旁边"的节流从 4 tick 改成 **2 tick**，摆放连着出时更跟手；
- **建造预览/落点回退旧版**：实测"真方块投影"在我们这儿渲染不出来、配套的落点微调界面 bug 也多，
  于是整体撤销——回到**彩色填充盒 + 金色框跟着玩家走 + 点两次「建造此图纸」确认**的老手感；
  真方块渲染、G 键微调界面与相关配置项一并删除。**「从建造区块内的箱子取材料」保留**（这项没回退）；
- **排班表时钟对齐游戏挂钟**：以前整张表比游戏时间**早 6 小时**（把日出当成了 0 点），于是"按挂钟填的
  12:00~16:00 实际跑在傍晚""深夜在加班、白天在睡觉"。现在时钟与 TLM 显示的游戏时间、F3 一致：
  早班 = **06:00~18:00**、晚班 = **18:00~06:00**（跨午夜）、全天 = 00:00~24:00；
  **已存的日程不做换算**——同一张表在新口径下就是"挂在挂钟上的那个时段"，这正是修复目的；

## 测试环境声明

| 模组 | 关系 | 测试通过的版本 |
|---|---|---|
| Touhou Little Maid（车万女仆） | 必装前置 | 1.5.3（Forge 1.20.1） |

> 其他版本（含更新/更旧的 TLM）未必兼容，请以实际测试为准。第三方软联动已移除，不再声明联动模组兼容性。

## 反馈

问题与建议请到 GitHub Issues：https://github.com/fadersketch/Promaid-mod/issues

## 许可证

MIT © 2026 —— 开源地址：https://github.com/fadersketch/Promaid-mod
