> 这是一个测试的版本，更新了很多内容，绝大部分功能正常但可能存在小瑕疵开发者未能注意到，所以特开了此测试版本。欢迎体验和反馈。收到反馈之后，会加入到正式的更新中，并在不久的日后发布正式版。此Beta版本会持续进行更新迭代，新beta版不会额外发行，而是覆盖原有的beta版。所以您所看到的beta版每天甚至每小时都有可能发生变化。所以请时刻关注，您所反馈的每个bug都会被尽快修复。

# Promaid v1.1.0 beta（测试版·已完整验证）

- Minecraft 1.20.1 · Forge 47.4.x
- 前置：车万女仆 Touhou Little Maid ≥ 1.5.0（推荐 1.5.3，本版在 1.5.3 下实测）
- 附件：`promaid-1.1.0.jar`（直接放入 mods 文件夹）
- 旧存档兼容：物品 / 任务 / 蓝图 / 记忆等持久化数据沿用 maid_smart 命名空间，v1.0.4 存档无缝升级

## 本次更新（实测二百七十二~二百九十，经完整实机验证）

- **女仆药剂手册（新物品）**：水瓶 + 书本合成（创造栏「工具与实用品」页可拿）。手持右击自己的女仆打开配置 GUI，为**该女仆**设置酿造模式——批量酿造（背包里有什么正向材料就酿什么，按配置的强化路线/成品形态统一处理）或定向酿造（只按目标药水的配方链精确下料，缺料停止下料等待，补料后自动继续）。GUI 支持中文搜索、药水效果 tooltip（等级 + 时长）、喷溅/滞留形态联动图标、悬停显示酿造材料清单（跟随鼠标）、翻页。保存后有系统提示；缺料时女仆立即叫停并报告（前面步骤照常做，缺的那步停下报告）。酿造时女仆播放动作。**mod 新配方自动适配**：Forge BrewingRecipeRegistry 运行时遍历，任何 mod 的新药水配方自动进入定向酿造，无需逐个适配
- **拥抱 / 摸摸头（情绪价值交互）**：默认键 **G【摸摸头】/ H【抱抱】**（原版按键设置可改键）。2 格内存在女仆按 G/H 即触发（取最近，判定与空手右击同款逻辑，零视线要求）。摸头 = 好感 +1 + 心形粒子 + 女仆语音 + 随机开心气泡；抱抱 = 好感 +3 + 双向回 1 心 + 粒子加倍 + 气泡。仅主人可触发（他人提示"她只让主人摸哦～"），摸头 8 秒 / 抱抱 30 秒冷却。触发后女仆播放**对应姿势动画**（摸头 = 头部轻摆，抱抱 = 双臂环抱，机制移植自 HeartPact：mixin 到 TLM 动画最终骨骼覆盖层，Bedrock/Gecko 双路径兼容，非动画文件）。系统消息带第三人称旁白（括号包裹）
- **农场耕地功能**：女仆现在会主动锄地——走到农田附近时，对耕种过但被踩坏的土地使用锄头（中心格不再跳过，判定与收割/种植同源），锄地目标纳入移动扫描，够得着就锄
- **女仆建造优化**：建造预览转向键默认 Z（原版按键设置可改）；建造投影全量修复（点云 12000 点空间均匀采样，近处实心/远处线框完整剪影，巨型图纸全貌可见）；建筑材料判定升级（缺料时先问玩家"建一半还是放弃"）；建造女仆静默非建造字幕（气泡保留）
- **药剂手册 GUI 多轮修复**：点击一个药水不再全部勾选（按药水注册名判定选中）；所有药水不再显示"无法酿造"（Forge binpatch 把配方表字段改成 Holder.Reference，反射取值后解包）；效果行正确显示等级（罗马数字）+ 时长（mm:ss）；翻页按键与页码不再重叠；删除"附近明明有 XX"遗留报告；同一种药水的三种形态不再被同时选中（切形态清选中态）

## 本次更新（实测二百九十一，经完整实机验证）

- **酿造台占用**：两个女仆抢同一酿造台 → 熔炉同款占用表（维度|坐标 → 占用女仆 UUID）——启动即登记、找台跳过他人占用、台子丢失/行为停止释放、占用者死亡懒清理，多女仆自动分散到不同酿造台
- **空瓶卡死修复**：女仆往酿造台放空瓶导致酿造完全无法进行——根因：收成品判定只认药水，空瓶（glass_bottle）既不是药水（不收走）又占着槽（不补水瓶）→ 卡死。修复：非药水物品（空瓶/杂物）一律收走腾位；取水瓶严格只认**饮用型**水瓶（排除喷溅/滞留水瓶——旧版只认 PotionItem 不区分形态，喷溅水瓶也会被当水瓶放进槽 0 卡死）
- **锄地草方块判定**：泥土变草方块后女仆彻底失去判定——根因：锄地判定只认泥土（dirt），草方块蔓延后不再锄。修复：判定扩展认草方块（grass_block）——原版锄头本来就能锄 dirt/grass_block → farmland，判定与锄地动作对齐
- **锄地频率**：冷却 2 秒 → 1 秒——"锄地检测的频率不行，导致没有办法及时锄地"
- **批量酿造材料轮换**：批量模式材料选择从"按背包槽位顺序取第一个"改为"优先选与上次不同的材料"——给多种材料时交替酿多种药水（A→B→C→A…），不再死磕第一种；只给单一材料时自然只酿那种（符合批量本意）

## 本次更新（实测二百九十二，经完整实机验证）

- **批量收成品检查配置**：3 分钟的夜视不会自动合成 8 分钟的——根因：批量模式收成品（是药水就收）不检查配置，3 分钟夜视一出现就被收走，强化步骤永远轮不到。修复：未到目标强化/形态的成品**不收**，留在酿造台继续推进；平凡/浓稠（无效果死路）照常收走腾位；该药水无对应强化变体（如治疗药水无延长版）放行不卡死
- **缺强化材料不转形态**：要求合成时间长的滞留型药水，却合成出 2 分钟版本——根因：缺红石/萤石时静默跳过强化直接转形态（滞留/喷溅瓶无法再强化，永远普通时长）。修复：缺强化材料 → 报告 + 不转形态——饮用瓶留在酿造台等材料，补上后自动强化，强化完才转形态

## 本次更新（实测二百九十三，经完整实机验证）

- **GUI 时长按形态折算**：药剂手册 tooltip 效果行旧版直接显示饮用型满时长——喷溅/滞留型药水的效果描述与实际不符。修复：按原版规则折算——喷溅 = 饮用 ×3/4、滞留 = 饮用 ×1/4（如夜视 3:00 → 喷溅 2:15、滞留 0:45），等级不变

## 测试版补充更新（实测二百二十~二百七十一，经完整实机验证）

- **药水效果强制生效**：女仆投药水改用强制施效（跳过"同类效果不比现有更强则拒绝"的规则）——再生药水连续投掷时每次都生效并刷新时长，目标身上不再出现"扔了药水却没效果"；施加后目标身上播紫色药水粒子（效果已给到的可见反馈）
- **药水抛掷动画**：触发投药水时，一个**药水**以抛物线从女仆飞向目标原有位置（纯动画，落地不施加任何东西）——治疗主人和治疗女仆都适用；效果仍由触发即生效逻辑直接施加（不依赖飞行）
- **药水触发即生效**：女仆投药水不再投掷实体（历次方案均被实测否决）——触发即消耗药水 + 抛药水动画，**直接给目标施加药水效果**；滞留型药水则在目标旁边生成滞留效果云（半径 3 格，持续到效果结束）
- **伐木搭方块修复（补全）**：女仆搭了几块柱子后站在柱顶、目标木材就在头顶 1~2 格时，真实头顶检查不再把目标木材当"堵头"拦死搭高——与放置格检查同口径排除目标木材，够不着的树她会一路垫方块爬上去砍完
- **宠物保护加强**：命名"玩家宠物/主人的宠物"或 MaidNoAttack 前缀的生物（含铁傀儡等任意生物）——名字判定改为三重检查（显示名/自定义名/基础名任一命中即免疫），女仆伤害总闸拦截（近战/箭矢/弹幕/枪械/爆炸溅射全覆盖）；若仍有个别漏判，新增诊断日志会记录目标名字原文（promaid.log 搜 "maid pet-miss"）便于精确定位
- **女仆互助传递药水/食物**：姐妹之间现在会传递喝的药水——普通（饮用型）治疗药水直接喂给受伤的姐妹（强制饮用，效果即时生效，空瓶返还）；姐妹正在战斗且血不满时还会喂食物（战斗续航）
- **烧制燃料修复**：女仆不再"用木头烧木头"——燃料选择改为纯燃料优先（煤炭/木炭/烈焰棒/干海带块/熔岩桶等不可烧制的可燃烧物），按燃烧时长评分（煤炭 1600 tick ≫ 原木 300 tick）；背包里没有纯燃料才退而用原木/木板（总比炉子熄火好）
- **伐木搭方块修复**：女仆现在会像挖矿一样搭方块够到高处木材——旧版防窒息检查要求头顶必须是空气，但伐木场景头顶正上方就是树干/树冠树叶，搭高永远失败（只会原地等待到超时弃置）；现在只拦真正会闷住她的实心方块，目标木材和树叶不再挡路，够不着的树她会垫方块爬上去砍
- **随手种树（独立模块）**：女仆手上有树苗、附近（半径 6 格）有可种土块（泥土/草方块等）时随手种一棵——与砍树互不干扰，触发随伐木模式；主手→副手→背包→地面捡苗 依次取用；CD 约 5 秒可调（伐木面板「补种树苗冷却」），总开关「随手种树」（默认开）；树苗识别含物品标签（红树幼苗/模组树苗等全兼容）
- **女仆手持光源发实光**：主/副手拿火把/灵魂火把/红石火把/灯笼/营火/萤石/南瓜灯等任意光源时，她脚底跟随一个隐形光块照亮周围——亮度与所持光源一致（火把 14/灵魂火把 10/红石火把 7/灯笼 15…，特殊光源白名单 + 通用发光方块自动识别）；不拿光源自动熄灭；不影响插火把判定
- **挖矿连锁采集扩展到障碍物**：挖开可挖穿挡路块（泥土/圆石/石头等）时，周围同类障碍物一并连锁破坏（上限 16 可调，掉落进背包）
- **主副手识别审计**：垫脚方块（挖矿/伐木/搭路）、插火把、种树等"随手用品"全部支持从主/副手取用——手里拿的东西不再被当成"没有"
- **伐木站桩修复**：对树干旁高几格的木头不再发呆——站树干旁 1~2 格也能锁定头顶木材逐节上砍，无需垫块
- **建造投影全量修复**：橙色幽灵方块不再"零星竖条"——点云 12000 点空间均匀采样、近处实心/远处线框完整剪影（金字塔级巨型图纸也全貌可见；此前的 50 万点+VBO 方案因显存占用过大已回退）
- **默认值定音**：所有连锁采集（矿/木/农场）默认开；伐木透视默认开；挖矿透视默认关；搭路默认关；种树/手持光源默认开
- **药水效果读取修正**：女仆投药水"根本连效果都没吃到"——旧版 `PotionUtils.getMobEffects` 只读 NBT 的 CustomPotionEffects 列表，**完全不读 Potion 字段**（再生/治疗/抗火等标准药水的效果存在 Potion 注册表里，读出来恒为空）；改为 Potion.getEffects + CustomPotionEffects 合并读取，两处投掷与自保饮用同口径
- **药水动画观感修复**：动画药水改用**实际药水完整复制**（同样形态 + 实际 Potion 效果，飞出去的就是再生药水瓶，落地自然呈现真实碎裂粒子/效果云，不再是空瓶白水花）；落点**时刻追踪**目标当前位置（既有弧线又跟目标移动，落点与目标几乎重合）；追踪上限 20→10→1 tick + 抛掷速度 4.0——效果给到之后动画药水立刻落地，不再"效果先到、瓶子还在天上飞半秒"
- **滞留药水效果云修复**：云参数全面对齐原版——旧版把时长当颜色写入（m_19714_ 实际是 setColor 不是 setDuration），云永远是暗色怪云；改用真正的 setDuration + setWaitTime/setRadiusOnFall/setRadiusPerTick，云从 0 半径长到 3.0，与真实滞留云外观一致
- **喂食效果与女仆自己吃一致**：投喂改走原版 eat 完整路径——食物效果、吃/喝音效全部由原版触发，不再手搓（蜂蜜瓶解中毒 + 空玻璃瓶返还保留）
- **女仆不踩坏农田**：女仆经过/落在农田上不再把它踩成泥土（FarmlandTrampleEvent 拦截，走路/跳跃/坠落都经过）——移动走位频繁也不会把玩家的农业区踩坏
- **排班系统修复（多轮）**：①排班开启后女仆一直保持未排班状态、锁定解除不了——战斗让位门过宽（手头任意攻击任务就被"战斗中"挡死）+ 跨维度操作静默失败（开关/设置只查玩家当前维度），已修复；②空闲状态排班不生效——可用性软探测在女仆空闲时永不切换，改为纯时间判定（任务状态跟着时间段落真实切换）；③排班有日程但工作状态不符——休息时段不再什么都不做，作息切到班次模式（早班=DAY/晚班=NIGHT/全天=ALL），GUI 与日程一致；④排班生效后快捷设置页 GUI 立即统一更新再锁定（新增状态同步包，段应用成功即推送）
- **全员模式应用修复**：满血/剪刀模式/解除排班的女仆有时不响应全员模式——批量应用跳过链过宽（残留战斗标记 + 自保标记自愈不对称：进入自保要血<30%、自愈却要 70%，30%~70% 残留永久跳过）；改为只跳过真本系统战斗（ASSIGNED 匹配当前任务），残留标记顺带清理，自愈条件放宽到 30%；批量应用全程可观测（逐只诊断日志 + 读回校验 + 单只隔离）
- **应用空闲修复**：点击"应用空闲"女仆仍显示别的模式——GUI 空串被拦掉不发包；空串改为当作 TLM 空闲任务发送，任务循环含空闲档回绕，服务端读回校验
- **手持光源修复**：女仆手上拿木头仍然会发光——旧版误用 getLightBlock（光阻挡值，木头恒 15）当发光值；改用 getLightEmission（自身发光，与光照引擎同源），木头=0 不发光，火把/萤石/岩浆块等正常
- **女仆拆床修复**：女仆睡觉时身体与床方块重叠，持光系统的光块直接替换床格把床顶掉（离开后光块清成空气，床彻底消失）——光块只写入空气格或本光块格，被床等任何方块占据则跳过，绝不覆盖

## 一、全新内容（v1.0.4 之后新增的功能）

### 1. 排班表（新物品，整套日程系统）
- **怎么获得**：纸 + 墨囊（或荧光墨囊）在工作台合成；首次进入世界时和《Promaid 手册》一起送一本到背包；创造模式物品栏「工具与实用品」页也能直接拿。
- **做什么**：给女仆排「几点到几点干什么」——每个时段最多 6 个任务槽自动均分该时段的工作时间，游戏内时间到点自动切换（白天/黑夜的作息也可以安排）。
- **锁死手动改**：排班开启期间，女仆的任务和工作模式被锁住——原版女仆面板、蓝图书、AI 对话命令都改不了（气泡会提示"排班中呢，任务由日程表管理，想改先关排班"）。
- **排班 > 战斗**：排班中的女仆不参与自主战斗、不响应任何参战触发——任务和模式完全由日程表管理，战斗与排班不再互相拉扯（想让她打架，直接在时段里放攻击任务）。
- **活动范围与散步**：排班/在家模式下活动半径默认 32 格（原版只有 8~16 格，范围稍大就出不去）；空闲时自己散步（间隔/半径/速度可调，不再站桩发呆）。
- **单独传送按键**：女仆详情页第 1 页新增「⚑ 传送到我身边」——只传这一只（不用再全员一键集合）；她坐着/骑乘/在家守排队列时保持原位并提示原因。
- **防频繁切换**：任何一次排班切换后有 3 秒最短持有期，段边界紧挨着也不会连切；A→B→A 的来回横跳有反向抑制。
- **切段后大脑自愈**：段任务应用成功 3 秒后若发现女仆脑内没有任何工作记忆（任务状态对、但大脑没在动），自动整脑刷新一次重建 AI——治"任务切了人却站桩"。
- **跨维度跟随**：排班/在家的女仆在主人过门、进下界/末地时会直接传送跟过来（跨维度不拦，同维度守家不跟）。

### 2. 自主战斗（自动参战 + 打完还原）
- **自动参战**：女仆被打、主人被任意来源攻击、主人开火打了敌对生物、被激怒的中立动物（记仇状态）等都会触发参战；空手时不参战（不会空手上去送）。
- **选武器**：按背包武器匹配所有攻击任务（含模组专属），模组武器权重 2.0 高于原版 1.0；近战/远程按目标距离分池——5 格内选近战、够不着选远程；拔刀剑、史诗战斗、万法皆通等模组武器必定用它们的专属战斗方式。
- **远程放风筝**：弓/弩/三叉戟/枪械的走位距离强制不小于 7 格（旧版算出来只有 4.8~6 格=贴脸，看起来"远程还近身"）；太近后退、太远逼近、理想区间绕圈。
- **战中换战术**：战斗中途近远武器条件变化会自动切近战/远程；模组专属任务永不被换掉（信任模组自己的机制）。
- **打完还原**：参战时快照任务+作息+在家模式三件套，战斗结束全部还原；还原不再被"任务被外部改了"中断——先还原、读回确认成功才清标记，失败保留下轮重试；另有 90 秒硬超时兜底，绝不会永远卡在战斗任务。
- **僵局逃逸**：威胁还在但 60 秒没有任何伤害往来，视为僵局照常还原（防止远程怪吊着女仆无限"战斗中"）。
- **动态威胁圈**：被谁打过就登记谁——登记的生物还活着、且 10 秒内再出现过，还原判定会把威胁圈放大包含它，远程风筝怪不再引发"还原/再参战"反复横跳。

### 3. 搭路（女仆会铺方块追你了）
- **做什么**：你和女仆之间隔着沟/悬崖/高空时，她面前脚下悬空就朝你铺方块踩过去；你比她高时原地垂直垫柱搭上来；像整合包的僵尸搭桥追人——你水平往远处飞她也能一路追（平路追逐不限距离）。
- **启动规矩（全部可调）**：球面距离 3 格以内不启动（太近走路即可）；水平距离达到 6 格、前方脚下悬空才平铺桥；她要竖直搭高需要你比她高 3 格（正好一个手长的差距——她搭到只差 3 格你就够得着收回）；空中追逐上限 50 格；贴近你（水平 6 格内）绝不再水平方向垫块，只走路。
- **速度与安全**：默认节奏 4 tick/块（约 5 块/秒，只比你手速快一档）；垫的方块 3 秒后自动回收进她背包（她站上面就延后）；走到刚垫的方块上到位即清水平速度（防惯性滑出 1 格宽桥面失足），目标格没有支撑（方块被回收/没放上）绝不往前推——不下断崖。
- **什么时候不搭**：周围 8 格有敌对生物、正在战斗、正在干活（挖矿/伐木/建造/站桩）、背包没方块、骑乘中、守家模式——都不搭；搭方块选材自动避开岩浆/火等危险方块。

### 4. 伐木任务（重新做的全新任务）
- 女仆自动找树砍树：模组原木也能识别（带 #minecraft:logs 或 #maid_smart:logs 标签）；空手也能砍（只是慢）；树冠挡路时把身边 3×3×3 的树叶直接破坏出掉落物；整片树冠清空掉落进背包。
- **原址补种与随手种树**：砍树掉的树苗永远直进背包；"随手种树"独立模块（见顶部测试版更新）贴身处理补种——她会手上有苗、附近有土块时随时种一棵，与砍树互不干扰。
- 连锁砍伐默认开启（默认 16 格链上限）；透视感知默认开——隔墙也能找到木材。
- 垫块踩上去、树洞发呆修复、发呆看门狗（连续 8 秒零进展自动重置行为，不会卡死在树里）；对树干旁高几格的木头不再站桩（逐节上砍）。

### 5. 挖矿与农场增强
- 连锁采集默认开启（16 格链上限）并**扩展到障碍物**：挖开泥土/圆石/石头等挡路块时周围同类一并连锁；挖矿中禁止拾取默认开（不蹲手捡破烂）；透视感知默认关（像玩家一样只发现视线无阻的矿，可开启）；软方块不耗耐久默认开（挖泥土不再费镐子）。
- 矿表自动迁移：老玩家的矿表没铜矿/只有旧表，启动时自动补全（铜矿之前不在默认表里）。
- 竖井修复：穿透预算现在把石头泥土也算进去（旧版跳过=预算形同虚设，女仆在矿洞里打无底竖井）。
- 农场：连锁收获默认开、批量种植默认开（各 24 格上限），收获即种，不再"走一步停一步"。

### 6. 烧制全面升级（原"烹饪"任务）
- 任务改名「烧制」：熔炉/高炉/烟熏炉三种炉子全兼容，按炉型喂料——熔炉烧食材+矿物、高炉烧矿石（受「熔炉烧矿物」开关控制）、烟熏炉烧生食。
- 背包里什么能烧就烧什么：带矿物/原料标签（模组矿物也认）且真有熔炉配方的都能喂；通用可烧制物兜底（默认开，装备类永不熔）。
- 多女仆分炉：炉子占用表——N 只女仆自动分散到 N 个炉子，绝不再挤着抢同一个炉子。

### 7. 回家与传送体系
- **同维度远距拉回**（48 格，可调）：跨区块跟丢时自动传回（旧版全靠 TLM 随机试探，经常静默失败）。
- **Y 轴拉回**：你比她高太多（超过 16 格）自动传回你脚下。
- **安全落点**：所有传送（跨维/同维/拉回/集合/自保/救援）都走同一个落点判定——你身边 16 格内找"脚下有碰撞面+身体没有碰撞+无流体+无岩浆火等危险+无实体占用"的格才落；找不到就不传，绝不把她扔半空摔死（高空/虚空也提示原因）。
- **搭路中的女仆不拉回**（她正站在自己铺的半空桥上，拉走会抽掉脚下的桥）；排班/守家中不响应跨维度传送；有主的女仆所在区块持续加载（不再跟丢冻结）。
- **一键集合**：未加载区块的女仆强载区块召回，15 秒兜底；坐着/骑乘保持原位不硬拉。
- **受困救援**：掉出世界底部、站在下界基岩顶上方的女仆自动传回主人身边。
- **主人死亡传送**：重生点方块校验（床/充能重生锚才有效）、下界基岩顶落点修复、死亡瞬间不追、复活后跨区块/跨维归队。

### 8. 其他新内容
- **蛋糕互动**：右击喂女仆蛋糕，她立刻吃（音效+粒子）、好感 +10、回血 14 点，蓝色系统消息与气泡播报。
- **女仆互助**：姐妹之间互相丢药水/喂食/金苹果/解负面，主人优先级永远最高；蜂蜜瓶喂完返还玻璃瓶；追踪药水弹命中优化。
- **落地雪**：细雪桶版"落地水"（下界也能用），1×1 单格（无容错，偏一格失败——追求稳请用水桶，这是机制固有难度）。
- **运行日志系统**：logs/promaid.log 独立日志文件（4MB 轮换），排班/战斗/传送/险境等关键动作全程留痕，遇到问题方便反馈。
- **模组武器专属**：拿拔刀剑/史诗战斗武器必定切它们的专属任务并自动装备；枪械（枪手联动等）有弹药闸——没弹药绝不切枪。
- **着火不传主人**：带火的女仆不会把你烧着（攻击取消+接触传火自动灭火，可配置）。

## 二、修复的问题（v1.0.4 时代及测试中存在的 bug）

### 崩溃类（进游戏/使用中端掉）
1. **排班表崩溃**：空槽位越界、附魔光效方案压垮服务端（isFoil 改为 0 级附魔 NBT）、跨维度回空数据包卡请求——三处已修。
2. **宠物免伤启动崩溃**：两次（mixin 目标校验失败 + 接口 mixin 不被接受），最终弃用接口混入、改为纯事件监听方案。
3. **跟随收紧 mixin 两次崩溃**：注入点选错（继承方法不匹配）+ 返回值回调参数用错类型——已修并验证。
4. **输入框 StackOverflowError**：设置文本颜色用错方法（真正的 m_94202_），照搬原版搜索框实现。
5. **"游戏启动后部署过 jar"导致运行中崩溃**：此崩溃是部署时机问题（JVM 懒加载读过期的 jar 索引），非代码 bug——已加部署护栏：游戏未关闭时拒绝部署，重启游戏即恢复正常。

### 建造与幽灵方块
6. **幽灵方块太小/出区块就消失/看不见**：旧渲染走调试色条带管线，盒与盒之间自动连出"跨盒三角形"——整片幽灵变成乱麻三角网。改逐面独立 QUADS 精确 1×1×1。
7. **幽灵渲染崩溃①**：手写顶点与一些模组（史诗战斗/ponder）的渲染 mixin 冲突，首次点建造即崩溃——回退到原版实验管线+每盒立即冲刷。
8. **幽灵渲染崩溃②**：想当然的"每盒冲刷"放在共享渲染源上，与描边互相踩状态——改为每帧独立专用源，两条崩溃路径都堵死。
9. **从外看不到内**：纯填充盒逐盒混合无排序，外层盖内层；近处（24 格内）填充+每盒无条件 12 条棱线（与区域框同管线，隔地形透显）——Litematica 式示意图观感，任意角度都能看到全部方块轮廓。
10. **红色区块框挡幽灵**：区块框的大填充面从外向内看恰好挡在相机与幽灵之间（红盒子遮罩）——移除两大填充面，只留棱线框+悬浮标签。
11. **投影走出区块就消失**：96 格距离剔除移除；远景透明度过低看不见→调高；点云超 32767 字符静默丢包→按 16000 分块传输（多包分片）。
12. **建造中进度条/进度文本贴出屏幕底边、字段重叠、页码压箭头**等界面问题批量修复；建造"瞬间完成"（建材等价误判）、"悬空方块补支撑破坏树冠"、"续建识别换朝向仍继续建" 均已修。
13. **建造确认流程重做**：两步确认简化为一步，「建造此图纸」未确认前金色框随你移动+青色幽灵投影，每次点击都会提示"可按 Z 键旋转朝向"（确认后不可改）；转向键默认改为 **Z**（原版社交互动占用了 P）。

### 排班表
14. **排班失效/任务不切换**：可用性检测"没活不切"把女仆钉死在原地——按时间段落真实切换（默认值跟随配置，可调）；保存后排班立即生效；切换时应用后读回校验，任务被 TLM 守卫静默拒绝（睡眠/活动中）时不会写"已应用"、10 秒后重试。
15. **排班开却一直被战斗状态顶住**：战斗标记残留（旧版崩溃/半途退出留下）→ 自动清理；让位判定改为"真实在战斗"，残留标记不拦。
16. **玩家手动改排班女仆的任务导致排班卡死**：尊重手动选择——排班检测到"本段尝试之后任务被外部改过"就不再死磕，下个时段边界再接管。
17. **排班书打开/收放魂符不重放**：排班标记随魂符保存，同段内收放不重放——重新入世界自动清去抖键并立即按当前时段应用一次。
18. **排班女仆不断瞬移**：救援系统的"虚空判定"用错了方法（拿世界高度当最低高度）——每 5 秒把守家女仆当"掉出世界"拽回主人身边；已改为真正的维度底部判定。
19. **排班中任务/作息绕过**：原版 GUI 改任务、改作息都能绕过排班——源头守卫 mixin 全堵住；面板改任务被拦时客户端立即弹回真实任务（服务端推送矫正包）。

### 战斗
20. **战斗结束不还原/还原卡死**（修了很多轮）：先还原后清标记+读回校验；90 秒硬超时兜底；还原快照补全（任务+作息+在家模式）；还原前自动装备原武器；外部改任务时尊重新任务、但把作息/在家模式还原回去。
21. **"再也不触发参战"/参战被吞**：残留战斗标记（idle 读数）不再被误判——只有真实还在战斗任务才算"已在战斗"。
22. **模组武器不用/选到又换走**：史诗战斗任务没覆写武器判定（恒 false）→ 补反射兼容层；参战重选抖动（3 秒内的 idle 抖动不再触发重选）；喂动物任务混进战斗候选池（它实现了攻击接口）→ 排除。
23. **还原扫描无限收敛崩溃**（AABB 收敛死循环导致永不触发）——已修。
24. **中立生物**：被激怒中立动物（狼/雪傀儡等记仇状态）打女仆→正确参战；被主人攻击的中立动物算开战；平静态不算威胁。

### 搭路/挖矿/伐木
25. **搭路只搭一块就停/不走上去**：冷却递减逻辑只在垂直腿生效（平桥第一块后永锁）+ 垫完靠寻路踩不上（半空寻路必失败）——新增持续推送机制（12 tick 踏入即停、斜上带起跳、期间免导航拉扯）。
26. **失足摔死**：搭路节奏回调到 5 块/秒 + 到位清水平速度 + 无支撑不推（见上）。
27. **搭太高同时被传走摔死**（05:11 事故全链还原）：搭路中一律不拉回；她脚下桥块"离开 2 秒就塌"——近距（≤4 格）持续刷新寿命。
28. **挖矿/伐木每 5 秒重置一次状态**（像收放魂符）：缓存强制期满重建——改为一律用到轮空（框内确无矿）才重建。
29. **挖矿/伐木垫块踩不上去（死循环）**：共享"垫块踏上"机制接入三条链路。
30. **挖矿挖竖井/盯铜矿不挖/软方块白耗耐久**：穿透预算计入实心块、默认矿表补铜、软方块不耗耐久默认开。
31. **伐木傻站/站树洞发呆/树旁高几格木头站桩**：伸手判定改目标方块最近点、5×5 列簇抬头扫描、目标水平偏开先走到树干柱底再挖。
32. **伐木女仆把别处垫的桥块当木头砍掉**：搭路/挖矿/伐木/自保四套放置登记表统一——跨系统互相认，谁垫的都不误挖。
33. **搭块被踩时刷新计时**：走开瞬间整柱到期摔落——改为重置寿命（走开后每块还有完整 10 秒/60 秒缓冲）；"到期回收"由定时器修正（崩溃残留导致 gameTime 倒挂永不过期）。
34. **骑乘中搭方块残渣**：挖矿骑扫帚时不再垫块。
35. **搭方块选材安全筛选**：火把等无碰撞方块绝不能垫脚；危险表（岩浆/火等）无条件排除。
36. **主副手识别**：垫脚方块/插火把/种树全部支持从主/副手取用（此前只认背包——手里拿的永远"没有"）。

### 烧制
37. **第二只女仆只加燃料不烧**：背包物品不在白名单+矿物标签识别不全面——通用可烧制物兜底（有熔炉配方就喂）。
38. **多女仆挤炉**：只有第一个炉子工作——炉子占用表，各自分散。
39. **日志刷屏**：烧炉每轮一条日志改状态迁移式；排班失败等日志 5 分钟限频。

### 传送与跟随
40. **"总提示没落脚点"**：落点判定过严（要求满方块+纯空气格）——放宽为碰撞箱为空+脚下有可见碰撞面（台阶/栅栏/传送门平台都算）且无流体/危险/占用。
41. **跨维度/home 女仆过门不跟**：home 模式拦停跨维度（她永远住在第一维度）——home 只拦同维度，跨维度照跟。
42. **跟随乱跑**：官方跟随任务只在启动时设一次寻路目标，被其他行为或长距离刹车清掉后就歇菜——每 tick 重断言（4 格内不动、超 4 格重新指向主人）。
43. **主人死亡后女仆被传去下界基岩顶/死亡地点**——非存活状态一律不追，落点校验+重生点归正。
44. **水桶/落地雪误入下界**：下界彻底不浇水（落地水/灭火/岩浆逃生全链）；落地雪 1×1。

### 自保与治疗
45. **高空摔死**（主人飞行时自保传送直扔半空）——安全落点判定后宁可不传；低血带着保命道具（绀珠之药/不死图腾）不再惊慌乱跑（开关见配置节）。
46. **女仆互救"没道具还说话/喂了空气"**：互助链加白名单预检+快照防竞态（同 tick 槽位清空的活引用）。
47. **喂姐妹蜂蜜不解中毒**：蜂蜜瓶饮用路径的手搓效果移除（原版吃法不改状态）。
48. **炉子/酿造/火把/盾牌从出生起没运行过**：canStillUse 缺失根因修复。
49. **着火的女儿传主人/乱喷水**：着火不传主人；下界不乱放水泡沫。

### 界面与文本
50. **HP 气泡显示 bubble.call... 代码**：语言文件缺失回退键——补全部语言键。
51. **杀怪后不停刷"邀功"气泡**：新增开关默认关。
52. **手册/配置面板多处排版问题**：分页溢出（第 4 页翻不到第 5 页）、控件重叠、标签偏移、注释与默认值不符（测出的"默认 X 实际 Y"虚假宣传全部纠正）。
53. **排班表 UI**：整体重写（单布局函数+固定分栏），早期 5 处控件重叠、附魔光效、图标（日历样式排班表/蓝色手册）、任务名中文化。

## 三、改了哪些配置

### 新增的配置项（手册 → 模组详细配置，均在游戏内可调）
- **排班节**：排班表系统总开关（默认开）、切换前可用性检测（默认跟随你当前配置=开）、反向切换窗口/阈值/冷却、最短持有期 60 tick、切段后大脑自愈（默认开）、还原宽限 3 秒、排班活动半径 32 格、空闲散步（间隔/半径/速度）。
- **搭路节**：搭路总开关（默认关）、搭路触发射程 32 格、空中搭桥 50 格、竖直启动高差 3 格、球面半径 3 格、平桥启动距离 6 格、搭路节奏 4 tick/块、方块清理 3 秒、回收进背包（默认开）、战斗搭方块清理 60 秒、威胁半径 8 格。
- **伐木节**：随手种树开关（默认开）+ 补种树苗冷却（默认 100 tick≈5 秒）、连锁砍伐（默认开）、透视感知（默认开）、发呆看门狗 8 秒、树苗补种。
- **挖矿节**：连锁采集（默认开）、透视感知（默认关）、自动收集（默认关）、穿透预算 22、向下搜索 12、深度惩罚 3、搭方块冷却 4、软方块不耗耐久（默认开）、骑乘中禁止搭方块。
- **杂项节**：手持光源发实光（默认开）、跟随收紧（默认开）、同维度拉回 48 格、Y 轴拉回 16 格、有增益也喂牛奶（默认开）、受困救援、危险方块表（可增删）、运行日志。
- **主动切换战斗节**：自动参战开关/半径 16 格、模组武器权重 2.0、原版武器权重 1.0、近战/远程偏好权重、还原延迟 10 秒、战术持有 40 tick、僵局逃逸 60 秒、威胁扩展 10 秒、空手不参战（默认开）。
- **自保节**：保命物品下保留逃跑（默认关）、火焰保护饰品识别（默认开）、溺水保护饰品识别（默认开）等。
- **对话节**：击杀邀功对话（默认关）。

### 默认值变更（与新功能联动）
- 所有连锁采集（挖矿/伐木/农场）：**默认开**；伐木透视：**默认开**；挖矿透视：**默认关**；搭路：**默认关**；随手种树：**默认开**（CD 100 tick≈5 秒）；手持光源发实光：**默认开**；
- 搭路竖直启动高差 6 → 3 格（= 玩家手长）；搭路节奏 2 → 4 tick/块（≈5 块/秒）；伐木/挖矿搭方块冷却 4 tick/块；散步速度 1.0 → 0.7；
- 40 处参数按你在用的配置同步为新默认（删除 promaid-common.toml 重新生成 = 你现在的参数，老配置不删也完全兼容）。

## 四、微调与机制优化（幕后但体验相关）

1. **性能**：排班/战斗/搭路等多处扫描限频与节流（10 tick 重检、每 5 秒心跳）；矿/木扫描分帧预算制（不再一帧全量扫描卡住服务器）；危险方块静态缓存；大蓝图请求按源缓存。
2. **动手感**：女仆搭方块全程播摆臂动画+放置音效与粒子（9 处放方块点统一）；插火把节奏减半（0.75 秒）+扫描范围 5×5；种树苗间隔 2.5 秒；钓鱼自动换竿；建造坐下状态全程锁移动。
3. **观感**：战斗搭方块 60 秒自然消失+可拆除；跳劈/近战手感（触发距离与可达距离一致、横扫修复、不后撤）；远程被贴身 2 格内自动 AOE 反击。
4. **诊断能力**：promaid.log 全链路日志（排班/战斗/传送/险境/投影/烧炉断点/种树扫描/持光点亮），收报错日志发我就能定位。
5. **诚实修正**：把注释/面板里写错的默认值全部纠正（287 键脚本复核 0 差异）；手册"详细介绍"按当前版本全面校对（旧的示例建筑换成现在随包送的蓝图、过期说明删除）。

## 测试环境声明

本版本经测试者验证，在以下模组的具体版本下可正常运行；其他版本（含更新/更旧的 TLM 与各软联动模组）未必兼容：

| 模组 | 关系 | 测试通过的版本 |
|---|---|---|
| Touhou Little Maid（车万女仆） | 必装前置 | 1.5.3 |
| maidmarriage（心契誓约） | 软联动 | 2.3.0 |
| callresponse（爱憎分明） | 软联动 | 2.0.4 |
| Heartfelt-connection（联动补丁） | 软联动 | 1.0.0 |

## 反馈

问题与建议请到 GitHub Issues：https://github.com/fadersketch/Promaid-mod/issues
本测试版本的任何小瑕疵都会在正式版前修复，感谢参与测试！

## 许可证

MIT © 2026 —— 开源地址：https://github.com/fadersketch/Promaid-mod
