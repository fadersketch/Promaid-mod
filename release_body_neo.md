# Promaid v1.1.0-beta.2（1.21.1 NeoForge 版）

> **本版本是针对 Minecraft 1.21.1 / NeoForge 的移植版（beta）**，功能与 1.20.1 Forge 正式版对齐。有问题欢迎反馈：https://github.com/fadersketch/Promaid-mod/issues

- **Minecraft 1.21.1 · NeoForge 21.1.250**
- **前置**：车万女仆 Touhou Little Maid ≥ 1.5.0（NeoForge 1.21.1 版；实测 1.5.3-neoforge+mc1.21.1）——**纯独立前置，不对任何其他模组做专属联动**
- **附件**：`promaid-1.1.0-neoforge-1.21.1.jar`（直接放入 mods 文件夹）
- 存档说明：1.21.1 存档与 1.20.1 不互通；1.21.1 内部沿用 maid_smart 命名空间
- **其他模组武器天然可用**：武器判定基于原版攻击力属性，模组武器照常参与自动装备与伤害计算，无需专门适配

## 本次更新

### 🛡️ 女仆绝不伤害主人（三层防护，重点修复）
粉丝反馈：带女仆到雪地，空闲模式打雪仗后女仆会对主人跳劈，脱甲约 4 心，主手武器越强越疼。
- **根因**：TLM 原版雪地打雪仗行为会把**主人**写进女仆 brain 的 `ATTACK_TARGET`（雪球目标），promaid 的单兵战术行为读到目标就接管走位+跳劈，打出真实近战暴击伤害；
- **修复**：① 伤害事件总闸——女仆造成的主人/友方伤害在攻击/受伤事件处直接取消；② 全部攻击注入点过滤（战术跳劈、近战横扫、中立威胁反击、自保反击含弹幕/射箭、LLM 攻击工具）；③ 定期清除仇恨——每 2 秒把主人/友方从 `ATTACK_TARGET`、实体目标、复仇目标里清掉；
- 结果：雪地打雪仗只会扔雪球，**绝不会对主人挥刀**；同主人的女仆之间也互不误伤。

### 🧹 彻底移除第三方软联动（纯 TLM 独立版）
与 1.20.1 正式版同步，把对特定第三方模组的专属联动彻底删除（保留按攻击力属性的通用武器逻辑）：
- 删除爱憎分明（饥饿门控、4 个配置项、调试页、极端饥饿反射）、心契誓约（拥抱/调试面板 mixin、关系标签反射、建造交互锁）、超越维度与精妙储存（整个 storage 包、两个绑定卡物品、全部取物/统计调用点及模型/贴图/配方/语言）；
- 删除 `MEMORY_RELATIONSHIP_ADAPTER`/`MEMORY_RELATION_SCAN`/`MEMORY_TRUST_DELTA`/`MEMORY_HEARTFELT_ANNIVERSARY` 配置项与界面行、`callresponse` 语言键；
- **保留**：按攻击力属性的通用武器判定/自动装备（模组武器天然参与，无需专属代码）。

### 🔧 1.21.1 移植实测修复（首批）
1. **放女仆即崩**：Mixin 注入点误指向无参常量所在的构造器 → 精确描述符 + 逐一 javap 验证 14 个 mixin 目标；
2. **首次进服不发手册/排班表**：`Inventory.add` 被误映射为 `contains` → 修复并按背包缺什么自动补发；
3. **6 个界面图层错乱**（文字发暗、渐变被盖）：1.21.1 的 `Screen.render` 开头自动画背景 → 6 个界面统一重写背景逻辑；
4. **建造进度条洋红污染**：1.21 删除合并纹理 `bars.png` → 改用 1.21.1 精灵 API（原版经验条同款暗底+亮绿）。

## 主要功能一览

**排班表**（日程调度：班次/6 段任务/当前时段高亮/一键集合/自动在家模式） · **建造系统**（81 套内置蓝图 + 外部图纸/LLM 生成/AI 设计器、工地线框投影与 Z 键旋转、工头模式、进度持久化、缺料自动替代、机械蓝图） · **挖矿 / 伐木**（矿表/木材表、连锁采集、透视感知、发呆看门狗、随手补种、骨粉催熟） · **烧制 / 酿造**（什么能烧就烧、多女仆多炉、药剂手册批量/定向酿造） · **农场 / 宰杀** · **战斗自保**（回血阶梯、逃生、保命道具优先、致死自动回魂符、主动切换战斗） · **主人免伤三层防护** · **被动技能**（落地水/雪、岩浆放水、主人死亡传送、搭路） · **单兵战术 / 贴身辅助 / 武器与工具自动切换** · **AI 记忆 / 主动对话 / 自主决策 / 感知情绪 / 17 个 AI 工具 / TTS 语音** · **交互**（摸摸头/抱抱、蛋糕互动、女仆互助、手持光源、宠物保护） · **区块保载与危险方块避让** · 内置 **26 章手册** · 全部功能游戏内配置面板逐项开关

完整逐条更新日志见仓库 `CHANGELOG.md`。

## 测试环境声明

| 模组 | 关系 | 测试通过的版本 |
|---|---|---|
| Touhou Little Maid（车万女仆） | 必装前置 | 1.5.3-neoforge+mc1.21.1 |

> 1.21.1 NeoForge 版为 beta：功能与 1.20.1 正式版对齐，但实机覆盖面有限；第三方软联动已移除，不再声明联动模组兼容性。

## 反馈

问题与建议请到 GitHub Issues：https://github.com/fadersketch/Promaid-mod/issues

## 许可证

MIT © 2026 —— 开源地址：https://github.com/fadersketch/Promaid-mod
