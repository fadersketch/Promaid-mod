# 车万女仆 AI 智能增强包（P0 好感度感知 + 跨模组平衡 + P1~P4 全功能）

> 全部基于原版模组副本修改，**原模组文件始终未被动过**。回滚方法见文末。
> 适用版本：Minecraft 1.20.1 Forge 47.4.21

## 交付物清单

| 文件 | 说明 |
|---|---|
| `patched/[车万女仆] touhoulittlemaid-1.5.3-forge+mc1.20.1-SMART.jar` | 主模组补丁（P0：好感度注入 AI 上下文 + 提示词情感感知 + 文案修正） |
| `patched/Touhou Little Maid-Love  Loathe-1.20.1-forge-2.0.0-BALANCED.jar` | 爱憎分明补丁（性能降频 + 好感仲裁轴 + 反射改官方 API） |
| `patched/maid_smart-1.5.23.jar` | 新增 addon 模组（P1 工具 + P2 任务 + P3 主动对话与情绪价值动作 + P4 烹饪酿造/自主决策/长期记忆 + 建筑功能 + 家庭保护 + 心契誓约剧情记忆 + 战斗自保（新状态机+保命道具+威胁感知+策略播报+无材料突围）+ 蓝图手册（材料缺口 0/N 中文）+ 建造状态前置校验 + 外部 JSON/结构文件蓝图（.nbt/.snbt/.litematic/.schem，上限 32768 格）+ 5 套预制精美建筑 + 站桩建造 + 障碍物预检 + 渐进挖掘（直接公式+进度持久化）） |
| `安装与回滚.bat` | 一键安装/回滚（原版自动备份到 backup/） |
| `original_tlm.jar` / `original_callresponse.jar` | 原版 jar 留档 |
| `src/` `src_cr/` `addon_src/` `out*/` `work*/` | 全部源码、编译产物、解包目录 |

---

## 一、主模组补丁（P0）：好感度 → LLM 反应联动

1. **AI 上下文注入好感度**（`MaidContexts`）：每次对话的 `<context>` 新增
   `- Favorability: level 2 (233/384)`（等级分界 64/192/384 与游戏内一致）
2. **系统提示词新增"情感感知"章节**（`StringConstant.FULL_SETTING`）：
   指示 LLM 按好感等级调节语气——0 级礼貌疏离 / 1 级友善 / 2 级依恋 / 3 级黏人占有；
   不直接说数值、态度渐变、低好感可委婉拒绝亲密请求
3. **修正** `switch_follow_state` 返回文案 `"Flow mode disabled"` → `"Follow mode disabled"`

## 二、爱憎分明补丁（BALANCED）：解决与心契誓约的性能与方向冲突

### 性能平衡（全量扫描降频）
| 类 | 修改 |
|---|---|
| EmotionBetrayalManager | 背叛状态机拆为"逐 tick 廉价路径（按 UUID 直查，不扫描）" + "每 20 tick 检测扫描"；dangerTimer +20/次（保持 10 秒触发时长不变） |
| EmotionPassiveManager | 扫描每 20 tick；黑暗/水/注视/同伴 5 个计时器按 20 倍换算（行为时长不变） |
| EmotionForgettingManager | 扫描每 20 tick；计时器 20 倍换算 |
| EmotionDotingManager / EmotionDevotedManager / HungerManager | 扫描每 20 tick（内部全为游戏时间制，无需换算） |

> 效果：原来 6 个管理器 × 每 tick × 32 格实体扫描（女仆多时开销线性膨胀），
> 现在绝大部分 tick 只跑廉价状态机，扫描降为每秒一次。行为时长与原来完全一致。

### 方向平衡（好感仲裁轴 + 状态优先级，v2）
以**主模组好感度为统一仲裁轴**，让三套数值（主模组好感 / 心契誓约心情 / 爱憎分明信任恐惧）收敛而非打架：

**状态优先级表**（从高到低，高优先级覆盖低优先级，无冲突）：

| 优先级 | 状态 | 触发条件 | 行为 |
|---|---|---|---|
| 1 | **已婚**（妻子） | maidmarriage 婚姻状态 | 永不背叛、永不淡忘；溺爱降级为"种花+吃醋驱逐"（不偷不翻） |
| 2 | **深爱**（伴侣） | 好感 ≥192（求婚线） | 同上（已婚是子集，规则统一） |
| 3 | **有感情** | 64 ≤ 好感 < 192 | 淡忘豁免；溺爱完整（偷/翻/种花）；背叛按原条件 |
| 4 | **陌生/冷淡** | 好感 < 64 | 爱憎分明完整机制（背叛/淡忘/溺爱全按信任恐惧判定） |

**各机制的触发条件与豁免（明确无冲突）**：
- **结婚**：好感 ≥192（Lv2）→ 双戒指求婚（心契誓约既定流程）
- **溺爱**：信任 ≥90 且恐惧 ≤10 → 好感 <192 完整；好感 ≥192 或已婚 → 只种花+吃醋驱逐
- **背叛**：信任 ≤10 且恐惧 ≥90 持续 10 秒 → 好感 ≥192 或已婚 永不触发
- **淡忘**：信任 ≤10 且恐惧 ≤10 持续 → 好感 ≥64 或已婚 永不触发
- **死忠**（信任 ≥80 且恐惧 ≥80）：护主减伤，与伴侣模式兼容（保留）

**数值回写桥**（`EmotionData`）：信任/恐惧的显著变化（±4 及以上）按 4:1 回写好感度
——信任大涨 → 好感增加；恐惧大涨（虐待）→ 好感侵蚀。仅主人有效、200 tick 冷却。
**闭环效果**：虐待会先磨掉好感（从第 1 级跌到第 3/4 级），再触发背叛/淡忘；
温柔相待会让好感（和 AI 的语气）一起升温——优先级随好感动态迁移。

**反射改官方 API**（`LittleMaidCompat`）：`getDeclaredField("EDIBLE_BLOCKS")` 反射
→ 官方 `getEdibleBlocks()` + `instanceof CakeEdible`，与心契誓约共用注册表，不再互相覆盖。

**婚姻状态软依赖**（`MarriageState`，v2 新增）：ModList 检测 maidmarriage +
官方 TaskDataRegister 读 marriage_data + 反射——零类依赖，未装 maidmarriage 时静默。

### 心契誓约（maidmarriage）
核查结论：**其 12 个 tick 监听全是会话状态机，无全量实体扫描，不需要性能补丁**。
其与爱憎分明的冲突已由上述仲裁轴解决（两者都以好感为共同基础）。

## 三、addon 模组（maid_smart）

通过官方 `@LittleMaidExtension` 机制被主模组自动发现，**零 mixin、零反射**。

### P1：新增 4 个 AI 工具（LLM 可直接调用）
| 工具 | 参数 | 作用 |
|---|---|---|
| `smart_move_to` | target=owner | 走到主人身边（一次性的，不改变跟随模式） |
| `smart_give_item` | item=物品id | 把背包中的指定物品交给主人（最多一整组；主人背包满自动退回） |
| `smart_set_pickup` | enable=bool | 切换自动拾取开关 |
| `smart_report` | 无 | 汇报自身状态（血量/好感/任务/作息/拾取） |

### P2：新增 2 个任务（自动出现在 switch_work_task 枚举里，LLM 可直接切换）
| 任务 | 说明 | 平衡设计 |
|---|---|---|
| `maid_smart:mine` 挖矿 | 找附近 18 种矿石 → 走过去 → 持镐开采 | 必须手持镐；每 20 tick 才挥一次（约 1 秒/块，慢于玩家）；只采白名单矿石；掉落走拾取模式 |
| `maid_smart:organize` 整理箱子 | 把背包物品搬进附近箱子 | 只进不出（不拿箱子里东西）；同种自动合并；只操作箱子 |

### P3：女仆主动对话 + 情绪价值动作（LLM 自己说话 + 自己动）
女仆在以下情境**主动触发 AI 对话**（气泡 + TTS，无需玩家先开口），**并配合游戏内动作**：

| 契机 | 说话内容 | 动作 |
|---|---|---|
| 主人重伤（血 <30%） | 担心关心 | 跑过来 + 递熟食（背包有的话） |
| 夜晚 | 困倦感慨 | 走过来 + 看向主人 |
| 好感升级 | 开心表达 | 撒爱心粒子 + 走过来 |
| 击杀敌人 | 邀功/撒娇 | 走过来 + 看向主人 |
| 长时间沉默（>20 分钟） | 找话题 | 走过来 |

**成本控制**：扫描每 20 秒一次且只扫 16 格；每只女仆两次发言间隔 ≥4 分钟；
每天最多 20 次；LLM 未启用时不触发。动作与说话共用同一配额。

### 建筑功能（v1.4，smart_build）
让女仆**自己搭建建筑**——材料从背包真实消耗：

**四种蓝图来源**：
1. **内置预设（离线可用）**：`maid_smart:hut`（小木屋 88 块）/ `maid_smart:gazebo`（凉亭 44 块）/ `maid_smart:fountain`（喷泉 19 块）/ `maid_smart:tower`（瞭望塔）/ `maid_smart:well`（水井）
2. **LLM 现场生成（联网知识）**：对话让女仆"造一个 5x5 的现代小屋" → LLM 生成 JSON 蓝图：
   `{"name":"小屋","blocks":[{"x":0,"y":0,"z":0,"block":"minecraft:oak_planks"},...]}`
3. **外部 JSON 蓝图（v1.5.11，玩家自扩展）**：把 JSON 文件放进
   `config/maid_smart/blueprints/` 文件夹（文件名即蓝图名，如 `my_house.json`），
   重启游戏自动注册为 `maid_smart_ext:my_house`——目录工具/蓝图卷轴/LLM 全部可见。
   格式与 LLM JSON 相同（`{"name":"我的小屋","blocks":[...]}`），
   也可只给 `blocks`；限制同 LLM 蓝图（≤200 块、平面 ±12、高度 0~8、方块白名单）。
4. **结构文件蓝图（v1.5.12，numen 式）**：**原版结构方块导出的 `.nbt` / 文本 `.snbt`**
   放进 `config/maid_smart/blueprints/` **或存档 `schematics/` 文件夹**即自动注册
   （增量扫描，放入即用无需重启）。支持 **rotation 旋转**（对话时 LLM 可传
   0/90/180/270）、**精确方块状态**（台阶/楼梯朝向/门/床）、**方块实体数据**
   （箱子内容/告示牌文字）。结构蓝图走黑名单通道（基岩/命令方块/液体禁止，
   其余任意方块），上限 8192 块；无物品方块/液体/门上半/床头自动跳过。
5. **Litematica / WorldEdit 图纸（v1.5.14，复杂精美建筑）**：`.litematic`（Litematica
   模组导出）与 `.schem`（WorldEdit/Schematica 导出）放入上述两个目录即自动注册。
   这是网上分享的精美建筑图纸的主流格式——**下载即用，女仆可原样复刻**（含精确
   方块朝向与箱子/告示牌等方块实体）。上限对齐 numen **32768 格**，支持旋转。
   ⚠️ 材料照常从背包真实消耗：上万格的大型建筑需要多次补料（缺料暂停→补料继续，
   已建部分自动跳过）。

**安全与平衡**：
- 方块白名单 ~45 种建筑方块（禁止 TNT/基岩等），LLM 越界直接拒绝
- 单蓝图 ≤200 块、平面 ±12、高度 0~8
- **材料判定（v1.4.1）**：调用时先预检背包，材料不足时**不擅自开始建造**，
  向玩家反馈缺失清单并确认策略：
  - **full（默认）**：材料不足 → 不建，由 LLM 询问玩家"先建一部分还是放弃？"
  - **partial**：玩家同意后 → 只把材料够用的**前缀步骤**加入计划（顺序截断，
    不会出现"墙中间缺一块"的悬空建筑），缺的部分等补料后继续
- 逐块从背包**真实消耗**；建造中缺料时气泡提示并暂停，补料自动继续
- **材料等价族（v1.5.2）**：蓝图要求橡木木板时，背包里**任意木板**（云杉/白桦/深色橡木…）
  都算数，放置时用实际消耗的方块；原木/石砖/圆石/玻璃/门同理（族表见 BlueprintLib）
- 每 10 tick 放一块（节奏慢于玩家）；被占用的位置自动跳过（不破坏玩家建筑）
- 计划随女仆存档（TaskDataKey），重进游戏继续建造
- 完成时气泡报告"建好啦！"；原点在女仆朝向主人方向前方 3 格

**使用方式**（触发流程 v1.5.18）：
1. **先让女仆进入建造状态**：任务页选"建筑"任务（或对话"去建造"）——
   女仆**立即站立不动**等待
2. **下达蓝图（二选一）**：
   - **蓝图手册**（`maid_smart:blueprint_book`，开局送/书+纸合成/`/give`）：
     右键 → 列表（含材料缺口，中文 0/N）→ 点击 → 附近女仆建造
   - **对话（LLM）**：smart_build 需先 switch_work_task 切建筑任务
3. 材料充足 → 开始；不足 → 先建材料够的部分 / 提示补料续建

**建造方式（v1.5.13 起）**：女仆**站桩隔空建造**——切到建造模式立刻停走，
以自身为中心（蓝图居中在脚下），每 0.5 秒隔空放一块（挥臂+放置音效），
不移动、不寻路。开始前**预检建造区域**：有树/建筑/箱子等障碍物 →
气泡"这里不太合适……"并拒绝；空气与自然地形（草/泥土/石头/沙等）可覆盖。

### 自保（v1.5.1，纯规则零 LLM，全局最高优先级）
女仆在**任何状态**下血量过低且附近存在威胁时，停止当前一切行为转入自保：

**优先级**：注册为 core 行为**优先级 250**。TLM 自身全部行为最高为 99
（MaidBrain 反编译证据：ClearSleep=99、拾取=10、跟随=3、恐慌/待命=1），
250 保证自保压过**一切状态**——战斗、任务、跟随、待命、睡觉、恐慌，
不会被任何行为（包括跟随状态）抢占。

**状态机**（滞回区间防抖）：
- **进入**：HP < 30% 且 12 格内存在威胁（当前攻击目标 或 最近敌对怪物），
  **不限定战斗任务**——挖矿/做饭/跟随/睡觉时被打同样触发，
  气泡"情况不妙……我先撤了！"，清空攻击目标
- **自保三策略**（每 tick 本地判定，无 LLM 延迟）：
  1. **使用物品**：吃背包里的金苹果/熟食/面包（真实进食 m_5584_：
     金苹果=吸收+再生直接回血，熟食=饱食触发回血），40 tick 冷却
  2. **逃跑**：朝威胁反方向以 1.4 倍速撤离（威胁 <12 格时）——
     **直接走 PathNavigation**（MoveToTargetSink 优先级 2 在自保期间
     被压制不执行 WALK_TARGET memory，必须自己寻路）
  3. **搭方块**（v1.5.4，借鉴 EpicSiege ESM_EntityAIPillarUp + numen UnstuckDetector）：
     - 威胁贴身（<4 格）时往脚下垫方块（圆石/泥土/石头/木板，0.75 秒一层），
       **头顶 2 格都必须是空气**才垫（防窒息，比原版 1 格更稳）
     - **卡住检测**：逃跑中连续 20 tick 位移 <0.3 格 → 判定被卡住
       → 向逃跑方向前方脚下**垫路铺台阶**（三格全空才垫，不破坏方块），
       翻墙/上坡脱困后继续逃
- **退出（恢复自保前的状态）**：
  - HP ≥ 70% → 解除
  - 威胁消失 且 HP ≥ 45% → 解除（危险没了就回去干活）
  自保期间**不改任务/跟随/待命设置**，解除后 Brain 自动恢复低优先级行为：
  战斗→重新索敌、挖矿→继续挖、跟随→继续跟，回到自保前正在做的事。
- **传送豁免（v1.5.2，maid_smart 自带 mixin）**：vanilla Brain 每个优先级组
  都能启动一个行为，所以自保（core 250）期间跟随（core 3）仍会启动并把女仆
  `teleportToOwner` 瞬移回主人身边送死。maid_smart 通过 mixin 注入
  `MaidFollowOwnerTask.checkExtraStartConditions`（开发名目标，Forge 自动重映射，
  与原版 TLM/maid_useful_task 相同的生态做法）：自保标记（`maid_smart_preserving`，
  血量 <70% 时有效）存在时跟随不启动 → 不传送；自保结束标记自动清除，跟随恢复。
  **防御双保险**：标记异常残留时，血量 ≥70% 也会放行。
  ⚠️ 此功能**不修改主模组字节码**（不 patch MaidFollowOwnerTask）——重编译类
  会破坏 maid_useful_task 等第三方 mixin 的注入点（曾导致崩溃），已恢复原版。

### 家庭保护（v1.2，防母女/同主互殴）
心契誓约的小女仆成年后也是 EntityMaid——爱憎分明的背叛/溺爱机制可能让
"妈妈打孩子"或"家人互殴"。`FamilyProtectionManager` 用 LivingHurtEvent 拦截：
**攻击者好感 ≥64 且与受害者同主人 → 取消伤害**（"深爱的女仆不打同主人的女仆"）。
已婚线（≥192）必然命中此规则；不同主人的低好感女仆互殴仍按爱憎分明原逻辑。

### P4：烹饪/酿造任务 + 自主决策 + 长期记忆

**新任务**（也自动出现在 switch_work_task 枚举里，LLM 可直接切换）：

| 任务 | 说明 | 平衡设计 |
|---|---|---|
| `maid_smart:cook` 烹饪 | 给熔炉补燃料（煤/木炭）与生食（9 种白名单），收取烧好的成品 | 食材燃料必须背包携带；每次处理 1 个动作；5 秒间隔（熔炉自身进度驱动，不瞬间完成） |
| `maid_smart:brew` 酿造 | 给酿造台补烈焰粉与材料（12 种白名单），三瓶药水齐时收取成品 | 药水瓶由主人准备；每次 1 个动作；5 秒间隔 |

**自主决策**（`AutonomousTaskManager`）：女仆空闲 + 白天 + 主人在附近时，
每 10 分钟（每天 ≤10 次）通过主模组 chat() 管线注入一条"自我指令"，
LLM 依据系统提示词的 Action First 规则自己调用 switch_work_task 切换任务，
并以气泡呈现决定（"我自己决定去挖矿了"）。

**长期记忆**（`MaidMemoryManager` + `MemoryMaidContext`）：
- 随女仆存档持久化（官方 TaskDataKey API），事件写入：主人喂食（记录喜欢的食物）、
  好感升级（记录关系等级）、主人死亡（记录恐惧）
- 记忆注入 AI 对话的 `<context>`（`- Memories: 我是Sketch的女仆; 主人经常喂我minecraft:beef; ...`）
  ——LLM 的言行因此有"连续感"，喂食/陪伴会真正改变女仆的说话方式

**心契誓约剧情记忆**（`StoryMemoryManager`，v1.3）：
- 把 maidmarriage 的**既定剧情状态**实时注入 AI 记忆（软依赖：装了才生效，没装完全静默）：
  | 剧情 | 记忆文本 |
  |---|---|
  | 结婚 | 我已与主人结为夫妻 |
  | 怀孕/双胞胎 | 我怀孕了，肚子里有了小生命 / 是双胞胎 |
  | 产后 | 我刚生过宝宝不久 |
  | 心情 | 低落/不太好/平平/很好/满心都是对主人的爱 |
  | 丧子 | 我刚刚失去了我的孩子，心里非常难受 |
  | 告白 | 我们互相告白过，关系已经确定 |
  | 自身是孩子 | 我是主人家里的小孩子，还在慢慢长大 |
- **实时读取**而非写死：结婚、怀孕、丧子等状态变化在下一次对话立即反映到 AI 言行——
  AI 会自称"妻子"、提及怀孕、为丧子难过，与心契誓约剧情零割裂
- 实现零类依赖：用官方 `TaskDataRegister.getValue` 读 maidmarriage 的 6 个 TaskDataKey，
  record accessor 反射调用；maidmarriage 未安装/更新都不影响

> ⚠️ 前置条件：女仆需要有**人设配置**（AI 聊天设置里设定过），否则主模组会提示"未设置人设"而不发言。

---

## 使用方法

1. 关闭游戏
2. 运行 `安装与回滚.bat` 选 1（三个原版 jar 自动备份到 backup/）
3. 启动游戏

## 测试指引

1. **好感感知**：新驯服女仆（好感 0）对话应礼貌疏离；好感 >192 后应明显亲近粘人
2. **平衡**：虐打女仆（恐惧上升）→ 好感被侵蚀 → 好感跌破 192 前不会背叛爆炸；
   好感 ≥64 不会淡忘解除主仆关系；女仆数量多时 TPS 不再随 tick 恶化
3. **新工具**：对话说"过来我身边"（smart_move_to）、"给我钻石"（smart_give_item）、
   "汇报你的状态"（smart_report）、"别捡东西了"（smart_set_pickup）
4. **新任务**：对话说"去挖矿/去整理箱子/去做饭/去酿造"（switch_work_task），
   或 GUI 任务页选择。烹饪需女仆背包带生食+煤；酿造需带烈焰粉+材料
5. **主动对话**：女仆在身边时故意受伤 / 等到晚上 / 提高好感 / 看她杀怪，
   她应主动冒出气泡说话（间隔 ≥4 分钟）
6. **自主决策**：白天把女仆设为空闲任务，10 分钟内她可能自己切换任务并解释
7. **长期记忆**：喂女仆食物几次 → 再对话，她应记得"你喜欢喂她什么"；
   好感升级后她会提起"关系变好了"；记忆随存档保留（重进游戏仍在）

## 回滚

运行 `安装与回滚.bat` 选 2 → 原版全部恢复。

## 技术说明

- 编译：Mojang 运行时 javac（JDK 21，`--release 17`），classpath =
  `forge-1.20.1-47.4.21-client.jar`（Forge 补丁 overlay，提供 getPersistentData 等）
  + `client-...-srg.jar` + 过滤后的 libraries（**排除旧版 DFU 4.1.27 等残留**）+ 模组 jar
- 爱憎分明补丁：8 个类重新编译回写（65 类不变），MANIFEST 保留 `MixinConfigs: mixins.callresponse.json`
- addon：23 个类（含 1 个 mixin）+ mods.toml + mixins.maidsmart.json + MANIFEST
  `MixinConfigs` + 中英语言文件，纯官方扩展 API（addMaidTask / registerAITool /
  registerAIMaidContext / registerTaskData）；**必须含 @Mod 入口类**（MaidSmartMod）——
  Forge 要求 mods.toml 声明的 mod 能在 jar 中找到 @Mod 类，且 TLM 的扩展扫描
  （AnnotatedInstanceUtil → ModList.getAllScanData）只认已加载 mod 的扫描数据
- **mixin 目标名必须用开发名**（如 checkExtraStartConditions）：mixin 应用在 Forge
  SRG 重映射之前，原版 mod jar 的类方法名是开发名；handler 方法体内的 MC 调用用
  SRG 名（重映射不动，运行时即正确名）
- 全程无 mixin 新增、无反射新增——补丁只在原逻辑上降频/加判断/换官方 API

## 版本历史

- **promaid 1.5.252x：拟人化排序加骨架优先（用户要求：先搭建骨架后填充）**
  - 在原有拟人化排序（结构→门梯→装饰）基础上，结构类（prio 0）内部再分
    **骨架优先**：水平外轮廓（x/z 位于蓝图边界）的方块先建，内部填充后建。
  - 排序键 = prio → 骨架 → y → x → z。效果：四面墙圈/四角柱/屋顶边缘
    从底到顶先立起来（建筑"轮廓"先成型），再逐层填充墙面与内部——
    更接近真人"先搭骨架再填墙"的建造习惯；中途中断时轮廓已完整，损失最小。
- **promaid 1.5.252w：材料表空气修复 + 大目录净化 + 悬空实锤日志**
  - **① 材料表出现空气 0/0（用户实测）**：countNeeds 把蓝图里的空气步骤也统计进
    需求（甘蔗农场 1223 个空气步骤）——空气步骤被"建"完后 built=1223 → 剩余 0
    → 显示"空气 0/0"。修复：countNeeds 排除 FORBIDDEN 黑名单（空气/基岩/屏障等
    生存不可获取方块）——不再进材料表、缺料计算默认玩家持有（空气步骤本就
    无限，主循环单独处理，不影响搭建）。
  - **② 手册大目录不再显示建造进度/进度条（用户要求）**：大目录保持干净，
    进度显示保留在建造面板/女仆管理等具体页面。
  - **③ 甘蔗农场大量方块"悬空放不上"（用户实测）**：日志实锤跳过 130 个
    （82 甘蔗 + 48 平滑石台阶）；加两条实锤日志——build skip（跳过方块+坐标）
    与 build place-fail（失败位置+原因+当前方块），重启后按日志定位根因。
- **promaid 1.5.252v：坐垫实心判定加固 + 诊断日志**
  - 坐垫岸格判定从 isAir/isLiquid 改为【碰撞箱】（m_60812_）：花/草/火把等
    无碰撞箱装饰方块不再被误当"地面"（旧判定会把它们当地面 → 坐垫浮在草上）；
  - 生成坐垫时打诊断日志：坐垫坐标 + 【下方位块注册名】——用户重启后看日志
    （auto-chair: …（下方位块 minecraft:xxx））即可确认坐垫是否真·贴地；
  - 手册速度/ETA 加限频诊断日志（hud book: plan=… speedBps=… etaSec=…，每 5 秒一条）
    ——验证服务端给手册下发的值，配合截图定位客户端显示问题。
- **promaid 1.5.252u：建造进度字段分行显示（用户要求）**
  - 进度条上方字段从一长串改为分行（\n 分隔，每行一个信息块、短小独立）：
    - 行1：建造进度：「名」 已建 X/Y 块（Z%）
    - 行2：等待补建 N 块 / 缺料：a×N、b×N…（无则省略）
    - 行3：参与女仆：N 只 · 建造中/【暂停中】 · 速度：×1
  - 客户端逐行右对齐绘制（每行独立截断补省略号）；进度条位置随实际行数
    自适应下移（不再固定 +22）；各页面进度区上移 10px 让位，不压底部按钮。
- **promaid 1.5.252t：进度条上方字段出屏修复（用户实测："进度条上方字段显示突出屏幕"）**
  - 根因 1：drawWrapped 的"右对齐"用 drawCenteredString（m_280653_）以 x-lw 为
    **圆心**绘制 → 文本右缘落在 x-lw/2，长文本明显偏左"悬空"（看起来超出预期位置）；
  - 根因 2：换行截断（maxLines=2）无声丢内容，第 2 行被硬切，观感像文字被屏幕边缘切掉；
  - 根因 3：超长蓝图名（如 olymp-final 全名）撑爆整行。
  - 修复：右对齐改 drawString 左锚点（右缘恰好在 x，且钳制 x≥2 绝不出左缘）；
    换行截断处补省略号…；蓝图名超 18 字符截断。
- **promaid 1.5.252s：坐垫贴地修复 + 手册进度条旁速度/预计时间**
  - **① 坐垫放进水里（用户实测："女仆竟然会把坐垫放在水里面…一格高的水"）**：
    - 日志实证：坐垫生成在 y=-60（水面+1/水面同层），地面在 -61 时坐垫悬浮在水面上方。
    - 根因：findBank 只查"水面+1/+2 层"——水面与地面同高（1 格深水塘）时正确的岸
      （水面层空气格）永远查不到 → 只能退回 +2 层 → 坐垫悬浮在岸边水塘上方。
    - 修复：岸格改为找该方向岸边柱的【最上面实心方块】，坐垫放在它上面（贴地）——
      1 格深水塘/常规水塘/台阶岸全部落座在地面，绝不悬浮、绝不进水。
  - **② 进度条旁没有速度/预计时间（用户实测）**：手册里原来的"速度"是建造档位按钮
    （×1/×1.5/×3），不是块/秒。
    - ProgressUpdatePacket 新增 etaSec（预计秒）+ speedBps（块/秒，复用 HUD 统计
      BuildHudTracker.speedEtaOf），手册每 2 秒轮询刷新；
    - 进度条右侧标签 = 百分比 · 块/秒 · 预计完成时间（小时/分/秒），超宽左移钳制防出屏。
  - **③ HUD 广播验证日志**：每 5 秒一条 "hud broadcast: plan=… placed=… total=… speed=… eta=…"
    ——确认服务端广播在跑、数值正确（latest.log 搜 hud broadcast）。
- **promaid 1.5.252r：修复进世界第一 tick 即崩（mixin 类被普通代码直接引用）**
  - **现象**：v1.5.252q 部署后启动正常、世界加载完成，第一个 ServerTick 即崩：
    `NoClassDefFoundError: com.maidsmart.mixin.FishingAutoChairMixin is invalid`（ProMaidExtension.onServerTick:137）。
  - **根因**：mixin 类被 mixin 插件特殊处理（加载链上被标记/变换），普通代码直接调用
    mixin 静态方法时 JVM 判定该类 invalid。旧版 mixin 从未被普通代码引用所以从未触发；
    v1.5.252q 把 sweep 静态方法挂到 mixin 上并由 ProMaidExtension 调用 → 第一 tick 崩。
  - **修复**：钓鱼坐垫全部逻辑迁出到【普通类】com.maidsmart.fishing.FishingChairService
    （生成/标记/查找/清扫/无掉落判定），FishingAutoChairMixin 只留两个 @Inject 注入壳
    委托给它；ProMaidExtension 调 FishingChairService.sweep，ChairNoDropMixin 调
    FishingChairService.isAutoChair。规则：**普通代码永远不能直接引用 mixin 类**。
- **promaid 1.5.252q：钓鱼坐垫重做 + 建造 HUD 修复 + 日志清理**
  - **① 钓鱼坐垫（用户实测："会象征性扔鱼竿但不抛出、又说没有坐垫又说自己带坐垫、坐垫被随机生成在离水域较远的地方"）**：反编译 TLM 钓鱼链路定位根因——MaidFindSitTask 只从 brain 传感器记忆 NEAREST_VISIBLE_LIVING_ENTITIES（需视线 + 快照刷新）找椅子/船，我们 v1.5.130 在 start TAIL 生成的坐垫**永远不会被传感器看见** → 女仆永不坐上 → 每 5 秒任务重启又生成一个新坐垫 → 岸边堆积、位置随机（findWaterSpot 的 dy 外层优先 + 首个命中，还会找进洞穴/远岸）。
  - **新流程（按用户方案重做）**：
    1. 原逻辑已找到椅子/船（sitEntity != null）→ 不动，走原模组钓鱼流程；
    2. 没有 → 实体直接扫描 8 格内空坐垫/船（绕过传感器可见性）→ 走过去/贴脸直接上骑；
    3. 还没有 → 在**离女仆最近**的可钓鱼水域岸边生成**带持久 NBT 标记**（promaid_auto_chair + 女仆 UUID）的 TLM 坐垫（EntityChair，与原版坐垫同款），并 `startRiding(force)` **直接坐上** → 坐上后 TLM 骑乘脑 MaidRideFindWaterTask（半径 6）自动找水抛竿——钓鱼全流程打通；
    4. **清理**（服务端每秒 sweep）：女仆当前任务 ≠ 钓鱼（任务解除）→ 删；脱离坐垫超 2 秒 → 删；女仆不在/下线 → 删。
  - **摧毁无掉落**：TLM 的 AbstractEntityFromItem.killEntity()（玩家潜行拳打/创造击碎）会按 DO_MOB_LOOT 掉落坐垫物品——新增 ChairNoDropMixin 拦截：标记坐垫被摧毁时只移除实体、不掉落任何物品（自动坐垫是虚拟的，掉物品=变相刷物品）。
  - 原 no_sit 气泡（lambda\$start$2）在「马上要生成坐垫」时 HEAD 压掉——不再出现「没有椅子」和「我自己带了个坐垫」两句打架。
  - 岸边查找放宽：不再要求 3×3 开阔水面（单个小水塘也能钓）；水面高度贴近女仆 ±2（不进洞穴）；距离最近优先（按距离环扫描）；支持 1 格深水塘（岸高 2 格）与常规岸（高 1 格）。
  - **② 建造 HUD（用户实测："速度和剩余时间显示没做好，又出现突出屏幕"）**：
    - 总块数改数可解析步骤（steps 含头部行，旧 size() 会让总数 +1、ETA 偏长）；
    - 速度 <10 块/秒显示一位小数（旧取整把 0.5 块/秒显示成 0）；
    - 长文本自动**折行成多行显示**（按字体宽度换行，优先空格断行、§ 颜色码完整保留，不再截断省略）；单行最长占屏 70% + 右缘留 4px，折行后超出屏幕高度的行直接不画——永不顶出屏幕。
  - **③ 日志清理**：移除 5 处每 5 秒/每女仆刷屏的诊断日志（build tick / brain-run / torch-placer diag / aid-owner diag / shield-share diag）——日志体积下降一个数量级，保留 build canUse（全局 5 秒一条）、build stall、auto-chair 等低频有用日志。
  - **④ 顺手修复**：MaidTeleportPreserveMixin 用 "maid_smart:build" 与 getUid().m_135815_()（= getPath，javap 实证）比较恒 false → 建造女仆瞬移拦截从未生效，改为 "build"。
- **promaid 1.5.252p：创造模式材料 -21 亿修复 + 拟人化建造顺序增强**
  - **① -21 亿（用户实测："创造模式下一些材料显示为负 21 亿"）**：根因 =
    `buildCatalogEntriesWithMaterials` 的 `have = built + maidHave + countPlayerMaterial(...)`,
    创造模式 `countPlayerMaterial` 返回 `Integer.MAX_VALUE`，built+maidHave ≥1 时
    **int 溢出成 `Integer.MIN_VALUE`（-2147483648）**；`realShortfall` 的
    `built + combinedHaveAll(MAX_VALUE)` 同样溢出。**修复**：两处创造模式短路——
    目录材料"已有"直接=剩余需求（显示 X/X 齐）、实时缺料直接返回空；
  - **② 拟人化排序效果弱（用户实测："很早写的拟人化建造顺序似乎没生效，仍是很常规的
    蓝图打印"）**：代码路径（centerSteps → 排序 → filterBuilt/dedupe 保序 → PlanState →
    恢复时重新排序）全部验证正确、排序生效；但 `buildPriority` 的装饰分类**覆盖不全**——
    灯笼/书架/陶罐/蜡烛/发光地衣/紫水晶簇/钟乳石/孢子花/大滴水叶/珊瑚/干草捆/可可豆/
    甜浆果/洞穴藤蔓/红树胎生苗/末地烛/锁链等常见装饰被归为结构（prio 0）与墙一起先建，
    效果被稀释（wizard_tower 实证：palette 里 lantern/bookshelf 全是 prio 0）。
    **修复**：扩充 prio 2 装饰分类（21 个新方块类，编译验证存在）；
  - **复测**：① 创造模式打开建造目录 → 材料显示"已有=需要"不再 -21 亿；② 创造模式
    缺料报告不再误报；③ 生存模式材料统计回归不变；④ 建含灯笼/书架/陶罐的蓝图 →
    装饰最后放置（墙/屋顶先成型）；⑤ 排序后去重/过滤/恢复顺序保持（回归不变）。

- **promaid 1.5.252o：修复爱憎分明"禁用饥饿/撑死"开关从未生效（2.0.2 包名变更）**
  - 用户实测："禁用饥饿和撑死似乎并没有生效"；
  - **根因**：游戏内爱憎分明为 **2.0.2**，其 HungerManager 作者包从
    `com.github.tartaricacid.callresponse` 改为 `com.github.JumDa5he.callresponse`；
    `LoveLoatheHungerGateMixin` 的 targets 写的是旧包名 → @Pseudo 找不到类 →
    **mixin 静默跳过 → 开关从未加载**（v1.5.199 起存在，非配置问题——配置
    loveLoatheHungerOff=true 一直是开的）；
  - **修复**：targets 改为 `com.github.JumDa5he.callresponse.compat.hunger.HungerManager`；
    方法签名已 javap 对账 2.0.2：`onServerTick(TickEvent.ServerTickEvent)` 与
    `onMaidDeath(LivingDeathEvent)` 均存在且签名匹配；
  - **复测**：① 开关开 → 女仆不再饿死/撑死/自动吃腐肉/速度惩罚（看日志无 starve
    死亡与 OverfedDeath 广播）；② 开关关 → 恢复原版饥饿行为；③ 未装爱憎分明时
    仍静默跳过不崩；④ 吃饭 +信任/-恐惧保留（onMaidEat 不受影响）。

- **promaid 1.5.252n：参与女仆数改为绑定数口径（手动绑定/全员加入计数一致）**
  - 用户实测："显示的参与女仆数量和实际参与有的时候不一致——全员加入数值正确，
    女仆管理手动添加数值不长"；
  - **根因**：`countBuildersNear` 按"**区块原点 ±128 格内**"统计绑定女仆——而建造是
    隔空的（setBlock 不需要女仆在场）。全员加入按钮只在区块内可用（绑定女仆都在附近
    → 计数正确）；女仆管理页手动绑定不要求玩家在区块内——远处绑定的女仆照样隔空
    建造，却因不在区块 ±128 格内而不计数 → 数值不增长；
  - **修复**：改为【绑定数】口径——直接统计 MAID_PLAN 中绑定该 planId 的女仆数
    （绑定与区块同维度由两个入口各自保证：BIND_MAID 拒绝跨维度、joinAll 按维度过滤）；
  - **复测**：① 区块内全员加入 → 数值 = 绑定数；② 女仆管理远处手动绑定 → 数值立即
    增长；③ 解绑 → 数值减少；④ 跨维度绑定仍被拒绝（回归不变）。

- **promaid 1.5.252m：悬空补支撑不再把树叶换成石头（保护树冠外观）**
  - 用户实测："为什么有的时候树叶会被替换成石头"；
  - **根因**：v1.5.252i 的 canSurvive 兜底"无条件把支撑格换成合法支撑"——树冠里的
    装饰（火把/按钮/红石线/梯子等）支撑格是树叶（树叶面不牢固，canSurvive 失败），
    兜底把树叶换成石头 → 树冠被"石头补丁"毁容；
  - **修复**：换支撑前检查支撑格——**树叶（LeavesBlock）不换**（附着类支撑格与下方格
    两处同保护），保持失败 → 延后 → 跳过（蓝图缺陷本来就不该强建；与 252i 前的行为
    一致，只是不再破坏树冠）；
  - **复测**：① 建大树/带树冠装饰的蓝图 → 树叶保持树叶，树冠装饰跳过（不再变石头）；
    ② 悬空火把/活板门/甘蔗等（支撑格为空气/水/石头）仍正常补支撑强建（252i 修复不回退）；
    ③ 树叶衰减保护（protectLeaves）不受影响。

- **promaid 1.5.252l：世界存档提取不再把石头/砂岩等建材当地形过滤（天安门 32 块根因）**
  - 用户实测：导入的地图错误——"天安门只有 32 个石块"；
  - **根因**：`TERRAIN_BLOCKS` 包含 `stone/deepslate/andesite/granite/diorite/tuff/
    sandstone/red_sandstone/calcite/dripstone_block` 等【双用途建材】，而世界提取
    `skipWorldBlock` 是【硬过滤】（不看占比）——天安门城楼主体是石头/汉白玉，全被当
    "地形"砍掉，只剩 32 块非石头方块（瓦/灯/旗帜）。原注释"不会误删建筑结构"只对
    trimTerrainLayers 的逐层占比判定成立，对硬过滤不成立；
  - **修复**：`TERRAIN_BLOCKS` 剔除全部双用途建材，只保留纯地形（泥土/沙/草地/矿石/
    基岩/雪泥黏土等）——世界提取保留 stone 等建材（山体石头一并提取，建筑优先完整）；
    trimTerrainLayers 占比判定同步受益（石头建筑层不再被当"还原区"压缩）；
  - **关于 rrr/SSS 与 92 万块（用户疑问，非 bug）**：① rrr/sss 是用户测试导入时 zip
    文件名（rrr.zip 等）→ 蓝图/区块名 = 文件名，创建过的区块在手册残留，可在手册
    区块页取消删除；② 世界提取 = 锚点（玩家最后位置/出生点）±256 格区域提取，区域
    内方块多则块数大（上限 100 万）——92 万块说明该区域含巨量方块（大建筑群/石头
    地形），属预期行为；
  - **复测**：① 重新导入天安门世界 zip → 提取出完整城楼（石头保留）；② 普通建筑
    .schem 导入回归不受影响；③ 纯地形（泥土山）提取仍被过滤/压缩；④ 矿石层仍压缩。

- **promaid 1.5.252k：删除「现代红石智能住宅·挖空版」蓝图（仅保留原版）**
  - 用户实测：挖空版严重悬浮，且因悬浮导致下部设施被补支撑的方块卡死、无法正常使用；
  - 根因：挖空版把内部填充挖掉后，红石线/火把等大量附着方块悬空，v1.5.252i 的
    "悬空强制补支撑"为它们垫的支撑块把下方红石机械/装置堵死——挖空设计与补支撑
    逻辑冲突，该蓝图本身不适用；
  - **处理**：① 删除蓝图文件 `config/maid_smart/blueprints/modern_redstone_house_hollow.schematic`
    （游戏运行中删除，重启后目录扫描彻底消失）；② 删除源码 EXT_CN_NAMES 的挖空版中文名映射；
    ③ **保留原版** `modernredstonesmarthouse8649399.schematic` 的映射与文件（注：原版文件
    当前已不在本地蓝图目录——用户侧早前已移除，如需可到 PM 原帖重新下载同名文件放入
    blueprints 目录即自动识别）；
  - 注：补支撑仅对"悬空附着方块"生效，正常蓝图（内部支撑完整）不受影响；
  - **复测**：① 手册蓝图目录不再出现"现代红石智能住宅·挖空版"；② 原版映射保留；
    ③ 其他蓝图（含内部挖空较少的 hollow 类）建造不受影响。

- **promaid 1.5.252j：建造 HUD——实时显示当前建造速度 + 预计完成时间（左上角，防错位/防出屏）**
  - 用户需求："给建筑功能加一个目前建造速度以及预计要完成时间的显示，注意防止错位、突出屏幕显示"；
  - **新增 BuildHudTracker（服务端）**：每 1 秒（20 tick）由 ProMaidExtension 调用 broadcast()——
    对每个进行中区块采样 placedCount 增量算速度（EMA 平滑 0.6/0.4），按剩余块数估预计完成时间，
    打包 BuildHudPacket 广播给所有玩家（PacketDistributor.ALL）；首帧只记基准（首秒速度显示 "--"）；
    总块数取 ps.steps.size() 缓存（不反复组装 toPlan）；统计随区块清除自动清理；
  - **新增 BuildHudPacket（S2C，id 18）**：8 字段 {planId, 显示名, 已建, 总数, 跳过, 速度, 预计秒(-1=未知), 暂停}；
  - **新增 BuildHudRenderer（客户端）**：左上角 (4,4) 显示，格式：
    `⛏ 建造进度 / olymp-final 12,345 / 493,527 (2%) [暂停] / 跳过 162 · 速度 1,234 块/秒 · 预计还需 6分32秒`；
    最多 3 个区块，超出折叠为"…还有 N 个区块"；
  - **防错位/防出屏（用户要求逐项落实）**：① 行高固定 10px 严格步进不重叠；② 每行按字体宽度
    截断到屏幕右缘内（§ 颜色码完整保留，截到 § 连码一起删）；③ 超过屏幕可用高度（h-12）的行不画；
    ④ 打开任何界面（含手册）时不渲染（手册详情页已有进度显示，避免双份）；⑤ F3 调试屏不渲染
    （options.f_92028_）；⑥ 小窗口自适应（用 guiScaled 宽高）；
  - **复测**：① 下达建造后左上角出现进度 HUD；② 速度随建造波动、ETA 递减；③ 暂停区块显示
    [暂停] 且速度冻结；④ 多区块只显示前 3 个 + 折叠提示；⑤ 打开手册/背包/F3 时 HUD 隐藏；
    ⑥ 最小窗口（如 640×360）无出屏无重叠；⑦ 无建造时 HUD 消失。

- **promaid 1.5.252i：钓鱼"自带坐垫"岸边判定修复 + 世界存档导入兼容带顶层目录的 zip + 悬空方块强制补支撑**
  - **钓鱼修复（用户实测："女仆在水边原地弹没有椅子气泡，修复没触发"）**：根因 =
    `FishingAutoChairMixin.findWaterSpot` 的岸边格判定要求"水面格【同一高度】的水平相邻格是
    空气"——自然河岸水位线以下的相邻格是泥土/沙子（实心），4 个方向全部不满足 →
    `findWaterSpot` 恒返回 null → 修复从未生效（v1.5.130 起存在）。**修复**：岸边格改为
    水面格**水平相邻 + 高 1 格**（y+1 的草地/泥土，下方实心、头顶空气）——自然河岸可命中；
  - 新增诊断日志：生成坐垫 / 找不到水域各打一条（找不到的限频 30 秒一条防刷屏），
    下次实测可直接从 latest.log 确认修复是否生效（搜 `auto-chair`）；
  - **世界存档导入修复（用户实测：`boulevardier-s-chief-s-house.zip` 导入报错）**：根因 =
    网上下载的世界 zip 几乎都带一层顶层目录（`<zip名>/<世界名>/level.dat`），解压后
    level.dat / region 在子层，而 `worldAnchor` 与 `.mca` 扫描只查**直接子文件** →
    "找不到锚点（playerdata/level.dat 玩家位置）" + "zip 内没有可识别的建筑文件" →
    误报导入失败。**修复**：`loadExternalFile` 解压后先 `findWorldRoot` 递归定位
    level.dat 所在目录为世界根；`extractFromWorldZip` 改用 `collectMca` 递归收集
    .mca——兼容根布局/单层/双层嵌套；锚点仍优先玩家最后位置、其次出生点；
  - **悬空方块强制补支撑（用户实测：女仆大量播报"悬空放不上，我跳过它啦"）**：日志实证
    ——ss 跳过 9 火把 / house-by-3a9z-3 跳过 162 活板门 / loft-home-beak 跳过 629
    （活板门+横幅）/ 甘蔗农场跳过 130 甘蔗；根因 = 外部蓝图作者在创造模式画的悬空方块，
    支撑格是**空气步骤/水/不合法方块**（甘蔗下方石头、活板门下是台阶）时旧版逻辑
    "蓝图有支撑步骤→延后等支撑"永远等不到（空气步骤/水步骤也是步骤）→ 3 次失败永久跳过；
    canSurvive 兜底也只补"下方空气/流体"，不合法方块不换 → 跳过。**修复**（MaidBuildBehavior）：
    ① doPlace 新增 `force` 模式——延后重试第 3 次起，支撑格空气/流体时**无视蓝图步骤直接
    补支撑**；② canSurvive 失败时**无条件把支撑格换成合法支撑**（甘蔗→沙子、植物→泥土、
    其他→石头）再重放，覆盖石头/水/台阶等不合法支撑；仍失败才跳过；
  - **复测**：① 女仆在水边无椅子 → 出现"我自己带了个坐垫"气泡、生成 EntityChair、
    走过去坐下开钓；② 日志出现 auto-chair 生成/未找到记录；③ 带顶层目录的世界 zip
    导入成功（锚点=出生点，±256 格提取建筑）；④ 无顶层目录的 zip 回归不受影响；
    ⑤ 普通 .schem/.nbt 文件导入回归不受影响；⑥ 悬空火把/活板门/甘蔗/横幅自动垫支撑
    建出（不再跳过），正常支撑的蓝图放置不受影响；⑦ 缺口补建/完成汇报正常。

- **promaid 1.5.252h：收官——手册大目录新增「详细介绍」阅读界面（全功能教程 + 术语 + 细则 + 242 版本更新日志）**
  - 用户最后一个任务：在大目录按格式添加"详细介绍"按钮——点进去是章节目录（类似建筑页面），
    每章以上一页/下一页翻页阅读；内容 = 项目全部功能 + 详细使用教程 + 专业术语解释 +
    最后几篇细则/运作逻辑 + 最后一篇 242 个版本更新日志（一笔带过，想看就看）；
  - **新增 GuideScreen（com.maidsmart.guide）**：章节目录页（分页按钮列表）+ 阅读页（正文自动换行分页，
    < 上一页 / 下一页 > + 页码），纯客户端 Screen；手册大目录新增第 5 个按钮"◆ 详细介绍"
    （§6 金色，170×28 与其余入口同格式）；
  - **新增 GuideContent**：19 个章节——① 开发者寄语（用户指定口吻：第 1 个 mod / 250+ 次测试 /
    不懂代码欢迎反馈）② 这是什么 ③ 建造教程 ④ 挖矿教程 ⑤ 烹饪酿造 ⑥ 战斗自保 ⑦ 贴身辅助
    ⑧ 单兵战术 ⑨ AI 记忆 ⑩ 主动对话播报 ⑪ 感知情绪 ⑫ AI 工具大全 ⑬ 语音系统 ⑭ 农场杂项
    ⑮ 专业术语解释 ⑯ 细则：药水与投喂规则 ⑰ 细则：建造运作逻辑 ⑱ 细则：记忆与对话成本
    ⑲ 更新日志（242 个版本）；
  - **更新日志章节**从打包进 jar 的 assets/promaid/guide/changelog.txt 读取——脚本从补丁说明.md
    提取全部 242 个版本条目（两种格式 `- **promaid 1.5.x` / `- **1.x.x`，去 markdown 符号），
    一行一条一笔带过；缺失/异常时显示提示行不崩溃；
  - **布局防重叠（用户要求逐项检查）**：手册大目录 5 按钮改高度自适应（行高按窗口压缩
    28/24/20/17/15 + 起始 y 上浮，按钮组底 ≤ h-44，不再压底部进度条 h-40）；
    GuideScreen 目录/阅读页全部按可用高度自适应（内容底 = h-56、页码 h-48、翻页按钮 h-30，
    层间间距 ≥ 8px 互不重叠）；正文逐字符换行（中英混排）+ 换行延续 § 颜色样式，
    每行右缘 ≤ w-12 不超屏；目录按钮文本/标题/版本行全走 fitText 截断；
  - 编译链自动发现新源文件（gen_compile.py 全量扫描），资源随 assets 目录整体打包；
  - **复测**：① 手册大目录 → 详细介绍 → 19 章目录（翻页正常）；② 各章节翻页阅读、页码正确；
    ③ 更新日志章节 242 行分页阅读；④ 小窗口（虚拟 427×240/220/190/180）按钮不压进度条、
    文字不出屏、无任何重叠；⑤ ESC / ← 返回 / ← 章节目录 均正常回到上级。

- **promaid 1.5.252h：首版复测修正（详细介绍按钮置顶 / 内容订正 / 伤害模式第 5 档修复 / 空页修复 / 更新日志不截断）**
  - 用户首版复测反馈 10 条，逐条处理：
  - **① 详细介绍按钮移到目录最上面**：大目录顺序改为 详细介绍 → 建造 → 女仆管理 →
    模组详细配置 → 女仆记忆（用户反馈"放最下面怪怪的"）；
  - **② 手册合成配方写进介绍**：蓝图手册可用工作台「书 + 纸」无序合成（配方
    data/maid_smart/recipes/blueprint_book.json 一直在，只是介绍里没写）——"这是什么"章节补充；
  - **③ 建筑任务等待行为订正**：切建筑任务女仆是**坐下等待命令**（v1.5.142 起强制坐下
    BUILD_SIT_TAG），介绍原来写"站住等待指令"是错的，已改（简介 + 建造教程两处）；
  - **④ 区块内右击进详情页写清楚**：建造教程新增"小技巧"——站在建造区块内右击手册
    直接进入该区块建造详情页（材料缺口/暂停/继续/速度/取消），区块外右击进大目录；
  - **⑤ 玩家对女仆伤害模式"货不对板"根治**：根因 = MaidSmartConfig defineInRange 上限
    写死 3（mixin 支持 0~4）——面板第 5 档"仅一点伤害"（值 4）保存不进去，看起来
    "选不了/不生效"。修复：defineInRange("playerDamageMode", 0, 0, 4)；
  - **⑥ 贴身辅助第 2 页空白**：根因 = buildLines 段落后追加的空行落在分页边界，末页只剩
    空白行。修复：去掉末尾空行；同时该章节补充 4 段实用说明（6 格触发距离/中毒牛奶蜂蜜/
    喷溅优先/缺货播报），内容更充实；
  - **⑦ 整理箱子已删干净**：代码零残留（ProMaidExtension 只注册 mine/cook/brew/build 四任务），
    手册"农场与杂项"里残留的"整理箱子"条目删除；
  - **⑧ 工头解释细化**：术语"工头"改为完整说明——一区块一个、玩家可在「区块名单」页手动指定、
    不指定则开始建造时自动从绑定女仆中随机选、建造状态/缺料/完成汇报统一由工头发送；
  - **⑨ 建造细则删过时"防窒息传送"**：v1.5.140 建造传送机制已整体删除（suffocateCheck 同删），
    细则里残留的"被埋传送到区外"删除；并按用户要求特此提醒——**最终的消息（完成/缺料/卡住）
    全部由工头汇报**（细则加"汇报归工头"条目）；
  - **⑩ 更新日志省略号**：原实现 fitText 截断加"…"，长版本号+标题被截成半句。改为与正文
    一致的自动换行（wrapText 完整显示），每条完整可见（242 条换行后约 400 行、约 31 页）；
  - **复测**：① 大目录第 1 个按钮是详细介绍；② 介绍第 1 章提到书+纸合成；③ 建筑任务女仆
    坐下等待；④ 区块内右击手册直达详情页；⑤ 面板伤害模式第 5 档"仅一点伤害"能保存生效
    （被打只掉 1 点）；⑥ 贴身辅助 1 页读完无空白页；⑦ 手册无"整理箱子"；⑧ 工头说明完整；
    ⑨ 细则无防窒息、有"汇报归工头"；⑩ 更新日志每句话完整显示。

- **promaid 1.5.252g15：治疗链只看血量（环境优先由分支顺序保证）**
  - 用户实测："低血跑到女仆面前不享受治疗"——根因是 g13/g14 给治疗链加的
    "情境跳过"条件（身上有抗火/水肺效果就不治）：抗火效果残留 2 分钟期间
    低血竟不治疗；
  - **修复（按用户修法）**：删除治疗链的额外跳过条件——**只看血量**（低于
    阈值就治疗）；"环境优先"靠分支顺序（g11 着火/溺水在治疗链前面）：环境
    处理成功则 aided 短路跳过治疗；环境没处理成功（没药水等）治疗兜底保命；
  - **复测**：① 一开始就低血跑过去 → 立刻喷治疗；② 女仆面前掉血 → 治疗；
    ③ 跳岩浆 → 先投抗火（环境优先），抗火没得投且低血 → 治疗兜底；
    ④ 泡水 → 先水肺；⑤ 情境药水效果期间低血 → 治疗照投（不再被效果挡）。

- **promaid 1.5.252g14：治疗链跳过条件改为"正处于危险情境中"（不再看效果残留）**
  - 实测反馈："抗火解除后低血，女仆不喷治疗了"——g13 治疗链用"身上有抗火/
    水肺效果"跳过治疗：爬出岩浆后抗火效果残留 2 分钟（滞留型 1/4 时长），
    期间低血竟不治疗；
  - **修复**：跳过条件改为【当前正处于危险情境】——着火中（m_6060_）或
    溺水憋气中（泡水且氧气 <60）才跳过治疗；脱离情境（不烧了/出水了）
    立即恢复治疗，不管身上效果残留多久；
  - 效果判定（hasEffectOn）只管"投不投情境药水"，与治疗链解耦；
  - **复测**：① 跳岩浆 → 只投 1 瓶抗火；② 爬出岩浆（效果还在）且低血 →
    女仆喷治疗；③ 泡水 → 只投 1 瓶水肺；④ 出水低血 → 治疗。

- **promaid 1.5.252g13：情境药水改用【效果判定】做正门（按用户规则重做）**
  - 用户澄清："一直扔的是同一种滞留抗火"——CD 记账没拦住；且用户规则：
    **"一旦主人身上有这种效果，投喂端其变体药水就都不需要投了"**；
  - **设计重构**：着火/溺水分支不再依赖 CD 记时间——正门改为
    `hasEffectOn(owner, 效果id)`（主人身上只要有抗火/水肺效果就不投，任何
    变体/形态都不需要；效果消失瞬间每 tick 检查立即重投，无空档）。CD
    只保留给治疗/再生/增益（无"身上效果"概念）场景；
  - **CD 记账提前**：throwPotionToOwner / giveDrinkablePotion 的 CD 记账移到
    气泡/日志【之前】——旧版在气泡之后，气泡若抛异常（catch 吞掉）药水已
    扔已消耗但 CD 没记 → 下一次又投（"一直扔同一种"的重大嫌疑）；
  - throw/give 增加 useCd 参数：着火/溺水传 false（效果判定在调用处），
    治疗/再生/增益传 true（CD 照旧）；
  - 治疗链：主人有抗火/水肺效果（只要有）时跳过治疗（情境效果在死不了，
    治疗纯浪费）；效果消失后恢复；
  - **复测**：① 跳岩浆 → 只扔 1 瓶抗火，效果期间（哪怕剩 1 秒）绝不再扔；
    ② 效果消失（还在岩浆）→ 立即补 1 瓶；③ 水肺同理；④ 日志
    "aid-owner throw: label=抗火" 只有 1 条/效果周期。

- **promaid 1.5.252g12：投掷间隔按女仆记 + 主人有效果不投 + 情境中跳过治疗**
  - 实测（只给一种滞留抗火，跳岩浆后"还在不停扔药水"）排查出三个真实问题：
  - **① aidCooldown 是共享 int**：多个女仆 tick 同一个行为实例，远处女仆
    也会无条件递减它——2 个女仆时 3 秒投掷间隔实际只剩 1.5 秒（女仆越多
    越频繁）→ 改为按女仆 UUID 记（Map，setAidCooldown）；
  - **② 投喂侧缺"主人已有效果不投"兜底**（自保侧有 hasFireResist，投喂侧
    没有）：新增 hasLongEffect（效果剩余 ≥30 秒视为有效）——着火分支
    抗火效果在就不投、溺水分支水肺效果在就不投（CD 只是保险，效果判定
    才是正门）；
  - **③ 情境中治疗链持续补治疗**：泡岩浆/泡水掉的血停在低值 → 情境药水
    CD 内，低血治疗链每 1.5~3 秒扔一瓶治疗（"解除完之后还在不停扔"扔的
    很可能就是治疗）→ 治疗链加条件：主人身上抗火/水肺效果还在（≥30 秒）
    时跳过治疗（情境效果在就死不了，治疗纯浪费）；效果消失后治疗链恢复；
  - **复测**：① 跳岩浆只扔 1 瓶抗火，效果期内不再扔任何药水；② 泡水只给
    1 瓶水肺；③ 日志确认（aid-owner throw 带 label）扔的是哪种药水；
    ④ 主人脱离情境且低血 → 治疗链照常。

- **promaid 1.5.252g11：投喂优先级重排——情境保命（着火/溺水）优先于低血治疗**
  - 实测发现：溺水时女仆只扔治疗、不扔水肺——根因是分支顺序：低血治疗链在
    水肺分支【前面】，溺水掉血后 hp 跌到阈值 → 治疗链先抢到（aided=true）
    → 水肺分支被短路跳过；治疗救不了溺水，纯浪费；
  - **重排触发链**：① 着火抗火 → ② 溺水水肺 → ③ 低血治疗链 → ④ 负面
    牛奶/蜂蜜 → ⑤ 低饱食喂食 → ⑥ 有威胁增益。情境保命（8 分钟 CD 内不
    重复投）优先，主人在情境中时不再被治疗"抢戏"；情境解除后普通低血照旧
    走治疗链；
  - **复测**：① 泡水憋气（氧气 <3 秒）且低血 → 先扔/塞水肺，不再先扔治疗；
    ② 跳岩浆且低血 → 先扔抗火；③ 普通战斗低血 → 照旧治疗链；④ 水肺 CD
    内溺水 → 不重复投（主人已有水肺效果）。

- **promaid 1.5.252g10：蜂蜜瓶归为药物（只在中毒喝，不当食物）**
  - 蜂蜜瓶既是食物也是药物——按药物对待：从 EmotionalActionExecutor.FOODS
    递食清单移除（旧版主人低饱食时可能被当食物喂掉，真中毒时反而没得喝）；
  - 保留的蜂蜜路径：女仆中毒自喝（drinkHoneyForPoison）、投喂主人中毒解除
    （MaidAidOwnerBehavior 负面分支）——全部不受影响；
  - 女仆自保吃食物（HEAL_FOODS）本来就不含蜂蜜 ✓；
  - **复测**：① 主人低饱食且背包有蜂蜜+熟食 → 只喂熟食，蜂蜜不动；
    ② 主人/女仆中毒 → 照常喝蜂蜜解中毒。

- **promaid 1.5.252g9：CD key 改为【效果种类】（变体不再各算一种）+ 投喂/自喝日志**
  - 实测发现（用户主动跳岩浆/进水测试）：g7 的 CD key = 药水注册名（含
    long_/strong_ 前缀和形态）——背包若有普通/延长/喷溅/滞留几种抗火，每种
    各是一个 key，女仆每 3 秒投一种，**6 瓶全扔完**；水肺同理；
  - **修复**：CD key 统一规范化为【效果种类】（去 long_/strong_ 前缀、形态
    不算区分）：同效果所有变体视为同一种——跳岩浆最多投 1 瓶抗火，8 分钟
    CD 内同效果全挡；自保侧同步（喝了 long_swiftness，swiftness 系不再喝）；
    迅捷/力量仍是不同 key（投了力量迅捷照投）；
  - **新增日志**（latest.log 搜关键词）：投喂投掷 "aid-owner throw"、
    塞背包 "aid-owner give"、自喝 "self drink"——以后直接在日志看到
    "扔了什么、给谁、当时血量"，排查浪费不用再靠目测；
  - **复测**：① 背包塞满普通/延长/喷溅/滞留抗火，跳岩浆 → 只投 1 瓶；
    ② 泡水溺水 → 只投/塞 1 瓶水肺；③ 日志出现 aid-owner throw/give、
    self drink 记录。

- **promaid 1.5.252g8：消灭药水浪费（饮用优先 / 喷溅优先 / 跳跃归情境）**
  - **① 自保增益药水优先喝饮用型**：喷溅/滞留型效果时长只有饮用型的 1/2~1/4
    （原版设计），旧版按槽位顺序取第一瓶，可能先"吃"滞留型（同样一瓶药水
    效果少一大截）→ 两遍扫描：先只找饮用型，没有才用喷溅/滞留兜底；
  - **② 跳跃药水（jump_boost）归情境药水**：被僵尸打不再喝跳跃（搭高/跑酷
    才有用），与水肺/夜视/抗火同类——情境判定列表补全；
  - **③ 投喂主人喷溅 > 滞留**：择优评分喷溅 +1（滞留扔给主人只是 30 秒云，
    主人移动中就吃不到，效果差一截）；
  - **复测**：① 背包同时有滞留迅捷+饮用迅捷 → 优先喝饮用型（8 分钟而非
    2 分钟）；只有滞留迅捷才兜底喝；② 被僵尸打背包有跳跃药水 → 不喝；
    ③ 投喂时背包有喷溅+滞留抗火 → 投喷溅。

- **promaid 1.5.252g7：药水 CD 改为【按药水种类】记（异种互不阻塞）**
  - 用户规则确认：同一种药水的 CD = 该药水持续时间，CD 内【同种不能再喝/投，
    但其他种照常】；瞬间治疗例外（短 CD 可连喝/连投）；投喂的 3 秒只是
    【投掷间隔】（连续两次投掷动作间隔），与种类 CD 相互独立；
  - **自保侧**：旧版单一全局 potionCooldown（喝一种药水后所有药水都不能喝，
    浪费）→ 改为 `potionCds` Map（key = 药水注册名，如
    minecraft:long_swiftness）：useBeneficialPotion / drinkPotionWithEffect /
    splashPotionWithEffect 内部自管 CD（= 该药水最长时长）；瞬间治疗
    useHealingPotion 短 CD 40 tick 可连喝；金苹果/蜂蜜固定短 CD；
  - **投喂侧**：CD key 从大类别（heal/regen/fire/water/buff）改为【药水注册名】
    ——投了力量 CD 内迅捷照投、投了抗火 CD 内水肺照投；throwPotionToOwner /
    giveDrinkablePotion 内部自管（同种 CD 内自动跳过）；治疗短 CD 60 tick；
    aidCooldown 3 秒保持为投掷间隔；
  - 情境判定不变：水肺/夜视/抗火仍只在对应情境由专项分支喝（g4/g5）；
  - **复测**：① 背包有迅捷+力量，被僵尸打 → 先投其一，另一瓶照投（只隔 3 秒
    投掷间隔），同种 CD 内不再投；② 喝了 long_swiftness → 8 分钟内同种不再
    喝，但再生/治疗照喝；③ 溺水喝水肺后 CD 内不再喝水肺；④ 治疗药水可 3 秒
    内重复喝/投；⑤ 着火/溺水/负面分支照旧。

- **promaid 1.5.252g6：塞给主人的物品优先放快捷栏空位**
  - 新增 `giveToOwnerHotbarFirst`：先扫主人快捷栏 0-8 的空位（顺手堆叠同
    类），快捷栏没空位再走常规背包插入——药水/牛奶/蜂蜜/食物三条投喂链
    （giveDrinkablePotion / giveItemToOwner / 喂食）统一生效；
  - 原因：旧版 insertItemStacked 从背包头扫起，可能塞进主背包深处，主人
    一眼找不到；快捷栏优先 = 投喂物直接出现在眼前，马上能用；
  - **复测**：快捷栏有空位 → 药水出现在快捷栏；快捷栏全满 → 才进主背包；
    背包全满 → 照旧退回并提示"背包满了"。

- **promaid 1.5.252g5：抗火药水也归为情境药水（不通用乱喝）**
  - 自保 `useBeneficialPotion` 的增益判定在 g4 排除水肺/夜视基础上，再加
    **抗火（fire_resistance）**——抗火只在着火时由专项分支喝（着火喝抗火
    链路照旧），被僵尸打时不再浪费抗火药水；
  - 投喂主人侧不受影响（着火才投抗火、溺水才投水肺，分支本身按情境触发）；
  - **复测**：被僵尸打 → 喝迅捷/力量，不喝抗火/水肺/夜视；着火 → 仍喝抗火。

- **promaid 1.5.252g4：投喂药水形态修正 + 情境药水不乱喝**
  - **① 投掷 vs 塞背包（明确规则）**：投掷【只选喷溅/滞留型】——饮用型扔出去
    原版只是碎瓶子不施加效果（旧版把饮用型也扔了，实测"瓶装型也被扔"）；
    饮用型只能塞主人背包（主人自己喝）；投掷优先（命中立即生效），背包没
    喷溅/滞留才塞饮用型；
  - **② 情境药水不乱喝**：通用增益判定排除**水肺/夜视**（情境药水——溺水时由
    专项逻辑喝）——旧版一切 BENEFICIAL 效果都当增益喝，实测"没溺水被僵尸打
    却在喝水肺"；战斗增益（迅捷/力量/抗火/再生/隐身等）照常喝；
  - **③ 投喂药水按类别 CD（= 药水时长）**：heal/regen/fire/water/buff 各自记
    冷却——治疗瞬间短 CD（60 tick），抗火/水肺/增益 CD = 药水时长，期间不
    重复投喂同类（防浪费）；
  - **复测**：① 女仆背包只有饮用型治疗药水 → 塞给主人；有喷溅型 → 扔追踪弹；
    ② 被僵尸打时背包有迅捷/力量 → 喝增益，有水肺/夜视 → **不喝**；③ 溺水时
    才喝水肺；④ 抗火/增益投喂后同类型不再重复给。

- **promaid 1.5.252g2：女仆投喂主人的药水补全（水肺/增益）**
  - 主人溺水（泡水且氧气 <1/5）→ 女仆扔/塞**水肺药水**；
  - 主人 16 格内有敌对生物且身上没有力量/迅捷 → 扔/塞**增益药水**（迅捷/力量
    择优：长效/强效优先）助战；
  - 原有：低血治疗链（治疗/金苹果/再生/喂食）、着火抗火、负面牛奶/蜂蜜——全部
    保留；投喂 3 秒冷却不变；
  - **复测**：① 主人泡水快窒息 → 女仆扔水肺药水（追踪弹命中）；② 主人被僵尸
    围殴 → 女仆扔/塞力量或迅捷药水；③ 着火/中毒/低血照旧。

- **promaid 1.5.252g：情境药水系统（溺水喝水肺 / 中毒喝蜂蜜 / 增益 CD=效果时长）**
  - **① 溺水喝水肺**：快窒息（水中氧气 <1/5）时尝试喝水肺药水——饮用型直接喝，
    喷溅/滞留型生成范围云（与抗火云同机制）；水肺生效后氧气恢复自然脱离；
  - **② 中毒喝蜂蜜**：中毒（hasEffect POISON）时喝蜂蜜瓶——原版蜂蜜清除中毒 +
    回饥饿（模拟：清除中毒 + 回 1.5 心），喝完留玻璃瓶；短 CD 可重复；
  - **③ CD 规则（所有药水）**：**除瞬间恢复外，其他药水 CD = 该药水最长效果
    时长**（上限 10 分钟防呆）——增益药水（迅捷/力量/再生/隐身等）喝一次管
    整个持续时间，不再每 2 秒一次；瞬间恢复（治疗）短 CD（40 tick）可连续喝；
  - **复测**：① 女仆泡水里快窒息 → 有水瓶肺药水就喝（气泡"溺水了"后不再掉血）；
    ② 被毒蛛/剧毒药水弄中毒 → 喝蜂蜜清除；③ 低血有威胁 → 喝迅捷/力量药水后
    冷却 = 药水时长（日志/行为间隔验证）；④ 治疗药水仍可短 CD 连喝。

- **promaid 1.5.252f：工作卡住播报降频（20 秒才说一次）**
  - "熔炉明明就在附近/附近没有熔炉/没镐/附近没矿/酿造台明明就在附近"等卡住
    播报（WorkStatusReporter）旧版每 DIALOGUE_REPORT_INTERVAL 秒就弹一次，
    持续卡住时刷屏；
  - 现改为：**脱离工作状态（卡住）持续满 20 秒（400 tick）才播报第一次**，
    之后每 20 秒最多一句；恢复正常（找到工作/正在干活）立即清除计时——
    烹饪、酿造、挖矿同一处逻辑全部生效；
  - **复测**：让女仆站在熔炉旁切"烹饪"任务但背包无食材 → 前 20 秒安静，
    之后才出现"熔炉明明就在附近…"；给她食材后恢复正常不再播报。

- **promaid 1.5.252e：去掉冶炼，回归纯烹饪**
  - 功能名改回"烹饪"（任务名/语言文件/动作摘要），类注释同步；
  - 冶炼相关已随 v1.5.252d 回退移除（无残留代码），本轮只清理名称；
  - 烹饪现状：任意熔炉类方块 + 食材白名单 + **燃料=数量最多的可燃烧物** +
    坐下站桩；酿造台全自动保留（可关自动下料配合 LLM 指令）。

- **promaid 1.5.252d：回退复杂化，回归简洁**
  - **① 烹饪和冶炼回归最初形态**：撤销手册两页（烹饪和冶炼/酿造）、per-maid
    设置、炉子类型选择（烟熏/高炉/熔炉）、烧制范围/燃料偏好几档、炉子独占——
    任意熔炉类方块即可，食材用内置白名单，补燃料/收成品/坐下站桩照旧；
  - **② 保留燃料修正**：**燃料不限于煤炭——凡是可燃烧物品（原版 isFuel）都
    可用，优先用背包中数量最多的那个**（32 木板 > 3 煤时烧木板）；
  - **③ 酿造保留 + 配合 LLM**：酿造台全自动两阶段酿药保留（坐下站桩照旧），
    设置"自动酿造"可关（关 = 只补烈焰粉+收成品，不主动下料——目标药水由
    LLM 指令/玩家指定）；不再有手册酿造页与 per-maid 开关；
  - **复测**：① 生肉+木板 → 女仆补木板作燃料（数量最多）② 锁定炉子坐下不动
    ③ 酿造台自动酿药/关闭后只维持。

- **promaid 1.5.252c：烹饪/酿造按女仆绑定调控 + 炉子/酿造台独占**（v1.5.252d
  已回退：功能过于复杂，用户决定回归简洁）
  - **① 按女仆精细调控（像建造绑定女仆）**：手册"烹饪和冶炼"/"酿造"页顶部新增
    **"操作女仆：<名字>"** 循环按钮——在女仆间切换，每只女仆的
    （炉子偏好/烧制范围/燃料偏好/自动酿造）**独立存储**（女仆 persistentData，
    未设置回落全局配置）；切换时向服务端查询该女仆当前值，修改时整组写回
    （新增双向网络包 CookSettingsPacket）；
  - **② 炉子/酿造台独占（一仆一炉/一台）**：服务端登记"方块位置 → 占用女仆"，
    其他女仆扫描炉子/酿造台时**跳过被占用的**；女仆停止任务/方块丢失/离开世界
    （EntityLeaveLevelEvent）/死亡时自动释放；
  - **③ 行为联动**：烹饪行为读 per-maid 三项设置；酿造行为读 per-maid 自动酿造；
    全局配置仍是默认值（未单独设置的用全局）；
  - **复测**：① 手册 → 烹饪和冶炼页 → "操作女仆"切换 A/B → 给 A 设"只用熔炉"、
    B 设"优先烟熏炉" → 两只女仆按各自设置工作；② 两只女仆同时烹饪 → 各自锁定
    不同炉子（距离内只有一个炉子时另一只站桩等待，不抢）；③ 酿造台同理；
    ④ 切走任务/女仆离开 → 炉子释放，另一只可用。

- **promaid 1.5.252b：手册新增"烹饪和冶炼"/"酿造"调控两页**
  - **① 手册两页**（大目录新增"烹饪和冶炼"和"酿造"入口，像建筑一样精细调控）：
    - **烹饪和冶炼页**：炉子偏好循环（自动按背包内容 / 优先烟熏炉 / 优先高炉 /
      只用熔炉）· 烧制范围循环（自动按炉子类型 / 只烧食物 / 只冶炼 / 食物+冶炼）·
      燃料偏好循环（数量最多可燃烧物 / 煤优先 / 木炭优先 / 煤或木炭优先）——解决
      "背包有多种食材怎么办"：可固定只用某类炉子、限定只烧食物或只冶炼；
    - **酿造页**：自动酿造开/关（关 = 只维持：补烈焰粉+收成品，不主动下料，由
      玩家/LLM 指令决定酿什么）；
    - 说明文字显示当前配置效果；深度白名单（具体物品 id）指引到"模组详细配置"；
  - **② 行为联动**：烹饪行为读取炉子偏好/烧制范围/燃料偏好三项配置；酿造行为
    读取自动酿造开关（关闭时跳过补水瓶/下料步骤）；
  - **复测**：① 手册大目录 → 烹饪和冶炼页 → 循环切换"只用熔炉" → 女仆烧制只用
    熔炉；"只冶炼" → 不再烧食物；② 酿造页关掉自动 → 女仆只补烈焰粉和收成品、
    不再自己下料；③ 设置面板"杂项"可见四项新配置。

- **promaid 1.5.252：烹饪和冶炼重写（烟熏/高炉/熔炉 + 燃料修正 + 坐下绑定）+ 酿造台坐下 + 灵魂核心移除补全**
  - **① 烹饪 → 烹饪和冶炼（改名）**：任务名/语言文件改为"烹饪和冶炼"；
  - **② 炉子类型选择**：锁定炉子时按【背包内容】决定——
    - 背包有可烹饪食物 → 优先找**烟熏炉**（Smoker）；
    - 有可冶炼物（原矿/铁金铜/锁链甲，id 特征判定）→ 优先找**高炉**（BlastFurnace）；
    - 都没有或找不到对应炉子 → **熔炉**；三者全无 → 任意炉子；
    - 补原料按炉子类型：烟熏炉只放食物、高炉只放可冶炼物、熔炉两者都收；
  - **③ 燃料修正**：不再只认煤/木炭——**凡是可燃烧物品（原版 isFuel）都可用，
    选背包中数量最多的那个**作燃料（如 32 个木板 > 3 块煤时烧木板）；
  - **④ 坐下绑定**：锁定炉子/酿造台并到达后**立刻坐下不动**（坐姿 + 站桩，
    每 tick 保持防状态机重置；行为停止/方块丢失恢复站立），与建筑一致；
  - **⑤ 灵魂核心移除补全**：物品/配方/开局赠送/绑定/迁移全部删除（v1.5.251e
    已做），本版确认无残留引用；旧绑定女仆记忆路由保留兼容；
  - **⑥ 手册附魔光效**：isFoil 覆写 + `foil check` 节流日志（验证渲染调用链）；
  - **复测**：① 给女仆生牛肉+煤炭 → 工作"烹饪和冶炼" → 附近有烟熏炉用烟熏炉，
    只给铁矿石+煤 → 用高炉；② 背包放 32 木板 + 3 煤 → 燃料用木板；③ 到炉子
    旁坐下不动；④ 手册背包/手上看附魔流光 + 日志 `foil check`；⑤ 创造/生存
    不再有灵魂核心。

- **promaid 1.5.251e：灵魂核心功能移除 + 手册附魔光效验证**
  - **① 灵魂核心整体移除**（制作成本高，暂不考虑跨存档）：物品/合成配方/创造
    页/开局赠送/绑定交互/跨存档迁移/双分身防护全部删除——手册、配置、记忆
    功能不受影响；
  - **② 数据兼容**：旧版本已绑定过灵魂的女仆（persistentData 残留
    maid_smart_soul_id）记忆**继续读写全局灵魂目录**（storeFor 路由保留），已
    写入的记忆不丢；未绑定女仆照旧走世界目录；
  - **③ 手册附魔光效**：isFoil 覆写保留 + **新增 10 秒节流日志**（latest.log 搜
    `foil check`）——若渲染端真的调用了 isFoil 但画面仍无光效，日志会证明
    调用链已通（排除"代码没生效"，剩下就是渲染层问题）；
  - **复测**：① 创造/生存都不再有灵魂核心（配方消失）；② 已绑定女仆的记忆
    照常可查；③ 手册在背包/手上应有附魔流光，日志出现 `foil check`。

- **promaid 1.5.251d：灵魂核心键位冲突修复 + 附魔光效 + 手册改蓝色**
  - **① 键位冲突（核心）**：TLM 原版"潜行右键女仆 = 坐下/站起"会抢占右键交互，
    物品的 interactLivingEntity 根本触发不到——**拿着灵魂核心潜行右键没反应**
    （v1.5.250 的跨存档入口实际不可用）。全部交互改走
    **`PlayerInteractEvent.EntityInteract` 事件**（在物品交互之前触发）：检测
    【主手持有灵魂核心 + 右键女仆】→ **取消原版处理**（坐下/打开配置界面）→
    执行灵魂操作（普通右键绑新/看信息，潜行右键绑旧魂/解绑）——三条件齐备
    才能触发，不再被原版指令抢走；
  - **② 附魔光效**：灵魂核心 + Promaid 手册覆写 `isFoil` 恒返回 true——
    两个物品**始终显示原版附魔那种流动光效**，一眼就是魔法物品；
  - **③ 手册改蓝色**：蓝图手册纹理重新调色——鲜明**蓝色**（原灰蓝知识之书
    饱和度拉高 + 色相校正，一眼区分）；
  - **复测**：① 手持灵魂核心潜行右键女仆 → 女仆【不会坐下】而是执行灵魂操作
    （绑定旧魂/解绑）；② 普通右键 → 绑定/看信息；③ 两个物品都有附魔流光；
    ④ 手册图标是蓝色。

- **promaid 1.5.251c：同存档双分身防护（一魂一仆）**
  - **绑定拒绝**：`bind()` 前检查该灵魂是否已被**当前世界另一只存活女仆**绑定
    → 拒绝并提示"该灵魂已被另一只女仆绑定（同一世界只能有一个分身），请先对
    那只女仆潜行右键解除绑定"（跨存档场景不受影响——检查范围 = 当前世界已加载
    实体）；
  - **加载兜底**：女仆加载（EntityJoin）时若发现世界已有另一只女仆绑定同一
    灵魂（魂符场景：甲绑 X → 收进魂符 → 乙绑 X → 放出甲）→ **自动解除新加载
    这只的绑定** + 提示主人先解绑那只再重绑；
  - **复测**：① 甲绑定灵魂 X 后，对乙潜行右键核心 → 提示"已被另一只女仆绑定"
    拒绝；② 甲收进魂符 → 乙绑 X 成功 → 放出甲 → 甲自动解绑并收到提示。

- **promaid 1.5.251b：记忆开关加固（磁盘优先 + 未加载女仆可关）**
  - 排查"手册记忆开关关不掉"：日志实证 `memory toggle enabled=false` 多次执行
    但 `memory display enabled=true`——**该次测试跑的是 v1.5.249（getBoolean
    修复前的版本，服务端 isEnabled 恒 true）**，v1.5.250 的修复尚未被复测；
  - **双保险加固**（不再依赖单一存储）：
    - 服务端 `isEnabled` 改为**磁盘（DISK_TOGGLES/toggles.json）优先**——
      Toggle 结果立即生效，不受实体 persistentData 状态影响；
    - **实体未加载也能关**：Toggle 找不到女仆（远处/瞬移卸载）时改走
      `setEnabledDiskOnly` 写磁盘 + 反馈"女仆不在附近（未加载），记忆开关已记录，
      她加载后生效"——isEnabled 磁盘优先保证她加载后立即读到新值；
  - **复测**：进游戏 → 手册 → 女仆记忆页 → 点"记忆:开" → 按钮变"记忆:关"、
    点女仆名查看显示"记忆关闭"；日志搜 `memory toggle` 一次 false 后
    `memory display` 为 false；关掉后对话不再积累新记忆（段落数不再涨）。

- **promaid 1.5.251：物品纹理换色 + 记忆世界/时间标注 + 灵魂共享存储（双向同步/克隆继承）**
  - **① 物品色调整改**：灵魂核心不再借用原版末影之眼（绿紫调）——生成专属
    纹理（**蓝色调**）；蓝图手册不再借用知识之书（灰蓝）——专属**紫色调**。
    两者与原版物品一眼可区分（模型改指 `maid_smart:item/...`）；
  - **② 记忆标注来源世界 + 获得时间**：所有记忆段落写入时自动加
    `world:<维度名>` 标签（AiMemoryStore 统一注入，无需改各写入点）+ 记录时间戳。
    显示端（手册记忆页 / 女仆配置界面）每条记忆带 **[来源世界] [获得时间]**
    （如 `[overworld] 08-11 14:30`）；
  - **③ 灵魂共享存储（核心架构）**：绑定灵魂的女仆记忆/情绪/关系/画像/片段/
    工作笔记全部读写**全局灵魂目录** `config/maid_smart/souls/<soulId>/memory/`
    （原世界目录仅未绑定女仆使用）：
    - **克隆继承**：新绑定女仆直接读同一目录 = 继承全部记忆 + 属性 + 既有关系；
    - **双向同步**：存档 A 的增长（新记忆/关系变化）自动反映到存档 B，回到 A
      依然在——两边是同一份数据，不是复制两份；
    - **物品栏平衡**：物品/装备不迁移（跨存档新女仆本来就从零开始），只带属性；
    - **进入新存档仪式**：迁移/导入成功自动写入"我跟随主人的灵魂来到了一个新
      的世界：<世界名>"（写入灵魂共享目录，所有世界分身可见）；
    - 旧世界目录记忆自动导入（灵魂目录空时），旧数据改名备份防重复导入；
    - 所有服务端读写点统一走 `SoulBindingService.storeFor(maid, level)`；
      客户端配置界面显示按 SyncPacket 缓存的 soulId 路由到灵魂目录；
  - **④ 详细操作指南**：新增《操作指南.md》（灵魂核心全流程/记忆系统/语音包/
    主动对话/常见问题），已部署到 `config/maid_smart/操作指南.md`；
  - **复测**：① 旧存档绑定女仆 → 新存档潜行绑定 → 手册记忆页看到全部旧记忆 +
    "[overworld] 时间"标注 + "进入新世界"条目；② 在新存档聊天产生新记忆 →
    回旧存档 → 新记忆仍在（双向同步）；③ 女仆配置界面显示记忆带世界/时间；
    ④ 灵魂核心/手册图标颜色与原版明显不同；⑤ 操作指南在 config 目录可看。

- **promaid 1.5.250：三处 SRG 错用根因修复（放水 / 记忆开关 / 记忆目录）**
  - **① 放水根因（本轮定案）**：`isNether()` 用 `Level.f_46428_` 当 NETHER 判定——
    **f_46428_ 实际是 OVERWORLD**（"overworld"），NETHER 是 `f_46429_`（"the_nether"）。
    判定完全反了：**主世界女仆被当成"地狱"** → 水桶步（`fireBucketStep` 里的
    `!isNether`）与找水坑（`findWater`）永远跳过 → "包里明明有水桶却不用水"、
    "着火不跳入水坑"、新存档放水问题的**共同根因**。已改 `f_46429_`；
  - **② 记忆开关根因（本轮定案）**：`CompoundTag.m_128441_(String)` 是
    **containsKey**（键是否存在），不是 getBoolean！`AiMemoryManager.isEnabled` 里
    `pd.m_128441_(PERSIST_TAG)` 在 putBoolean 后**恒返回 true** → 服务端 isEnabled
    恒 true → 打开界面 Query 回包永远 Sync(true) → 客户端 CLIENT_STATE 被覆盖回
    "开"（"关了又自己打开"的**真根因**；v1.5.226/242/249 修的都是显示层，底层
    读值一直错的）。已改 `m_128435_(tag) != 0`（getByte != 0 = getBoolean 等价）；
    同类修复：`BuildArchive` 迁移路径读 "paused" 也用了 containsKey（建筑暂停
    状态读取错），一并改掉；
  - **③ 记忆目录 SRG 修复 + 数据迁移**：`memoryRoot` 用 `LevelResource.f_78174_`
    当世界根目录——**f_78174_ 实际是 "advancements"**（ROOT 是 f_78182_ = "."），
    记忆数据全写到了 `<世界>/advancements/promaid_memory`（读写一致能工作但位置
    错误）。已改 f_78182_，首次启动自动把旧目录搬到 `<世界>/promaid_memory`，
    搬不动回退旧位置（数据不丢）；
  - **④ 下界也尝试用水桶**：既然 ① 的维度判定一直反着，旧"地狱放水瞬间蒸发 →
    跳过"逻辑从未在真正下界生效过。现在下界女仆**每 40 tick（2 秒）尝试倒一次水**
    （放置动作/蒸汽粒子可见，满足"喜欢用水桶"的预期），蒸发后流转珍珠/抗火/逃跑，
    不设 waterPos 防止每 tick 傻倒水刷屏；
  - **⑤ 诊断加维度**：`lava/fire escape resources` 日志新增 `dim=` 字段，之后
    复测一眼看出女仆所在维度；
  - **⑥ 盾牌共享逻辑修正（"没拿盾却收到盾"根因）**：`MaidShieldShareBehavior`
    旧触发条件 = "主人盾耐久低【或为空】"——**把"主人没拿盾"也当"需要补盾"**，
    女仆会把背包里的盾（多半是拾取怪物掉落的低耐久盾：僵尸/骷髅 8.5% 持盾）
    硬塞给没用盾的主人（副手空塞副手、否则塞背包）；塞完主人手里就有低耐久盾，
    下轮又触发"耐久低→再塞"，恶性循环。修复：
    - **主人没拿盾（主手/副手都不是盾）→ 不补**——只有正拿着盾且耐久 ≤50% 才换；
    - 剩余耐久 **≥30%** 才有资格给（烂盾留女仆自己用，不污染主人背包）；
  - **⑦ 灵魂核心补全（跨存档记忆终于可达）**：此前灵魂核心只有"新建灵魂绑定"
    入口——新存档召唤的女仆没有 soulId，旧记忆永远迁不过来（`bind()` 无调用者，
    "跨存档记忆在哪里触发"——根本触发不了）：
    - **潜行右键未绑定女仆 = 绑定到主人名下最近使用的旧灵魂**（跨存档迁移入口，
      bind() 检测到旧档案后自动复制旧女仆记忆目录）；
    - 物品悬浮提示补全三条用法说明（右键绑定 / 潜行右键迁旧魂 / 潜行右键已绑定
      解绑）；
    - 新增合成配方：回响碎片 + 8 金锭（生存可获取，不再只能创造拿）；
  - **⑧ 删除击退搭高垫水**：`triggerPillarBurst` 里的水桶垫水缓冲（v1.5.199 加，
    配置 `COMBAT_WATER_BUCKET_PILLAR` + 面板"击退搭高垫水"行一并删除）——脚下
    放水+收回的收放循环严重拖慢向上搭高节奏；
  - **⑨ 移除已废除的施工区避让**：配置 `COMBAT_BUILD_AREA_AVOID` /
    `COMBAT_BUILD_AREA_SCAN` 与其面板两行（"施工区避让"/"避让扫描间隔"）删除
    ——功能代码早已废除，只剩面板残留；
  - **⑩ 每日主动对话上限 + 超限系统提示**：AI 工具设置新增"每日主动对话上限
    （次/女仆）"（复用 dialogue.proactiveDaily，默认 12；主动对话区原有同配置，
    两处改同一个值）；女仆当天主动发言达到上限后不再开口，并**给主人发系统消息
    "今天的主动对话已达上限（12 次），明天再聊吧"**——每女仆每游戏日只提示一次，
    防扫描循环刷屏（旧版超限是静默 return，没有任何提示）；
  - **⑪ 中日文处理软化：审查打回 → 内嵌翻译**：旧版 LLM 回复语言不符时（目标
    中文但回复汉字占比 <40%/假名占比 ≥10%）→ 丢弃回复 + 追加强化提示词**重新
    生成**（最多 2 次，超限显示"已放弃"——烧 token、打断对话流、体验生硬）。
    新版：语言不符时**把原文交给 LLM 翻译成目标语言**（一条 system 翻译指令 +
    原文），译文直接作为最终回复显示——不重刷、不打断、不丢内容；译文仍非中文
    （极少见）→ 放行原文，不再循环；日志搜 `lang translate` 可看原文与译文；
  - **⑫ 灵魂核心开局赠送**：合成成本高（回响碎片+8 金锭）但跨存档延续女仆是
    核心体验——玩家每次进入**新存档**自动送一个灵魂核心（标记写玩家
    persistentData，每存档一次，重进不重复给；背包满则掉在脚边），系统消息
    附用法说明（右键绑定 / 潜行右键迁移旧灵魂）；
  - **⑬ 系统语音包骨架（21 条女仆气泡）**：收集全部女仆规则气泡文本
    （自保/辅助/建造/钓鱼/灵魂迁移等完整句，过滤拼接碎片与系统消息）生成
    `config/maid_smart/system_voice/manifest.json`（text → voice_XXX.ogg 映射）
    已随本版部署——命中即免 TTS 直接播放；音频 .ogg 需用你的语音模型按
    `voice_script.txt` 制作清单生成（同目录有清单文件），放回同目录或打包 zip
    用面板按钮导入即可，无需改代码；
  - **⑭ 语音包文件选择导入按钮**：设置面板·语音页新增"选择文件导入"按钮——
    打开系统文件选择框选 .zip 自动导入（原"导入路径"手填方式保留）；
  - **⑮ 灵魂核心仪式感：进入新存档记录进记忆**：跨存档迁移成功（自动迁移 /
    潜行右键手动迁移均覆盖）时，往女仆记忆写入一条高重要度事件：
    "我跟随主人的灵魂来到了一个新的世界：<世界名>"（永久段，每新存档一条）——
    女仆会记得自己来到过每一个世界；
  - **复测**：① 主世界给女仆水桶 + 着火/掉岩浆 → 日志搜 `placeClutchWater ok`
    （放水成功，不再"没招了"）；② 点记忆"关" → 界面/手册显示关且保持（日志
    `memory toggle enabled=false` 只出现一次，不再反复）；③ 记忆文件位置
    `<世界>/promaid_memory/`（不再是 advancements 下）；④ 跨存档：旧存档女仆
    右键灵魂核心绑定 → **新开存档**→ 开局收到系统消息"灵魂核心开局赠送"+ 背包
    里有一个 → 召唤新女仆 → **潜行右键**灵魂核心 → 提示"已绑定灵魂 X，并迁移了
    她的记忆！"→ 手册记忆页能看到"我跟随主人的灵魂来到了一个新的世界"；
    ⑤ 女仆背包只有烂盾时不再给主人盾；⑥ 设置面板不再有"击退搭高垫水/施工区
    避让"，连击搭高不再放水（速度恢复）；⑦ 主动对话日上限调小（如 2）验证：
    女仆第 3 次想主动开口时系统消息提示"已达上限"，当天不再提示第二次；
    ⑧ 让女仆说日语（对话中说"用日语回答"）→ 日志搜 `lang translate` 出现，
    聊天气泡显示的是翻译后的中文；⑨ 语音页点"选择文件导入"弹系统文件框；
    ⑩ 生成 1-2 条 ogg 放 system_voice/ → 面板"重新加载" → 触发对应气泡时播放
    语音包音频（不再每次调 TTS）。

- **promaid 1.5.249：记忆开关"关了又开"根因修复 + 放水双向诊断**
  - **① 记忆开关根因**（日志实证 08:18 七秒内 4 次 `memory toggle enabled=false`）：
    `AiMemoryTogglePacket.handle` 用 `TRACKING_ENTITY` 广播 SyncPacket——**只发给
    "正在追踪该女仆"的玩家**；玩家在女仆配置界面/离女仆远时收不到 → 客户端
    `CLIENT_STATE` 不更新 → 界面显示旧值"开" → 用户再点又发 false（"关了又自己
    打开"的直接体感）。修复：SyncPacket **双发**（`PLAYER.with(操作玩家)` 必达 +
    `TRACKING_ENTITY` 追踪玩家）；
  - **② 放水诊断加强**（"有水桶却放不出水"仍无解，日志里 `fire escape resources`
    有、`placeClutchWater fail/error` 没有）：新增
    `placeClutchWater ok:`（成功，10 秒节流）与
    `fire bucket step: placeClutchWater returned, waterPos=...`（调用侧状态）——
    区分"内部静默失败"vs"成功但没生效"；
  - **复测**：① 点记忆"关" → 界面/手册即时显示关且保持（不再反复 toggle）；
    ② 着火/掉岩浆 → 日志搜 `placeClutchWater ok`（成功）或 `fail/error`（失败原因）。

- **promaid 1.5.248：农场"走一步停一步"修复（批量种植/连锁收割性能优化）**
  - 根因：每次 MaidFarmPlantTask.start 都**同步执行全量 BFS**（批量种植 96 格 +
    连锁收割 96 格），且**每格 plantBack 重扫整个背包**（getAvailableInv + 全槽
    找种子）——大农田（limit 96）时服务端 tick 被大量重复计算阻塞 → 女仆移动
    卡顿，表现为"古早版走一步停一步"；
  - 修复：
    - `batchPlantAround` 加 **40 tick（2 秒）冷却** + **背包一次缓存复用**
      （新增 `plantWith`：给定背包找种子种植，不再每格重扫）；
    - `sweepChain`（连锁收割）加 **10 tick 冷却** + 背包复用补种；
    - `plantBack` 改为复用 `plantWith`（收割补种同源）；
  - **复测**：大农田执行农场任务 → 女仆正常流畅移动（与空闲/跟随一致），批量
    种植/连锁收割仍生效（2 秒/0.5 秒一批，多轮清完）。

- **promaid 1.5.247：修正"包里什么都没有"误导文本**
  - 根因：`announceNoResource` 的"包里什么都没有，我没招了！救救我！"是**误导性
    表述**——实际含义是"本场自救手段（垫块/水桶/珍珠/抗火/金苹果）全部失败"，
    不代表背包真空（女仆包里明明有水桶/草方块也会这么说）；且**着火链路不用草
    方块**（垫块只属于岩浆链路，着火时垫起来没用）；
  - 修复：文本改为"**自救手段都用尽了，我没招了！救救我！**"；
  - **放水失败排查中**：日志实证 hasWaterBucket=true（水桶判定正常）+ 配置
    waterBucketLava=true，但 placeClutchWater 放水后 waterPos 仍 null——v1.5.246
    诊断版已部署（`placeClutchWater fail/error` / `fire bucket step skipped` 日志），
    复测后把相关日志发我可定位；
  - **复测**：着火/掉岩浆自保失败时，喊话为"自救手段都用尽了"，不再说"包里
    什么都没有"。

- **promaid 1.5.246：放水失败定位诊断（有水桶却放不出水的排查版）**
  - 日志实证（新存档 08:02）：`fire escape resources: bucket(getKey)=true` 且
    配置 `waterBucketLava=true`——**水桶判定和配置都正常，但 `placeClutchWater`
    放水动作失败（waterPos 仍为 null）**导致着火自救链断裂；
  - 已排查：`Blocks.f_49990_`（水）字段在 srg jar 存在、编译产物 getstatic 正确
    （非 NoSuchFieldError）；`hasWaterBucket` getKey 判定正常；
  - 本版新增 3 处诊断（下次测试日志直接定位失败点）：
    - `placeClutchWater fail:` 候选格（上1/自身/上2）的 canPourWaterAt 状态
    - `placeClutchWater error:` 放水抛出的异常（不再静默吞）
    - `fire bucket step skipped:` 没进水桶步时 配置/水桶判定 哪个没满足
  - **复测**：着火/掉岩浆 → 女仆应放水；若仍不放，latest.log 搜
    `placeClutchWater fail/error` 或 `fire bucket step skipped`，把日志发我即可定位。

- **promaid 1.5.245：自保会话进出防抖 + 高血量着火不喊救命（日志审查修正）**
  - 日志审查（07:00-07:24）确认：战术（230）跳跃攻击/冲刺暴击正常、辅助行为
    （投喂/火把/盾牌）diag 正常、brain-run 链路完整——战斗逻辑符合设计；
    发现两个自保问题：
  - **① 会话进出抖动**：07:22 大正女仆酒狐 hp=100%→95% 间隔 0.5 秒两次
    `self preserve start`——danger 短暂消失（浅岩浆接触/移动卡墙抖动）就退出、
    下一 tick 又进入。修复：退出加 **20 tick（1 秒）稳定窗口**（满足退出条件后
    先观察 1 秒，期间不退出），消除频繁 start；
  - **② 高血量着火喊"我没招了"**：hp=95%/75% 掉岩浆爬出后 0.1 秒喊救命——
    火约 15 秒自然灭顶多掉 15 血，100% 血不会死。修复：着火链路全失败处加
    **血量门槛（≥60% 不喊救命、不设让位窗口）**——安静朝主人走/反击自救，
    火自然灭；岩浆链路（每秒 4 血）保持必喊；
  - **复测**：① 岩浆边反复进出不再频繁 start；② 高血量着火 → 安静自救不喊话，
    低血量（<60%）灭火失败才喊"我没招了"；③ 战术/辅助行为回归正常。

- **promaid 1.5.244：WorldProbe 启动预热加载（"让女仆帮忙填坑"报错修复）**
  - 报错（日志实证）：`Async tool execution failed: java.lang.NoClassDefFoundError:
    com/maidsmart/dialogue/WorldProbe`（LLM 调用 perception_query 感知工具时，
    ModuleClassLoader 懒加载 WorldProbe 失败）——填坑对话里 LLM 先 look_around/
    build_site 查地形，WorldProbe 首次加载即崩，整个 LLM 请求失败；
  - 排查：部署 jar 里 WorldProbe.class 完整（javap 正常、URLClassLoader 加载
    LOADED OK、Java jar 工具可见条目）——类文件没问题，是 Forge ModuleClassLoader
    在异步工具线程首次懒加载时的偶发问题；
  - 修复：ProMaidExtension 注册 AI 工具时**启动预热加载**
    `Class.forName("com.maidsmart.dialogue.WorldProbe")`，进入模块缓存后异步
    调用即可复用；预热结果打日志（`WorldProbe preloaded OK` / `preload failed`）；
  - **复测**：让女仆"帮我填一下这个坑" → LLM 正常调 look_around/build_site/inspect，
    不再报 WorldProbe 错误；日志搜 `WorldProbe preloaded` 确认启动加载成功。

- **promaid 1.5.243：着火链路补末影珍珠逃生（"跳入水坑和用末影珍珠都没有了"）**
  - 着火链路顺序补全：灭火器 → 水桶（头顶放水）→ 跑向水坑（findWater，原有）→
    **末影珍珠** → 抗火药水 → 金苹果/附魔金苹果 → 全失败交回自保（朝主人走）；
  - 珍珠复用 `pearlEscapeFromLava`（找半径 12 安全落点，落点排除岩浆/火等危险方块），
    灭火/找水失败时瞬移脱离火源区域；成功记 resourceUsed，不触发"我没招了"；
  - **复测**：身上着火且灭火手段（灭火器/水桶/水坑）都失败 → 扔出末影珍珠瞬移
    脱离火源；珍珠冷却/没有时继续抗火→金苹果→朝主人走。

- **promaid 1.5.242：记忆开关"关了一会儿又变回开"修复（显示 + 持久化双管）**
  - **① 女仆配置界面显示回弹**（MaidConfigMemoryMixin）：
    - 界面打开时的状态查询**去掉静态去重**——每次打开都发 Query 拿服务端真实状态
      （旧版同进程第二次打开跳过查询，缓存过期时按钮显示旧值"开"，用户一点反而又开）；
    - "AI 记忆"开关按钮**每次渲染同步文本**（旧版只在 initAdditionWidgets 设置一次，
      Query/Sync 回来按钮不更新——显示旧值 + 点击基于旧值 = 关掉又开）；
  - **② 持久化备份**（AiMemoryManager）：`setEnabled` 同时写
    `promaid_memory/toggles.json`；`isEnabled` 服务端在 persistentData **无标记**
    （女仆实体被移除/重载/跨维度导致 persistentData 丢失）时**回落磁盘备份**——
    不再直接回落全局配置"开"（"关了的开关又显示回开"的持久层根因）；
  - **③ 诊断日志**：`memory toggle`（每次切换）、`memory display`（手册记忆页
    每次打开时的服务端真实状态）、`memory state` 读取结果——下次测试日志直接
    看出 persistentData 是否有 tag、回落来源；
  - **复测**：① 关记忆 → 界面/手册显示关；重开界面、重启游戏、女仆重载后仍显示关；
    ② 关着的女仆收进魂符再放出 → 仍关；③ 日志搜 `memory display` 确认服务端真实值。

- **promaid 1.5.241：逃跑避让岩浆 + 搭高防跌落修复（getValue 全链替换为注册名比较）**
  - **① 逃跑避让岩浆**（僵尸追击仍走进岩浆的修复，日志实证 07:08 高丽野阿吽被追
    53 秒后死于熔岩）：
    - `flee` 路径【中间】每 2 格检查危险（旧版只查最终目标点，"半路踩岩浆"漏网）；
    - `fleeEscaping` 8 方向选择时**排除路径上（2~5 格）有岩浆的方向**（旧版只按怪物
      密度选方向，不避岩浆）；
    - `avoidLavaMovement`（移动层避让）**会话内也调用**——被追逃跑时同样改道离岩浆
      最远安全点（旧版只在会话外生效，低血被追时形同虚设）；
  - **② 搭高防跌落修复**（edgeGuard）：脚下无支撑时查 **4 个斜下对角支撑**钳住——
    旧版"脚下无支撑直接 return"，滑出垫块边缘那一刻脚下变空气、edgeGuard 立即失效
    人就掉下去（"搭方块中途潜行防跌落没生效"的根因）；
  - **③ getValue 全链替换**：`hasDangerAt`（危险方块判定）改 `getKey` 注册名比较——
    疑似 `ForgeRegistries.BLOCKS.getValue` 偶发返回 null 导致岩浆/火不被识别为危险，
    逃跑径直走进岩浆；`hasWaterBucket` 同步改 getKey（v1.5.240 已改）；
  - **复测**：① 被僵尸追且逃跑方向有岩浆 → 自动绕开/改道，不再走进去；② 搭高途中
    被击退/快速移动 → 站垫块边缘不跌落（斜下支撑钳制）；③ 掉岩浆 → 水桶/垫块正常自救。

- **promaid 1.5.240：修复"有水桶却说没招了"——hasWaterBucket 改 getKey + 放水链
  改 Blocks 静态引用 + 着火链路全失败诊断日志**
  - 日志实证：主手 `water_bucket`（self preserve start weapon=）却 0.1 秒喊
    "包里什么都没有，我没招了！"——"我没招了"全部来自着火链路（无 lava escape
    resources 日志），水桶自救链失效；
  - `hasWaterBucket` 改用 `getKey` 注册名比较（不依赖 `ForgeRegistries.ITEMS.getValue`）；
  - 放水链 4 处（placeClutchWater/placeWaterOnLava/recoverWaterNow/tickRecoverWater）
    的 water/air 获取改原版静态引用 `Blocks.f_49990_`/`Blocks.f_50016_`；
  - 着火链路全失败处新增 `fire escape resources` 诊断日志（hand / bucket(getKey) /
    getValueNotNull / fireResist），下次测试一锤定音确认 getValue 是否异常；
  - **复测**：给女仆水桶+草方块掉岩浆 → 垫块/倒水自救，不再喊"我没招了"。

- **promaid 1.5.239：修复启动崩溃（真正根因）——追踪药水 mixin 目标类改声明者**
  - v1.5.238 把方法名改成 m_8119_ 后仍崩（日志："could not find any targets
    matching 'm_8119_' in ThrownPotion"）——**ThrownPotion 没自己声明 tick**，
    m_8119_ 是从父类继承的；Sponge `@Inject` 只匹配目标类**自己声明**的方法，
    不匹配继承方法；
  - 修复：`@Mixin` 目标从 ThrownPotion 改为**声明了 m_8119_ 的
    `ThrowableProjectile`**（ThrownPotion 的祖父类），handler 开头
    `instanceof ThrownPotion` 过滤——雪球/鸡蛋/末影珍珠等投掷物直接 return；
  - 规则备忘：目标 MC 类用 SRG 名 + 方法必须由目标类自己声明（其他 MC 类 mixin
    均符合，仅此 mixin 两处都踩坑）；
  - **复测**：游戏正常启动；女仆投治疗/再生/抗火药水 → 紫色追踪弹锁定主人命中。

- **promaid 1.5.238：修复启动崩溃——追踪药水 mixin 方法名漏写 SRG 名**
  - 根因：v1.5.231 加的 `HomingPotionMixin`（目标 MC 类 ThrownPotion）的
    `@Inject(method = "tick")` 写了开发名，但 promaid 构建链不经 MixinGradle、
    无 refmap，运行时 MC 类方法名是 SRG（m_8119_）→ mixin 应用期找不到
    'tick' → `InvalidInjectionException` → 游戏启动即崩（日志：
    "Critical injection failure ... could not find any targets matching 'tick'"）；
  - 修复：`method = "m_8119_"`（ThrownPotion 继承自 Projectile 的 tick SRG 名）；
    审计其余 MC 类目标 mixin（ChunkFreezeMixin/GravityFreezeMixin/LeavesBlockMixin/
    MaidBaubleTotemMixin/MaidCakeEdibleMixin/MaidMoveSuppressMixin 等）——
    全部已用 SRG 名，仅此一处遗漏；
  - **复测**：游戏正常启动；女仆投治疗/再生/抗火药水给主人 → 药水弹紫色追踪弹
    锁定主人飞行直至命中（不再崩溃）。

- **promaid 1.5.237：末影珍珠逃生改原版投掷特效（紫色轨迹 + 落地传送爆炸）**
  - 旧版：直接 setPos 瞬移 + 末影人音效（特效不明显）；
  - 新版：生成原版 `ThrownEnderpearl` 实体瞄准安全落点抛物线投出（紫色投掷轨迹），
    命中地面后由原版逻辑把女仆传送过去（落地 32 个紫色传送粒子 + 音效）；
    非玩家投掷者只 `teleportTo` + `resetFallDistance`，**不受珍珠传送伤害**；
    投掷失败（异常）兜底直接瞬移，保证逃生不失效；
  - 覆盖两处：战斗珍珠逃生（反威胁 16 格）与岩浆珍珠逃生（安全格扫描）；
  - **复测**：低血贴脸/掉岩浆时女仆扔珍珠 → 看到紫色珍珠飞出去 → 落地紫色爆炸后
    女仆出现在安全落点。

- **promaid 1.5.236：岩浆误报"卡住"修复 + 农场批量种植（与连锁收割同设置格式）**
  - **① 岩浆卡住误报修复**：卡墙播报的岩浆上下文保护窗口从 30 tick（1.5 秒）放宽到
    100 tick（5 秒）——垫块顶出后坑沿压头/岩浆坑场景实测仍偶发"掉进岩浆里还说自己
    卡住了"（语义矛盾）；5 秒内泡过岩浆一律静默兜底不播报，交给 antiSuffocate 顶出；
  - **② 农场批量种植**（`misc.batchPlant`，默认开启）：每次处理农田时，以当前格为
    中心 BFS 蔓延（水平 4 方向），把相连农田里的**空耕地**（上方空气、canPlant 通过）
    一次全种上（种子真实消耗）——女仆到田里一次种一片，不再一格跑一趟；
    - 与连锁收割**同机制**（canPlant 检查 + 真实消耗种子）、**同设置格式**：
      `misc.batchPlant`（开关）+ `misc.batchPlantLimit`（上限，默认 24，4~96）；
    - 面板"农场"段新增【批量种植】/【批量种植上限（格）】两行；
    - 已有作物（未成熟/成熟）跳过，不覆盖；种子耗尽自然停止；
  - **复测**：① 掉岩浆 → 只说"掉进岩浆里了！我垫上来！"，不说"我卡住了"；
    ② 大农田让女仆执行农场任务 → 空耕地被成片种上（一次 24 格上限），成熟作物
    照常连锁收割；③ 面板可关批量种植、可调上限。

- **promaid 1.5.235：自保播报精简（不聒噪）——只留危险/求救，删光动作汇报**
  - **删除**（12 处动作成功汇报，成功动作不喊话）：
    放水垫一下 / 喝了一瓶抗火药水 / 抗火药水泼在身上 / 最后的手段(金苹果) /
    灭火器把自己扑灭 / 喝了一瓶治疗药水 / 用了一个(附魔)金苹果补充状态 /
    用末影珍珠瞬移逃走了 ×2 / 呼……应该安全了吧 / 被逼到墙角了，我弹开敌人
    迅速搭高 / 它们贴得太近了，我搭方块垫高躲开 / 喝了一瓶XX药水(增益)；
    对应死字段（announcedHeal/announcedPearl/announcedPillar/exitNotified）一并清理；
  - **保留**（危险/求救信号，均每场一次或限频）：
    掉进岩浆里了 / 溺水了 / 我卡住了 / 附近有岩浆绕开走 / 包里什么都没有，我没招了
    / 情况不妙，我先撤了！（文本精简）/ 我回到主人身边了（60 秒限频）；
  - **复测**：女仆喝药/吃金苹果/放水/搭高/灭火/珍珠都静默，只有进入危险或求救时
    才喊话——日志不再被动作气泡刷屏。

- **promaid 1.5.234：阶梯判定思想推广——环境逃生补全阶梯 + 回血资源阶梯化**
  - **① 环境逃生补全阶梯**（前级判定有结果才轮到后级）：
    `300 岩浆 > 280 着火 > 270 溺水 > 260 卡墙 > 避让（移动层）`——
    溺水掉血比卡墙急，排前；避让只是预防性绕行，移到所有"正在掉血"的逃生之后
    （岩浆/着火/溺水/卡墙判定有结果后才绕行）；
  - **② 回血资源阶梯化**（`tryHealLadder` / `tryInstantHeal`，前一档失败才下一档）：
    - 无威胁/威胁 8 格外安全距离：增益药水(再生省瓶) → 治疗药水(瞬间) → 金苹果
      (珍贵最后) → 食物(饱食)；
    - 威胁贴脸/濒死：只做即时动作 `tryInstantHeal`（治疗药水 → 金苹果，不打断
      搭方块/反击，不围殴时吃食物/增益）；
    - **缺物品不弹气泡**（v1.5.234b）：治疗/金苹果/食物各档缺失【不再显示提示】
      （避免刷屏）；仅保留岩浆/着火链路全失败的"包里什么都没有，我没招了！
      救救我！"（v1.5.232，每场一次）；
  - **③ giveUpMovement 兜底补全**：环境链路全失败让位后，除泡岩浆游岸边/着火朝
    主人走外，溺水继续上浮找空气、卡墙 antiSuffocate 顶出——"恢复正常状态"
    后仍保留最低限度保命移动；
  - **复测**：① 溺水+附近岩浆 → 先游上岸不绕行；卡墙+附近岩浆 → 先脱困再绕；
    ② 低血无威胁 → 增益→治疗→金苹果→食物逐档尝试，缺物品【不弹气泡】；
    ③ 被围殴贴脸低血 → 只喝治疗/金苹果（不吃食物），不打断搭方块。

- **promaid 1.5.233：判定阶梯（岩浆 300 > 着火 280 > 自保 250）+ 链路全失败让位恢复**
  - **① 判定阶梯**：岩浆判定必须有结果（成功 return / 全失败让位）后本 tick 才结束，
    下 tick 才轮到着火判定；两者都做出判断后才进行其他判断（自保战斗/传送/战术/
    任务）——防止岩浆/着火"左右脑互搏"（掉岩浆又着火时岩浆先处理，脱离后才灭火）；
  - **② 链路全失败 → 恢复正常状态**：岩浆/着火链路全失败时记 `envGiveUpTick`，
    之后 **5 秒（100 tick）让位窗口**内恢复正常状态，判断丢给其他链路——
    大概率触发自保 250（战斗/传送/搭高）但【不是直接锁定在自保】；窗口内仅保留
    无物品保命移动 `giveUpMovement`（泡岩浆→游向最近岸边 / 着火→朝主人正常速度走）；
    5 秒后仍危险则再试环境逃生（防永久摆烂：传送/被救后可能已脱离）；
  - **复测**：① 空背包掉岩浆 → 喊"我没招了" → 让位窗口内游向岸边，若主人救起/传送
    走则恢复正常；② 掉岩浆+身上着火 → 先处理岩浆（垫块/珍珠等），脱离后才灭火，
    不再互搏；③ 全失败后若其他链路（传送/战斗）接管，不锁定在自保。

- **promaid 1.5.232：岩浆/着火链路按用户规范重写 + 避让降级为仅移动 + 全失败"我没招了"播报**
  - **① 岩浆链路重写**（触发=进入岩浆，解除=脱离岩浆）：
    抗火随时断开（身上已有抗火 → 整条链路直接否定恢复正常，哪怕走了一半）；
    → 安静走出（1.5 秒内到安全格，血≥40%）→ 垫方块脱离液面（垫成功=脱离成功，
    交给着火分支处理身上的火）→ 还在岩浆则 水桶 → 末影珍珠 → 抗火药水 →
    金苹果/附魔金苹果 → 物理岸边兜底（findLavaEdge，空背包也游回岸边不站桩烧死）；
    移除原"水桶优先垫方块"顺序与"按维度分优先级"（地狱放水蒸发由内部跳过自然流转）；
  - **② 着火链路重写**：灭火器 → 水桶（**头顶放水**，placeClutchWater 候选链改为
    上方1→自身→上方2）→ 跑向周围水坑 → 抗火药水 → 金苹果/附魔金苹果 →
    全失败则本链路失败，交回自保机制（贴脸反击/朝主人走，不逃跑）；
    有抗火时火不伤人 → 直接恢复正常（等自然灭）；
  - **③ 岩浆避让降级**（用户选择"感知即危险过于粗糙"）：附近有岩浆【不再进危险态、
    不触发整场自保】——建造/跟随/插火把不再被打断；避让只做移动层修正
    （avoidLavaMovement：水平移动方向 3 格内会踩进岩浆才改道离岩浆最远安全点，
    方向干净则完全不干预）；
  - **④ 链路全失败播报**：岩浆/着火链路走到底仍无解 → 每场一次
    "包里什么都没有，我没招了！救救我！"（resourceUsed 追踪本场是否用过任一
    自救资源：垫块/水桶/珍珠/抗火/金苹果/灭火器）；
  - **⑤ 缺物品提示补齐**：治疗药水缺失 → "包里没有治疗药水，我撑不住啦……"
    （每场一次）；搭方块无材料已有（announceNoMaterial）；
  - **复测**：① 掉岩浆 → 1.5 秒走出/垫块/水桶/珍珠/抗火/金苹果顺序生效，脱离后
    着火分支灭火；② 身上有抗火泡岩浆 → 恢复正常行走（不惊慌、不垫块）；③ 空背包
    掉岩浆 → 喊"我没招了"同时游向岸边；④ 岩浆桶放旁边 → 女仆正常干活，仅快踩进时
    绕开；⑤ 着火 → 头顶放水灭火，没水 → 跑水坑，都没 → 喊救命后朝主人走。

- **promaid 1.5.231d：语言检测改自动重刷（直到中文才放行）+ 背包满明确"我给不了东西"**
  - **① 语言检测重试**：LLM 回复非中文时【不再丢弃+提示】——自动追加强化提示词
    （"上一次回答语言不符合要求，请严格使用简体中文重新回答"）后【重新请求 LLM】，
    直到输出中文才放行；同一对话最多重试 2 次，超限才放弃并提示（日志搜
    `lang check` 看重试过程与原文；重试计数按 callback 记，WeakHashMap 防泄漏）；
  - **② 背包满提示**：主人背包放不下时女仆明确说"主人背包满了，我给不了东西
    （XX放不进去）"；
  - **复测**：① LLM 偶发日文 → 自动重刷成中文（不再直接提示"已忽略"）；② 连续
    3 次日文才放弃；③ 主人背包满 → 女仆喊"我给不了东西"。

- **promaid 1.5.231：着火不逃窜（朝主人走）/ 追踪药水弹 / 死亡传送全维度 / 女仆烧死兜底 / 记忆防注入污染**
  - **① 着火行为重做（v1.5.231c）**：灭火手段（灭火器/水桶/找水/抗火）全失败后
    【不再朝怪物最少方向逃窜】——逃窜太快主人收不进魂符、还容易跑进更危险的地方；
    改为【朝主人方向、正常速度（1.0）走】，让主人能追上收进魂符或就近救火；
    无主人/主人不在身边 → 原地站着等火自然灭（比乱跑强）；贴脸有怪物仍先反击；
  - **② 追踪药水弹**：新 mixin HomingPotionMixin——投掷药水 persistentData 打
    目标 UUID，每 tick 修正速度方向【锁定主人飞行直至击中】（不再靠抛物线，
    "喷溅治疗药水基本没喷中过"根治）；择优保留（strong_healing > healing 等）；
  - **③ 着火不用水桶（用户测试场景）**：hasWaterBucket 加【主手/副手】检查
    （主人"给"的水桶可能在手上）；placeClutchWater 改为【先收旧水再放新水】——
    岩浆分支放的水位置不对导致着火分支 waterPos!=null 直接跳过灭火；
  - **④ 女仆烧死兜底**：背包全空的女仆掉岩浆——所有资源手段失败后【持续导航
    游向最近岸边】（半径 12），不再原地站着烧死（实测空背包女仆 4 秒烧死）；
    掉岩浆时打 `lava escape resources` 诊断日志（bucket/blocks/potion/pearl/hp）；
  - **⑤ 死亡传送全维度**：女仆追踪表（join/leave 注册 + 每 5 秒刷新位置）——
    【未加载区块的女仆】同步加载其所在区块后传送（旧版 getEntities 只扫已加载
    区块 → 远处女仆静默漏传，"其他女仆全部消失"根因）；
  - **⑥ 记忆防注入污染**：爱憎分明（Love Loathe）MaidResponder 把【当前状态/
    信任值/恐惧值/心情】拼进 USER 消息注入对话 → 记忆提取器反复把注入内容
    提取成"用户重复设定"；修复：isChatMessage 过滤纯注入短消息 + buildPrompt
    规则"忽略模组注入的状态描述"；新增 `memory write` 诊断日志（关开关后看
    段落数是否停止增长）；
  - **⑦ 金苹果归类**：逃生链走【药水类】位置（岩浆/着火分支抗火药水之后、
    最后手段；附魔金苹果自带 5 分钟抗火）；作为食物仅【极端饥饿】（反射读
    爱憎分明 HungerData ≤9）且背包无其他食物时才吃；
  - **⑧ 语言检测开关**（dialogue.langCheck，默认开）：LLM 回复落地时检测文字
    是否中文（汉字占比/假名占比），不符则丢弃该回复并提示"回答语言与设定
    不符"（日志搜 `lang check` 看原文）；
  - **⑨ 给予物品背包满提示**：主人背包满时女仆喊"主人背包满了，东西放不进去"；
  - **实测清单**：
    1. 着火 → 灭火手段失败 → 朝主人方向正常速度走（不逃窜，主人可收魂符）；
    2. 主人血低 → 女仆扔治疗药水 → 药水追踪主人必中；
    3. 掉岩浆 + 包里有水桶/手上拿水桶 → 放水自救；逃出后着火 → 脚下放水灭火；
    4. 空背包女仆掉岩浆 → 游向岸边不烧死（日志 `lava escape resources` 看资源）；
    5. 主人死亡 → 全部女仆（含远处未加载）传送到重生点旁；
    6. 日志搜 `memory write`——关闭开关后段落数不再增长；对话不再出现
       "重复设定"式记忆；
    7. 对话持续中文；LLM 偶发日文输出 → 被检测丢弃并提示。

- **promaid 1.5.230：放水判定补强（岩浆放水 fallback + 着火多位置）**
  - **① 岩浆放水 fallback**：placeWaterOnLava 原只试"最近岩浆格上方 1 格"——上方被
    实心堵住（岩浆在封闭空间/垫高后脚下隔方块）直接放弃；现在放不了上方格 →
    **直接替换岩浆自身格**（液体可被水替换），水流替换岩浆源 = 冷却为黑曜石更直接；
  - **② 着火/缓冲放水多位置**：placeClutchWater 原只试"自身格 → 上方 1 格"——
    屋檐下（自身实心 + 头顶实心）两处都放不了 → 灭火失败；现在候选链
    **自身 → 上方 1 → 上方 2**，上方 2 格放水水流下来淋到身上灭火；
  - 放水判定汇总：空气/可替换/液体（岩浆水都能被水替换）均可放；水桶只判定
    不消耗；3 秒收回且**只删水不删其他方块**（黑曜石/垫块保留）；
  - **实测清单**：
    1. 掉进头顶被实心堵住的岩浆坑 → 水直接替换岩浆源冷却，不再"倒不出水"；
    2. 屋檐下着火 → 上方 2 格放水淋灭；
    3. 回归：落地水/岩浆逃生放水/击退缓冲放水均 3 秒（落地水 1 秒）自动收回。

- **promaid 1.5.229：岩浆判定全面优化（感知半径/逃生顺序/抗火豁免/地狱特判）**
  - **① 感知半径扩大**：findNearbyLava 主世界 3 → **5 格**（提前预警绕行岩浆湖，
    "感知岩浆能力约等于没有"的根因）；**地狱收窄到 3 格**（地狱岩浆遍地，半径 5
    会让女仆永远"附近有岩浆"常驻危险态无法工作）；
  - **② 逃生顺序按维度分优先级**（旧版统一"抗火→珍珠"）：
    - 主世界：**末影珍珠优先**——0 消耗、瞬时脱离岩浆（每 tick 4 血），失败
      （没珍珠/冷却/无安全落点）才消耗抗火药水；
    - 地狱：**抗火优先**——8 分钟持续免疫，珍珠落点大概率还是岩浆/悬崖，白费
      且消耗末日；抗火失败才珍珠兜底；
    - 链路仍为：放水（冷却脚下岩浆）→ 垫方块 → 逃生手段 → 找自然水源（仅主世界）；
  - **③ 抗火"将过期"判定**：hasFireResist 剩余 <30 秒（600 tick）视为【没有】——
    泡岩浆时效果 30 秒内过期会多掉血，提前续杯无空档；
  - **④ 抗火豁免补全（"喝了抗火还惊慌失措"另一半根因）**：danger 判定的
    `cachedNearLava != null` 项原本不看抗火——地狱有抗火时附近有岩浆仍进危险态
    乱跑；现在 `cachedNearLava && !hasFireResist`，避让分支同样豁免（有抗火 =
    岩浆无害 = 正常行走，不绕路）；
  - **⑤ 避让落点改"离岩浆最远"**：旧版选最近安全点 → 沿岩浆边缘贴边走甚至回头
    （感知半径扩大后更明显）；现选半径 8 内离岩浆最远的点 → 持续远离，单调脱离
    威胁区；
  - **实测清单**：
    1. 主世界路过岩浆湖：5 格外开始绕行（"附近有岩浆，我绕开走！"只报一次）；
    2. 掉进岩浆：放水→垫块→有珍珠直接瞬移脱离（不消耗抗火），没珍珠才喝/扔抗火；
    3. 地狱：有抗火时岩浆里正常行走不再惊慌；抗火剩 <30 秒自动续杯；
    4. 地狱无抗火：优先喝/扔抗火，失败才珍珠；
    5. 垫高脱离液面后不再误报"被卡住"（±2 感知仍在）。

- **promaid 1.5.228：投喂/火把/盾牌从出生起就没运行过的根因修复（canStillUse）+ 死亡传送落点贴地 + 日文根治**
  - **①【重大】投喂/火把/盾牌为什么不工作的最终真相**：v1.5.227 加的三条运行时诊断
    直接锁定——`aid-owner constructed` 53 次（注册正常）、`canUse first-call: enabled=true`
    8 次（Brain 在调、返回 true）、`aid-owner diag` **0 条**（tick 从不执行）；
    - 反编译原版 1.20.1 Behavior：**`canStillUse` 默认返回 `false`**！行为 tryStart 后
      下一个 tick 的 tickOrStop 就调 canStillUse → false → 立即 stop → **tick() 永远
      执行不到**。投喂/火把/盾牌三个行为从 v1.5.189 诞生起就【一次都没运行过】；
    - WaterClutch/SelfPreservation/建造行为都重写了 canStillUse=true，所以一直正常；
    - 修复：三个行为补 `canStillUse → true`（字节码验证）；
  - **② 女仆"消失"第二层**：v1.5.227 的落点防卡方块已生效，但 04:00:45 实测发现
    女仆落在【空气格】后悬空坠落（"落地水"刷屏 = 落点偏高在树顶/高台）；修复：
    找到空气格后**继续向下走到地面贴地落**，无坠落无位移；
    - 只让【离目标最近的】女仆播报"马上赶到你身边"（旧版 4 只女仆死亡瞬间各喊
      一遍 = 4 条消息刷屏）；
    - 新增**重生后保险传送**（PlayerRespawnEvent）：死亡传送万一漏传，主人重生时
      把 >64 格外的女仆拉到身边；
  - **③ 敌人警示限频漏洞**：enemy_appear/enemy_approach/enemy_gone 三个子事件分开
    限频 → 敌人反复出现/接近会各刷一条（04:00:18 与 04:01:14 间隔 56 秒各报一次）；
    合并为同一个 key "enemy"，真正 300 秒一条；
  - **④ 日文对话根治**：`outputLanguage` 配置留空 = **默认强制中文（zh_cn）**——
    旧版"留空跟随 TLM/客户端"导致日文对话持续出现（日志 04:00:23/25 仍是日文）；
    想跟随其他语言显式填 ja_jp/en_us；
  - **⑤ 记忆"关不掉"最后一层**：手册女仆记忆页在记忆关闭时**只显示"记忆关闭 +
    提示"**，不再列出任何记忆内容（服务端直接拦截）——"关了但内容还在列表里"的
    体感消除；开关/防连点/显示同步（v1.5.226/227）均已生效（04:02 日志 4 只女仆
    各关一次、无回弹）；
  - **实测清单**：
    1. 主人饿/血低/着火 → 女仆自动投喂/扔药水（日志出现 aid-owner diag + 喂食气泡）——
      【这是投喂功能第一次真正运行】；
    2. 主人身边黑暗格 → 女仆插火把（torch-placer diag）；
    3. 主人盾牌耐久低 → 女仆给盾牌（shield-share diag）；
    4. 主人坠亡 → 女仆全部出现在重生点附近地面（不卡方块不坠落），只有一条
       "马上赶到你身边"；
    5. 对话持续中文（不再中途变日文）；
    6. 敌人警示 5 分钟最多一条；记忆关闭后手册页不显示任何记忆内容。

- **promaid 1.5.227：主人死亡传送安全落点（女仆"消失"）+ 系统消息刷屏根治 + 记忆开关显示修 + 投喂/火把运行时诊断**
  - **① 女仆消失（最严重）**：日志实证——主人高空坠落死亡 → MasterDeathTeleportHandler
    把女仆传去**重生点方块坐标**（床/出生点），女仆 1.8 格身高被塞进方块/墙里 →
    模型被实体剔除（装了 entityculling）→ 看起来"直接消失"，实际活着卡在地形里
    （03:16:22 主人摔死，三只女仆 brain-run 一直到 03:20 都在跳，服务端没死）；
    - 修复：`findSafeLanding`——从重生点向上扫描 24 格找【脚部+头部都是空气】的
      安全落点（两格净空，不卡方块）；传送后清速度向量（残留速度会带女仆冲进
      地形）；找不到安全格（密闭房间）退回原坐标上方 1 格；
  - **② 系统消息刷屏（根因找到）**：PerceptionManager 的限频 key 用的是【完整文案】，
    而文案里含距离/血量数字（"有敌人靠近…离我 71.5 格了"每次不同）→ key 永不
    重复 → 30/300 秒限频**完全失效**（日志实证敌人警示每 10~25 秒刷一条、受伤提示
    70%/60%/30%…连发）；
    - 修复：限频 key 改为**事件类型**（enemy_appear/enemy_approach/enemy_gone 共用
      300 秒敌袭限频；owner_hurt/owner_low/owner_look/weather 共用 30 秒）；
    - "我回到主人身边了，先缓缓……"播报加 60 秒独立冷却（5 秒传送冷却反复连传连喊）；
  - **③ 记忆"关不掉"的三层原因 + 修复**：
    - 开关服务端其实生效（日志 11 条确认消息都在），但①双击会把"关"翻回"开"
      （实测 03:14:31 关闭 → 32 启用成对出现）→ 两个 UI（女仆配置界面/手册记忆页）
      都加 600ms 防连点；
    - ②客户端显示陈旧：persistentData 不同步客户端，新会话首次打开界面按钮显示
      旧值 → 新增 C2S 状态查询包（id 17），界面打开时主动拉真实状态纠正显示；
    - ③"关了但那一栏还在"：女仆配置界面记忆面板在关闭时**隐藏记忆列表**，只显示
      "记忆提取：关"（v1.5.226 已修事件源不再写入——关掉后段落数不再涨）；
  - **④ 投喂/火把/盾牌仍不生效——已确认注册/配置/调度全对，加运行时诊断定位**：
    - 字节码级验证：ProMaidExtension$1.getCoreBehaviors 注册了全部 7 个行为
      （250/240/230/200/190/185/180）、TLM MaidBrain 完整 addAll、canUse 无条件
      读配置且配置=true——理论必然运行，但 brain-run 显示 190/185/180 从未运行、
      "aid-owner diag" 0 条 → 行为在运行时确实没被 Brain tick；
    - 新增诊断（下次进游戏看日志即可定位到哪一环断的）：
      - `aid-owner constructed` / `torch-placer constructed` / `shield-share constructed`
        —— 行为类被实例化 = 注册表被 TLM 消费；
      - `aid-owner canUse first-call: enabled=true` —— Brain 每 tick 调 canUse =
        行为在脑里且调度正常；
      - `extra-core registered: ...` —— 女仆 Brain 构建时消费了 promaid 的注册表；
      - 若 constructed 出现但 canUse 不出现 → 行为不在女仆的脑里（TLM 侧问题）；
        若 canUse 出现但 tick 不出现 → 行为被 start 后立刻 stop（状态机问题）；
  - **⑤ 日志轰炸**：外部蓝图解析失败 WARN 只提示一次（www/qqq/aaa.nbt 每秒几十条
    "解析返回 null"刷屏，目录每 2 秒重扫 78 个文件）；
  - **实测清单**：
    1. 主人死亡（坠落/怪物）→ 女仆全部出现在重生点附近安全位置，一只都不消失；
    2. 有敌人靠近时系统消息最多 5 分钟一条（不再是每 10 秒刷屏）；
    3. 记忆开关：点一下即关（连点无效）、重开界面显示保持"关"、面板列表消失；
    4. 日志搜 aid-owner constructed / canUse first-call / extra-core registered——
       把这三行截图发我，就能定位投喂为什么不跑；
    5. 喂食/火把/盾牌在本次版本内如果仍然无效，靠第 4 步日志定位后下版修。

- **promaid 1.5.226：AI 记忆开关真正生效（事件源守卫 + 客户端状态同步）**
  - **① 事件源绕过开关（"关了记忆还在涨"的根因）**：喂食（onFeed）、好感升级
    （onFavorabilityLevelUp）、玩家死亡（onPlayerDeath）三个事件源写记忆段落时
    **不检查 isEnabled**——只有提取/注入路径检查。玩家关掉记忆后，喂食/好感升级
    仍会不断写入新记忆 → 表现"无法关闭女仆的记忆"；
    - 修复：三个事件源统一加 `isEnabled(maid)` 守卫，关闭后不再新增任何段落
      （已有记忆保留，可用手册"清空记忆"删除）；
  - **② 客户端显示陈旧（重开界面开关弹回"开"）**：per-maid 开关存在实体
    persistentData，**只在服务端读写、不同步客户端**——重开女仆配置界面时按钮
    读到的还是旧值，显示"开"（实际服务端已关）；
    - 修复：新包 `MemoryStateSyncPacket`（id 16，S2C）——服务端 setEnabled 后
      广播给追踪该女仆的所有客户端，客户端缓存进 `AiMemoryManager.CLIENT_STATE`，
      `isEnabled` 客户端路径优先读缓存 → 界面开关/记忆面板显示即时与真实状态一致
      （手册记忆页列表本就由服务端下发，不受影响）；
  - **③ 顺带确认：导入按钮没丢**——"⬆ 导入建筑 / ⬆ 导入地图"在**手册→建造目录页
    右上角**（y=8 顶部一排，"← 大目录"在左上角对应位置），v1.5.225 已随编译链
    修复首次真正部署（jar 内字节级验证过）；
  - **实测清单**：
    1. 关闭记忆开关 → 喂食/好感升级/玩家死亡不再新增记忆段落（日志/手册段落数不再涨）；
    2. 关闭后重开女仆配置界面 → 开关保持"关"（不再弹回"开"）；
    3. 手册女仆记忆页的开关与 TLM 配置界面开关状态互相一致；
    4. 手册→建造目录页右上角看到"⬆ 导入建筑 / ⬆ 导入地图"两个按钮。

- **promaid 1.5.225：修复编译链（5 个编译错误）+ 锄头黑名单 + 抗火豁免补全 + Brain 诊断**
  - **① 编译链断裂（本次最关键的根因）**：`javac` 一直有 5 个编译错误
    （BlueprintBookScreen 目录页分支用了不存在的 `w` 变量、MaidAidOwnerBehavior
    用了不存在的 `m_41656_`、PromaidConfigScreen 用了错误的 Minecraft.player 字段
    `f_91069_`（实为 `f_91074_`））——**javac 全部编译失败但被误报成功**，打包的
    jar 一直用【旧 class】：
    - MaidTtsVolumeMixin 的 static 修复（v1.5.215）**从未编译进去** → 每次 TTS
      播放都 FATAL 应用失败 → **MaidAISoundInstance 构造失败 → TTS 没声音**
      （用户反馈"TTS 已启动但没有声音"的根因）；
    - 投喂/火把/盾牌的诊断日志（v1.5.215）从未部署 → "辅助行为没 tick"之谜；
    - 导入地图按钮（v1.5.224）从未部署 → "手册里没有新按钮"；
    - 修复 3 处源码 + 重新编译验证（javap 确认 static/新类/日志全部进入产物）；
  - **② 锄头不作为武器**：TLM 的 TaskAttack.isWeapon 判定"主手属性里有攻击伤害
    属性"→ 1.20.1 锄头带攻击伤害属性被当武器（日志实证 weapon=golden_hoe）；
    新增 TaskAttackWeaponMixin：任何锄头（木/石/铁/金/钻/下界合金）都返回
    "不是武器"，攻击/自动装备判定排除；
  - **③ 抗火豁免补全**：之前只豁免着火（envDangerSoft），岩浆（envDangerCritical
    = m_20077_）未豁免 → 喝抗火泡岩浆不掉血仍触发"惊慌失措"（垫高/倒水/逃跑）；
    现在 `envDangerCritical = m_20077_() && !hasFireResist`——有抗火时泡岩浆
    完全无伤，不算危险，正常行走；
  - **④ Brain 运行行为诊断**：新增 `brain-run` 日志（每 30 秒打印女仆 Brain 正在
    运行的行为类名，latest.log 搜 "brain-run"）——定位投喂/火把/盾牌行为是否
    被 Brain 调度（之前 diag 全无输出 = 行为没 RUNNING，待本版部署后验证）；
  - **实测清单**：
    1. 重新部署后 TTS 播放 → FATAL 消失，声音恢复（音量倍率生效）；
    2. 女仆不再拿锄头攻击/装备锄头当武器；
    3. 喝抗火后泡岩浆 → 不再惊慌失措（正常行走，不掉血）；
    4. 日志搜 "brain-run" → 看到女仆 Brain 运行的行为列表（投喂/火把/盾牌在不在）；
    5. 导入地图按钮/导入建筑按钮出现在手册右上角（编译修复后首次真正部署）。

- **promaid 1.5.224：手册"导入世界地图"按钮——玩家自助提取，无需手动放文件**
  - **回答流程问题**：提取其实已全自动（zip 放蓝图目录自动扫描提取；"导入建筑"
    按钮选 .zip 也走完整链路）——本版加**专门的"导入地图"入口**，手册内自助完成
    并显示提取结果；
  - **实现**：
    - 手册建造目录页右上角并排两个按钮：**"⬆ 导入建筑"**（.schem/.litematic/
      .nbt/.snbt/.schematic/.json/.zip，含版本警告）+ **"⬆ 导入地图"**（.zip
      世界存档/建筑包）；
    - "导入地图"确认框说明：世界存档（内含 level.dat + region/*.mca）自动以
      玩家最后位置为中心提取建筑（排除地形、锚点 ±256 格，1.8-1.13+ 均可）；
    - 文件选择器 → WorldImportPacket（包 15）→ 服务端 importWorldFile：复制 →
      解析提取 → 注册 → **回详细结果："导入成功: xxx（提取 N 块，尺寸 宽×高×深，
      已注册…）"**；失败明确原因（非 zip/无建筑/版本不兼容，无效副本自动删除）；
  - **实测清单**：
    1. 手册建造页 → 右上角"导入地图" → 确认 → 选 Tiananmen Palace.zip →
       聊天框回"导入成功（提取 48759 块，尺寸 273×55×131）"→ 目录出现蓝图；
    2. 选非 zip → "导入失败: 世界地图导入只支持 .zip"；
    3. 选没有建筑的世界 zip → 明确失败提示，副本已删除；
    4. "导入建筑"按钮行为不变（版本警告 + 通用格式）；
    5. 回归：zip 放目录自动扫描提取/纯建筑包导入不受影响。

- **promaid 1.5.223：世界存档 ZIP 提取（"Tiananmen Palace.zip" 实测打通）**
  - **需求**：支持 ZIP map 解析，从世界存档 zip 中提取建筑蓝图进行构建；
  - **实现**（BlueprintLib.extractFromWorldZip）：
    - 锚点：playerdata/*.dat 玩家最后位置 → level.dat Data.Player.Pos → SpawnX/Y/Z；
    - 解析锚点所在 region（3×3 邻域）的全部已生成 chunk（mca 头部定位表 →
      gzip/zlib 解压 → NBT）；
    - **双格式兼容**：1.13+（Sections palette 位解包）与 1.8-1.11 旧格式
      （section Blocks+Data，旧数字 id → 注册名映射表 40+ 种）；
    - **建筑聚焦**：排除基础地形（TERRAIN_BLOCKS + 水/岩浆/雪/冰）+ 锚点 ±256 格
      范围——实测天安门 zip（1.12.2 存档，172 万块非空气含 96.6% 地形）过滤后
      **提取 48,759 块纯建筑**（石砖 19670 + 陶瓦 14930 + 台阶/木板/楼梯/石英，
      范围 273×55×131）；
    - 归一化到 0 起始 + 地形层压缩（trimTerrainLayers）→ plan 步骤；
    - zip 里的 structures/*.nbt（结构方块导出件）照常解压注册（零件蓝图）；
  - **接入**：loadExternalFile 的 .zip 分支——发现 level.dat/region/*.mca 视为
    世界存档，世界文件解压到 `蓝图目录/<zip名>/` 子目录（防穿越），提取成功则
    作为该 zip 的蓝图（优先于 structures 零件）；
  - **实测清单**：
    1. 把"Tiananmen Palace.zip"放进 config/maid_smart/blueprints/ → 手册出现
       "Tiananmen Palace"（约 4.8 万块天安门）+ 5 个零件（structures/*.nbt）；
    2. 选蓝图 → 区块显示/建造 → 女仆按蓝图搭建（材料按新口径计算）；
    3. 旧版本世界（1.8-1.11/1.12.2 存档）都能提取；1.13+ 存档同样支持；
    4. 超上限（100 万）截断兜底、无锚点/无建筑文件明确失败提示；
    5. 回归：普通 .schem/.litematic/.nbt/.json/.zip（纯建筑包）导入不受影响。

- **promaid 1.5.222：构建链上限放开到 50 万 + ZIP 打包蓝图解析**
  - **① 构建链上限统一到 50 万级**：
    - 排查结论：`structureMaxBlocks`（结构文件上限）默认已是 100 万、用户配置
      100 万 ✓；真正的瓶颈是 `designMaxBlocks`（AI 子 Agent 设计上限）范围只有
      **100~20000**（默认 20000）和 `maxBlocks`（LLM 现场生成）范围 16~2000——
      "构建链被卡在 2 万块"的根因；
    - 修复：`designMaxBlocks` 默认与上限都改为 **500000**（50 万，范围
      100~500000）；`maxBlocks` 上限 2000 → **500000**（默认 200 保留，LLM
      现场输出有限）；`structureMaxBlocks`/`structureMaxVolume` 维持（100 万 /
      1677 万，覆盖 50 万级）；
  - **② ZIP 打包蓝图解析**（"ZIP map 提取建筑蓝图"）：
    - EXTENSIONS 增加 **.zip**（扫描目录/手册导入都支持）；
    - `loadExternalFile` 处理 .zip：解压 zip 内**所有**建筑文件
      （.schem/.litematic/.nbt/.snbt/.schematic/.json，跳过 .zip 防递归、
      路径穿越防护只取纯文件名）到蓝图目录，**返回第一个建筑文件的解析结果**
      （该 zip 整体注册为一个蓝图）；解出的子文件会被扫描器**各自注册**——
      一个 zip 一次导入多个建筑；
    - 手册"导入建筑"文件选择器过滤同步加 *.zip；
    - zip 内没有建筑文件 → 解析失败提示（不含建筑文件的 zip 无法导入）；
  - **实测清单**：
    1. 面板 build → AI 设计蓝图上限可调到 50 万；LLM 蓝图最大方块可调 50 万；
    2. 把含 .schem/.litematic 的 zip 放进 config/maid_smart/blueprints/ →
       手册出现该 zip 蓝图（第一个文件）+ 各子文件蓝图；
    3. 手册"导入建筑"选 .zip → 解压 + 注册 + 回"导入成功"；
    4. zip 内多个建筑 → 全部注册（各自独立可建）；
    5. 50 万块级蓝图（如 modernredstonesmarthouse 547686 块）→ 正常注册/显示/建造；
    6. 空 zip/无建筑 zip → 失败提示，不污染手册。

- **promaid 1.5.221：区块材料表单（0/88 页）公式修正——已建 + 女仆背包 + 玩家背包**
  - **根因**：用户说的"在此区块右击打开的详细页面材料所需界面（0/88 格式）"——
    数据来自 `buildCatalogEntriesWithMaterials`：
    - 需要（分母）= **蓝图总需求**——不扣"区块内已累计搭建的"，建到一半表单
      还显示全量（0/88 永远是 88）；
    - 已有（分子）= **仅玩家背包**——不算已建、不算女仆背包 → 表单严重虚高；
  - **修复**（按用户公式）：
    - 需要 = 蓝图总需求 − **区块内已累计搭建**（遍历本维度活跃区块计划，
      placedSet 命中步骤按方块类型计数，含恢复时预登记的世界已建）；
    - 已有 = **已搭建 + 绑定女仆背包（本维度所有绑定了区块计划的女仆）+ 玩家背包**；
    - 手册打开/右击区块的所有入口（sendCatalog 唯一调用点）自动生效；
    - 区块进度文本（statusText/realShortfall）核对无误（早已是"总需求 − 已建
      世界扫描 − 绑定女仆+主人背包"），无需改动；
  - **实测清单**：
    1. 建到一半打开区块详情页：某材料显示 "已建+背包 / 剩余需求"（如建了 30 块
       砖、背包 20 块 → "50/58"），不再 0/88 全量；
    2. 女仆背包里的材料计入已有（不再漏算）；
    3. 建完（已建 = 总需求）→ 需要 = 0，材料表显示全部充足（绿）；
    4. 无活跃区块的蓝图 → 行为不变（已建=0，与旧版一致）；
    5. 进度文本缺料行与表单口径一致（互相印证）。

- **promaid 1.5.220：手册"导入建筑"按钮（文件选择器 + 版本警告确认 + 服务端解析校验注册）**
  - **① 导入入口**：手册建造目录页右上角新增 **"⬆ 导入建筑"** 按钮（与左上角
    "← 大目录"对称）——点击弹出系统文件选择器（FileDialog），选择
    .schem/.litematic/.nbt/.snbt/.schematic/.json 蓝图文件 → 发送到服务端；
  - **② 导入前版本警告**（用户要求）：点击导入按钮先弹原版确认框——
    "请先确认：蓝图文件与当前 MC 版本（1.20.1）一致。版本不符的文件导入后
    可能出现建筑损毁（方块错位、状态丢失、无法解析）。确认来源版本一致后再
    继续导入。"——点"继续导入"才打开文件选择器；
  - **③ 服务端导入流程**（BuildImportPacket，包 14）：
    - 校验扩展名（不支持的格式直接拒绝）；
    - 复制到 `config/maid_smart/blueprints/`（同名已存在则拒绝，提示直接用）；
    - **解析校验 = 版本兼容粗检**：用与扫描完全相同的解析流程（loadExternalFile）
      试解析——解析失败说明格式/版本不兼容（如其他版本导出的结构文件），
      **删除无效副本**并提示"可能是版本不兼容，蓝图需与当前 MC 1.20.1 一致"；
    - 校验通过 → 重新扫描注册 → 回系统消息"导入成功: xxx（N 块，已注册，
      可在手册建造目录找到）"；
  - **实测清单**：
    1. 手册建造页 → 右上角"导入建筑" → 确认框显示版本警告 → 继续导入；
    2. 文件选择器选 .schem/.litematic/.nbt/.snbt/.schematic/.json → 聊天框回
       "导入成功"（含方块数）→ 手册目录立即出现该蓝图；
    3. 选不支持格式（如 .zip）→ "导入失败: 不支持的格式"；
    4. 选损坏/版本不符文件 → "导入失败: 文件无法解析（可能是版本不兼容…）"，
       无效副本已删除，不污染手册；
    5. 同名文件 → "导入失败: 同名文件已存在"；
    6. 取消文件选择 → "已取消导入"。

- **promaid 1.5.219：链路调试页按钮重叠修复（刷新 vs 重要度 / 页码 vs 标记删除恢复）**
  - **① 刷新按钮与"↑重要度"重合**：旧版刷新按钮固定 `x=8`、`y=h-48`——与动作
    按钮组同一行；窗口窄时（默认 427 逻辑宽）6 个动作按钮（476px）占满整行从
    x=8 开始 → 刷新按钮（8-78）正好压住"↑重要度"（8-73）；
    修复：**刷新并入动作按钮组**（7 个按钮统一居中布局，窄窗口按宽度自适应缩小，
    永不重叠）；
  - **② "第 X/Y 页"与"标记删除/恢复"重合**：页码画在 `y=h-44`，而动作按钮组
    占 `h-48~h-30` → 页码文字正好落在按钮组中间；
    修复：页码移到**翻页按钮行上方**（`y=h-28`，翻页按钮从 h-26 开始）——与
    动作组/翻页按钮都不相交；
  - **实测清单**：
    1. 手册 → 女仆 → 链路调试页：底部一行 7 个按钮（↑重要度/↓重要度/标记删除/
       恢复/强化/停用关系/↻刷新）并排不重叠；
    2. 窗口缩到最小 → 按钮自适应缩小，仍一行放完不重叠；
    3. 对象行多页时："第 X/Y 页"显示在翻页按钮上方，不与任何按钮重叠；
    4. 刷新/动作按钮功能不变（未选中行提示照旧）。

- **promaid 1.5.218：建造悬空方块根治（门/活板门/中继器支撑判错）+ 材料缺口公式修正（已搭建+主人+女仆）**
  - **① 民居 477 个"悬空搭不上的方块"根治**：
    - 根因（supportDirection 附着方向判错）：门（DoorBlock）、活板门（TrapDoorBlock）、
      中继器/比较器（DiodeBlock）都有 `facing` 属性，被统一判成"需要 facing 方向的
      墙面支撑"——但 facing 是**开合/输出方向**不是附着面：
      - 门：half=lower/upper 不匹配 "top" → 返回 facing（开门方向）→ 支撑格是门口
        空气 → 永远不满足 → 延后 3 次永久跳过 = "悬空放不上"（每扇门 ×2 步骤）；
      - 活板门：half=bottom 不匹配 "top" → 返回 facing（开合方向）→ 同样永远跳过；
      - 中继器/比较器：facing 是输出方向 → 被判需要输出方向的墙 → 跳过；
    - 修复：supportDirection 特判——**门 → 下方支撑（门框/地板）；活板门 →
      half=top 上方 / bottom 下方；中继器/比较器 → 下方**（与原版 canSurvive 一致）；
      修复后这些方块支撑格正确、正常建造，不再误跳过；
  - **② 材料缺口公式修正**（用户要求：材料 = 已累计搭建 + 主人背包 + 绑定女仆背包）：
    - `estimateMissing`（建造中缺料提示）：旧版 = 蓝图总需求 − 背包（主人+女仆），
      **漏扣"已搭建"**——建到一半缺口显示还是全量需求；新版 = 总需求 − 已放置
      （Progress.placedSet 命中）− 永久跳过（skippedIdx）− 主人背包 − 女仆背包；
    - `WorkListTool`（LLM 查进度/缺口）：buildNeedText 旧版 = 全量需求 − **仅女仆
      背包**（漏扣已建 + 漏算主人背包，缺口严重虚高）；新版 = 总需求 − 已搭建
      （世界状态扫描）− 主人背包 − 女仆背包；buildProgressText 背包口径同步改为
      combinedHave（主人+女仆）；
  - **实测清单**：
    1. 重开民居蓝图（含门/活板门/中继器）→ 全部正常放置，不再出现"悬空放不上"跳过；
    2. 建造到一半缺料 → 提示的缺口 = 剩余未建块数 − 背包已有（不再虚高）；
    3. 问女仆"还缺什么材料" → 缺口扣已建 + 扣主人/女仆背包；
    4. 建造完成消息不再出现"跳过 N 个悬空方块"（除非蓝图本身真画了悬空块）；
    5. 回归：支撑补放（火把/按钮/梯子等）/延后补建/材料续建流程不受影响。

- **promaid 1.5.217：图腾触发系统消息 + 盾牌诊断日志 + 语音包按钮反馈 + 玩家伤害模式 4 + 音量注释**
  - **① 不死图腾触发反馈**：女仆自己被图腾救活 → 聊天气泡"不死图腾救了我一命！"；
    主人被共享图腾救 → **主人系统消息"女仆的不死图腾救了你一命！"** + 女仆气泡
    "主人挺住！我的不死图腾给你用！"（原版特效 35 不变）；
  - **② 盾牌替换诊断日志**（"女仆不会把盾牌给我"）：`shield-share diag` 每 5 秒
    打状态快照（主人距离/主人手里盾耐久%/女仆背包盾数）——一眼看出是"主人盾
    耐久 >50% 不换"、"女仆没带盾"还是"距离太远"（与投喂/火把同批定位手段）；
  - **③ 语音包按钮即时反馈**（"查看状态/重新加载在界面内没有任何反馈"）：
    点击瞬间客户端先提示"已请求…，结果请看聊天框"（服务端处理完本来就会回
    聊天消息，之前纯发包无任何视觉反馈）；
  - **④ 玩家伤害模式 4"仅受到一点伤害"**：单次伤害上限 1 点（还原 ÷5 后
    `Math.min(原始伤害, 1.0)`）——被打有受伤反馈但完全不疼；面板循环选项 +
    配置文件注释同步；
  - **⑤ TTS 音量倍率注释写清楚**（"乘以 1.5 是什么意思，减伤都消失了在原来
    基础上乘 1.5 还是计算了原版减伤之后乘以 1.5"）：明确说明**这是 TTS 语音
    播放音量倍率，与伤害/减伤无关**——TLM 播放 TTS 的原始音量为 1.0（偏小），
    此值直接乘在播放音量上（1.5 = 音量放大 50%，2.0 = 放大一倍，0.5 = 减半）；
    配置文件 comment + 面板说明同步；
  - **实测清单**：
    1. 女仆饰品栏图腾触发 → 气泡提示 + 图腾 -1；
    2. 主人致命伤 → 系统消息 + 女仆气泡 + 特效 + 女仆图腾 -1；
    3. 日志搜 "shield-share diag" 看盾牌为什么不给；
    4. 语音页点"重新加载/查看状态" → 界面立即提示已请求，聊天框收到结果；
    5. 伤害模式切"仅一点伤害" → 剑/弓/高伤武器打女仆单次最多 1 点；
    6. 配置文件看 volumeMultiplier 注释 → 明确是音量倍率与伤害无关。

- **promaid 1.5.216：岩浆脱困五项修正（walk-out 时限/抗火豁免/逃跑防抖/范围云/卡墙误报）**
  - **① walk-out 时限 3 秒 → 1.5 秒**（用户实测"3 秒还是太长"）：`walkOutTimer <= 60`
    改为 `<= 30`——1.5 秒走不出浅接触岩浆立即转主分支 2 上手段；
  - **② 抗火豁免**（"服用了抗火药水后仍然惊慌失措"）：`envDangerSoft` 的着火判定
    改为 `m_6060_() && !hasFireResist(maid)`——喝了抗火后身上还在烧（火不灭但
    不掉血）**不再算环境危险** → 不惊慌、不逃窜、不进着火分支，等火自然灭即可；
    岩浆（m_20077_）不受豁免（泡岩浆里必须离开，抗火时间有限）；
  - **③ fleeEscaping 方向防抖**（"疯狂逃窜，逃得特别远"）：旧版每 tick 重选逃跑
    方向——怪物分布稍变目标就跳，来回折返累积成"疯狂逃窜"；新增
    `fleeDirCooldown` 20 tick（1 秒）内保持同一逃跑目标，直线跑出危险区再评估；
  - **④ 滞留/喷溅抗火药水不留范围云**：旧版 ThrownPotion 低抛投掷（速度 0.25
    垂直向上）实测不产生溅射/滞留云（投掷物理不确定）；改为**直接在自己位置
    生成 AreaEffectCloud**（setPotion + setRadius 4 + setWaitTime 10 + setDuration
    600 + setOwner）——云 tick 时对半径内实体（含自己）施加抗火效果，100% 生效，
    与滞留药水同机制；
  - **⑤ 卡墙误报**（"被岩浆烫到却说被卡住了"）：
    - 根因 A：垫高脱险后脚下隔一层垫块（y-1 是方块、y-2 才是岩浆），旧
      findNearbyLava 只扫 y±1 → 感知不到 → danger 解除 → 进入卡墙分支；
    - 根因 B：垫高瞬间位移滞后，headInSolid 短暂误报；
    - 修复：findNearbyLava 扫描 **dy ±1 → ±2**（垫高后仍能感知脚下岩浆，走
      "附近有岩浆，我绕开走！"而非卡墙）；卡墙分支加**岩浆上下文保护**
      （30 tick 内泡过岩浆 → 静默 antiSuffocate 兜底，不播报"我卡住了"）；
  - **实测清单**：
    1. 浅接触岩浆 → 1.5 秒走不出立即垫方块/倒水（不再傻走 3 秒）；
    2. 喝了抗火 → 身上着火不惊慌不逃窜，正常站立等火灭（不掉血）；
    3. 无抗火着火 → 灭火失败逃跑时方向固定 1 秒，不再来回折返逃窜；
    4. 扔滞留/喷溅抗火 → 脚下生成 4 格范围云，抗火效果立即生效；
    5. 垫高脱险后不再误报"我卡住了"（30 秒内岩浆上下文静默）；
    6. 回归：walk-out/垫高/倒水/珍珠/着火链/坐下锁不受影响。

- **promaid 1.5.215：岩浆水桶时序修复 + TTS 音量 mixin FATAL 修复 + 投喂/火把诊断日志**
  - **① 岩浆搭方块后不释放水桶（实测反馈）**：
    - 根因（时序）：主分支 2 顺序为"垫方块 → 水桶"——垫方块成功会把女仆**顶出
      液面** → `m_20077_`(isInLava) 变 false → 危险解除 → 岩浆分支退出，
      **水桶永远轮不到执行**（用户实测："进入搭方块流程之后，不会释放水桶"）；
    - 修复：**水桶提前到垫方块之前**——还在岩浆里时 `findNearbyLava` 一定找得到，
      先放水冷却脚下岩浆（变黑曜石）→ 再垫方块站上去，双重保险；
  - **② MaidTtsVolumeMixin 应用失败（FATAL 刷屏 + TTS 音量功能失效）**：
    - 根因：`@ModifyArg` 注入点在 `MaidAISoundInstance.<init>` 的 **super() 调用处**，
      构造器 super() 之前不能访问 this，handler 必须 static——当前非 static →
      mixin 每次应用都失败（日志 FATAL 反复刷）；
    - 修复：handler `maidSmartTtsVolume` 改为 static（只读配置，不碰实例字段，
      无副作用）；
  - **③ 自动投喂/插火把"没实现"——加诊断日志定位**（功能本身配置默认全开：
    aidOwnerEnable/torchPlacerEnable=true；注册机制验证无问题：TLM ExtraMaidBrain
    全部 addAll 进 CORE activity）：
    - `aid-owner diag`（每 5 秒）：主人距离/血量/饱食/着火/负面 + 女仆背包
      治疗药水数/食物数——一眼看出是条件没满足（主人满血不饿）还是背包没物品；
    - `torch-placer diag`（每 5 秒）：主人距离/3×3 最暗方块亮度/火把槽位——
      一眼看出是场景太亮、主人太远、还是没带火把；
  - **实测清单**：
    1. 掉进岩浆深陷 → 先放水冷却（最近岩浆上方一格，3 秒收回）→ 再垫方块登顶；
    2. 小岩浆坑（垫一块就脱险）→ 水也先放了（不再"垫完不释放水桶"）；
    3. TTS 音量倍率生效（FATAL 消失）；
    4. 日志搜 "aid-owner diag"/"torch-placer diag" 可看到投喂/火把为什么不触发；
    5. 回归：walk-out/抗火/珍珠/水源/着火链不受影响。

- **promaid 1.5.214：修复进入世界卡死（Server thread 自锁）——农场扫描/自保扫描加区块加载检查**
  - **现象**：一进游戏就卡死，玩家可移动但生物全停，服务端 tick 冻结无报错；
  - **诊断**（jstack 线程 dump 实证，Server thread 堆栈）：
    ```
    MaidFarmPlantTask.sweepChain:610
    → FarmSweepMixin.handler$maidsmart$sweepSurrounding（start TAIL 注入）
    → Level.m_8055_（getBlockState）→ ServerChunkCache → BlockableEventLoop
    → LockSupport.parkNanos（TIMED_WAITING 永久等待）
    ```
  - **根因**：`FarmSweepMixin` 的连锁收获 BFS（蔓延上限 96 格）与 3×3 扫种在
    **Server thread** 上对**可能未加载的区块**调用 `getBlockState`——ServerLevel
    对未加载区块会调度加载并**等待自己处理的任务队列** → Server thread 等自己
    → 自锁死锁（watchdog 测不到：卡在 parkNanos 不经过 tick 边界，且不抛异常）。
    农田位于区块边缘/正在加载时必触发（女仆任务 start 立即执行扫描）；
  - **修复**：所有 Server thread 上的方块扫描一律先查 `m_46749_`（hasChunkAt），
    未加载区块直接跳过：
    - `FarmSweepMixin.sweepSurrounding`：3×3 每格检查（未加载不收割不补种）；
    - `FarmSweepMixin.sweepChain`：BFS poll 时检查 + **邻居入队前检查**（未加载
      不蔓延，防止继续跨区块）；
    - `SelfPreservationBehavior.findBlockBy`（找水/安全格/岸边/空气格/珍珠落点等
      全部扫描的公共入口）+ `findNearbyLava`：同样加检查——同类自锁风险全面免疫；
  - **实测清单**：
    1. 进入有农场女仆的存档 → 正常加载，不再卡死；
    2. 农田在区块边缘 → 已加载部分正常收割/补种，未加载部分跳过（下轮任务再收）；
    3. 连锁收获（chainHarvest）跨区块 → 只蔓延已加载区块，不触发区块加载；
    4. 自保岩浆感知/找水/珍珠落点扫描 → 行为不变（都在已加载范围，检查零影响）；
    5. 回归：自保/建造/对话/农场任务其余功能不受影响。

- **promaid 1.5.213：坐下受伤立刻站起来（锁移动判定保留，仅加受伤起身）**
  - **背景**：v1.5.212 的"坐下锁移动"每 tick 判定**保留不动**（安然坐着时任何
    来源都动不了她 + 战术让位）；用户补充：受伤时无需额外判定，
    **只需要坐下后受到伤害会立刻站起来**——站起来后自然脱离锁判定，
    战术/自保/逃生恢复完整移动能力；
  - **修复**：`onMaidHurt`（LivingHurtEvent）开头新增——
    女仆处于 `isMaidInSittingPose` → `m_21837_(false)`（setInSittingPose 解除，
    TLM 覆写同步原版姿势 + 数据通道）。**任何来源都起身**：怪物攻击 /
    环境伤害（岩浆每秒 4 血也走 LivingHurtEvent，坐着被烫立即起身逃生）/
    玩家误伤；
  - **实测清单**：
    1. 坐下安坐 → 战术/避让/导航全部锁定，原地不动（v1.5.212 行为不变）；
    2. 坐着被僵尸打 → 立刻站起来 → 自保/战术/逃生全部恢复；
    3. 坐着掉进岩浆（环境伤害）→ 立即起身 + 岩浆逃生分支正常执行；
    4. 坐着被主人误伤 → 也会站起来（被打该躲）；
    5. 未受伤的安坐完全不受干扰（无任何 tick 成本变化）。

- **promaid 1.5.212：删除施工区避让行为 + 坐下锁移动根治（每 tick 判定）**
  - **① 删除 BuildAreaAvoidBehavior（施工区避让，恐惧式）**：
    - 背景：自保 antiSuffocate 已每 tick 防窒息兜底（卡进方块强制上移），
      旧"非建造女仆接近施工区块会逃离"（优先级 150）没有存在意义——
      建造女仆搭方块不会再闷住人，非建造女仆也不用再躲施工区；
    - 删除：ProMaidExtension 行为注册表移除 Pair.of(150, ...) + 源文件删除，
      jar 中该类已不存在（javap 验证）；注册表注释保留说明；
  - **② 坐下锁移动根治**（"坐下但仍然在移动"）：
    - 根因（TLM 源码实证）：`MaidFindSitTask`/`EntitySit` 是坐垫骑行；
      TLM 自己的行为都检查 `isMaidInSittingPose`（跟随 FollowOwnerVehicleTask /
      逃跑 MaidRunAwayTask / 游泳 MaidSwimManager / UpdateActivityFromSchedule），
      **但 promaid 自己的战术（230 走位/后撤/跳劈）和避让（150 逃离）不检查**
      → 坐下后这些行为仍会移动女仆（战术让位只认 MOVING_SURVIVE）；
    - 修复（SelfPreservationBehavior.m_6725_ 每 tick，v1.5.202 常驻检查器）：
      - 坐下且无环境危险 → 每 tick 清 WALK_TARGET 记忆 + `navigation.stop()`
        （m_26573_，javap 实证置 path=null）+ 清速度（Vec3(0,0,0)）；
      - `moving` 判定并入 sitting → 坐下时 MOVING_SURVIVE 恒标记 → 战术（230）
        每 tick 让位，不会下一 tick 又把她拉起来走；
      - 危险时（岩浆/着火/溺水/窒息）不锁——必须站起来逃命；
      - 坐下时低血自保的瞬发动作（喝药/传送回家）保留（不依赖移动）；
  - **实测清单**：
    1. 女仆坐下 → 战术走位/后撤/跳劈全部不再触发，原地纹丝不动；
    2. 坐下时附近有施工区 → 不再逃离（行为已删）；
    3. 坐下时掉进岩浆/着火 → 站起来逃生（不锁，保命优先）；
    4. 坐下 + 低血 → 仍会喝药/传送回家（坐姿保命）；
    5. 站起来 → 移动/战术/跟随全部恢复正常（MOVING_SURVIVE 自动移除）；
    6. 回归：建造防窒息（antiSuffocate）/自保/落地水不受影响。

- **promaid 1.5.211：着火分支尾部"贴脸反击"兜底（逃出岩浆带火的 15 秒窗口收口）**
  - **背景**：刚逃出岩浆、身上带火（m_6060_ 仍 true）时 danger 恒为 true →
    战斗/濒死自保被压制直到火灭。灭火手段（灭火器/水桶/找水/抗火）全失败 +
    主人传不回去时，旧版只能 fleeEscaping 逃跑等火自然灭（约 15 秒），
    这期间被怪追着打不还手，低血可能被打死；
  - **修复**：着火分支兜底（fleeEscaping 之前）新增贴脸反击——
    新 `findNearestThreatNearby(maid, 2.5)` 找 2.5 格内最近威胁（isThreat，
    与 findThreat 不同：不要求视线，贴脸默认看得见），反击冷却好 →
    `counterAttack` 顺手还手（御币弹幕 / 弓弩点射 / 近战挥砍，与威胁分支
    同一套武器判定），置 12 tick 冷却——"打一下 → 跑一步"节奏，拉开身位
    不被白打；延续 v1.5.202 "边打边保命"哲学；
  - **实测清单**：
    1. 着火 + 灭火手段全失败 + 僵尸贴脸 → 先还手再跑（不再纯挨打）；
    2. 着火 + 贴脸怪用御币 → 弹幕还击；弓弩 → 点射；近战 → 挥砍；
    3. 反击后 12 tick 冷却 → 冷却期间 fleeEscaping 逃跑（打跑交替）；
    4. 没有贴脸怪 → 行为与旧版完全一致（直接逃跑）；
    5. 火灭后（m_6060_ false）→ 威胁分支正常恢复（濒死自保/战术不受影响）；
    6. 回归：岩浆分支/传送回家/战术(230)让位逻辑不受影响。

- **promaid 1.5.210：着火分支"抗火/找水"位置互换**
  - 着火链从 `灭火器 > 水桶 > 抗火 > 找水 > 逃跑` 调整为
    **`灭火器 > 水桶 > 找水 > 抗火 > 逃跑`**：
    - 自然水（半径 12）是 **0 成本**资源，先导航过去跳进水里立即灭火；
    - 抗火药水是消耗品（喝/扔一瓶少一瓶），留给**无水解救**的场合
      （岩浆深陷等），不再在着火时优先消耗；
    - 水桶仍是仅次于灭火器的第二优先（便宜资源原地灭，不用跑河）；
    - 地狱不受影响：跳过水桶/自然水判定后直接抗火或逃跑；
  - **实测清单**：
    1. 着火 + 灭火器 → 灭火器扑灭（不变）；
    2. 着火 + 水桶 → 原地倒水灭（不变）；
    3. 着火 + 无灭火器无水桶 + 12 格内有河 → 跑向河跳进去灭火（不再先喝抗火）；
    4. 着火 + 附近无水 + 有抗火药水 → 喝/扔抗火（顺序不变，只是排到找水后）；
    5. 地狱着火 → 灭火器 → 抗火 → 逃跑（水判定全跳）；
    6. 回归：岩浆分支（垫块→水桶→抗火→珍珠→水源）不受影响。

- **promaid 1.5.209：岩浆逃生分支重构——walk-out 加时限 + 主分支 2 逃生链路重排 + 岩浆内/烧伤职责分离**
  - **① walk-out 加时限 + 低血门槛**（"安静走出去可能被灼烧伤害烧死"的收口）：
    - 根因：旧版只要找得到安全格就"安静走出"，不看走了多久、血量多少——浅接触
      确实能一步上岸，但安全格若在岩浆对岸/被隔断，女仆要裸奔趟几格岩浆（每秒 4 血
      + 半路被点燃），低血时可能没走到就烧死；且途中不喝抗火、不倒水；
    - 修复：新增 `walkOutTimer`（m_6725_ 每 tick 累计连续处于岩浆内的 tick 数，
      脱离即归零）——**连续 3 秒（60 tick）还没走出岩浆，或血量 <40%**，一律跳过
      "安静走出"，转主分支 2 上手段（垫方块/倒水/抗火/珍珠）。"安静走"只对
      真·浅接触（一步可上岸）有效，不再是裸奔赌步行；
  - **② 主分支 2 逃生链路重排**（用户指定顺序，每 tick 依次尝试）：
    1. **垫方块**（lavaStepUp，有方块先垫高脱离液面）；
    2. **水桶**（新 `placeWaterOnLava`：放【最近的岩浆格上方一格】，水流下渗冷却
       脚下岩浆成黑曜石/玄武岩，3 秒后 tickRecoverWater 收回、水桶不消耗）——
       **去掉了旧的 `findWater(radius12)==null` 门槛**：有水桶直接用，不再先跑 12 格
       找河（旧逻辑"有自然水就不倒水"与"水桶是便宜资源"意图冲突）；
    3. **抗火药水**（饮用直接喝、喷溅/滞留扔脚下；**已有抗火效果不重复喝/扔**——
       新 `hasFireResist` 检查，防 8 分钟免疫期间浪费第二瓶）；
    3b. **末影珍珠**（新 `pearlEscapeFromLava`：半径 12 内找安全落点——自身非岩浆、
       脚下实心、站立位+头顶 2 格空气防活埋、无危险方块——瞬移脱离岩浆；
       没有威胁目标，用安全格扫描而非反威胁方向）；
    4. **自然水源**（radius 12 → 导航过去，兜底）。
  - **③ 岩浆内 / 烧伤职责分离**：岩浆分支只做"逃出岩浆"一件事；脱离岩浆后身上
    的火由【着火分支】接管（灭火器 → 水桶 → 抗火 → 找水 → 逃跑）——每 tick 重入
    environmentalEscape 天然衔接（m_20077_ 变 false 后 m_6060_ 恒兜住火没灭的情况）。
    着火分支的抗火同样加 `hasFireResist` 检查（岩浆分支喝过就不重复浪费）；
  - **④ 补充调查结论（写入补丁说明备忘）**：
    - 低血无条件喝药兜底【存在】：m_6725_ 三处回血点（无威胁/威胁非濒死/贴脸濒死），
      共同条件 `potionCooldown<=0 && hp<50%` → useHealingPotion || useGoldenApple，
      不依赖威胁状态/距离；唯一例外是 danger（岩浆/着火）期间——environmentalEscape
      先 return，符合"先逃出/抗火，脱离危险后回血分支自动接管"的设计；
    - 作战中跌入岩浆【无冲突】：自保(250)先于战术(230) tick，岩浆时写 MOVING_SURVIVE，
      战术 m_6725_ 开头 `isMovingToSurvive` 本 tick 让位（不抢导航/垫高/倒水）；
      脱离危险后标记移除、战术恢复（逃生期间不攻击 = 保命优先的预期行为）；
  - **实测清单**：
    1. 浅接触岩浆 + 一步可上岸 → 仍安静走出（不播报不垫高不倒水不喝药）；
    2. 岩浆池深陷 → 3 秒内找不到安全地面 → 转主分支 2：垫方块 → 放水冷却 → 抗火/珍珠 → 找水；
    3. 低血（<40%）掉岩浆 → 直接主分支 2，不赌步行；
    4. 有水桶但旁边有河 → 直接放水冷却脚下岩浆，不再跑向河（3 秒后水消失）；
    5. 已有抗火效果再掉岩浆/着火 → 不重复喝第二瓶；
    6. 背包有末影珍珠 + 半径 12 有安全落点 → 瞬移脱离岩浆（"用末影珍珠瞬移逃走了！"）；
    7. 上岸后身上着火 → 自动走着火分支灭火链（兜底烧灼伤害）；
    8. 作战中跌入岩浆 → 自保接管移动、战术让位，脱离后恢复战斗；
    9. 回归：落地水/珍珠/传送/搭高/着火水桶/地狱跳过水判定不受影响。
  - **① 着火时水桶不用的根因**：旧顺序 = 灭火器 > **找自然水** > 水桶——附近有河/水塘时
    永远先跑向自然水（"只会找附近的水源"），身上水桶反而被跳过；改成
    **灭火器 > 水桶（原地倒水立即灭，非地狱）> 抗火药水 > 找自然水 > 逃跑**——
    水桶的水是便宜资源，不用跑 12 格找河；
  - **② 抗火药水判定（掉岩浆 / 着火时，新增）**：
    - 背包扫饮用型抗火药水（Potion 注册名 fire_resistance / long_fire_resistance）→
      **直接喝掉**（返还空玻璃瓶，施加 8 分钟抗火，"喝了一瓶抗火药水！"）；
    - 喷溅/滞留型 → **低抛扔在自己脚下溅射**（"抗火药水泼在身上！"）；
    - 岩浆深陷分支（半径 6 内无安全地面）：**先抗火（立即止血 4hp/s）** → 垫高 → 倒水；
      浅接触能走出去的分支不变（安静走开，不用消耗）；
    - 着火分支顺序：灭火器 → 水桶 → 喝抗火 → 扔抗火 → 自然水 → 逃跑；
  - **③ 地狱跳过一切水判定（新）**：`isNether` 判定维度——
    地狱里水桶倒水/找自然水全部跳过（放水瞬间蒸发，做无用功）：
    着火 = 灭火器 → 抗火（喝/扔）→ 逃跑；岩浆 = 抗火 → 垫高 → 导航安全点；
  - **实测清单**：
    1. 女仆着火 + 背包有水桶（旁边有河）→ 原地倒水灭火，不跑向河；
    2. 女仆着火 + 无灭火器无水桶 + 有抗火药水 → 饮用型直接喝、喷溅/滞留扔脚下；
    3. 深陷岩浆 → 先喝/扔抗火再垫高（不再边烧边垫）；
    4. 浅接触岩浆 → 仍安静走开（不消耗药水/方块）；
    5. 地狱着火 → 不用水桶不找水，抗火或逃跑；地狱岩浆 → 抗火 + 垫高（不倒水）；
    6. 回归：walk-out/垫高/倒水回收 3 秒/落地水/自保不受影响。
- **promaid 1.5.207：玩家对女仆伤害策略（面板可调——TLM 原版压制 / 完全免疫 / 无限制 / 有上限）**
  - **根因**（TLM 反编译实证）：`EntityMaid.m_6469_`（hurt）对"玩家且是女仆主人"的攻击
    执行 `amount = Mth.m_14036_(amount / 5.0f, 0.0f, 2.0f)`——**伤害 ÷5、封顶 2 点**：
    - 原版剑 7 点 ÷5 ≈ 1.4，再被护甲减伤 ≈ 0 → "玩家打不到女仆"；
    - 更好的战斗（Better Combat）这类高伤武器 8~12 点 ÷5 后仍能打满 2 点 + 击退反馈
      → "剑能打到女仆"。两者走同一条路径，只是数值观感差异；
  - **新配置（combat 段，面板"被动技能"栏）**：
    - `playerDamageMode`（循环选择）：**0 = TLM原版**（÷5 封顶 2，默认）；
      **1 = 完全免疫**（任何玩家都打不到女仆，含弓弩/投掷物——HEAD 直接 return false）；
      **2 = 无限制**（解除 ÷5 压制，像打普通生物一样全伤害）；
      **3 = 有上限**（单次伤害 = 女仆最大生命 × playerDamageMaidCap）；
    - `playerDamageMaidCap`（0.01~0.5，默认 0.1）：模式 3 用——20 血女仆单次最多 2 点；
  - **实现**：新 mixin `MaidPlayerDamageMixin`（@Mixin TLM EntityMaid）——
    HEAD 注入处理"完全免疫"；@Redirect 拦截 TLM 的 `Mth.m_14036_(amount/5, 0, 2)`
    （m_6469_ 内唯一一处）——模式 2/3 时把已 ÷5 的值还原/按比例封顶；
    模式 0 完全保持 TLM 原行为；非玩家攻击（怪物/环境）不受任何影响；
  - **实测清单**：
    1. 默认（0）：原版剑打女仆几乎不掉血、高伤武器打出 2 点（= 之前行为，不变）；
    2. 完全免疫：玩家无论用什么武器/弓弩都打不到女仆（不掉血不受伤动画）；
    3. 无限制：剑打女仆全伤害（20 血女仆几刀倒）；
    4. 有上限（0.1）：单次最多 2 点（多段攻击叠加仍会累积）；
    5. 怪物/环境伤害不受影响（女仆正常战斗、正常掉血）；
    6. 面板战斗自保 → 被动技能栏可见新行，切换即时生效。
- **promaid 1.5.206：投喂/治疗策略全面化——状态感知优先级链，不再只投喂单一药水/食物**
  - **盘点结论（原版 1.20.1 全量）**：
    - 可食用药水按 Potion 注册名分类（1.20.1 实证）：治疗 = healing/strong_healing
      （效果 instant_health，4/8 点）、再生 = regeneration/long_/strong_、抗火 =
      fire_resistance/long_fire_resistance；形态 = 饮用 / 喷溅 / 滞留；
    - 食物清单从 9 种熟食扩展为 **21 种全量安全有益食物**（EmotionalActionExecutor.FOODS）：
      熟牛排/猪排（12.8 饱和）、金胡萝卜（**14.4 最高**）、兔肉煲（10/12）、南瓜派、
      蘑菇煲、甜菜汤、烤马铃薯、熟鸡/羊/兔/鳕/鲑、面包、胡萝卜、苹果、西瓜片、
      甜浆果、发光浆果、曲奇、干海带、蜂蜜瓶；
    - 危险/负面【不喂】：腐肉/蜘蛛眼/毒马铃薯/河豚/紫颂果（瞬移）/可疑炖菜（随机效果）/
      一切生食（raw_*，生鸡肉中毒概率）；
  - **最优策略（MaidAidOwnerBehavior 主逻辑重构，每 3 秒检查一次、命中即止）**：
    1. **血量 < 30%**：喷溅治疗药水 → 滞留治疗药水 → 金苹果/附魔金苹果（即时吸收+再生）
       → 喷溅/滞留再生药水 → 普通治疗药水塞主人背包（"快喝！"）→ 喂高饱食物（自然回血）；
    2. **着火**：喷溅/滞留抗火药水 → 普通抗火塞背包；
    3. **负面效果**（中毒/凋零/缓慢/虚弱/失明/饥饿/反胃/黑暗/漂浮）：牛奶桶塞背包（全解）
       → 蜂蜜瓶（解中毒 + 回饱食）；
    4. **饱食度 < 12**：按饱和度恢复量喂最优（金胡萝卜 > 熟牛排/猪排 > 兔肉煲 > …）；
  - **实现细节**：
    - 药水识别统一用药水注册名（ForgeRegistries.POTIONS，自保侧同款已验证方式），
      删除旧的"效果字段比对"路径（v1.5.205 修的 f_19616_ 坑不再有同类风险）；
    - 投掷逻辑统一（throwPotionToOwner）：喷溅优先（立即溅射命中）、滞留次之（范围云
      持续生效）；普通饮用型药水/牛奶/蜂蜜瓶走"塞主人背包"（背包满自动退回）；
    - 食物饱和度排序沿用 foodSaturation（nutrition × saturationModifier × 2.0）；
    - 金苹果/附魔金苹果仍走即时增益路径（不进食物队列）；
  - **实测清单**：
    1. 主人低血 + 女仆背包有喷溅治疗药水 → 投掷"主人别怕，治疗药水来了！"；
    2. 只有普通治疗药水 → 塞主人背包并提示快喝；
    3. 只有金苹果 → 即时施加吸收+再生；只有再生药水 → 投掷再生；
    4. 主人着火 → 投掷抗火药水；主人中毒/凋零 → 牛奶/蜂蜜塞背包；
    5. 主人饥饿 → 金胡萝卜/熟牛排优先（饱和度最高），不再只给面包苹果；
    6. 女仆背包有腐肉/生肉 → 不喂（白名单外）；
    7. 回归：自保回血链/金苹果/落地水/岩浆逃生不受影响。
- **promaid 1.5.205：对话语言根治（TLM 聊天 GUI 也强制中文）+ 治疗药水判定修复 + 图腾扣减修正**
  - **① 对话"前几句中文，之后变日文"根治**：
    - 根因（字节码实证）：TLM `MaidAIChatManager.tryToChat` **每次对话**都执行
      `chatLanguage = clientInfo.language()`——玩家在 TLM 聊天 GUI 发起的对话传的是
      **客户端游戏语言**（用户客户端 = 日语）→ 聊几句后 chatLanguage 被覆盖成日语，
      之后所有对话（含 promaid 主动对话）都按日语生成。旧修复（v1.5.198）只改了
      promaid 自己的 3 个主动对话调用点，拦不住 TLM 侧的语言覆盖；
    - 修复：新增 mixin `MaidChatLanguageMixin`——在 `tryToChat` 入口
      （@ModifyVariable argsOnly）把 ChatClientInfo.language 直接替换为配置
      `dialogue.outputLanguage`（非空时），方法体内两处读取（chatLanguage 字段 +
      提示词 ${chat_language}）都吃到新值 → **覆盖所有调用方**：玩家 GUI 对话 /
      promaid 主动对话 / TLM 其他对话全部中文；留空 = 跟随 TLM/客户端（原行为）；
      语音仍走 TTS 配置（日语）不受影响；
  - **② 女仆不扔治疗药水（喂主人/药水/金苹果"没实现"的真相）**：
    - 根因（字节码实证）：`MaidAidOwnerBehavior.potionHealEffect` 比对
      `MobEffects.f_19616_`——1.20.1 里治疗效果注册名是 **"instant_health"**（旧名
      healing 已改名），对应字段是 **f_19601_**；f_19616_ 是后面的另一个效果 →
      **治疗判定永远不匹配 → 喷溅治疗药水路径静默死亡**（金苹果/喂食字段全部
      验证正确，功能本身在，只是药水判定死了）；
    - 修复：改用注册表查找 `minecraft:instant_health`（ForgeRegistries.MOB_EFFECTS），
      彻底避开 SRG 字段名；其余喂食/金苹果/火把功能经 javap 逐字段验证全部正确
      （cooked_* 熟食、golden_apple f_42436_/enchanted f_42437_、torch 物品 f_42000_/
      Blocks.f_50081_、regen f_19605_/absorption f_19617_/fire_res f_19607_）；
  - **③ 图腾扣减修正（配合 v1.5.204）**：consumeTotem 兜底路径 `m_41764_(1)` 修正为
    `m_41774_(1)`——javap 实证 `m_41764_` 是 **setCount**（直接把数量设成 1！）、
    `m_41774_` 才是 shrink（减 1）——旧版 count=1 的图腾永远扣不掉（"图腾不消耗
    直接无敌"的另一半根因）；extractItem 主路径不受影响；
  - **部署内容 = v1.5.204 + v1.5.205 全部变更**（204 因游戏运行中未部署）：
    不死图腾真实消耗（女仆/主人共享双修复）、浅岩浆安静走开/深陷才垫高、水桶
    无方块也能倒水自救（canPourWaterAt 允许替换液体）+ 着火水桶灭火、附近岩浆
    感知绕开（"面前一格岩浆也逃"）、放水 3 秒自动回收（会话外也回收）、建造
    两步流程解锁（重开手册不再重置预览标记）；
  - **实测清单**：
    1. TLM 聊天 GUI 与女仆对话 → 全程中文（不再聊几句后变日文）；
    2. 女仆主动对话/系统消息 TTS → 文本中文、语音日语；
    3. 主人低血 + 女仆背包有喷溅治疗药水 → 投掷治疗药水（"主人别怕，治疗药水来了！"）；
    4. 无药水但有金苹果 → 金苹果即时增益；无金苹果 → 喂熟食；饱食度 <12 → 递食物；
    5. 主人周围黑暗 + 女仆背包有火把 → 自动插火把（1.5 秒冷却）；
    6. 图腾触发后数量真实 -1（用光正常死亡）；主人被救后女仆图腾 -1；
    7. 回归：岩浆逃生/水桶自救/落地水/建造两步流程/自保/战术不受影响。
- **promaid 1.5.204：不死图腾真实消耗 + 岩浆感知/逃生大修（播报/无水自救/避让）+ 放水 3 秒回收 + 建造两步流程解锁**
  - **① 不死图腾不再无限复活**（女仆 + 主人共享双修复）：
    - 女仆自己：饰品栏图腾复活后**只 revive 不扣减** → 每次致命伤都复活 = 无敌；
      现在 `consumeTotem`（extractItem 直接操作存储）真实消耗 1 个，扣完清空槽位；
    - 主人共享：旧版对 `getStackInSlot` 返回的栈 `m_41764_(1)`——封装 handler 若返回
      副本则扣不掉 → 主人在女仆旁边**无限复活**；改为 `extractItem(slot,1,false)`
      真实扣减（TLM BaubleItemHandler/ItemStackHandler 均实证继承 Forge extractItem）；
    - 手里已有图腾仍走原版（不双份消耗）；配置 combat.totemShareEnable 不变；
  - **② 岩浆播报乱说"垫上来"**（"被烫伤后不管有没有方块都说遇到岩浆要垫上来"）：
    - 根因：`m_20077_()`（isInLava）对**浅接触也 true**（脚碰到岩浆流体即触发），
      旧版无条件播报 + 垫高——明明旁边就是实心地面，走出去更优；
    - 修复：岩浆分支先找安全地面（半径 6）——**能找到 → 安静走出去**（不播报、
      不垫方块、不倒水）；只有**深陷**（周围找不到安全地面）才播报"掉进岩浆里了！
      我垫上来！"并垫高 + 倒水；
  - **③ 只有水桶没有方块失去自救**（"包里有水却不自救的笑话"）：
    - 根因（字节码实证）：1.20.1 的 `m_60815_()`（1 参 canBeReplaced）由
      `Properties.canBeReplaced` 标志位直接决定，水/岩浆的 Properties **没有
      replaceable 标志 → 恒为 false**；旧 placeClutchWater 只认 m_60815_() →
      站在岩浆里"自身格 + 上方格都是岩浆"判定放不了 → 永远倒不出水（垫了方块
      站到方块上、上方是空气才能放）；
    - 修复：新增 `canPourWaterAt`——空气 / 可替换 / **液体（岩浆/水）**都允许倒水，
      直接用水替换脚下的岩浆（水灭火 + 周边岩浆变黑曜石/圆石垫脚）；
    - 着火分支也补上水桶自救：灭火器 → 找自然水 → **有水桶直接倒水灭火** →
      才兜底逃跑（旧版着火分支完全没用过水桶）；
  - **④ 感知不到岩浆/流动岩浆**（"面前一格岩浆也没有逃跑的迹象"）：
    - 新增附近岩浆感知（每 10 tick 扫半径 3、y ±1，静态/流动岩浆都是
      minecraft:lava 块一次覆盖）：发现岩浆 → 纳入环境危险 → 环境逃生新分支
      "附近有岩浆，我绕开走！"（每场一次提示）→ 导航到离岩浆 ≥4 格的安全站立点
      （找不到就原地不动，绝不往岩浆方向走）；深层岩浆湖（脚下 2+ 格）不误报；
    - 顺序：深陷岩浆 > 着火灭火 > 附近岩浆避让 > 溺水 > 卡墙（灭火优先于避让，
      避免"着火 → 避让 → 永远轮不到灭火"互相卡住）；
  - **⑤ 放下的水一直不消失**（"应该 3 秒后自动消失"）：
    - 根因：tickRecoverWater 排在会话状态机的**提前 return 之后**——放水救完人
      会话立即结束 → 永远轮不到回收，水永久留在世界；
    - 修复：回收提前到 tick 最开头（会话外也跑）；回收时长 **1 秒 → 3 秒（60 tick）**；
  - **⑥ 建造卡在第一步死循环**（"强制显示一次区块，再次点击建造却永远进不了正式建造"）：
    - 根因：`BlueprintAreaPreview.clear()` 每次打开手册都被调用（open → 关金色框），
      旧版顺带把"已看过预览"标记 previewSeen 重置 → 第 1 步提示"再次打开手册点击
      确认建造"，但重开后标记已清 → 点击又走第 1 步（强制预览 + 退出手册）→ 无限循环；
    - 修复：clear() **只关金色框渲染、不再重置标记**（跨手册会话保留）；新增
      `resetSeen()`——只在**确认创建区块成功后**调用（下一轮建造仍需先看范围，
      防误操作保留）；两步流程真正打通：第一次点击看范围 → 重开手册点击 → 确认弹窗 → 正式创建；
  - **实测清单**：
    1. 女仆饰品栏/背包图腾触发复活后数量 -1，用光后正常死亡（不再无敌）；
    2. 主人致命伤被共享图腾救起后，女仆背包图腾 -1（不再无限复活）；
    3. 脚踩浅岩浆/岩浆边缘 → 安静走出去（不播报"垫上来"）；深陷岩浆池 → 播报+垫高；
    4. 只有水桶没有方块掉进岩浆 → 直接倒水替换脚下岩浆自救（不再"有桶不救"）；
    5. 着火且无灭火器/无自然水 → 有水桶会倒水灭火（3 秒后水自动消失）；
    6. 面前 1 格岩浆 → "附近有岩浆，我绕开走！"并走开（不踩进去）；
    7. 击退垫水/岩浆灭火的水 3 秒后自动消失（会话结束也不残留）；
    8. 建造流程：第一次点击看金色框 → 重开手册再点击 → 确认弹窗 → 正式创建区块；
       创建成功后下一轮建造重新先看范围；
    9. 回归：落地水/珍珠/传送/搭高/战术战斗/环境提示/气泡 TTS 不受影响。
- **promaid 1.5.203：搭高安全高度配置化（与落地水触发高度配对）**
  - **背景**：落地水触发高度 = 3.0 格（fallDistance 阈值，面板可调）；搭高惯性/补完
    高度旧版写死 4 格——从 4 格柱顶跳下，fallDistance 累计比实际下落慢（0 加速 +
    0.5 系数），到 3.0 时实际已下落约 3.5 格、离地只剩 0.5 格 → findLandingPos
    放水落点判定在临界窗口经常失败 → "搭 4 格跳下触发不了落地水"；
  - **修改**：新增配置 combat.pillarSafeHeight（搭高安全高度，默认 **5**，范围 2~12）——
    搭高惯性/非濒死补完/濒死搭高的安全高度统一读此配置（旧三处写死 4 全部替换）；
    默认 5 格跳下时 fallDistance 到 3.0 还有约 2 格放水窗口 → **稳定触发落地水**，
    水减速怪物（小配合）；面板战斗页"逃生与自保"栏新增"搭高安全高度（格）"行；
    想更稳可把"落地水触发高度"调低（2.0~2.5）或把安全高度调高；
  - **实测清单**：
    1. 女仆搭高补完到 5 格 → 跳下 → 稳定触发落地水（"落地水！"播报）；
    2. 面板调整搭高安全高度 4↔6 观察触发窗口变化；
    3. 落地水触发高度调 2.5 后 4 格跳下也能触发；
    4. 回归：搭高惯性/补完/濒死搭高/强制搭高 30 格不受影响。
- **promaid 1.5.202：轻量级自保（落地水格式）——边打边保命，不再与战斗冲突**
  - **背景**：旧自保是"重状态机"——core 优先级 250 每 tick 清攻击意图 + mixin 断
    sensor/StartAttacking 目标源 → 战术（230）拿不到目标 → "只逃不打"、被尸壳围殴
    磨死（日志实证：8 分钟 jump attack 打不死、传送回家 2 秒后站着被杀）。
    用户要求：自保应该像落地水一样是轻量被动技能，与战斗自保互补不冲突。
  - **落地水格式重构**（与 WaterClutchBehavior 同构）：
    - canUse 只查开关（常驻检查器），m_6725_ 每 tick 内部判定"是否需要保命"，
      不需要时**零干预**（不碰目标/移动/任何行为，战术/任务照常）；
    - 会话进出由内部 sessionActive + PRESERVE_TAG 维护：环境危险或血 <30% 上升沿
      进入（sessionEnter：重置会话字段/图腾/松弓/日志），血 ≥70% 且环境安全后结束；
    - 常驻模式下无"退出→再进场"抖动 → **v1.5.200 退出稳定期删除**（配置项一并移除）；
  - **边打边保命**（核心修复）：
    - 自保**不再每 tick 清攻击意图**；删除 HostileSensorPreserveMixin +
      StartAttackingPreserveMixin（目标恢复写入/重设）——core 行为本就并行
      （Brain 按优先级逐个运行所有 canUse 的行为），自保（250）只做保命动作，
      战术（230）同 tick 并行战斗；
    - 战术 isTacticsEnabled **去掉"战斗任务"限制**——任何任务/跟随/待机被怪物锁定
      在接战范围内都会进入战术战斗（"该打就打"）；举盾判定同步放开；
    - **移动接管协调**：自保执行移动类保命动作（濒死垫高/逃跑、环境逃生导航）时
      置 MOVING_SURVIVE 标记 → 战术本 tick 让位（不抢移动控制，防走位/跳劈与
      垫高互相覆盖）；瞬时动作（喝药/金苹果/传送）不占标记 → 战斗照常；
    - **威胁分支收窄**：血 ≥30% 只做瞬时喝药 + 搭高惯性补完（移动交给战术）；
      血 <30% 濒死才进入全套保命移动（垫高/逃跑/珍珠/反击）；环境致命/软危险
      （岩浆/着火/溺水/窒息）判定提前到威胁判定之前——任何威胁状态下先保命
      （之前岩浆里被怪打会"边打边烧"）；
    - 传回主人身边后不再立即结束自保（会话由 m_6725_ 维护，血低继续回血/警戒）；
  - **面板**：战斗页合并为"逃生与自保（被动保命）"一栏——自保开关/阈值/搭高/逃跑/
    珍珠/落地水/水桶垫水全部与落地水并列；删除"退出稳定期"行；
  - **实测清单**：
    1. 女仆低血被尸壳围殴 → 战术跳劈/走位照常（不再只逃不打），边打边喝药；
    2. 血 <30% 濒死 → 垫高/逃跑/珍珠保命，战术让位不抢移动；
    3. 岩浆/着火/溺水/窒息 → 立即环境逃生（任何血量、任何威胁状态），有提示；
    4. 非战斗任务（挖矿/跟随）被怪锁定 → 也会战术战斗（该打就打）；
    5. 血 ≥70% 且环境安全 → 自保零干预（行为常驻但无任何动作）；
    6. 面板战斗页 → "逃生与自保（被动保命）"一栏包含落地水与自保全部配置；
    7. 回归：落地水/珍珠/传送/搭高惯性/水桶垫水/环境提示/气泡 TTS 不受影响。
- **promaid 1.5.201：喂食按饱和度恢复量排序 + 金苹果改即时增益道具（不再当常规食物）**
  - **喂食优先级 = 回复的饱和度**：
    - `EmotionalActionExecutor.giveFoodToOwner`（饱食度低时递食物）从"按背包槽位顺序选
      第一个"改为**按饱和度恢复量排序选最优**（MC 公式：nutrition × saturationModifier × 2.0；
      熟牛排/熟猪排这类高饱和度食物优先，面包/苹果殿后）；`MaidAidOwnerBehavior.
      feedHealingFood`（血量低时喂治疗食物）同样排序；
    - 新公共方法 `EmotionalActionExecutor.foodSaturation`（m_41720_=getItem、
      m_41473_=Item.getFoodProperties()、m_38744_=getNutrition、m_38745_=getSaturationModifier，
      javap/常量池实证）；
  - **金苹果/附魔金苹果不再作为常规食物**（全局语义）：
    - `EmotionalActionExecutor.FOODS` 与 `MaidAidOwnerBehavior.HEAL_FOOD_ITEMS`、
      `SelfPreservationBehavior.HEAL_FOODS` 全部移除金苹果——女仆不把金苹果当食物啃、
      不塞主人背包当干粮；
    - 改为**像喷溅药水一样即时使用**：`MaidAidOwnerBehavior.useGoldenApple`（主人血量低、
      无喷溅治疗药水时：消耗 1 个，直接给主人施加原版金苹果效果——吸收+再生，附魔金苹果
      额外抗性/抗火，附魔优先）+ 播报"主人，金苹果给你！"；自保 `SelfPreservationBehavior.
      useGoldenApple` 同款（自身回血链：增益药水 → 治疗药水 → **金苹果** → 熟食，贴脸分支
      也纳入）；
  - **附带发现**：TLM 1.5.3 字节码里调用的 `Item.getFoodProperties(ItemStack, LivingEntity)`
    两参方法在 1.20.1 **不存在**（1.20.1 只有无参版）——运行时必静默失败（NoSuchMethodError
    被吞），是 TLM 喂食/工作餐判定一直失效的隐藏原因（promaid 用无参 m_41473_ 不受影响）；
  - **实测清单**：
    1. 主人饥饿 → 女仆优先递熟牛排/熟猪排（饱和度最高），不再乱递面包/苹果；
    2. 主人低血且背包有金苹果 → 女仆直接给主人施加吸收+再生（金苹果不进主人背包）；
    3. 女仆自己低血自保 → 治疗药水 > 金苹果 > 熟食（金苹果不再被当食物吃）;
    4. 附魔金苹果优先于普通金苹果使用；
    5. 回归：自保回血链/投喂/治疗药水投掷不受影响。
- **promaid 1.5.200：自保退出稳定期（防 70% 上下浮动频繁进出）**
  - **问题**：自保退出条件是"血 ≥70% 立即解除"——20 血无护甲前期，被怪摸一下就是
    15~25% 血量波动，血条在 70% 上下浮动时自保"解除 → 又被打 → 再进场"来回横跳，
    行为显得怪异；
  - **方案（防抖/滞回）**：新增配置 combat.exitStableTicks（默认 40 tick = 2 秒，0 = 关闭）——
    血量达到退出线后需**连续 N tick 无威胁（cachedThreat==null）且血量未在下降**
    才真正解除自保；期间掉血或出现威胁 → 稳定计时清零重来；
  - 效果：血条在 70% 附近怎么抖都不影响状态，只有真正安全（无威胁且不掉血 2 秒）
    才退出；退出的"呼……应该安全了吧"播报与传送回家照常；
  - 面板战斗页新增"退出稳定期（tick）"行；
  - **实测清单**：
    1. 无护甲 20 血被怪打，血在 65%~75% 浮动 → 自保不再频繁解除/重进（日志不再反复
       "self preserve start"）；
    2. 真正脱离战斗 2 秒后才播"呼……应该安全了吧"并退出；
    3. 稳定期内被打一下 → 计时清零继续自保（不回正常模式）；
    4. 传送回家路径不变（血≥50% 且 6 格无威胁退出）；
    5. 填 0 恢复旧行为（立即退出）。
- **promaid 1.5.199：自保逻辑链修复（搭高积极性/传送保命/围殴喝药）+ 水桶垫水 + 岩浆逃生 + 环境提示 + 爱憎分明饥饿开关**
  - **背景（日志实证）**：08-09~08-10 女仆 9+ 次被尸壳群殴致死；"self preserve start hp=95%/80%…" 高频刷屏
    （自保高血量反复触发）；"我回到主人身边了，先缓缓……" 后 2~4 秒死亡（传送回家站着被杀）；
    "搭两个方块就不搭了"（搭高被反复打断）；饿死/撑死/被围殴时不喝治疗药水（回血分支被威胁分支压住）。
  - **自保进场抖动修复**：envDangerSoft 的卡墙判定从"身体中心在实心"（bodyInSolid）收窄为
    **"头部卡实心"（新增 headInSolid）**——狭窄地下地形被尸壳推挤只卡下半身不再误报，
    高血量不再反复进自保；antiSuffocate 兜底仍用 bodyInSolid（挪位置无害）；
  - **传送回家后保命**（"回到主人身边→站着被尸壳打死"的根因）：
    - teleportHome 成功**不再立即结束自保**（不清 PRESERVE_TAG）——传回时血可能仍 <30%、尸壳仇恨还在；
    - canContinue：传送回家后改为**血 ≥50% 且身边 6 格无可见威胁**才退出（期间继续回血/警戒/搭高）；
    - canUse 免触发窗口（5 秒）：窗口期内身边有可见威胁 → **放行重新进场**（原窗口把重新进场全挡住）；
  - **搭高惯性**（搭方块积极性下降修复）：threat==null 时若本次自保已开始搭高（pillarBaseY>=0）
    且高度 <4 → 继续 buildUp 到安全高度再停（地下怪贴墙/绕墙时 hasSight 判定让威胁 5 tick 级抖动，
    "搭两块就停→被围殴打下来"）；
  - **回血逻辑链修复**：威胁分支（贴脸反击/搭高前）插入——potionCooldown<=0 且 hp<50% 且
    useHealingPotion 成功 → 置冷却（治疗药水是即时动作，不打断搭方块/反击；被围殴/饿死撑死
    血量见底也能喝药）；
  - **击退搭高水桶垫水**（新）：triggerPillarBurst（连续近身 2 tick 触发"击退+强制搭高"）时，
    配置 combat.waterBucketPillar（默认开）且包里有水桶 → 脚下可替换格放水缓冲（被击退上抛/
    落地接住），**1 秒后收回**，播报"放水垫一下！"（水桶不消耗，与落地水一致）；
  - **岩浆逃生放水**（新）：environmentalEscape 岩浆分支垫高后，配置 combat.waterBucketLava（默认开）
    且周围 12 格无水源、包里有水桶 → 在自己垫的方块上放水灭火（1 秒后收回；接触的岩浆源可能变黑曜石）；
  - **环境提示**（新，每场一次）：掉进岩浆"掉进岩浆里了！我垫上来！"；溺水"溺水了！我游上去！"；
    卡墙窒息"我卡住了，快喘不过气了！"（着火已有灭火器播报）；自动走 v1.5.198 系统消息 TTS 朗读；
  - **爱憎分明饥饿/撑死测试开关**（misc.loveLoatheHungerOff，默认 **true=关闭**）：
    - 新 mixin `LoveLoatheHungerGateMixin`（@Pseudo @Mixin(targets=HungerManager)，未装爱憎分明静默跳过）：
      onServerTick HEAD cancel（饿死扣血/撑死+OverfedDeath/自动进食（**含腐肉→越吃越饿**）/速度惩罚全禁）
      + onMaidDeath HEAD cancel（防御旧标签"被撑死"取消死亡广播）；
    - 吃饭 +信任/-恐惧（onMaidEat）保留；记忆适配层读 EmotionData 与 HungerManager 独立不受影响；
    - promaid 自保 HEAL_FOODS 白名单本就没有腐肉（实证），腐肉来自爱憎分明自动进食，开关关闭后消失；
  - **配置新增**：combat.waterBucketPillar / waterBucketLava（默认开）、misc.loveLoatheHungerOff（默认开）；
    面板战斗页 +2 行、杂项页 +1 行；
  - **实测清单**：
    1. 地下被尸壳围殴：低血进自保 → 搭高不再"搭两块就停"；传回主人身边后继续警戒/喝药，不再站着被杀；
    2. 高血量（>85%）不再反复 "self preserve start"（日志验证）；
    3. 贴脸被围殴 hp<50% 会喝治疗药水；
    4. 连续近身触发"击退+强制搭高"时包里有水桶 → 脚下放水 1 秒后收回；
    5. 掉进岩浆 → 垫高 + 无水源有水桶 → 放水灭火 1 秒收回；有"掉进岩浆里了！"提示；
    6. 卡墙窒息/溺水有提示；
    7. 爱憎分明饥饿被禁用：女仆不再自动吃腐肉、不再饿死/撑死（面板开关可恢复）；
    8. 回归：落地水/珍珠逃生/传送/威胁判定/记忆/建造不受影响。
- **promaid 1.5.198：TTS 音量倍率 + 系统消息朗读（训练一次缓存复用）+ 系统语音包导入 + 记忆 API 绑定 + 对话语言强制**
  - **TTS 音量倍率**（修复"TTS 播放声音太小"）：
    - 根因：TLM 的 MaidAISoundInstance 播放音量硬编码 1.0f，无任何音量配置；
    - 新增客户端 mixin `MaidTtsVolumeMixin`（mixins.promaid.json 新增 "client" 段，专服不加载）：
      在构造 super 调用处把音量参数（index 2）按配置倍率放大；
    - 配置：语音页 `voice.volumeMultiplier`（0.1~5.0，默认 2.0）——LLM 对话 TTS 与系统消息
      TTS 都经 MaidAISoundInstance 播放，一并生效；
  - **系统消息朗读 + "训练一次"语音缓存**：
    - 新服务端管理器 `SystemTTSManager`（com.maidsmart.voice）：ChatBubbleLimitMixin 拦截
      addTextChatBubble 后把通过限频的真实系统气泡（感知/工作/自保/建好啦等规则消息）汇入朗读；
    - 播放优先级：① 系统语音包命中（免 TTS）→ ② 语音缓存命中 → ③ 调 TLM TTS 站点合成；
    - ③ 的自定义回调 `SystemTtsCallback`（继承 TLM TTSCallback）成功时把音频字节**落盘缓存**
      `config/maid_smart/voice_cache/<sha256(text|model|lang)>.ogg`（=训练一次保存，此后同一
      文本直接复用、不再调 TTS API），再发给主人播放；失败只记日志（不刷 LLM 气泡/不弹错）；
      缓存上限 `voice.cacheMaxFiles`（默认 200，超出删最旧）；
    - 门禁：voice.systemEnabled + TLM AIConfig.TTS_ENABLED + 站点存在启用 + 有主人 +
      文本可朗读（含中文/空格，过滤 TLM 翻译 key 与省略号）+ per-maid 冷却
      voice.systemCooldownS（默认 8 秒）；
    - system/player2 本地播放站点走 TTSSystemAudioToClientMessage 实时播放（无字节可缓存）；
  - **系统语音包（manifest + ogg，面板导入）**：
    - 目录 `config/maid_smart/system_voice/`：manifest.json + ogg（可放子目录）；
      格式 `{"entries":[{"text":"建好啦！","file":"build_done.ogg"},{"text":"材料不够","file":"no_material.ogg","mode":"contains"}]}`，
      mode 默认 exact，contains 按顺序匹配；
    - 新类 `SystemVoicePack`：加载/惰性读字节/匹配/导入（文件夹或 zip，zip-slip 防护）；
    - 网络包 12/13（VoicePackImportPacket / VoicePackQueryPacket，结果直接回玩家聊天框）；
    - 面板"语音"页：音量倍率/系统消息朗读开关/朗读冷却/语音包开关/导入路径（保存即导入）/
      重新加载/查看状态/缓存上限；
  - **记忆 API 绑定**（设置面板可填独立 API，格式同 TLM，留空跟随 TLM）：
    - 新增 memory.apiUrl / apiKey / apiModel（String，默认空）；AiMemoryExtractor 的
      url/密钥/model 三处改为"非空用自定义、空回退 TLM 女仆当前 LLM 站点"（headers/
      hasThinkingField 仍用 TLM 站点）；密钥明文存 config/promaid-common.toml（与 TLM
      sites/llm.json 一致）；面板 AI 记忆页新增"记忆 API（留空 = 跟随 TLM）"小节 3 个输入行，
      清空即回退该项到 TLM（TextRow 提交逻辑允许空值写入，IdentityHashMap 按函数身份标记）；
  - **对话输出语言强制**（修"女仆回复突然全是日语"）：
    - 根因说明：TLM 每次对话把 chatLanguage = 客户端游戏语言（tryToChat 覆写），系统提示词按
      ${chat_language} 要求输出语言——文字是日语 = 客户端语言/女仆 ChatLanguage 是日语；
    - 新增 dialogue.outputLanguage（默认空）：填 zh_cn/en_us/ja_jp 强制该语言输出；
      新工具类 `ChatInfoUtil.fromMaid`（配置非空时替换 ChatClientInfo.language），替换 3 处
      对话调用点（ProactiveDialogueManager ×2、AutonomousTaskManager ×1）；留空 = 跟随 TLM/客户端；
  - **实测清单**：
    1. 配置面板出现"语音"板块；音量倍率 2.0 → TTS（LLM 回复 + 系统消息）明显变响；
    2. 系统气泡（"建好啦！"/"有敌人靠近了！"）会朗读；同一文本第二次不再调 TTS API
       （voice_cache/ 出现 ogg = 训练一次已保存）；
    3. 导入系统语音包 zip → 匹配文本播放包内音频、不再走 TTS；"重新加载/查看状态"可用；
    4. AI 记忆页填独立 API → 提取走自定义 API；清空三栏 → 回退 TLM 配置；
    5. 对话输出语言填 zh_cn → 女仆回复中文；清空 → 恢复 TLM/客户端行为；
    6. 回归：气泡限频/变色、感知、先查后做、建造、记忆提取不受影响；
    7. 客户端 mixin 仅客户端加载（专服不受影响）。
- **promaid 1.5.197：自绘蓝图大小无限制 + 只入册不自动动工（防止拆周围）**
  - **大小无限制**：
    - 建筑师子 Agent 提示词删除"keep it SIMPLE (under 300 blocks)"引导——不再刻意缩小设计，
      块数上限仅由配置面板 build.designMaxBlocks（默认 500，可调 100~20000）兜底；
    - 新增 `looseValidate`（SmartDesignTool 内）：自绘蓝图改用宽松校验——只查
      designMaxBlocks 块数上限 + 坐标范围（±maxRange / y 0~maxHeight），不再用
      BlueprintLib.validate（其 maxBlocks=200 上限会拒绝大蓝图）；
    - `BlueprintLib.parseJson` 块数硬上限从 maxBlocks()（200）放宽到 structureMaxBlocks()
      （100 万）——否则大蓝图落盘后扫描注册（loadExternalFile → parseJson）会失败进不了手册；
      smart_build 的 LLM 现场 JSON 路径仍走 validate() 限 200（行为不变）；
    - 手册目录显示上限 catalogMaxBlocks = structureMaxBlocks（100 万），大蓝图在手册完全可见；
  - **只入册、不自动动工**（smart_design 设计完成后）：
    - 不再自动创建区块/绑定女仆/切建筑任务（旧版会自动开工，建造是强制拆挡路方块，
      可能把周围建筑/地形全拆了）；
    - 现在只落盘入册（id = maid_smart_ext:xxx）+ 提示玩家："你想让我在哪里建造？确认后说
      '用这张图纸建造'或'开始建「xx」'"——玩家确认后走 smart_build（可指定位置/旋转），
      确认前女仆不会自己开工、不会拆任何东西；
  - **实测清单**：
    1. "帮我设计一座大教堂" → 建筑师自由设计（可 >200 块），完成后只入册，不自动动工；
    2. 设计完成后女仆回复"已存入手册，确认后说…"，周围方块纹丝不动；
    3. 玩家说"用这张图纸建造" → smart_build 用 maid_smart_ext:xxx 建（可指定位置）；
    4. 手册里能看到新自绘蓝图（目录显示上限 100 万块）；
    5. 回归：smart_build 现场 JSON 仍限 200 块（validate 不变）；内置蓝图快速路径/模板优先
       不受影响。
- **promaid 1.5.196：LLM 查询-行动闭环 + 感知查询工具 + 工作清单 + 画蓝图防超时（移植 PatchouliAI）**
  - **背景**：AI 女仆画蓝图/建造频繁超时——根因是 TLM 工具轮次上限 16 次（LLMCallback
    `beginToolBatch` → `toolTurnCount > 16` 直接 onFailure），而"查环境→查材料→画蓝图→
    建区块→绑定→开工"的链式调用很容易撞上；另外 LLM 单次生成 500 块 JSON 蓝图极易
    超响应长度/超时。PatchouliAI 的解法是"先查后做 + 批量快照 + 预置模板优先"，
    本次移植到 TLM 工具链：
  - **A1 感知查询工具 `perception_query`**（单工具聚合 6 种查询，1 次工具轮拿到全部信息）：
    - `look_around [r]`：ASCII 空间感知网格（@ . ^ , v # ~ ! x T ?，含朝向/图例）
    - `terrain [r]`：地形平坦度报告（平均/最低/最高高度、高度差、可建面积比）
    - `build_site [w] [d]`：建造场地分析（障碍物分类 TREE/WALL/LAVA/WATER/HEIGHT_DIFF、
      地基高度、清理建议、可建性）
    - `inspect <x> <y> <z>`：单方块详情（ID/硬度/掉落工具需求/状态）
    - `scanblock <id> [r]`：按方块 ID 扫描（坐标+距离，最多 32 个）
    - `scanentity [hostile|passive|player|all] [r]`：实体扫描（血量/距离）
  - **A2 世界探查引擎 `WorldProbe`**（纯逻辑，SRG 适配）：实现上述全部查询；
    方块注册表用 ForgeRegistries（避开 srg BuiltInRegistries 字段歧义）；
    getDestroySpeed=m_60800_(lv,pos)、canBeReplaced=m_60815_()、isAir=m_60795_() 已实证
  - **A4 工作清单 `work_list` 工具 + `ai_worklist` 上下文注入**：
    - `set <json>`：写入跨轮任务计划（[{content,status,priority}]，只允许一个 in_progress）
    - `get` / `clear`：读取/清空
    - `query_todo`：任务计划 + 当前建造进度（已建 N/M 块 + 缺料清单）
    - `build_need`：当前绑定蓝图的材料缺口（剩余需要 vs 背包持有，补料后女仆自动续建）
    - 注入：`ai_worklist` 上下文让 LLM 每轮都看到当前任务计划（零额外工具轮次）
  - **超时链路改造**：
    - **smart_design 零 LLM 快速路径**（借鉴 PatchouliAI BuildCommandInterceptor）：描述命中
      内置蓝图关键词（水井/小木屋/凉亭/喷泉/瞭望塔等）或外部蓝图文件名/中文名时，
      onCallAsync 直接返回工具结果"已有现成图纸，用 smart_build 建造"——完全不走子 Agent，
      零 token、零超时；仅未命中才分发建筑师子 Agent；
    - **smart_design 建筑师子 Agent 提示词重写**：优先用内置/外部蓝图模板
      （smart_build_list 列出 maid_smart:hut/gazebo/fountain/tower/well + maid_smart_ext:xxx），
      命中模板直接回复"用现成图纸"不再生成新 JSON；仅明确要求全新建筑才设计，
      且限制 300 块以内——复杂 AI 蓝图建造时才不会超时；
    - **onSuccess 模板建议识别**：子 Agent 回复"已有现成图纸"（非 JSON）时转述给玩家，
      不再误报"格式不对"（修复提示词与解析分支不一致的链路漏洞）；
    - 查询工具/工作清单工具全部可配置开关（配置面板 AI 工具页：perception_query / work_list）
  - **配置新增**：`TOOL_PERCEPTION`（perception_query 工具开关，默认开）、
    `TOOL_WORK_LIST`（work_list 工具开关，默认开）；zh_cn/en_us 翻译键 + 面板行
  - **注册**：ProMaidExtension.registerAITool 注册 2 个新工具；registerAIMaidContext
    注册 ai_worklist 上下文（主对话自动注入工作清单）
  - **实测清单**：
    1. 对话"帮我在这里建个小屋"→ LLM 应先用 perception_query build_site 探查场地再 smart_build；
    2. "建一个水井/瞭望塔"→ smart_design 子 Agent 应优先命中内置模板（不再生成 JSON）；
    3. "建城堡"→ 子 Agent 回复用现成图纸或生成 ≤300 块简单蓝图，全程无超时；
    4. work_list set/query_todo/build_need：下达建造后问"还缺什么材料"→ build_need
       精确报告缺口；补料后女仆自动续建（已建跳过）；
    5. 多轮大型建造不再触发"Tool turn count exceed"超时。
- **promaid 1.5.195：威胁判定扩大——所有携带仇恨的生物（含中立反咬/主人被攻击）+ 文案"敌人"**
  - **统一威胁判定**（PerceptionManager 新增公共静态 `isThreat(candidate, maid)`）：
    候选生物是【敌对生物（Monster 兜底，即使暂未锁定目标也算）】或【对女仆/主人
    携带仇恨】（getTarget==女仆/主人，`mob.m_5448_()` 已实证）——中立生物被激怒
    反咬（铁傀儡/蜜蜂/狼等）、主人被攻击的生物都算威胁；
  - **判定扩大传导 4 处**：
    - PerceptionManager.dangerActive（敌袭冻结/气泡红色）与 scan 采集 hostiles
      （感知播报）：Monster.class → Mob.class + isThreat（0.5s TTL 缓存保留）——
      中立反咬也会冻结主动对话、触发"敌人靠近"感知、气泡变红；
    - SelfPreservationBehavior.findThreat 第③分支（最近的威胁扫描）：Monster.class →
      Mob.class + isThreat（getTarget 是 O(1) 字段读，先过滤再做昂贵的 hasSight
      raycast，性能不受影响）；
    - 另 3 处 Monster.class 扫描同步扩大：fleeEscaping 突围方向密度 / anyVisibleMonsterAround
      传送回家安全闸 / pushBackNearby 近身击退——突围、传送、击退都认"带仇恨生物"；
  - **系统提示文案（"敌对生物/怪物"→"敌人"）**：
    - "有敌对生物靠近了！离我 X 格" → "有敌人靠近了！离我 X 格"
    - "怪物在靠近…" → "有敌人靠近…"；"附近的敌对生物都清掉了，安全了！" → "附近的敌人都清掉了，安全了！"
    - bubble() 限频匹配词加"敌人"（保持敌人事件 300 秒长限频）；
    - ProactiveDialogueManager.worldBrief（喂 LLM 的世界简报）：Monster 计数 →
      isThreat 计数，"附近有 X 只怪物出没" → "附近有 X 个敌人"。
- **promaid 1.5.194：搭方块中途窒息封头（替换"搭完后糊脸"）+ 所有系统对话青色**
  - **搭方块中途的额外判定（纯被动，无气泡）**——采纳用户指出的问题：旧"糊脸"
    等搭高到顶（30 格）/安全高度后才操作，30 格时怪物早丢索敌、封头无意义：
    - 删除 smackFace()/smackedFace/announcedSmack 与两处触发点、"糊它一脸"气泡；
    - 新增 `trySuffocateDuringBuild`：每次搭高（buildUp）成功后顺手对还贴在脚下
      （< 近身距离 4 格）的威胁尝试窒息——往它【眼睛格】（脚底+1，MC 窒息判定格）
      放头封块 + 头顶盖帽（脚底+ceil(身高)），尝试让它窒息扣血；
    - 与搭高共享 buildCooldown → "搭一块 → 封一次头 → 再搭一块"交替节奏；
      眼睛格已实心自动跳过（不重复烧方块）；身高 <1.4 的低矮怪（蜘蛛）跳过；
      每场自保最多 3 次尝试（suffocateBudget 重置），方块永久留下；
    - 搭到顶后改为静默站在最高处等威胁消失（不再操作怪物）。
  - **所有女仆系统对话特殊颜色（青色 §b，区别于主动对话）**：
    - 系统对话 = addTextChatBubble（感知/工作状态/自保/投喂/盾牌/"建好啦"/钓鱼坐垫/
      灵魂迁移/记忆里程碑/挖矿提示 + TLM 内建喂食/坐垫/吃饭气泡），LLM 主动对话/
      回复 = addLLMChatText（独立方法）——天然分离，mixin 只处理系统对话；
    - 聊天框镜像：正常状态 `§7[名字] §f文本` → **青色 `§b[名字] §b文本`**；
    - 世界内气泡也变色（新增 addTextChatBubble RETURN 注入）：getChatBubble 取回
      TextChatBubbleData → setText(translatable(text).withStyle(敌袭? RED : AQUA))
      → forceUpdateChatBubble；try/catch 兜底；
    - 敌袭中仍为红色 `§c[警示][名字] §c文本`（v1.5.193 效果不变）；
    - LLM 主动对话/回复保持 TLM 原生外观（世界内白 / 聊天框灰 `<名字> 文本`），
      与青色系统对话一眼区分。
- **promaid 1.5.193：女仆说话的时宜——敌袭状态冻结情绪价值/主动对话 + 系统提示特殊颜色**
  - **敌袭状态判定**（PerceptionManager 新增公共静态 `dangerActive(maid)`）：
    女仆周围 12 格内存在存活敌对生物（Monster，与感知同半径同判定）；
    带 0.5 秒 TTL 缓存（高频调用不反复扫描）；**独立于感知总开关**（关掉感知
    播报，敌袭冻结仍生效）；非服务端/异常返回 false（冻结失败只不冻）。
  - **敌袭时冻结"提供情绪价值的东西"**：
    - 7 阶段主动对话（ProactiveDialogueManager.tryScanFire）：战斗期间整条链路暂停
      ——不烧配额、不推阶段、不记冷却，解除敌袭后恢复继续（记忆话题/长沉默确认全冻结）；
    - 事件驱动 4 处理器（好感升级/击杀邀功/主人重伤/主人死亡 fireEvent）：战斗中还剩怪
      时不邀功/不感慨/不聊情绪；主人受伤信息由感知红字气泡即时覆盖，不会漏报；
    - 自主决策（AutonomousTaskManager）：战斗中不自主切换任务（先保命/打架，别惦记干活）；
    - 情绪价值动作（走向主人/递食物等）只在 fire 内部执行 → 冻结自动覆盖。
  - **系统提示特殊颜色**（ChatBubbleLimitMixin 气泡同步主人聊天框）：
    敌袭中女仆气泡以**红色 §c + [警示] 前缀**同步（感知"有敌对生物靠近/主人受伤"
    天然红色醒目）；正常状态维持灰色 `§7[名字] §f文本`——"危险提示"与普通对话一眼区分。
- **promaid 1.5.192：Promaid 手册——女仆工作链路调试面板 + 改名**
  - **改名"蓝图手册"→"Promaid 手册"**（仅用户可见点，物品注册 id/通道/配方路径不动，
    旧存档物品不消失）：物品名（zh_cn/en_us lang）、界面标题与首页标题、
    登录赠礼聊天消息、maidmarriage 调试面板提示语、mods.toml 描述、全部源码注释；
  - **手册"女仆记忆"页升级为双 tab：记忆 / 链路调试**（新增 3 个网络包 id 9/10/11，
    沿用字符串序列化 + owner/OP 权限校验）：
    - **链路调试页展示女仆完整工作链路与各项状况**（服务端实时收集，点刷新/进页即拉取）：
      - 工作链路：任务 / 日程 / 拾取 / 卡住原因（复用 WorkStatusReporter.reasonOf——
        骑乘/没镐/没矿/熔炉诊断等，不弹气泡）；
      - 状态：血量 / 好感等级（0-3 级 + 数值/384）/ 位置（维度+坐标）/ 背包占用 n/36；
      - 情绪：PAD 全维度 + 亲密/冲突/思念 + 修复债（AffectManager.render）；
      - 记忆：开关 / 段落（含永久）/ 关系 / 画像 / 片段 / 提取水位；
      - 主动对话：阶段状态机快照（ProactiveDialogueManager.stageInfo——阶段/本轮已发/修复模式/主人聊天时间）；
      - 工作笔记：WorkingNoteTool.readNote；绑定区块 planId。
    - **可调试记忆对象列表**（段落按重要度降序 + 关系按置信度降序），点击选中 →
      底部动作按钮：↑重要度 / ↓重要度 / 标记删除 / 恢复 / 强化（salience+1 访问+1）/
      停用关系（inactive 不再注入检索）——全部走 AiMemoryStore 新增公开调试方法
      （markDeletedByHash/restoreByHash/adjustSalienceByHash/reinforceByHash/
      paragraphByHash），成功后服务端回发最新快照即时刷新；
    - 未选中对象点动作 → 聊天栏提示"先点击选中一行记忆再执行动作"；
    - 权限：仅女仆主人或 OP 可查看/操作（与清空记忆同模式），非主人静默拒绝。
  - **服务端数据源小改动**：ProactiveDialogueManager 的 states/dailyCount/lastEventTime
    改 static + 新增 stageInfo(maid)（网络包无实例上下文查询）；
    WorkStatusReporter 卡住原因抽公共静态 reasonOf（原私有 idleReason 同步改静态）。
- **promaid 1.5.191：女仆大脑升级——7 阶段主动对话 + 配额改革 + 记忆维护周期 + 回复反馈学习**
  - **配额改革（解决"主动对话经常没反应/烧配额"的根本问题）**：
    - 全局日上限 dialogue.apiDailyLimit 默认 10 → **40**（范围 0-400，**0=不限**——
      旧版 0 语义是"永远禁言"，配置说明写着 0~100，填 0 期望不限却永久闭嘴，属 bug）；
    - 每女仆日上限 dialogue.proactiveDaily 默认 4 → **12**（7 阶段状态机需要更多额度）；
    - **配额只在真正开火前获取**：先做阶段选择（纯计算无副作用），选中才 tryAcquire，
      失败不推进阶段、不烧冷却、不烧日计数，下轮重试——修掉旧版"扫描空契机也烧全局
      配额"（探测不出话题时一次配额白白消耗）；自主决策同步修复（旧版先记冷却/计数
      再试配额，配额满白烧 10 分钟冷却——"自主决策没反应"根因）。
  - **主动对话重写为 7 阶段状态机**（对齐 maidsoulcore ProactiveStage）：
    LIGHT_FOLLOWUP（90s，仅主人提问未答时）→ TOPIC_PUSH（300s，记忆偏好/日常话题）
    → WORLD_OBSERVE（600s，真实世界简报：夜晚/雷雨/下雨/附近怪物数/主人血量/当前任务，
    不瞎编）→ RELATION_CANDIDATE（900s，关系记忆）→ FINAL_NOTICE（1200s，接近本轮
    上限才收尾）→ LONG_SILENCE_CHECK（1800s，"主人还在吗"每日上限 dialogue.longSilenceMax=2）
    → IDLE（空闲，主人互动或沉默 dialogue.proactiveIdleMin=60 分钟重启）；
    - 每轮主动会话最多 dialogue.maxReplies=4 次——7 阶段不会一次性全喷，
      多数阶段因"无内容"跳过，实际一轮常为 2-3 次；
    - **沉默计时 bug 修复**：旧版用"女仆自己上次发言"计时（主人没说话也算沉默、
      主人刚说完反而不算）→ 改用 ReplyFeedbackTracker 的真实"主人聊天时间"；
    - 主人互动（聊天/喂食）→ 周期重置（回到起点重新节奏化，不硬凑话）；
    - 事件驱动（好感/击杀/重伤/死亡）保留 4 处理器，走统一 fireEvent，
      计入日上限与全局配额，但不吃每轮 maxReplies 预算（紧急事件优先）。
  - **回复反馈学习**（dialogue.replyFeedback 默认开，聊天观察器每 5 秒扫历史）：
    - 负面反馈（"别说了/好烦/不是这样/听不懂/算了…"）→ 该话题冷却
      （dialogue.topicBackoffMin=60 分钟不再提）+ 写 error_mark 记忆 + 修复模式
      （下次主动发言更克制）；
    - 修复信号（"我说的是/你理解错/重新说…"）→ 修复模式（先确认再开口）；
    - 正面反馈（"谢谢/说得对/真贴心…"）→ 解除修复模式 + 强化对应记忆
      （salience+1，记得更牢）；
    - **真沉默计时**：记录主人最后一次聊天的 gameTime，主动对话阶段推进依据；
    - 主人道歉（"对不起/抱歉…"）→ 情绪层安抚（onOwnerApology，不视为负面反馈）；
    - 主人提问检测 → LIGHT_FOLLOWUP 阶段依据 + 情绪层 onOwnerQuestion；
    - 聊天历史被清除 → watermark 兜底重置，不卡死。
  - **记忆维护周期**（memory.maintenanceMin=10 分钟，由调度器定时跑，非写入触发）：
    - 固化：boundary/promise/relationship_event/self_memory 或 salience≥9 →
      permanent + 保护 30 天（与写入策略对齐，重启不再丢"该永久"的记忆）；
    - 年龄衰减：7 天未更新且无访问 → salience-1；30 天未访问且 salience≤3 → 遗忘删除；
    - accessCount 半衰：14 天未访问减半（防无限增长）；
    - 关系置信度衰减（memory.relationDecayDays=60 天）：非永久且无访问 →
      ×0.85，<0.15 → inactive（不再注入/检索）——旧版关系置信度只增不减，永不过期；
    - error_mark 传播：被主人否定的段落 → 同类段落（同话题/结构相似）打 error_affected
      + salience 封顶 2（主动会话与记忆检索都不再提起被否定内容）；
    - 每次维护写 maintenance_log.jsonl（人工可查维护行为）。
  - **记忆/检索跳过被否定内容**：AiMemorySearch 四路、AiMemoryContext 三节、
    主动对话 memoryTopic 全部过滤 error_mark/error_affected 段落——
    被否定的记忆不会再被当成"重要记忆"反复提起；
  - **情绪层补充**：onOwnerApology（道歉安抚：valence+0.06/conflict-0.12/hurtDebt-0.10/
    repairDebt-0.14/intimacy+0.05）、onOwnerQuestion（提问：arousal+/longing+）；
    真聊天（非右键）也算"主人对话"（旧版只有右键喂食计入）。
  - **面板新增**：对话页 5 行（每轮发言上限/空闲重启/长沉默确认上限/回复反馈学习/
    话题冷却），记忆页 2 行（维护周期/关系置信度衰减）。
- **promaid 1.5.190：AI 双手工具 + 记忆系统修复 + 面板修复（全链路排查）**
  - **新 AI 工具——让女仆拥有"玩家双手"（配置面板 AI 工具页可关）**：
    - `smart_craft`（帮主人合成）：LLM 传目标物品 id → 服务端按配方从女仆背包
      取材料合成 → 成品交给主人背包（满则退回女仆）→ 缺材料报"缺什么×几个"；
      失败自动回滚已抽材料（物品守恒，不凭空生成/复制）；
    - `smart_place`（帮主人放方块）：从女仆背包取出方块放到指定坐标
      （x/y/z 可传，默认女仆前方 2 格）——"帮我把这里填上/补一块"类指令的落点；
      支持 replace 覆盖空气/水；每次最多放 8 个、距离 ≤12 格、悬空不放。
  - **记忆系统修复（反复测试发现的 bug）**：
    - accessCount 指数膨胀：旧版合并段落时 accessCount【求和】→ 每次 query_memory
      触碰翻倍（2N+1），改取 max；
    - 每次写入全量重写 6 个 jsonl（服务端 IO 热点）→ 防抖写盘
      （memory.lazySave 默认开：标脏后由调度器批量落盘；meta 提取位置仍立即写）；
    - `query_memory` 关了记忆还能读旧记忆 → 补 isEnabled 检查（与 remember 一致）；
    - `remember` 的 importance 参数 schema 是 string 但 codec 是 int（解码必失败）
      → 改 IntegerParameter(1-10)；
    - `remember type="event"` 落错 PREFERENCE 层且不建事件片段 → 修正确分类 +
      建 Episode；event/emotion 不再污染"主人画像"；
    - 清空记忆（手册按钮）不清工作笔记 → clearAll 连 working_note.txt 一起删，
      面板文案同步"段落/关系/画像/工作笔记"；
    - 记忆投影截断越界（setLength(maxChars) 后再加提示语超限）→ 预留后缀长度。
  - **主动会话引用记忆**：沉默找话题时从长期记忆挑一条（偏好/关系/承诺/今日回顾，
    memory.proactiveTopic 默认开）作为话题——主动会话终于"记得你"；
  - **配额/冷却修复**：全局 API 配额先判后记（旧版配额满仍烧 4 分钟冷却+日计数）、
    主人死亡事件补日上限（旧版 4 个事件只有它绕过）；
  - **聊天记录与记忆的边界**：原版清除聊天记录【不影响】已存记忆（promaid_memory
    与对话历史完全分离）；新增强力兜底——历史被清空后提取器 watermark 自动推进，
    不会"新对话永远攒不满 8 条"；
  - **v1.5.190b 二次修复（合成/放置链路实测前加固）**：
    - smart_craft 空配方槽误报缺料：有序配方 getIngredients() 返回 3x3 全 9 格（空槽 = Ingredient.EMPTY）→ 跳过空槽，材料齐了不会再被缺某种材料x1卡住；
    - smart_craft 成功提示回滚判定反转（旧版 remain 为空时误报放回背包）→ 修正；
    - smart_place 覆盖判定修正：replace=true 时用 canBeReplaced（水/草丛/雪等），旧版只覆盖流体导致放不进水面/草丛；
    - 两个工具的 ResourceLocation 解析加 try/catch（非法 id 直接抛异常，旧版会打穿 LLM 工具调用线程）→ 返回id 格式不对。

    - 女仆配置界面"AI 记忆"开关按钮右半/点击区永远在屏幕外（MaidConfigButton
      硬编码 164 宽 + x=w-110）→ 右对齐 w-174，可点击；记忆面板改到容器内左上，
      不再压住原生开关列；
    - maidmarriage 调试面板"AI记忆"按钮覆盖"应用到女仆/刷新当前值"并挂出面板
      → 移到面板底部下方独立行（屏幕小时钳制 h-26）；
    - 蓝图手册建造详情 5 按钮行（inRegion）拉到 cx+260 出屏 46px → 均分自适应
      总宽 ≤ w-16；女仆详情区块列表 / 区块名单女仆列表无上限 → 分页 + 翻页；
      女仆管理进度文本与第 6 行按钮重叠（按 allMaids 算页数）→ 按建造女仆算；
    - 配置面板：主页重复 COMBAT 按钮（左列残留 + 右列顶部同款）→ 去重；
      主页/矿表子页矮窗口不自适应 → 行高/网格行数按可用高度压缩。
  - **工作播报去重**：同一女仆每轮只检查一次（旧版 3 个玩家同区时熔炉/酿造台
    诊断扫描跑 3 遍 33×9×33）；`smart_report` 修 getTask() 为 null 的 NPE；
    工作笔记工具补记忆开关检查（关记忆时拒绝读写）。
- **promaid 1.5.189：全能管家扩展——农业畜牧运维 + 玩家贴身辅助 + 环境交互**
  - **农业与畜牧运维（任务制增强）**：
    - 连锁采集默认开启：农场 BFS 连锁收割（misc.chainHarvest）与挖矿矿脉连锁
      （mine.chainMining）默认 true（用户要求"连锁采集也应加入"）；
    - 剪羊毛掉落【直进女仆背包】（MaidShearDropMixin——TLM 原版剪完毛掉在羊
      旁边，可能滚落/被草吞；与 farm 收割直进背包对齐）；
    - 养蜂闭环（MaidHoneyIgniteMixin）：采蜜后若蜂箱下方无营火、女仆背包有
      营火 → 自动放置营火（防蛰，原版机制）；
    - 畜牧数量控制（MaidFeedAnimalCapMixin，杀幼保成）：同种动物超过上限
      （默认 50）时击杀多余幼年（默认关，激进操作，面板可开）。
  - **玩家贴身辅助（被动 core 行为，非工作状态）**：
    - 自动投喂/治疗（MaidAidOwnerBehavior，优先级 190）：主人饱食度 < 阈值
      （默认 12）自动喂熟食；主人血量 < 阈值（默认 30%）自动投掷喷溅治疗药水
      （ThrownPotion 落地溅射，玩家同款）或喂金苹果；
    - 被动插火把（MaidTorchPlacerBehavior，185）：主人周围 3×3 方块亮度 <
      阈值（默认 7）自动消耗背包火把插火把照明；
    - 共享盾牌（MaidShieldShareBehavior，180）：主人盾牌耐久 < 50% 或为空时
      从女仆【背包】取剩余耐久最高的盾给主人（绝不拿自己副手）；
    - 共享不死图腾（MaidBaubleTotemMixin 扩展）：主人致命伤时，女仆背包/饰品
      栏的不死图腾优先救主人（特效同原版：生命 1 + 再生/吸收/抗火 + 图腾粒子）。
  - **环境交互**：
    - 挖矿避岩浆/危险方块：MaidMineBehavior 目标矿判定加 isDangerAt（岩浆/火/
      岩浆块/仙人掌/甜浆果/营火——复用自保 DANGER_BLOCKS）→ 全量扫描与缓存轮
      都跳过危险目标，女仆不再钻岩浆挖矿；
    - 远程敌人走位躲避（蛇形走位）已由现有自保/战术行为覆盖，无重复改动。
  - **配置面板**：combat"被动技能"段新增【贴身辅助】子节（投喂/治疗/插火把/盾牌/
    图腾开关+阈值）；misc 段新增畜牧数量控制开关+上限；连锁默认开同步面板文案。
- **promaid 1.5.187b/188：死循环修复 + 区块范围精确化 + 建造确认流程 + 战斗调整**
  - **游戏全静止（死循环）修复**：jstack 实证 Server thread 卡在 fastutil
    LongAVLTreeSet.locateKey 死循环——`combinedHaveAll` 用全图 ±3E7 AABB 扫
    visibleChunks 树触发 fastutil 病态 Subset 迭代（SubsetIterator.firstEntry 每次
    从根重新 locateKey，巨集内死循环）。修复：4 处全图扫描全部改安全小盒——
    `BuildPlan.scanAreaMaids`（按各建造区块 box 外扩 64 格收集，去重）、
    女仆管理 findMaidByUuid 改玩家 128 格。
  - **区块范围不准（实际搭建超出区块）**：collectBuildRegions 之前把蓝图中点
    origin 当"框中心"下发 → 红色区块框整体偏移 ≈1 格（框外扩了实际搭建区）；
    且客户端预览/playerPlanId 又对"中心"减半 → 双重偏移。修复：服务端直接下发
    planRegion 的精确 min 角 (r[0..2])，客户端预览/区块内判定不再减半。
  - **区块内右击跳转失败**：inPlanRegion 判定公式（减半偏移）与实际范围错位 →
    玩家站在实际建造区内却判"不在区块内" → 右击进不了对应详情页。同一修复解决。
  - **建造确认流程（防误操作，固定流程）**：
    1. 第一次点【建造此图纸】→ 强制打开金色区块预览 + 系统提示"确定范围后再次
       打开手册点击建造"，直接退出手册看框选位；
    2. 再次打开手册再次点击 → 弹确认框【确定建造在这里？】明确告知"女仆搭建会
       直接摧毁区块内障碍物"→ 点确认才真正创建区块，并提示去绑定女仆。
    - 区块控制一体化：站在区块内打开手册 → 自动显示红色区块框（先看范围再操作）。
  - **战斗**：
    - 大跳 CD 生效：start 不再重置 jumpDashCooldown（旧版每场战斗进场清零，
      10 秒 CD 形同虚设）——大跳后 10 秒内无论是否脱离战斗都不再大跳；
    - 群怪抑制跳劈：多敌（索敌范围 ≥2）【完全不跳劈】——尽可能横扫补伤害
      （旧 12% 概率仍会跳，用户反馈"面对群怪反而特别喜欢用跳劈"）；
      单敌保持主体跳劈（70%）。
- **promaid 1.5.187：删掉站桩围壁，改为"拿方块糊怪物的脸"**
  - 原四面围壁（垫到安全高度后在自己四周各放 1 块半人高掩体）存在感低、
    效果不明显（用户反馈）→ 整个删除（buildWallAround/wallFaces 及相关字段）。
  - 替换为【糊脸】：垫到安全高度/强制搭高到顶后，往威胁怪物【头顶】盖一块
    方块（盖帽）——怪物被压在方块下出不来（被困原地）、远程怪被挡住视线
    射不了箭；头顶格 = 脚格 + 怪物身高取整（僵尸/骷髅 → y+2，蜘蛛 → y+1）。
  - 头顶已被方块盖住（或地形）→ 视为糊脸完成，站桩等威胁消失；
    背包没材料 → 冷却后重试；威胁已死 → 跳过。
  - 气泡播报："我拿方块糊它一脸，看它还追不追！" / "我拿方块糊它一脸，
    谁也够不着我！"
- **promaid 1.5.186：自保搭高统一——不再按敌人近战/远程划分**
  - **配置面板"搭高与逃跑"段**：原"近战搭高上限（默认10）/远程搭高上限（默认30）"
    + "搭方块冷却/远程搭方块冷却"四个控制项，合并为唯一控制项
    **【至多向上搭多少个方块】**（默认 30，范围 5~64）——不再依据近远划分；
    冷却项删除（代码固定 4 tick ≈ 0.2 秒/块，太快实体来不及被顶起会窒息）。
  - **自保行为**：搭高上限统一读唯一配置；安全高度（垫几块后站桩等）统一 4 块
    （原近战 2 / 远程 4，取远程值：防箭矢够安全、近战怪更够不着）；
    强制搭高/围壁/节奏全部去 ranged 分支。
  - 旧配置 key（pillarMaxMelee/pillarMaxRanged/buildCooldown/buildCooldownRanged）
    自动失效，新 key pillarMax 生效。
- **promaid 1.5.185：大跳劈距离修复——触发距离与实际可达距离一致**
  - **问题**：隔很远（8~12 格）就开始大跳，跳完还剩 3~7 格，非常尴尬。
  - **根因（两处）**：
    1. 大跳触发上限 JUMP_DASH_MAX=12，但旧注释"滞空 17 tick × 水平 0.5 =
       位移 8.5 格"算错了——水平速度在空中每 tick 衰减 0.91，0.5 初速滞空
       ≈19 tick 的全程位移实际只有 ≈4.6 格；
    2. 水平初速公式 dist×0.05 不按实际可达距离给速度：6 格起跳只给 0.3 初速，
       落点距敌 3+ 格，下落中挥刀 3D 距离 >3.1 根本砍不中。
  - **修复**：
    1. JUMP_DASH_MAX 12 → **6.5**（大跳实际可达并能在下落中命中的上限），
       超过 6.5 先走近再跳（跳过去也够不着）；
    2. 水平初速公式改 (dist-2.5)/7（夹在 0.25~0.5）：下落中段（起跳后
       ≈15~17 tick）恰好进入 3.1 挥刀命中窗口，落点距敌 ≈1.4~2.4 格，
       贴近不扑空——"判定大跳的距离 = 实际大跳到达的距离"。
- **promaid 1.5.184：跳劈重构——普通跳劈 = 玩家同款垂直跳 + 玩家式暴击结算**
  - **普通跳劈（不再"跳太高"、不再"莫名其妙跳一下"）**：
    - 垂直速度固定 = 玩家跳跃数值 0.42（LivingEntity.jumpFromGround 同款，
      峰高 ≈1.25 格，与玩家完全一致）——旧版水平前扑 0.3 已删除：**不改变
      平面速度**，仅增加向上的垂直速度（行走持续、空中滑翔逼近），跳劈
      辨识度由空中挥砍 + 暴击特效提供；"莫名其妙跳一下"的观感根因（跳劈
      距离内水平速度与导航互相干扰、落点漂移、跳了砍不到）随之消除；
    - 起跳后隔 0.35 秒（7 tick，下落中）挥刀，触发玩家式暴击结算
      （对齐 Player.attack 暴击分支，javap 实证）：
      伤害 = ATTACK_DAMAGE × 1.5 + 锋利附魔加成（倍率只乘攻击力、附魔后加，
      与玩家完全一致）→ 挥刀动画 + 暴击粒子 + 暴击音效（玩家暴击同款"叮"声）；
      命中后按玩家同款触发火焰附加（主手点燃）+ 荆棘反击（目标护甲）。
  - **大跳劈（跳劈斩）**：保留抛物线大跳（垂直 0.75 峰高 ≈3.5 格不触发落地水、
    水平初速按距离缩放滑翔）+ 10 秒 CD；下落中挥刀走同一套玩家式暴击结算
    （×1.5 + 附魔），命中瞬间额外【大击退】劈飞（1.5 力度，方向 = 女仆→目标，
    玩家冲刺跳劈的动量击退手感）；粒子 16 颗更帅。
  - 修正：攻击力属性实证为 Attributes.f_22281_（f_22277_ 是跟随距离，
    早期假设有误）；EnchantmentHelper 附魔加成 m_44833_、火焰附加 m_44914_、
    荆棘 m_44823_ 全部按 Player.attack 反编译逐一核对。
- **promaid 1.5.183：修复绑定失败 + 女仆管理显示问题**
  - **绑定绑不上（两处根因）**：
    1. 女仆详情页按钮逻辑错误——【已绑定】才显示解绑，【未绑定】一律显示
       【绑定到选中区块】（旧版建筑女仆只给解绑按钮，未绑定区块的建筑女仆/
       非建筑女仆没有绑定入口）；绑定自动切建筑任务；
    2. 服务端 UNBIND 解绑不传 planId（解绑不针对具体区块），但 v1.5.180 的
       目标区块校验对所有非轮询动作都要求 planId → 解绑永远被"区块不存在"
       拒绝。修复：UNBIND 跳过 target 校验。
  - **显示问题**：女仆管理页区块列表文字行与女仆名单按钮重叠（文字底 54 行 vs
    按钮 52 起）→ 按钮整体下移 10px；女仆管理页区块行/女仆详情页标题/区块名单
    页标题全部改 fitText 像素级截断（长名字/长区块名不再突出屏幕）。
- **promaid 1.5.182：女仆管理 UI 重构（详情页绑定 + 区块名单/工头）**
  - **女仆管理页简化**：只显示【有效区块列表】+【建造状态女仆名单】——删除了
    页面上的解绑/设工头按钮；点击女仆行 → 跳转【女仆详情页】：点选区块 +
    【绑定到选中区块】/【解绑（离开当前区块）】。
  - **区块名单页**：在建造区域内右击手册 → 详情页额外多出【名单】按钮 → 显示
    【绑定在该区块的女仆名单】+ 每行【设为工头】（一区块一工头）。
  - **自动选工头**：女仆开始建造时（行为启动）若该区块还未预设工头（名单页未
    设置），从绑定该区块的女仆中随机挑一只当工头。
- **promaid 1.5.182：普通跳劈 = 玩家跳跃高度 + 修复"莫名其妙跳一下"（跳劈动作同步）**
  - 普通跳劈垂直 0.5 → 0.42（峰高 ≈1.25 格，与【玩家跳跃高度】完全一致——不再
    比玩家高，观感不奇怪）。
  - 修复"战斗中莫名其妙跳一下"：旧版普通跳劈是【纯垂直跳】（不朝目标移动），而
    挥砍判定要求距离 ≤3.1 格——起跳距离在 3.1~4.5 之间时（单敌 70% 概率触发）
    跳起来原地落下永远砍不到 = 只有跳没有劈（动作同步没到位）。现在普通跳劈 =
    垂直 0.42（玩家高度）+ 水平小幅朝目标（0.3，小跳前扑）——跳过去正好劈中，
    跳劈辨识度由前扑 + 空中暴击挥砍提供。
  - 大跳（跳劈斩 0.75 + 10 秒 CD）不受影响。
- **promaid 1.5.181：大跳劈降低高度（不触发落地水）+ 大跳 10 秒 CD**
  - 问题：抛物线大跳垂直初速 0.95（峰高 ≈5.6 格）太高——跳劈落地直接触发落地水
    （自动放水），落脚不帅且打断战斗节奏。
  - 修复：
    1. **大跳垂直 0.95 → 0.75**（峰高 ≈3.5 格）——v1.5.154 历史实证值：落距 3.5
       无伤、不触发落地水；仍比正常跳劈高一大截，抛物线弧线依旧明显；
    2. **大跳（跳劈斩）10 秒冷却**（200 tick）——CD 期间：远距不再必跳（先走近
       再跳）、近距 30% 的跳劈斩概率被抑制（只做普通垂直跳）；**正常跳劈不受
       影响**（普通垂直跳 0.5、峰高 1.7 格，2 秒一跳的节奏照旧）；
    3. 正常跳劈高度保持 0.5——只比玩家高一点（"让旁人看得出来是在跳劈"）。
  - 落地清 fallDistance 逻辑保留（跳劈进攻动作不自损）。
- **promaid 1.5.180：多区块共存 + 无女仆创建 + 女仆-区块显式绑定（架构级重构）**
  - **创建区块不再需要女仆在场**：目录页"建造此图纸"直接以玩家脚下为原点创建
    区块；唯一硬性要求 = **不能与其他区块重叠**（重叠拒绝 + 警告）；区域内障碍物
    只【警告】不阻止（提示"创建后女仆建造时会拆掉这些阻挡"）。同蓝图同原点
    重下达 = 续建（解除暂停继续）。
  - **女仆-区块显式绑定**：女仆管理页顶部区块列表（可点选，高亮）→ 女仆行
    【绑定】= 绑定到选中区块（切建筑任务 + 记录绑定，persistentData 持久化）；
    【解绑】= 切空闲任务 + 解除绑定。**只有绑定该区块的女仆参与建造**——
    未绑定女仆站桩等待。
  - **多区块共存**：存储层从"每维度单计划"升级为"全局多区块"（PlanState 按
    planId 键控：计划/进度/包围盒/强制加载/工头/暂停全部 per 区块）；存档
    BuildArchive 存 SavedPlan 列表（旧单计划档自动迁移）；进度/缺料/暂停/取消
    全部按区块独立；区块列表跨维度展平（同名自动编号「X」「X2」）。
  - **区块内控制**：区块内右击手册 → 该区块详情页，暂停/继续/取消/速度/全员
    加入仅针对本区块（带 planId，服务端校验玩家在目标区块内）；多区块红框
    预览（每个区块一框 + 名称悬浮文字）。
  - **LLM 入口**：SmartBuildTool/SmartDesignTool 下达 = 创建区块 + 自动绑定
    该女仆开工（重叠检查同手册）。
  - 文件：BuildPlan（核心重构）、BuildArchive、BlueprintBuildExecutor、
    BlueprintBookNetworking、BlueprintBookScreen、BlueprintBookItem、
    MaidBuildBehavior、SmartBuildTool、SmartDesignTool、BuildActivityForceMixin、
    ChunkFreeze、BlueprintAreaPreview、BlueprintLib。
- **promaid 1.5.179：缺料数字改为实时计算（需求 − 已建 − 绑定女仆/主人背包）**
  - 问题：旧版详情页"缺料"从【延后集】（放置时缺料的累积快照）统计——不扣
    区块内已建方块、不扣绑定女仆和主人背包里现有的材料，数字虚高/失真。
  - 修复：缺料 = **总需求 − 区块内已建（与蓝图匹配的方块，实时扫描，等价族
    同规则）− 背包（该维度所有绑定女仆 + 主人）**，每次状态刷新（2 秒轮询/
    打开手册）实时计算：
    - BlueprintLib.countBuiltMaterials：按维度直接扫描已建方块（计划区块已被
      强制加载，getBlockState 全内存命中，大蓝图单次毫秒级）；
    - BlueprintLib.combinedHaveAll：主人背包 + 该维度所有建筑任务女仆背包合计
      （创造模式无限材料保护沿用）；
    - BuildPlan.statusText / realShortfall：详情页缺料列表改实时值（前 3 种）；
    - BlueprintBuildExecutor.combinedShortfall / anyMaterial：下达预检同样改为
      combinedHaveAll 口径（所有绑定女仆 + 主人）。
  - 区块内右击手册 → 直接跳转该区块建造详情页（v2.0 已实现，本次确认），
    之后所有操作（暂停/继续/取消/速度/全员加入）仅针对本区块。
- **promaid 1.5.178：区块内控制限制 + 女仆管理升级（区块列表/命名/绑定解绑）**
  - **区块内控制**：暂停/继续/取消/速度/全员加入/逐只暂停/设工头等区块控制功能，
    **必须站在建造区块内**（红色区块框内）右击手册才显示按钮；服务端同步校验——
    区块外发来的控制操作会被拒绝（提示"必须站在建造区块内才能操作"）。区块外
    右击手册 = 正常目录（可下达新计划：无计划创建/暂停中覆盖/未暂停拒绝）。
    - 注：v1.5.171 曾因"计划在远处无法取消 = 死锁"取消该限制——现按用户明确
      要求重新引入（区块外无法控制该区块；取消/暂停须进区块）。
  - **女仆管理升级**：
    1. **有效建造区块列表**：女仆管理页顶部显示当前所有有效建造区块（跨维度），
       命名 = 建筑名称，同名自动编号（「小木屋」「小木屋2」「小木屋3」），
       附维度名（主世界/下界/末地）+ 状态（建造中/暂停中）+ 坐标。
    2. **女仆绑定显示**：女仆管理页改为显示全部女仆（不限任务/距离），每行显示：
       名字 + 任务名 + 绑定区块（所在维度计划的显示名，未绑定留空）+ 建筑状态
       （建造中/缺料:xx/暂停）+ 工头标记。
    3. **手动解绑/绑定**：每行【解绑】按钮 = 女仆切到空闲任务（touhou_little_maid:idle），
       离开建筑状态、不再参与区块建造（v1.5.177 暂停解绑语义的逐女仆版）；
       【绑定】按钮（非建筑女仆） = 切到建筑任务（maid_smart:build），加入所在
       维度区块（无计划则站桩等待下达）。绑定/解绑是女仆任务切换，区块外也可操作；
       逐只暂停/设工头/全部暂停仍限区块内。
  - 文件：BlueprintBookNetworking（collectBuildRegions/去重命名/bindDisplayMap/
    BIND_MAID/UNBIND_MAID/区块内校验/包字段扩展）、BlueprintBookScreen（按钮
    inRegion 限制 + 女仆管理页重写）、BlueprintBookItem（包构造带区块列表）。
- **promaid 1.5.177：暂停 = 解除绑定——暂停后女仆可以停下来去干别的事**
  - 症状：暂停区块建造后，女仆仍被钉在原地（停不下来），无法切任务/去别的区块。
  - 根因（三处叠加）：
    1. MaidBuildBehavior.canUse（m_6114_）只查任务不查暂停 → 暂停时行为照常运行，
       而站桩代码（setStill + 清移动目标 + 停导航）在 paused 检查【之前】执行
       → 每 tick 重新站桩，暂停只停"放置"没停"站桩"；
    2. canStillUse（m_6737_）同样只查任务 → 行为永不停止 → stop 的站桩清理
       （m_6732_）走不到；MaidWorkTags.isStill 的防残留只认"任务切走"，
       暂停时任务仍是 build → MaidMoveSuppressMixin 持续冻结移动；
    3. execute 的 TYPE_BUSY：暂停状态的计划仍算"女仆绑定" → 去别的区块下达
       新蓝图照样被拒。
  - 修复：
    1. canUse + canStillUse 均加暂停检查（全局 isPaused / 逐只 isMaidPaused）→
       暂停时行为停止评估 → 框架调 stop → 清站桩标记 + 退出 home 模式，
       女仆恢复自由（可走动、可切任务）；
    2. BuildActivityForceMixin：暂停中不强制 WORK activity → 女仆恢复正常作息；
    3. execute：全局暂停中允许为新区块下达新计划（旧暂停计划进度丢弃：
       清计划/档案/强制加载）；同蓝图同原点重下达仍 = 续建（解除暂停继续）。
    4. 恢复建造（手册恢复/同蓝图重下达）→ 行为重新启动，自动重新站桩。
- **promaid 1.5.176：修复游戏加载世界时崩溃（MixinTransformerError 致命错误）**
  - 症状：进存档/女仆实体加载时服务端线程致命崩溃（2026-08-09 11:32 会话实证，
    "卡了"后 java 进程退出，无 crash-report——mixin 致命错误只进日志）。
  - 根因：mods 文件夹实际运行的是修改版 TLM（touhoulittlemaid-1.5.3-forge+mc1.20.1-SMART.jar，
    8/6 放入），其中 MaidUpdateActivityFromSchedule 有 **3 个同名重载**
    （public (EntityMaid,Brain) / public (EntityMaid) / private (ServerLevel,EntityMaid,Brain,long)）；
    v1.5.173 的 BuildActivityForceMixin 只按方法名注入 → 命中第一个 public 重载 →
    注入签名校验失败（Expected (EntityMaid,Brain,CallbackInfo)V，
    found (ServerLevel,EntityMaid,Brain,long,CallbackInfo)V）→ Mixin apply failed →
    创建女仆实体时 MixinTransformerError 崩溃。
  - 修复：@Inject method 指定完整 descriptor，精确命中 private 4 参汇聚点
    （javap 实证 SMART jar 中该方法存在）。注入逻辑不变（任务 = maid_smart:build 时
    强制 WORK + 跳过 schedule 切换，休息时段照常建造）。
  - 核对：其余 28 个 mixin 目标（vanilla StartAttacking / EntityMaid.doSweepHurt 等）
    在 SMART 版下 v1.5.166-172 会话已实际运行验证，签名无冲突。
- **promaid 2.0：建造系统彻底重构（单计划状态机 + 统一已建感知 + 区块内右击进详情）**
  - **为什么重构**：旧系统多套机制纠缠（GLOBAL_PLAN 内存 + BuildArchive 持久化 +
    女仆 TaskData 引用行三处存储；filterBuilt 条件过滤；区块内按钮限制），修复
    成本过高，按用户要求全新起草。
  - **新架构（完整简单）**：
    ① **一份存储**：计划只存 GLOBAL_PLAN（内存）+ BuildArchive（持久化重启续建）
    ——彻底移除女仆 TaskData 引用行（存档残留/意外恢复/重复拒绝卡死的根源）；
    旧遗留 TaskData 数据无害（getPlan 不再读取）。
    ② **单计划状态机**：每维度同时最多一个计划 = 女仆-区块绑定；切任务不解除
    （切回自动续建）；取消/完成才解除。
    ③ **统一已建感知**：任何下达都扫描区块内与蓝图匹配的方块视为已建（不拆、
    只补缺）——材料缺口 = 需求 − 已建 − 背包（主人+女仆），提示准确
    （旧塔在时只报"缺灯笼"，不再虚报"缺石砖×34"）；想推倒重建需手动拆。
    ④ **续建语义**：同蓝图同原点重下达 = 继续建造（保留进度/游标，解除暂停，
    不拒绝）；不同蓝图/原点 = 拒绝（"女仆已绑定另一个计划，先取消/暂停"）。
    ⑤ **区块内右击手册** → 直接进入该计划建造详情页（进度/缺料/暂停/取消/
    全员加入/速度）；区块外右击 = 正常目录（全新蓝图）。
    ⑥ **缺料等待**：女仆站桩等待（已确认），手册详情页进度文本带缺料列表
    （"缺料：灯笼×1"，从延后集统计前 3 种）；补料自动续建。
    ⑦ activity 强制 WORK（v1.5.173 BuildActivityForceMixin）保留。
  - 纯 promaid 改动：BuildPlan、BlueprintLib、BlueprintBuildExecutor、
    BlueprintBookNetworking、BlueprintBookItem、BlueprintBookScreen。
  - 兼容：BuildArchive 字段不变，旧存档直接可读；旧 TaskData 引用行被忽略。

- **promaid 1.5.175：跳劈斩改抛物线大跳（用户指定"那种抛物线曲线"）**
  - 跳劈斩（dash 大跳/远距跳劈）运动参数重调：
    - 垂直初速 0.75 → **0.95**（峰高 ≈5.6 格——明显的大抛物线弧线，不再是
      贴地滑翔）；
    - 水平初速 0.3 恒定 → **按距离缩放**（近跳慢/远跳快，滞空 ≈24 tick 内落点
      恰好落在敌人附近——不过头、不扑空）；
    - 大跳覆盖距离 8.5 → **12 格**；
    - **跳劈落地清摔落距离**——抛物线大跳自然摔落 5.6 格会掉血，跳劈是进攻
      动作不应自损（落地瞬间 fallDistance 清零，无伤）。
  - 纯 promaid 改动：MaidCombatTacticsBehavior。

- **promaid 1.5.174：跳劈触发重构（距离触发 + 人数分流）+ 取消"平A"概念**
  - **距离触发**：离目标太远（> 4.5 格，跳劈近距上限之外）→ 冷却好就【大跳】
    跳过去给跳劈（朝敌人滑翔的跳劈斩），**不论敌方多少**；太远（>8.5 格）
    跳不过去就先走近再跳。
  - **人数分流（跳劈距离内）**：
    - 单敌（索敌范围 8 格内敌人 <2）→ **主体跳劈**（70% 概率触发），跳劈空档
      由 TLM 正常攻击穿插横扫补伤害；
    - 多敌（≥2）→ **反过来尽可能横扫**（跳劈概率降到 12%）。
  - **取消"平A"状态**：删除跳劈落地后 8 tick 禁横扫窗口（JUMP_SWEEP_BLOCK_UNTIL）
    ——旧版跳劈落地后的"平A"（禁横扫的普通攻击）用户认为没有意义；现在跳劈
    落地后立即恢复正常攻击（含横扫判定），空档即横扫补刀。空中不横扫保留
    （跳劈暴击保持单体）。
  - 纯 promaid 改动：MaidCombatTacticsBehavior、MaidSweepMixin。

- **promaid 1.5.173：建造不启动根因修复（TLM 时间表 activity≠WORK → 任务行为不被评估）**
  - **根因（日志实证 + TLM/原版字节码）**：TLM 的 MaidUpdateActivityFromSchedule
    每 tick 按时间表（Schedule）切换女仆 activity——游戏内休息/睡觉时段切到
    REST/HOME/IDLE。任务行为（MaidBuildBehavior）注册在 WORK activity，只有
    activity 激活才被 Brain 评估。休息时段下达建造 → setPlan/setTask 全部成功、
    "好！我来建XX啦"气泡照发，但行为 canUse 根本不执行 → 不建造。
    症状特征（10:42 会话实证）：core 行为日志正常（jump attack 有日志）、
    WORK 任务行为日志一条没有（build canUse 零条）。
  - **修复（双保险）**：
    ① 新增 BuildActivityForceMixin——注入 TLM updateActivityFromSchedule
    （private static 4 参汇聚点），女仆当前任务是 maid_smart:build 时强制
    setActiveActivity(WORK) 并跳过时间表切换——下达建造 = 玩家明确意图，
    休息时段也必须建；任务切走后恢复正常作息；
    ② BlueprintBuildExecutor 下达后立即 setActiveActivity(WORK)（不等下一次
    schedule 切换）。
  - 纯 promaid 改动：BuildActivityForceMixin（新）、BlueprintBuildExecutor、
    mixins.promaid.json。

- **promaid 1.5.172：连锁采集改为同时破坏（FTB Ultimine 式）+ 挖矿换镐改目标驱动**
  - **连锁采集"看不出来"**——旧版实现是"挖完一块自动去挖队列里的下一块"
    （逐块挖，日志实证 queued=5 连续挖）——功能在工作但视觉上像普通挖矿不停手，
    用户期望的是 FTB Ultimine 那种【破坏一个、周围一圈一起爆】。
    **改为同时破坏**：目标矿挖完瞬间，把队列里相连的同族矿一次性全部破坏
    （掉落物直接进女仆背包，放不下落地）；音效/粒子只在目标矿播（防 16 连爆音
    刷屏），镐耐久只扣目标矿一次。新增诊断日志 "mine chain burst"（一次爆几块）。
  - **换镐太心急**——旧版一切换到挖矿任务就判定换镐（任务级 ensurePickaxeIfEmpty：
    空手/非镐立刻从背包装镐，不管附近有没有矿）。
    **改为发现矿石后才判定**：去掉任务级换镐，换镐完全由 MaidMineBehavior
    在【找到目标矿时】调 ensureForTarget（手中镐能挖不换、挖不动才从背包装备
    能挖的）；空手状态等发现矿石再说。
  - 纯 promaid 改动：MaidMineBehavior、MaidToolAutoEquip。

- **promaid 1.5.171：控制按钮不再依赖玩家位置（修复"继续建造按钮消失"）+ canUse 诊断 + 清理残留存档**
  - **继续建造/暂停/速度/取消按钮消失**（用户反馈）——v1.5.162 的"控制按钮仅当
    玩家位于建造区块内才显示"被证明太严：计划在远处/地下矿洞时玩家根本找不到
    区块 → 按钮永不出现 → 无法继续/取消 = 死锁（日志实证：10:18-10:27 会话
    反复"重复建造已拒绝"，玩家无法操作远处计划）。
    **修复**：改为【只要存在进行中的建造计划（progressPct ≥ 0）就显示控制按钮】
    （全员加入按钮同理）；红色区块框仍由服务端推送指示计划位置。
  - **下达成功但女仆不建造**（"口头答应+材料进背包但不建"）——新增 canUse 诊断
    日志（latest.log 搜 "build canUse"，每 5 秒一条：任务 UID/计划大小/全局暂停/
    逐只暂停）——下次复测直接看日志定位是"任务没切过来/activity 不对"还是
    "计划/暂停状态问题"。
  - **清理残留建造存档**：maid_smart_build.dat 里有一份 10:12 下达的瞭望塔计划
    （@375,-60,61 cursor=1，矿洞内 0 进度）——已备份（.bak_20260809）后清空
    blueprintId——下次进游戏不会再自动恢复在矿洞里续建，也不会再卡"重复建造
    已拒绝"。
  - 备注：10:27:55 的崩溃（ClassNotFoundException: SelfPreservationBehavior
    $AttackerRecord）是【游戏运行中替换 jar】导致（类文件本身完好）——部署一律
    在游戏关闭后进行。
  - 纯 promaid 改动：BlueprintBookScreen、MaidBuildBehavior。

- **promaid 1.5.170：建造死锁修复（被拒时看不到计划位置→无法取消）+ 恢复诊断日志**
  - **背景（日志+存档实证）**：玩家在矿洞里下达瞭望塔（originPlayer 已开，
    起点=玩家脚下 y=-60）→ 塔建在矿洞；旧塔在时下达会"秒完成"（已建过滤，
    只补缺的灯笼）；随后再次下达 → "重复建造已拒绝"——但**被拒时客户端不推送
    红色区块框**（旧逻辑只在 TYPE_OK 时推）→ 玩家不知道计划在哪 → 走不到
    区块 → 手册控制按钮（含取消）只在区块内显示 → 无法取消、无法下达新计划
    = **死锁**；游戏退出时计划未清 → 存档残留（maid_smart_build.dat
    实测：瞭望塔 cursor=1 未动）→ 下次进游戏自动恢复续建（恢复点在地下）。
  - **修复**：
    ① 重复建造拒绝的提示附【当前计划位置坐标】——玩家立刻知道去哪取消
    （BlueprintBuildExecutor + SmartBuildTool 两处）；
    ② 手册下达蓝图无论成功/失败/被拒，一律推送计划区块标记（红色固定框）——
    被拒时也能看到计划区块在哪，走过去用手册控制按钮取消；
    ③ 档案恢复（restoreFromArchive）新增诊断日志
    "build plan restored from archive"——下次"女仆为什么又在建"直接看日志。
  - 纯 promaid 改动：BlueprintBuildExecutor、SmartBuildTool、
    BlueprintBookNetworking、BuildPlan。

- **promaid 1.5.169：手中正在使用的工具/武器/盾牌不触发切换，除非即将用坏自动换下一个**
  - **主手规则**（武器/镐统一）：主手已装备且符合任务白名单 → 不触发任何切换；
    只有两种例外会换：
    ① 主手物品【即将用坏】（剩余耐久 ≤10%）→ 自动从背包装备下一把
    （评分照旧：武器 DPS>附魔>耐久；镐按挖掘等级对标/质量>附魔>耐久，
    背包扫描仍走黑名单保护——换来的不会是快坏的）；
    ② 主手不符合白名单（拿错武器）→ 照旧换。
    背包无可用品时保持现状继续用（用坏为止，不空手）。
  - **盾牌规则**（与主手大致类似）：副手已装备盾且未即将用坏 → 不动；
    副手盾【即将用坏】→ 自动从背包装备下一把；副手空 → 补盾；
    副手是非盾物品 → 一律不动（尊重搭配）。背包扫描统一黑名单保护 +
    评分 附魔词条数 > 剩余耐久（与武器/镐一致，旧版只按剩余耐久挑）。
  - 纯 promaid 改动：MaidToolAutoEquip。

- **promaid 1.5.168：附魔装备黑名单保护（即将用坏）+ 评分翻转（附魔>耐久）+ 镐切换挖掘等级对标**
  - **附魔装备保护（黑名单）**：剩余耐久 ≤ 总耐久 10% 的物品视为"即将用坏"，
    之后的任何背包切换都不会再选择它（附魔与否统一保护——切到一把马上坏的
    等于没换）；**耐久恢复（>10%，玩家修复/取出维修）后自动解除**（动态判定，
    无需持久化）。主手正在使用的物品不受影响（不强制换，避免浪费）。
  - **评分规则推翻**：切换优先级从 耐久>附魔 改为 **附魔词条数 > 耐久**
    （武器与镐统一）——低耐久风险已由黑名单兜底，10% 以上的耐久差异不再
    压过附魔。
  - **镐切换新增最高优先级 = 挖掘等级对标**（ensureForTarget 换镐时）：
    在能挖目标矿的镐里，先看哪把镐的挖掘等级**最贴近**该矿物（恰好够用的
    最低等级优先，不浪费高级镐），然后附魔词条数，然后剩余耐久。
    - 例：10 把石镐 + 1 把铁镐，面前铁矿 → 石镐（tier 1 恰好对标铁矿）
      优先于铁镐（tier 2 多出 1 级）；10 把石镐里附魔的先选；附魔石镐
      若只剩 1 点耐久 → 黑名单跳过，换下一把。
    - 空手装备时没有目标矿可对标 → 仍按"挖掘等级最高 > 附魔 > 耐久"拿
      最好的镐（与对标评分分开，避免空手拿最低级镐）。
  - 纯 promaid 改动：MaidToolAutoEquip。

- **promaid 1.5.167：武器切换优先级重写（DPS>耐久>附魔）+ 三叉戟自保确认覆盖 + 记忆调试面板**
  - **武器切换规则（按用户指定重写）**：
    - 进入战斗模式后，主手已有武器且符合任务白名单（攻击=近战武器/弓=弓/弩=弩/
      三叉戟=三叉戟/弹幕=御币）→ 不切换（玩家战术安排优先）；
      空手或手持武器不符合白名单 → 才切换。
    - 切换优先级 = **DPS（最终结算后）> 耐久度 > 附魔词条数**：
      DPS = 最终伤害 × 攻速；最终伤害 = 攻击力属性 + 附魔最终结算（锋利
      1+0.5×(级-1)、亡灵杀手/节肢杀手 2.5×级取最大、火焰附加 4×级总伤）；
      攻速 = 4.0 基础 + 攻速属性（剑 1.6/斧 1.0/三叉戟 1.1）。算不出 DPS
      （无攻击力属性——弓/弩）→ 退化为 DPH（也没有 → 0 分段，同白名单内
      按耐久/附魔择优，弓弩伤害取决于箭矢无法从武器结算，合理）。
      御币同理（有攻击力属性，按通用公式）。
    - 矿镐：切换优先级 = **耐久 > 附魔**（材质等级不再参与评分——切换只发生在
      "空手/非镐/挖不动目标矿"时，能挖性已过滤，既然都能挖就比耐久和附魔）；
      **人工调整不造成影响**：玩家亲手放的镐（高耐久/高附魔/任意等级）只要
      能挖当前目标矿就绝不触发女仆切换。
  - **三叉戟自保失效确认覆盖**：TaskTridentAttack 字节码实证——专属行为
    MaidTridentTargetTask / MaidAttackTridentTask 都只【读】ATTACK_TARGET 不写，
    唯一目标写入源是 StartAttacking → v1.5.166 的 StartAttackingPreserveMixin
    已完整覆盖三叉戟任务，无需额外改动。
  - **记忆系统调试+可视化面板（手册 → 女仆记忆）**：
    - 女仆列表：每行直接显示该女仆的段落数（记忆量可视化）+ 系统调试统计行
      （女仆总数 / 记忆开启数 / 段落总数）。
    - 单个女仆页新增调试块：记忆存储目录（世界目录/promaid_memory/<uuid>）、
      统计（段落/永久/已删/关系/画像/片段）、来源分布 top5（聊天/喂食/好感等
      各积累了多少条）。
    - 段落可视化：重要度颜色分级（金色 ≥8 / 绿色 ≥5 / 灰色 <5）+ 访问次数。
  - 纯 promaid 改动：MaidToolAutoEquip（评分重写）、BlueprintBookNetworking、
    BlueprintBookScreen。

- **promaid 1.5.166：弓/弩自保失效修复（StartAttacking 断源）+ 御币武器切换修复 + 自保诊断日志**
  - **自保在使用弓/弩时失效——根因（TLM/原版字节码实证）**：StartAttacking 是
    声明式 OneShot 行为，只要 ATTACK_TARGET 记忆为空且附近有敌对目标，它就
    【每 tick 重设 ATTACK_TARGET】；自保虽每 tick 末尾清目标（优先级 250 最后
    执行），但 TLM 弓/弩/弹幕任务注册的 MaidAttackStrafingTask / MaidShootTargetTask /
    MaidCrossbowAttack 在任务组优先级 5 已先看到被重设的目标 → 每 tick 重新拉弓/
    装填/横向走位（MoveControl 直接移动 + 瞄准），与自保逃跑导航互相覆盖 →
    女仆持弓/弩时站在原地对射绕圈，不逃不传 = "自保失效"。近战不受影响
    （只写 WALK_TARGET 记忆，自保每 tick 清空 + 直连导航覆盖）。
  - **修复**：新增 StartAttackingPreserveMixin——注入 StartAttacking 设置目标的
    唯一触发函数 m_257271_，自保标记存在时直接返回 false，目标永不重设 →
    整条 TLM 战斗链（弓/弩/弹幕/近战）在自保期间彻底熄火，自保完全接管；
    另在自保进场时强制 stopUsingItem（松开弓/弩拉弓装填状态，m_5810_）。
  - **御币武器切换失效——根因**：①弹幕任务（danmaku_attack）在自动装备机制里
    走 default 直接返回 false——空手/拿剑的弹幕女仆永远不会掏出御币；
    ②御币是 ProjectileWeaponItem 子类且带攻击力属性，攻击模式下被自动装备
    机制当成"合格近战武器"留下不换，女仆拿着御币在攻击任务里打不出弹幕。
  - **修复**：MaidToolAutoEquip 新增弹幕任务 → 主手不是御币就从背包装一把御币；
    攻击模式近战判定排除御币（与镐/弓/弩同规则），拿御币时自动换成真武器。
  - 自保进场/退出传送新增诊断日志（latest.log 搜 "self preserve start" /
    "self preserve exit teleport"），下次复测直接贴日志即可定位。
  - 纯 promaid 改动：StartAttackingPreserveMixin（新）、SelfPreservationBehavior、
    MaidToolAutoEquip、mixins.promaid.json。

- **promaid 1.5.165：血机制解除自保后第一件事 = 定位主人并传送过去**
  - 按用户确认调整退出传送：血恢复到 70% 退出自保时，只要不在主人 5 格内就
    【无条件传送】回主人身边（不再要求"主人身边安全"、不再设 24 格距离门槛——
    血已恢复，传过去帮忙/护卫主人）；仅保留 5 秒传送冷却防反复连传。
  - 纯 promaid 改动：SelfPreservationBehavior（teleportHomeOnExit 放宽条件）。

- **promaid 1.5.164：建造区块红色固定框 + 自保退出传送 + 跳劈连贯性 + 挖矿诊断日志**
  - 区块显示升级：一旦建造区域定下来（有进行中/暂停中的计划），世界内自动显示
    【红色固定框】——实际建造区域（服务端下发的区块标记），定在原地不随玩家移动，
    取消/完成建造自动消失；金色预览框（玩家选位置用）保持不变、可同时显示。
    下达建造成功立即推送（不等 2 秒轮询），打开手册也会同步。
  - 自保传送修复：血恢复退出自保时若离主人超过 24 格 → 尝试传送回主人身边
    （主人身边安全才传，与 teleportHome 同判定同冷却），播报"我回到主人身边了"。
  - 跳劈连贯性：单敌跳劈冷却 25 → 40 tick（2 秒一次，不再"兔子跳"）；坠落早退
    阈值 2.5 → 4.0（跳劈斩落距 3.5 不再被误判"坠落中"→ 落地后立刻恢复移动/攻击）。
  - 设置界面：删掉与"保存并返回"定位重合的"完成"按钮，只保留一个。
  - 挖矿排查：连锁采集/自动收集加诊断日志（mine behavior start / chain filled /
    chain empty / autoCollect），下次测试看日志确认是否生效及原因。
  - 纯 promaid 改动：BlueprintAreaPreview（红色固定框）/ BlueprintBookNetworking
    （region 推送）/ SelfPreservationBehavior（退出传送）/ MaidCombatTacticsBehavior
    （跳劈节奏）/ PromaidConfigScreen（删按钮）/ MaidMineBehavior（诊断日志）。

- **promaid 1.5.163：连锁采集 / 连锁收获的范围可自定义**
  - 挖矿页新增【连锁采集上限（块）】：一次连锁挖掘的最大方块数（4~64，默认 16）。
  - 杂项页新增【连锁收获上限（格）】：农场一次连锁收割的最大格数（4~96，默认 24）。
  - 纯 promaid 改动：MaidSmartConfig / PromaidConfigScreen / MaidMineBehavior（refillChainQueue
    读配置）/ FarmSweepMixin（sweepChain 读配置）。

- **promaid 1.5.162：建造区块标记机制 + 控制按钮按区块显示 + 去掉强制建造/强制续建**
  - 控制按钮动态显示升级：续建/暂停/速度/取消/全员加入只在【玩家站在建造区块内】才显示
    （服务端下发计划区块标记：中心点+宽高深，2 秒轮询刷新）——看到区块范围（渲染框）进到
    里面才有控制权；区块外只显示 返回目录/建造此图纸/区块显示。
  - 删除"强制建造"和"强制续建"两个按钮及全部相关机制（FORCE_BUILD 状态/网络字段/处理分支）：
    建造【默认强制执行】——不再做障碍物预检，未建部分的任何非蓝图方块（含玩家改造/树木）
    由建造行为运行时拆成掉落物再放置（渲染已给出建造范围 = 玩家自己的选择）。
  - 【有建造计划的区块不能重复建造】：手册/对话/AI 设计任何入口再次下达蓝图时，若该维度
    已有进行中/暂停中的计划 → 拒绝并提示"用手册的暂停/继续控制，或先取消再开始新的"。
  - 取消建造 = 删除区块标记（已建部分不再被识别为半成品，下次建造按【障碍物】强制拆掉重建）；
    暂停建造 = 保留标记（半成品续建识别照常）。filterBuilt 已建过滤改为只在有保留标记时生效。
  - 纯 promaid 改动：BlueprintBookScreen / BlueprintBookNetworking（ProgressUpdate 与
    OpenBlueprintBookPacket 增计划区块字段）/ BlueprintBuildExecutor（TYPE_BUSY 拒绝）/
    BuildPlan（删 FORCE_BUILD + 加 getPlan(level)）/ BlueprintLib（filterBuilt 标记感知）/
    SmartBuildTool / SmartDesignTool / BlueprintBookItem。

- **promaid 1.5.161：进阶采集——挖矿连锁采集/自动收集 + 农场连锁收获/自动收集（默认关闭）**
  - 挖矿（mine 段）：【连锁采集】挖完一块矿后 BFS 找相连的同族矿石排队自动接着挖
    （矿脉一次挖完，最多 16 块；只连锁矿不连锁石头；借鉴 FTB Ultimine 连锁破坏思路）；
    【自动收集】挖掘掉落物直接进女仆背包（getDrops 同款精准/时运判定，放不下才落地；
    搭方块 10 秒自动销毁的掉落物也会给附近挖矿女仆）。默认均关闭，配置面板挖矿页可开。
  - 农场（杂项段）：【农场连锁收获】收割时以目标格为中心 BFS 蔓延连锁收割相连农田里的
    成熟作物（一次最多 24 格，大农田多轮清完，与 v1.5.130 连收连种同机制真实消耗种子）；
    【收获物自动收集】收割产物（作物/种子等）直接进女仆背包不落地。默认均关闭。
  - 纯 promaid 改动：MaidMineBehavior（连锁队列 + 自动收集）、FarmSweepMixin（BFS 蔓延 +
    自动收集）、MaidSmartConfig / PromaidConfigScreen（4 个新开关）。

- **promaid 1.5.160：详情页控制按钮随"有无进行中计划"动态显示**
  - 首次进入建筑详情页且周围没有进行中的建造区块时：底部只有
    【返回目录 / 建造此图纸 / 区块显示】（居中排布），全员加入、暂停、速度、
    强制续建、强制建造、取消全部隐藏。
  - 开始建造后（客户端 progressPct >= 0，每 2 秒轮询自动刷新）：
    全员加入出现在行1，暂停/继续、速度、强制续建、强制建造、取消出现在
    材料下方的行2——取消/暂停/续建等按钮"开始建造才出现"。
  - 纯 promaid 改动：BlueprintBookScreen。

- **promaid 1.5.159：手册建造面板 UI 重做 + 区块显示（建造范围预览）**
  - 【建造目录页精简】点击"建造"后，页面只剩：建筑目录列表 + 上一页/下一页 +
    返回大目录 + 退出（ESC）——删去原来的全部建造控制按钮。
  - 【建筑详情页】点击目录里的建筑进入详情页——原来所有建造相关 UI 都移到这里：
    材料清单（翻页）+ 返回目录 / 全员加入 / 建造此图纸 / 区块显示（行1）+
    暂停继续 / 速度 / 强制续建 / 强制建造 / 取消建造（行2）。
  - 【区块显示】新 UI：点"区块显示"→ 退出手册，世界内以【玩家为中心】显示该建筑
    占地大小的【金色框】（半透明金色填充 + 金色线框 + 顶部悬浮文字显示尺寸），
    随玩家位置移动而移动（方便选位置）；颜色用金色，区别于原版 F3+G 的白色区块
    边界；再次打开手册即关闭预览。渲染照抄 TLM MaidAreaRenderEvent 的坐标约定与
    TLM RenderHelper（已验证可运行），蓝图尺寸由服务端下发（Entry 新增字段）。
  - 纯 promaid 改动：BlueprintBookScreen / BlueprintBookNetworking /
    BlueprintBookItem / BlueprintLib / 新增 BlueprintAreaPreview。

- **promaid 1.5.158：自保提示矛盾消除——威胁播报/退出播报区分 + 传送免触发窗口**
  - 【矛盾场景（日志实证）】传送回家 → 血仍 <30% → 立刻又进场 → 已在主人身边 →
    退出 → 又进场……循环刷"情况不妙/应该安全了吧"矛盾提示（07:07 每 6 秒一次）。
  - 【修复】
    1. "情况不妙"改由【扫到第一个威胁】才播报（每场一次）——无威胁的低血自保
       安静回血/回家，不喊话 → 无威胁的循环完全静默；
    2. 退出播报区分原因：血 ≥70% 退出 → "呼……应该安全了吧。"；真传送成功 →
       "我回到主人身边了，先缓缓……"（不暗示绝对安全——与可能紧接着的
       "情况不妙"语义连贯："撤了"→"回到身边"，不矛盾）；
    3. 传送回家后 5 秒【免触发窗口】：canUse 不进场（血 <30% 也不进，环境自保
       不受限）——给"回到主人身边"一个喘息期，杜绝"退出→立刻进场"抖动循环。
  - 纯 promaid 改动：SelfPreservationBehavior。

- **promaid 1.5.157：搭高不再扔珍珠 / 传送重试封顶 5 秒（旧配置 60 秒不跟随修复）/ 自保解除播报**
  - 【搭高不再扔珍珠】日志实证（07:06:38 "用末影珍珠瞬移逃走了！"）：珍珠判定在
    策略链第一位，搭高后血仍 <30% 且威胁在 8 格内（直线距离）就会扔珍珠——高处已
    安全却还瞬移。修复：搭高状态（pillarBaseY ≥ 0 / forcedPillar）跳过珍珠逃生。
  - 【传送不积极根因】配置实证：用户配置文件 teleportCooldown=1200（60 秒）——
    v1.5.152 的新默认 200 不会覆盖旧配置 → 传送失败一次后 60 秒不重试 →
    "主人身边没怪也不传送"。修复：重试间隔用 min(配置, 100)（封顶 5 秒）——
    旧配置 1200 实际也 5 秒重试；传送成功即结束自保，不会循环。
  - 【自保解除播报】退出自保（血 ≥70% 或传送成功）瞬间气泡提示一次：
    "呼……应该安全了吧。"（不再无声无息恢复）。
  - 纯 promaid 改动：SelfPreservationBehavior。

- **promaid 1.5.156：建造"瞬间完成"根因修复——建材等价不再算"已建"**
  - 【根因（日志实证）】07:14:41 下达瞭望塔（35 块）→ 0.25 秒后"建造完成"。
    用户蓝图"精灵瞭望塔"含 stone_bricks（石砖），而石砖等价组含
    deepslate_bricks；下达原点在深板岩层（y=-60，远古城市）——远古城市的地板
    墙壁全是 deepslate_bricks → 34 个石砖步骤全被等价匹配判为"已建" → 游标
    直接滑到尽头 → 瞬间"完成"（蓝图根本没建）。
  - 【修复】新增 BlueprintLib.isBuiltEquivalent："已建"判定的等价只限地形类组
    （dirt / grass_block，v1.5.55/56 的泥土↔草方块互搏场景）；建材类组
    （石砖/圆石/木板/门/玻璃等）"已建"只认同方块——深板岩砖不再是"石砖已建"。
    材料消耗（背包匹配）仍用完整等价不受影响。全部 12 处"已建"判定点替换：
    主循环 / 延后轮询 / scanGaps / filterBuilt / findObstacles / findExistingOrigin
    匹配与锚点 / restoreFromArchive 预登记 / SmartBuildTool 进度统计。
  - 效果：在远古城市/深板岩层等自然结构上建造时，目标位置不再被误判"已建"——
    会正常拆除障碍（可拆方块）并放置蓝图方块；若在空地建则行为完全不变。
  - 纯 promaid 改动：BlueprintLib / BuildPlan / MaidBuildBehavior / SmartBuildTool。

- **promaid 1.5.155：跳劈全程禁横扫 / 挥砍延迟 / 跳劈斩弧线加高 / 跳劈不后撤**
  - 【跳劈全程禁横扫】MaidSweepMixin 扩展：空中攻击不横扫（原有）+ **落地后 8 tick
    内**平A也不触发横扫特效（战术行为落地时写入 8 tick 时间戳标记，mixin 查询）——
    跳劈观感统一为"单体劈砍"，不混横扫特效。
  - 【挥砍延迟】起跳时刻记录 gameTime，挥砍延迟到空中后半段：普通跳劈起跳后 6 tick
    才挥刀（滞空约 10 tick）；跳劈斩 11 tick 才挥刀（即将落地）——"先跳起来、
    稍微隔一会再出伤害"，观感同步。
  - 【跳劈斩弧线加高】起跳 0.65→0.75（峰高 ≈3.5 格，弧线明显，摔落 3.5<4 无伤）、
    水平 0.35→0.3（滞空约 18 tick 滑翔约 5.4 格）。
  - 【跳劈不触发后撤】"打一刀退一步"只适用于横扫——跳劈期间（jumpAir）冷却写入
    不再触发后撤（跳劈是接近动作，跳完不该后退）；落地后的横扫平A仍正常后撤。
  - 纯 promaid 改动：MaidCombatTacticsBehavior / MaidSweepMixin。

- **promaid 1.5.154b：跳劈斩改为"跳向"敌人（抛物线弧线，非贴地冲刺）**
  - 用户纠正：跳劈斩不应该是"冲向"（贴地突进），而是"跳向"（跳跃弧线落到敌人
    身边）。调整：起跳 0.55→0.65（峰高 ≈2.5 格，明显腾空）+ 水平速度 0.5→0.35
    （滞空约 16 tick 滑翔约 5.6 格，覆盖跳劈距离）——观感为"腾空划过一道弧线
    跳向敌人劈砍 + 大击退"。

- **promaid 1.5.154：跳劈斩（30% 概率冲向敌人 + 大击退），其余普通垂直跳**
  - 跳劈更帅：单敌跳劈时 30% 概率触发【跳劈斩】——水平冲向敌人方向起跳
    （水平 0.5 速 + 起跳 0.55 高），空中劈中目标瞬间把敌人【大击退】劈飞
    （力度 1.5，方向 = 女仆→目标），暴击粒子加到 16 颗；其余 70% 保持普通
    垂直跳（仅改变高度，现状 12 颗粒子）。跳劈斩/普通跳劈的暴击音效一致。
  - 诊断日志：jump attack trigger 带 dash 标记（true=跳劈斩），命中日志区分
    jump dash crit / jump crit hit。
  - 纯 promaid 改动：MaidCombatTacticsBehavior。

- **promaid 1.5.153：单敌必跳劈 + 自保触发/取消机制重定义**
  - 【单敌必跳劈】用户指出"优先"不该还有 75% 概率在横扫——单敌（目标周围 <2 怪）
    时跳劈条件去掉概率判定：冷却满 + 在跳劈距离内（1.8~4.5 格）【必跳】，
    冷却 25 tick（≈1.25 秒一跳）；目标周围 ≥2 怪时横扫优先（不跳，平A横扫清群）。
  - 【自保触发条件】第 1 套自保（战斗自保）触发条件【仅有血量 <30%】——不再要求
    12 格内有威胁（低血本身就该小心；进场后无威胁时原地回血/实时传送回家，
    有威胁时逃跑/搭高/反击）。第 2 套自保（环境自保）不变：岩浆任意血量、
    火/溺水/窒息 85% 以下立即触发。
  - 【自保取消机制】只有两种：①血量恢复到 exitRatio（默认 0.60→0.70，70%）及
    以上；②成功传送回主人身边（传送成功立即取消）。取消后若血量仍 <30% 且
    再次遇险 → canUse 会再次触发自保（用户指定的循环）。原"威胁消失 20 秒退出"
    "安全回归血量 45% 退出"条件已删除（threatGoneTicks 弃用）。
  - 纯 promaid 改动：MaidCombatTacticsBehavior / SelfPreservationBehavior /
    MaidSmartConfig。

- **promaid 1.5.152：跳劈/横扫按敌人数分流 + 自保全程实时传送判定**
  - 【攻击方式按敌方数量分流】（战斗模式）——目标周围 3.5 格内敌人 ≥2 →
    横扫优先（保持地面平A，TLM 自动横扫清群，不跳劈）；单个敌人 → 跳劈优先
    （触发概率 0.12→0.25、冷却 40→25 tick，追击中明显更频繁地跳劈暴击）。
  - 【自保状态说明】自保时不会触发跳劈（战术行为只在本行为激活且目标存在时
    接管移动，自保每 tick 清攻击目标 → 战术不激活）；自保的近身还击走 m_7327_
    （地面 + 剑时横扫范围伤害），贴脸群怪时有效。
  - 【自保实时传送】不再等"威胁消失 20 秒"——自保全程每 tick 实时判定能否回
    主人身边（主人身边 5 格内无可见怪即传，传成功立即结束自保）；自保刚触发时
    也立即尝试（主人身边安全就直接回家）。传送冷却默认 1200→200 tick（10 秒），
    传不走（主人身边有怪）10 秒后重试。威胁消失的 threatGoneTicks 仅保留给
    "威胁消失退出自保"判定使用。
  - 纯 promaid 改动：MaidCombatTacticsBehavior / SelfPreservationBehavior /
    MaidSmartConfig。

- **promaid 1.5.151：传送安全半径默认 4→5 格（防远程怪；魂符可兜底绝对安全）**
  - 用户指定：主人身边判定半径默认改为 5 格（防止还有远程怪在射程内）；判定不用
    太大——反正传回主人身边后，主人直接拿魂符把女仆收起来就是绝对安全。
  - 说明：v1.5.150 起传送只判主人身边安全（撤销"自己身边无怪"拦截）。
    已生成的旧配置（teleportSafeRadius=4）不会自动跟随新默认值——可在配置面板
    "战斗自保 → 传送安全判定半径"调到 5。
  - 纯 promaid 改动：MaidSmartConfig / PromaidConfigScreen。

- **promaid 1.5.150：自保传送只判定主人身边安全（撤销"自己身边无怪"拦截）**
  - v1.5.91 的双判定（自己 + 主人身边都无怪才传）在"被怪追着跑"时永远不通过——
    自己身边永远有怪 → 女仆一直在逃跑、怪一直在追，永远传不回主人身边
    （用户反馈）。现只保留【主人身边】安全判定（半径 4 可配）：主人身边无可见
    怪物即可传送，传送成功立即结束自保。防"传回主人身边送死"的初衷不变。
  - 纯 promaid 改动：SelfPreservationBehavior.teleportHome。

- **promaid 1.5.149：跳劈"只跳不打"根因修复——空中挥砍由行为亲自执行 + 明显暴击提示**
  - 【根因】旧版跳劈只是"跳起来"，攻击仍由 TLM 攻击行为执行——它通常在【落地后】
    才挥砍（onGround）→ 不满足"空中命中"→ 暴击粒子永不触发（"跳劈不明显"根因，
    日志无从确认因从未打过跳劈日志）。跳劈触发条件还依赖 TLM 攻击冷却记忆
    （!cooling），冷却期间不跳 → 触发率进一步降低。
  - 【修复】
    1. 跳劈挥砍改由本行为亲自执行（tickJumpCrit）：起跳后空中贴近目标
       （≤2.6+0.5 格）→ 直接 m_7327_ 挥砍——空中命中（原版暴击判定），TLM 横扫
       逻辑下空中不横扫 = 跳劈暴击保持单体；一次跳劈只砍一刀。
    2. 明显暴击提示：12 颗大扩散暴击粒子（v1.5.143 是 8 颗）+ 玩家暴击同款音效
       （minecraft:entity.player.attack.crit，明显的"叮"声）。
    3. 跳劈触发去掉 !cooling 依赖（挥砍不再依赖 TLM 冷却时机），频率由
       JUMP_COOLDOWN(40 tick) + 12% 概率控制 → 追击中约 2 秒一跳，明显可辨。
    4. 诊断日志：跳劈触发（jump attack trigger）与暴击命中（jump crit hit）各打
       一条——下次测试后可直接从日志确认"有没有产生跳劈/暴击"。
  - 纯 promaid 改动：MaidCombatTacticsBehavior。

- **promaid 1.5.148：半成品原点识别改为"世界方块种类 ∩ 蓝图"交集锚点（用户方案）**
  - v1.5.147 的"蓝图前 3 稀有方块"仍是从蓝图侧出发——最稀有方块没建时要白试几轮。
    按用户方案重写 findExistingOrigin：
    1. 先扫描女仆周围区域（半径 48、y±12），**统计区域内实际存在的方块种类**（跳过
       空气/液体/自然地形）；
    2. 与蓝图所需方块比对取**交集**（含等价族，如蓝图要求橡木木板、世界是云杉木板）；
    3. 交集方块按【世界出现次数】升序作锚点（世界越稀有优先，候选少判定快），
       最多试 3 种、每种候选上限 256——缺料没建的方块根本不在世界里，不会当选锚点
       （瞭望塔：灯笼没建 → 交集只剩石砖 → 自动落到石砖）。
    效果：取消建造/重进游戏后重新下达，半成品识别直接命中，不再原点漂移重复建造。
  - 纯 promaid 改动：BlueprintLib。

- **promaid 1.5.147：御币弹幕还击（遵循御币原版攻击方式）/ 取消后续建识别兜底**
  - 【御币弹幕还击】御币（ItemHakureiGohei）是 ProjectileWeaponItem 子类——v1.5.145
    的"弓/弩贴脸放箭"会错误地命中御币（消耗箭射箭，不是御币的攻击方式）。
    修复：counterAttack 先判御币（ItemHakureiGohei.isGohei）→ 走 TLM DanmakuShoot
    弹幕射击（与 danmaku_attack 任务完全一致的流程：随机颜色/弹型 + 瞄准射击；
    伤害 = 攻击力属性 ×2.2、速度/精度公式照抄 TaskDanmakuAttack 字节码实证；
    无重力、aimedShot 贴脸瞄准）。弓/弩仍走放箭，近战/三叉戟仍走挥砍。
  - 【取消后续建识别兜底】findExistingOrigin（半成品原点识别）原来只拿
    【最稀有方块】当世界扫描锚点——瞭望塔最稀有的是 1 个灯笼，恰好缺料没建 →
    锚点不存在 → 续建识别失败、原点漂移重复建造。修复：按稀有度升序取前 3 种
    方块作为候选锚点逐个扫描（命中上限 64 防性能爆炸），任一种匹配成功即恢复
    原点。取消建造/重进游戏后重新下达蓝图，半成品识别更可靠。
  - 纯 promaid 改动：SelfPreservationBehavior / BlueprintLib。

- **promaid 1.5.146：跳劈高度回调（0.8 ≈4 格 → 0.5 ≈1.7 格，和玩家差不多）**
  - 用户反馈 v1.5.143 的 0.8 大跳（≈3.9 格峰高）太高——现在回落到 0.5：
    峰高 ≈1.7 格（玩家跳跃 1.25 格），和玩家跳劈差不多、顶多高一点点，
    视觉可辨但不会"飞起来"；落地摔落距离 <3 格不掉血。暴击特效不变。

- **promaid 1.5.145：自保近身反击支持远程武器（弓/弩贴脸直接放箭）**
  - 背景：v1.5.143 的近身反击用 m_7327_（doHurtTarget）挥砍——剑/斧/三叉戟有效，
    但弓/弩敲一下几乎没伤害（弓攻击力≈0），远程女仆自保被贴脸 = 纯挨打。
  - 【修复】SelfPreservationBehavior.counterAttack 按主手分发：
    - 近战武器/三叉戟/弹幕 → 维持 m_7327_ 挥砍（三叉戟戳 7 点伤害，横扫走 TLM 逻辑）
    - 弓/弩（ProjectileWeaponItem）→ **贴脸直接放箭**（shootPointBlank）：
      原版箭矢实体直射威胁（ArrowItem.m_6394_ 创建 → setOwner → shoot 1.6 速，
      瞄准胸口、贴脸必中；普通/药水/光灵箭均可），消耗背包 1 根箭，背包没箭则不射
      （继续逃跑）；不模拟拉弓/装填（贴脸自救简化），挥臂动画提示。
  - 纯 promaid 改动：SelfPreservationBehavior。

- **promaid 1.5.144：生存模式续建修复（缺料步骤不再被截断丢弃）/ 缺料播报中文名**
  - 【根因（日志实证）】材料不足时"先建材料够的部分"用的是【顺序截断】：从第一个
    缺料步骤开始 break，把缺料步骤【及其后所有步骤】从计划里永久丢弃。瞭望塔 =
    34 石砖 + 1 灯笼（灯笼在末尾）→ 截断后计划里根本没有灯笼 → 34 块建完直接
    "建造完成"（日志：05:22:55 下达 35 块，05:23:00 就宣布建好）；补料后重新下达，
    又因原点漂移/已建判定错位重复建造或再次截断 → "生存模式无法续建"。
  - 【修复】BlueprintBuildExecutor.execute（手册入口）与 SmartBuildTool（LLM/卷轴
    入口）的 partial 模式【不再截断】：材料不足时也**全量下发完整计划**，缺料步骤
    由建造行为按步延后（deferred 集 + 缺料播报 + 每 20 tick 轮询），补料后自动续建
    ——"把缺的材料放进你自己的背包，我会自动拿料接着建（不用再点手册）"这句话现在
    真正生效。只保留"完全没材料"的拒绝（避免女仆空建干等）。
  - 【补料自动续建链路】缺料步骤进延后集 → 女仆每 20 tick 轮询 → 主人背包有料 →
    tryTakeFromOwner 自动转给女仆 → 放置 → 全部补齐后正常宣布完成。
  - 【缺料播报中文】"材料不够了，还缺 minecraft:lantern ×1" → 中文名（"还缺 灯笼
    ×1"，与手册/阻挡播报同一套中文名机制）。
  - 纯 promaid 改动：BlueprintBuildExecutor / SmartBuildTool / MaidBuildBehavior。

- **promaid 1.5.143：障碍播报带矿物名 / 自保近身回击 / 跳劈加高+暴击特效**
  - 【障碍播报带矿名】挖矿"前方有xx挡住了矿物"现在会同时报【被挡的是哪种矿】的
    中文名（与挡路方块同一套中文名机制）——"前方有泥土（坐标…）挡住了钻石矿石
    （坐标…）"，不用再对着坐标猜矿种。
  - 【自保近身回击】自保逃跑中敌人贴脸（<2.5 格）时直接挥刀反击（m_7327_
    doHurtTarget，含横扫特效），不再纯逃跑——纯逃跑永远脱不了困，边跑边打才能
    拉开身位。反击不写 brain 目标（自保"每 tick 清攻击意图"的机制不变），
    打完继续原策略（逃跑/搭高/围壁）；冷却 12 tick ≈ 玩家平A节奏。优先级高于
    珍珠/搭高/围壁/逃跑全部分支——被围殴时先还手再脱身。
  - 【跳劈加高】垂直初速 0.42（原版跳跃）→ 0.8：峰高约 3.9 格（原版 3 倍，
    一眼看出"起跳了"）；摔落距离 3.9 < 4，落地不掉血（摔落伤害 = floor(距离-3)）。
  - 【跳劈暴击特效】跳劈空中命中瞬间（攻击冷却记忆刚写入 + 空中状态）在目标身上
    喷玩家跳劈同款暴击粒子（minecraft:crit，8 颗，一次跳劈只喷一次）。检测独立于
    meleeTick 放在坠落早退之前——下落中（fallDistance > 2.5）命中也能捕捉到。
  - 纯 promaid 改动：MaidMineBehavior / SelfPreservationBehavior /
    MaidCombatTacticsBehavior，无新文件。

- **promaid 1.5.142：进阶装备控制（副手盾牌 + 饰品栏图腾）/ 跨维度跟随 / 蛋糕可吃 / 建造强制坐下**
  - 【战斗自动装盾】进入任何战斗任务（攻击/弓/弩/三叉戟/弹幕）时，副手【空手】就
    自动从背包装盾（挑剩余耐久最高的一把，与换武器同一轮询节奏）；盾牌爆掉副手变空
    后自动补新盾。副手已有任何物品（包括 mod 的副手装备）一律不动——尊重模组副手
    搭配。仅战斗任务生效，挖矿/建造等不装盾。
  - 【饰品栏不死图腾】触发自保时，自动把背包里的不死图腾放进 TLM 饰品栏
    （BaubleItemHandler，30 格）：饰品栏已有图腾不动、有空槽才放、满了不覆盖现有
    饰品。原版图腾复活只查双手 → 新增 MaidBaubleTotemMixin（@Inject
    LivingEntity.m_21262_ HEAD）：死亡时双手没有图腾、但饰品栏有图腾 → 按原版流程
    复活（生命 1 + 清效果 + 再生 II/吸收 II/抗火 + 图腾粒子音效，消耗 1 个图腾）；
    手里有图腾仍走原版路径（不双份触发）。字节码实证：m_21262_=checkTotemDeathProtection、
    效果字段与数值逐条对齐原版。
  - 【跨维度跟随】主人穿过传送门/被传送进另一维度后，跟随模式的女仆（非在家模式、
    未坐下、未骑乘、存活）约 5 秒内自动传送到主人身边（末影人传送音效提示）。
    实现：手动跨维度移动（setRemoved(CHANGED_DIMENSION) → setPos 主人身边落点 →
    addFreshEntity 新维度）——不走 changeDimension，因为 TLM 的 override 只会
    "原地随机瞬移并返回 null"（TeleportHelper 实证），原版跟随任务又要求同维度。
    落点找主人身边第一个"站立格空气+脚下实心"位置（向下 16 格），主人高空飞行/虚空
    时本次不传、落地后再跟。开关：配置面板 杂项→跨维度跟随（默认开）。
  - 【蛋糕可吃】1.20.1 的蛋糕物品是普通 BlockItem（无 CakeItem 类），没注册
    foodProperties → 女仆进食判定（TLM 工作餐 isWorkMeal → Item.isEdible 查字段）
    永远不认蛋糕。修复（MaidCakeEdibleMixin，@Mixin Item）：对注册名
    minecraft:cake 的物品，m_41472_（isEdible）返回 true、m_41473_
    （getFoodProperties）返回营养 2/饱和度 0.6（≈蛋糕切片）——进食动作/音效/粒子
    自动走正常食物流程（使用动画由 isEdible 派生）。玩家右键蛋糕仍是放方块，不受影响。
  - 【建造强制坐下】进入建造任务立即强制坐下（TLM 原生坐姿），且玩家无法让她站起
    （每 tick 重新按压坐姿——GUI/交互站起在下个 tick 被按回去）。原理：坐下后 TLM
    跟随行为（检查 canBrainMoving = 非坐姿）不再启动 → 建造中不再被跟随传送
    小范围瞬移拉走；建造是隔空放置（行为从不移动），坐下不影响搭方块。切出建造任务
    自动站起。实现：MaidBuildBehavior.tickBuildSit 由 core 行为
    MaidToolAutoEquipBehavior 每 tick 调用（任何 activity 都跑，覆盖夜晚切班窗口）。
  - 【悬空放不上说明】"有个XXX悬空放不上，我跳过它啦～"不是 bug：蓝图里画了
    没有支撑的方块（悬空火把/按钮/藤蔓/甘蔗/雪层等附着方块，或悬空的沙/砾石等重力
    方块），建造时没有可附着的面/底座，连续失败 3 次后按设计永久跳过并提示一次。
    女仆会自动补垫合法支撑（蓝图没有该支撑步骤时：甘蔗→沙子、植物→泥土、其他→石头）；
    支撑格本身也是蓝图步骤（支撑后建）时先延后等支撑建好，支撑链本身悬空才最终跳过。
    完成汇报里"（跳过 N 个蓝图里悬空放不上的方块）"是同一机制。常见于创造模式画的
    悬浮装饰（吊灯/浮空平台/树冠藤蔓/浮空沙岛）。
  - 注册：mixins.promaid.json + 编译清单新增 MaidBaubleTotemMixin /
    MaidCakeEdibleMixin / follow/MaidDimensionFollow。

- **promaid 1.5.141：平A横扫修复 / 后撤加强 / 蛇形走位 / 攻击词条过滤**
  - 【平A横扫（根因修复）】反编译实证：TLM 的 EntityMaid.m_7327_ 攻击成功后本就会调
    doSweepHurt（横扫公式与玩家完全一致：伤害 = 1 + 横扫之刃倍率×攻击力），但触发
    条件要求倍率 > 0——**无"横扫之刃"附魔时从不横扫**（这就是"女仆跟原版差距大"
    的根因）。修复（MaidSweepMixin）：
    1. @Redirect EnchantmentHelper.m_44821_：无附魔时强制给 0.5（横扫之刃 I 的 50%
       攻击力倍率）——**每次平A都横扫**，有附魔按附魔倍率更高；
    2. @Inject doSweepHurt HEAD：空中（跳跃/下落中 = 跳劈）不横扫——**跳劈暴击
       保持单体伤害**（用户要求，与玩家"移动攻击不触发横扫"一致）。
    注：横扫仍需主手剑（ToolActions.SWORD_SWEEP）——斧/三叉戟不横扫，与玩家一致。
  - 【打一刀退一步】后撤加强：攻击完成瞬间后撤 10→14 tick、距离 3.0→3.5 格
    （更明显躲开敌人攻击范围），1.3 倍速撤离后重新逼近。
  - 【蛇形走位】近战追击从"固定横向偏置的弧形接近"改为**蛇形**（每 8 tick 左右
    摆动逼近）——面对远程敌人时配合时机举盾 = **举盾蛇形推进**（PVP 打骷髅标准）。
  - 【远程保持】弓/弩/三叉戟的"保持理想射程 + 横移绕圈放风筝"逻辑不变（已满足
    "会尝试绕圈和拉开距离"）。
  - 【攻击词条过滤】isMeleeWeapon 排除 镐/弓/弩（用户指定：攻击模式不能拿这三样）；
    三叉戟/斧/剑等保留（自动装备按词条匹配——攻击模式拿弓/弩/镐会被换成近战武器）。
  - 注册：mixins.promaid.json + 编译清单新增 MaidSweepMixin。

- **promaid 1.5.140：挖矿拾取/中文播报 / 删除建造传送 / 搭建速度档位硬限 / 删除伐木 / 搭方块边缘保护 / 换武器词条匹配**
  - 【挖矿空闲拾取】"挖矿中禁止拾取"改为按目标状态管理：有目标（正在挖/走向目标）
    才登记 MINING 标记让拾取让位；**附近无矿空闲时退出标记，拾取任务恢复正常**
    （与其他工作任务空闲时一致，不再"干站着不捡"）。
  - 【中文播报】方块名回退改用 BlueprintLib.cnName（CN_NAMES 精确表 + 颜色/材质
    规则兜底）——mod 方块"被挡住/挖不动"汇报不再显示英文 id。
  - 【删除建造传送】用户拍板整体删除：setPlan 的主人清场传送、suffocateCheck
    的被埋救援传送、全部辅助方法（resolveOwner/findSpotOutsidePlan/
    isInsidePlanBox/teleportSafe/regionTeleportReady 等 236 行）移除；
    建造女仆永不瞬移由 MaidTeleportPreserveMixin 继续兜底。其余建造逻辑不变。
  - 【搭建速度】档位硬限修复：旧逻辑 `Math.max(GLOBAL_QUOTA, ...)` 在 TPS 高时
    档位速率被能力上限顶掉 → ×1 也全速（"小建筑没过程"根因）。现在 ×1=1 块/秒、
    ×1.5=1.5、×3=3（块/秒，多女仆按 2^(n-1) 加速；GLOBAL_QUOTA 仅作物理天花板）；
    极速档不受限。
  - 【删除伐木任务】maid_smart:woodcut 全套移除（Task/Behavior/配置/面板伐木段/
    lang/工作播报分支/自动装备斧分支/编译清单）——任务列表恢复 挖矿/烹饪/酿造/建筑。
  - 【搭方块边缘保护】自保搭高进行中每 tick 钳制位置到脚下支撑方块中心
    ±(0.5-碰撞箱半宽)（照搬挖矿 v1.5.87 pillarGuard 的潜行防掉落机制，只钳位置
    不改速度）——战斗搭方块/垫高时不再因移速过快冲出边缘飞出去。
  - 【换武器词条匹配】推翻 v1.5.138"空手才换"：改为**主手物品符合当前任务词条
    不换；不符合才从背包挑最高分换**——攻击模式拿弓 → 换剑；弓模式拿钻石剑 →
    换弓。挖矿机制不变。

- **promaid 1.5.139：挖矿耐久根治——软方块判定从"硬度"改为"镐正确工具"**
  - 【背景】用户反馈"就算挖的是矿物也不掉耐久"——排查确认：扣耐久代码与字节码
    均正确（矿物 hardness=3 必进扣耐久分支，与 softNoDurability 配置无关）；
    真正的问题在 isSoft 判定：旧版用 `hardness ≤ 1` 判定"软方块"，而 mod 矿物
    （匠魂/魔法 mod 等的 1 硬度矿石）会被误判为软方块 → 叠加
    softNoDurability=true（旧配置）→ 挖 mod 矿物也不扣耐久。
  - 【修复】isSoft 改为"镐是否为正确工具"判定（DiggerItem.m_8096_）：镐是正确
    工具的方块（原版+mod 全部矿物/石头/深板岩，无论硬度）一律算硬、必扣耐久；
    只有草/土/沙/粘土等"镐不是正确工具"的方块才豁免。两处调用（扣耐久 +
    阻挡计数）同步更新。默认 softNoDurability=false（v1.5.138）保持不变。

- **promaid 1.5.138：挖矿耐久对齐伐木 / 自动换武器只换空手 / 伐木补强（砍树叶+垫高+中文名）/ 建造传送根治**
  - 【挖矿耐久】MINE_SOFT_NO_DURABILITY 默认 true → false：软方块（草/土/沙）开路
    也扣镐耐久，与伐木一致（每次挖块都扣）。已生成旧配置请在面板"软方块不耗耐久"关闭。
  - 【自动换武器】战斗（attack/ranged/crossbow/trident）与伐木：**空手才从背包挑评分
    最高装备；主手有任何物品一律不换**（玩家战术安排优先）。挖矿机制不变
    （只保证主手有镐 + 按目标矿升级镐）。
  - 【伐木补强】为对齐（并超过）maid_useful_task 的伐木：
    1. **砍树叶**：BFS 收集原木+树叶（上限 512），斧砍树叶即时回收树苗/苹果/木棍，
       不留半截秃树（它用剪刀方案，我们用斧更快）；
    2. **垫方块站高**：够不着高树（垂直差 > 3.5 格）时脚下垫方块逐层升高
       （背包取数量最多的安全实心方块；防窒息检查 + 8 tick 节奏），丛林巨树
       也能整棵砍完（对齐它的 BlockUp）；
    3. **任务中文名**：lang 补 `task.maid_smart.woodcut` = "伐木"（v1.5.133 漏加，
       任务栏显示英文的根因）；清理 organize 残留翻译。
  - 【建造传送根治】"下达建造后女仆被传送走"（主人没动）根因：v1.5.121 的 home 模式
    只在建造行为 doStart 后强制，切任务→行为启动前窗口期 FollowOwner（CORE 3）
    若在跑且距离主人 > 7 格 → teleportToOwner 把女仆瞬移回主人身边。修复：
    MaidTeleportPreserveMixin 增加"建造女仆（任务=maid_smart:build）任何时刻禁止
    teleportToOwner"——从 TLM 源头一网打尽，站桩定位由建造行为自己负责。
    setPlan 的主人清场传送逻辑本身正确（只传站在施工区内的主人），予以保留。

- **promaid 1.5.137：修复两个 bug（虚空误判疯狂传送 / 手册按钮重叠）**
  - 【Bug 1】深板岩层地下基地被判定为"虚空"疯狂传送到主人身边——v1.5.135 的
    虚空保护用 `y < -40` 判定，主世界 y<-40 的深板岩层（地下基地/矿洞"平地"）
    被误判。按用户要求【删除虚空传送逻辑】——TLM 原生已有虚空传送保护，
    不画蛇添足：envDangerCritical 去掉虚空条件、删 voidEscape 方法。
  - 【Bug 2】手册（配置面板）目录页"战斗自保"按钮与"保存并返回"重叠——
    v1.5.133 加 WOODCUT 后左列 6 按钮，末位 COMBAT y=211 压住底部保存按钮
    （h-34=206，240 默认高度）。修复：重排为 5+5 两列（COMBAT 移到右列顶部）
    + 按钮压缩（26→22、间距 5→4），末位按钮底 182，与保存按钮间距 ≥24 永不相交。
  - 【顺手修正】环境判定映射（全部反编译实证）：
    1. 岩浆判定 v1.5.135 误用 m_20070_（实为 isInWaterOrLava——站水里也会进
       "岩浆逃生"）→ 改 m_20077_（实为 isInLava，查 LAVA 流体标签）；
    2. 溺水判定误用 m_20077_（实为 isInLava）→ 改 m_20069_（实为 isInWater）
       + 空气值 < 60；
    3. m_6060_ = isOnFire 确认无误（f_19831_ 火焰计时）；
    4. 战术行为坠落保护误用 m_20162_（实为 isSneaking，getFlag(1)）→ 改用
       fallDistance > 2.5 直接判定。

- **promaid 1.5.136：战斗指挥工具（smart_attack）——AI 对话直接下令开打**
  - 【功能】对女仆说"去打那只僵尸 / 攻击最近的怪 / 帮我打它 / 谁打你你就打谁"，
    LLM 调用 smart_attack 工具执行，战斗链路闭环（指挥 → 锁定 → 战术走位 → 自保兜底）。
  - 【三种目标模式】（target 参数，LLM 按语境选择）：
    1. nearest：最近的可攻击敌对生物（12 格 Monster 扫描 + TLM canAttack
       m_6779_ 完整过滤防误伤）；
    2. owner_target：主人最近攻击的目标（LivingEntity.m_21214_ = getLastHurtMob，
       TLM DefaultMonsterType 同款用法实证——Player 非 Mob 没有 getTarget）；
    3. attacker：最近攻击过女仆的生物（5 秒窗口，LivingHurtEvent 记录，
       覆盖其他 mod 的非 Monster 类敌对生物）。
  - 【执行】自动切到 TLM 原生 attack 任务（TaskManager.findTask，如不在战斗任务）
    → ATTACK_TARGET 记忆 + Mob setTarget 双重锁定（TLM StartAttacking 只选空目标
    不会覆盖）→ 播报"明白！我去解决它！"。
  - 锁定后单兵战术行为（core 230）自动接管走位/举盾，自保（250）兜底保命；
    没武器时 MaidToolAutoEquip（200）自动装战斗武器。
  - SelfPreservationBehavior 新增 public recentAttacker(maid) 供工具查询。

- **promaid 1.5.135：自保盲区修复（自然伤害/无来源伤害/非 Monster 生物）+ 骷髅对策**
  - 【背景】原版自保的威胁检测只认"当前攻击目标 + 原版 Monster 类"——其他 mod 的
    敌对生物若不继承 Monster 完全漏判；火/岩浆/溺水/窒息/虚空等自然伤害不触发
    自保（女仆站岩浆里会烧死）；虚空掉落 = 永久丢失。
  - 【威胁三源】findThreat 新增"最近攻击者"：LivingHurtEvent 记录 5 秒内攻击者
    （任意生物类，含 mod 生物/中立反咬/PVP；主人误伤豁免），窗口内存活且可见
    即视为威胁——配合原有"当前目标（任意生物）+ Monster 扫描"。
  - 【环境逃生】（combat.selfPreserve 总开关内，无生物威胁时触发）：
    1. **岩浆/虚空**：无血量门槛立即逃生——岩浆每秒 4 血；虚空（y<-40）无条件
       传送回主人/世界出生点（必死 vs 可能安全，传送优于永久丢失）；
    2. **着火**：灭火器（TLM 灭火器生成灭火 agent 扑灭自己）> 最近水源（半径 12）
       > 开阔地突围；85% 血量以下才触发；
    3. **溺水**：氧气不足 1/5 时朝上方空气格游（导航上浮为主 + 上浮速度辅助，
       应对 moveControl 覆盖 velocity 的时序），再找岸边；
    4. **卡墙窒息**：最近双格空气（半径 4），找不到靠 antiSuffocate 强制上移；
    - 防抖 6 tick（0.3 秒，防路过火源/被推挤误触发）；危险消除后安全 20 tick
      才退出（防着火尾声闪烁反复进出）。
  - 【骷髅对策】时机举盾增强（v1.5.134 的贴身才举盾导致骷髅射程内不挡）：
    - **远程敌人**（RangedAttackMob 或手持弓/弩/三叉戟/雪球/鸡蛋/末影珍珠/投掷药水）
      → 15 格内有视线就举盾：盾挡箭矢推进（举盾中战术导航照常驱动移动），
      贴身再放盾砍——PVP 打骷髅的标准操作；
    - **近战敌人**：保持攻防交替（冷却中+贴身举盾）；
    - **弓/弩任务**（主手投射武器）：放风筝优先，不举盾。
  - 实现：SelfPreservationBehavior 新增 8 个方法（环境判定/逃生/扫描），
    hasSight 公开给战斗战术共用；ProMaidExtension 注册自保静态事件订阅。

- **promaid 1.5.134：单兵作战战术（PVP 式战斗）——删除战斗协同与整理任务**
  - 【删除 1】战斗协同（v1.5.132 MaidCombatCoordination + 协同/协同距离配置 +
    面板"战斗协同"段）——经查其 isCombatTask 用了 m_135827_（=getNamespace）
    拿的是命名空间而非任务 path，共享目标表从未真正生效；且"多女仆集火"
    本身不如单兵操作感实在，整体移除。
  - 【删除 2】整理任务（maid_smart:organize + MaidOrganizeTask/Behavior +
    整理相关 6 项配置 + 面板"整理"段 + 工作播报分支）——"倒背包进箱子"
    场景鸡肋，任务列表中彻底移除（历史存档中已切到整理任务的女仆
    切任务即可，不会报错）。
  - 【新增】单兵作战战术（combat.tactics，core 行为优先级 230，默认开）：
    1. **近战战术**（combat.tacticsMelee）：贴脸绕圈侧移（默认半径 2.2 格，
       12~20 tick 随机换向，不吃正面刀）；**打一刀退一步**（攻击完成瞬间
       后撤 10 tick 再逼近的 hit&run 拉扯）；追击弧形接近（径向+横向偏置
       不直线贴脸）；跳劈接近（冷却满+中距离时模拟跳跃，下落命中=原版暴击）；
    2. **远程战术**（combat.tacticsRanged）：距离管理——保持在武器最大射程
       60%（可调 tacticsKiteRange）放风筝（原版 MaidRangedWalkToTarget 会
       走到贴脸 0 格才射，弓贴脸=废物）；理想距离内横移绕圈大半径侧移；
    3. **时机举盾**（combat.tacticsShield）：mixin 拦截 TLM 原版 MaidUseShieldTask
       （原版"8 格内有目标就一直举盾"=站桩挨打），改为攻击冷却中+目标贴身
       才举盾、冷却满放盾攻击的攻防交替节奏。
  - 【实现】移动接管与自保同款：每 tick 清 WALK_TARGET + 直连导航
    （navigation.moveTo），MaidMoveSuppressMixin 新增战术分支（isActive 时
    取消 MoveToTargetSink，TLM 战斗走位行为写的 WALK_TARGET 不执行）；
    1.20.1 Brain 反编译实证"启动循环无高优先级阻断 break + tick 低优先级先跑"，
    常驻 core 行为不影响 WORK 组战斗行为、且天然最后覆盖导航。
  - 【修复】MaidWorkTags.isNonCombatWork 用错方法（m_135827_=getNamespace
    恒不匹配 → 战斗女仆也被"干活不被打断"闸门影响）——javap 实证
    m_135815_=getPath 后修正，战斗女仆恢复吃饭/恐慌豁免。
  - 配置面板"战斗自保"段新增"单兵战术"小节：总开关/近战/远程/时机举盾/
    绕圈半径/远程理想射程倍率，全部可回滚。


- **promaid 1.5.133：新增伐木任务（maid_smart:woodcut）——TLM 缺失的高需求任务**
  - 【背景】TLM 1.5.3 没有伐木任务（社区高需求，只有第三方 maid_useful_task 有）。
    挖矿的整套成熟经验直接复用。
  - 【功能】
    1. **自动找树**：节流扫描（1.5 秒一次）附近最近原木（注册名判定
       `_log/_wood/_stem/_hyphae`，覆盖原版全部原木含 mod，不依赖标签字段）；
    2. **整树连砍**：找到目标后 BFS 收集整棵树（26 邻接原木，上限 256 块防
       丛林巨树失控），从根往上逐块砍，树叶自然掉落（蓝图树叶不衰变 mixin
       只影响施工区，不误伤）；
    3. **掉落回收**：砍下的原木/树苗/苹果/木棍即时收进背包（同挖矿
       pickupOreDrops）；
    4. **树苗补种**：整棵树砍完在树根自动种回树苗（16 种原木→树苗映射，
       配置 woodcut.plantSapling 默认开，可持续伐木不毁林）；
    5. **直接导航**：移动同挖矿 v1.5.127（navigation.moveTo + 中间点接力），
       走路无停顿；
    6. **自动换斧**：MaidToolAutoEquip 新增 woodcut 分支——空手/非斧自动从
       背包装斧（评分 = 材质等级：下界合金>钻石>铁>石>木/金）；
    7. **玩家同款砍伐公式**：斧正确工具判定 + 效率附魔 + 速度系数（默认 1.2）
       + 单 tick 封顶 0.25，有渐进破坏观感；砍伐扣斧耐久。
  - 配置：新增"伐木"板块（配置面板首页新增"伐木"入口）——找树搜索半径
    （20）/ 向上找树范围（16）/ 砍树速度系数（1.2）/ 砍完补种（开）。
  - 任务图标铁斧；enableLookAndRandomWalk=false（沿用 v1.5.123 惯例）。

- **promaid 1.5.132：战斗任务增强——协同作战（共享目标 + 护主集火）**
  - 【根因】TLM 所有战斗任务（attack/ranged_attack/crossbow_attack/trident_attack/
    danmaku_attack）的目标选择都走 StartAttacking + `IAttackTask.findFirstValidAttackTarget`，
    而后者的目标来源只有 NEAREST_VISIBLE_LIVING_ENTITIES（**视野传感记忆**）→
    女仆各打各的：A 女仆发现了怪，B 女仆没发现（视野外/传感外）永远不帮忙
    （"原生有走位但无协同"）。
  - 【修复】新增 `combat/MaidCombatCoordination`（core 行为优先级 230，战斗任务
    专属），共享目标表（以主人 UUID 为键）+ 三路写入：
    1. **广播**：任一战斗女仆锁定目标（ATTACK_TARGET 记忆非空）→ 写入共享表
       （10 秒刷新）；
    2. **接收**：同主人、同维度、自己当前无目标的战斗女仆 → 表内目标存活且
       距离 ≤ 协同距离（默认 12 格）→ 写入自己的 ATTACK_TARGET——攻击/追击/
       射击行为（MaidMeleeAttack / SetWalkTargetFromAttackTarget /
       MaidShootTargetTask）自动接管，**无需改任何 TLM 战斗行为**；
    3. **护主**：主人被攻击（LivingHurtEvent）→ 共享目标 = 攻击者（30 秒），
       附近全部战斗女仆立刻集火。
    - 防御：接收时用 `maid.m_6779_(target)`（= IAttackTask.canAttack 完整过滤：
      排除玩家/装甲架/有主宠物/忽略列表/怪物类型）——与 TLM 原生选目标同一套
      规则，**不会误伤友方宠物/其他女仆/玩家**；自己已有目标时不覆盖（近的
      优先）；目标死亡/出范围自动清表；按维度隔离。
  - 配置：combat.coordinate（默认开）+ combat.coordinateRange（默认 12，
    8~32）；配置面板"战斗自保"段新增"战斗协同（v1.5.132）"小节。
  - 【离线验证（睡觉期间补做，防明天启动崩服/功能不触发）】
    1. 10 个新 mixin 的全部 @Inject/@Redirect/@ModifyConstant 目标方法与
       描述符，逐一对部署的 SMART jar 字节码核对——**12 个方法 + 4 个描述符
       全部精确匹配**（mixin 配置 required:true，任一不匹配会启动即崩，已排除）；
    2. 钓鱼自动椅子链路走查：EntityChair → AbstractEntityFromItem → **LivingEntity**
       （确认），MaidNearestLivingEntitySensor 收集"所有活着的 LivingEntity"
       → 生成的椅子下一 tick 进 NEAREST_VISIBLE_LIVING_ENTITIES → filterEntity
       命中 → 走过去坐下——链路通；
    3. 农场连收时序（HEAD 记目标位 / TAIL 清扫 3x3）、战斗协同边界（目标死亡
       清表/出范围清表/跨维度隔离/m_6779_ 防误伤/已有目标不覆盖）、原生任务
       时长反射（每女仆 brain 初始化时对新建实例写入，无副作用）——全部走查通过。

- **promaid 1.5.131：AI 聊天/记忆大脑链路全面核查 + 记忆可诊断性改进**
  - 【链路核查结论（TLM 1.5.3 字节码逐点验证）】
    - 扩展发现：@LittleMaidExtension → TLM `AnnotatedInstanceUtil.getModExtensions()`
      （ModList 扫描 + 无参构造）→ CommonSetup 阶段调用全部 5 个注册钩子
      （addMaidTask/addExtraMaidBrain/registerTaskData/registerAITool/
      registerAIMaidContext）——任务能注册即证明扩展实例化成功、所有钩子都执行了。
    - 聊天：MaidAIChatManager.chat 入口链（主人检查→LLM_ENABLED→token 额度→
      site 配置→人设）——日志实证工作（"天黑了，我有点困"）。
    - 记忆写入：喂食（EntityInteract）/好感升级（FavorabilityManager 确认 fire
      MaidFavorabilityLevelChangeEvent）/玩家死亡——事件链路通。
    - 记忆提取：每 20 秒调度 maybeExtract → 对话历史攒满 threshold 条 → 自建
      HTTP（复用 TLM 站点配置，不烧玩家配额）——链路通。
    - 记忆注入：AiMemoryContext（IMaidContext）→ normalChat 每请求渲染进
      [context] 块——链路通。
    - 记忆工具：query_memory / remember / working_note 注册进 ToolRegister ✓。
    - Prompt 注入：PromaidPromptMixin @ModifyVariable 按类型匹配 systemChat 的
      String 参数——mixin 应用成功（游戏正常运行即证明）。
    - 自主决策：TaskManager.getIdleTask() 确认存在，引用比较语义正确。
    - **当前"流程不触发"的最大嫌疑是外部 LLM API 配置**（日志：DeepSeek 401
      key 失效 + opencode.ai 超时）——聊天/主动对话/记忆提取全部依赖站点可用。
  - 【改进 1：记忆提取失败不再静默】AiMemoryExtractor 失败路径加日志
    （err 类型 + HTTP 状态码）——服务器日志可见"提取失败（401/超时）"的真实原因。
  - 【改进 2：首次提取门槛降低】memory.extractThreshold 默认 12 → 8 条
    （日常短聊也能攒满触发提取；范围仍 4~64 可调）。

- **promaid 1.5.130：产出型任务专项增强（农场连收连种 + 钓鱼主动找水域）**
  - 【农场：一次到田里处理一整片】`FarmSweepMixin`（@Mixin MaidFarmPlantTask）——
    原版一次只处理 TARGET_POS 的 1 个目标格（收割→清目标→再找→走过去→再收），
    大块农田来回跑。修复：start 收割完目标格后（TAIL 注入），把目标格水平 3x3
    内已成熟的作物一并收割并顺手补种（与原逻辑同机制：canHarvest 检查 +
    真实消耗种子 + 挥锄动画；HEAD 注入先记下目标格位置——原逻辑末尾会清
    TARGET_POS）。来回跑次数降到约 1/8。
  - 【钓鱼：没椅子/船不再空转】`FishingAutoChairMixin`（@Mixin MaidFindSitTask）——
    原版找不到椅子/船只弹 no_sit 气泡原地空转（"钓鱼空转"缺陷）。修复：原逻辑
    没找到时，扫描女仆 16 格内开阔水域（3x3 水面 + 水上方空气 + 岸边可站格），
    在岸边生成一个 TLM 坐垫椅子实体（EntityChair，与原版坐垫同款），女仆走过去
    坐下开钓（下一轮 start 自动找到新椅子坐下）；5 秒冷却防循环；找不到水域才
    保留原气泡。气泡提示"这里没有椅子，我自己带了个坐垫，找个水边坐下钓鱼～"。
  - 总开关：`misc.produceTaskEnhance`（默认开），配置面板"杂项"新增"产出任务增强"。

- **promaid 1.5.129：TLM 原生任务通用呆滞修复 + 干活不被打断（两个总开关可回滚）**
  - 调查结论：TLM 1.5.3 原生任务（farm/挤奶/钓鱼/剪毛/蜂蜜/甘蔗等）全部受三个
    机制拖累——① 行为走 Behavior(Map) 单参构造默认 **60 tick** 单轮重启（每 3 秒
    doStop 重启并重挂限频计时）；② WORK 活动 MaidRunOne@20 的 **RandomStroll 随机
    散步会覆盖任务设的 WALK_TARGET**（与当初挖矿"一步一停"同根因）；③
    MaidCheckRateTask 限频是 1~2 倍随机间隔（挤奶 40→40~80 tick 才查下一次）。
    另外 MoveToTargetSink 走路满 150~250 tick 会"刹车"清目标。
  - 【修复 A：原生任务流畅化（misc.nativeTaskSmooth，默认开）】
    1. `NativeTaskSmoothMixin`（@Mixin MaidBrain）：在任务行为注册进 WORK/
       RIDE_WORK 的瞬间（ImmutableList.copyOf @Redirect）把除 TLM 基建行为
       （切班/讨食/吃饭/偷吃/随机散步）外的任务行为时长改为无限
       （f_22525_/f_22526_ final 字段反射写入，每女仆初始化一次）。
    2. `MaidRunOneYieldMixin`（@Mixin MaidRunOne）：任务正走向目标（大脑已有
       WALK_TARGET 或 TARGET_POS）时 tryStart 让位——随机散步不再覆盖任务目标
       （"走一步停一步"根治；任务空闲/冷却间隙仍允许散步）。
    3. `MoveToTargetSinkDurationMixin`：sink 时长 150/250 → 600/1200
       （30~60 秒才"刹车"一次，走路基本连续；我们的 5 个任务已用直接导航不受影响）。
    4. `MaidCheckRateHalveMixin`（@Mixin MaidCheckRateTask）：限频整体减半
       （0.5~1 倍间隔），挤奶等"完成一个单位→发呆 2~4 秒"缩短到 1~2 秒。
  - 【修复 B：干活不被打断（misc.workUninterrupted，默认开）】
    适用范围 = 非战斗任务（任务 UID ∉ {attack, ranged_attack, crossbow_attack,
    trident_attack, danmaku_attack}）——Promaid 5 任务 + 全部原生产出任务命中，
    战斗女仆不受影响（MaidWorkTags.isNonCombatWork）。
    1. `MealTaskGatingMixin`（@Mixin MaidWorkMealTask）：干活中不吃饭——工作餐
       本质是刷好感（进食动画 2~3 秒打断干活）；TLM 1.5.3 饥饿值经全 jar 扫描
       实证为惰性数据（仅 EntityMaid NBT 读写，无消耗、无饿死机制），不吃饭
       无害；受伤自疗餐是独立任务不受影响。
    2. `StealEdibleGatingMixin`（@Mixin StealEdibleMoveBlockTask + UseTask）：
       干活中禁止偷吃（拆/吃甜浆果丛等）。
    3. `PanicGatingMixin`（@Mixin MaidPanicTask）：干活中、血量 ≥ 30% 时小伤
       不恐慌逃跑（< 30% 仍正常恐慌，且自保行为会接管，不会致死）。
    4. `SchedulePullbackMixin`（@Mixin MaidUpdateActivityFromSchedule）：干活中
       切班时跳过 restrictTo + 拉回工位（不再被硬拽回家；活动切换照常）。
  - 配置面板"杂项"新增 2 个开关（原生任务流畅化 / 干活不被打断），出问题可
    即时关闭回滚。
  - 经验教训：新增源码必须同步追加到 compile_promaid.txt 编译清单（本次 8 个
    新 mixin 漏加导致首版 jar 缺类；顺带解释了历史上 AiMemoryExtractor$Extraction
    缺类崩溃的同类根因）。已加 _diff_files.py 校验脚本防止再漏。

- **promaid 1.5.128：输入框 StackOverflowError 根治（真正的 setTextColor 是 m_94202_）+ 崩溃日志确认旧 jar 缺内部类**
  - 【输入框"卡一下、永远打不进字"的终极根因——方法名映射错误】反编译 EditBox
    字节码实证：`m_94192_(int)` 是 **setCursorPosition**（方法体 = 设光标 +
    `m_94174_` 触发 responder），**不是** setTextColor！v1.5.124 起的 responder
    里 `box.m_94192_(颜色)` 实际把颜色值当光标位置设置，且每次调用都再次触发
    responder → `setCursorPosition → responder → setCursorPosition` **无限递归
    StackOverflowError**（崩溃栈实证 `EditBox.m_94192_ ↔ PromaidConfigScreen
    lambda$sectionButtons$1` 循环 55+ 帧）。StackOverflowError 被
    KeyboardHandler 的事件包装捕获（`Screen.m_96579_`）→ 每次按键：卡顿 +
    文本永远写不进 → 这同时解释了"输入了会卡一下"和"无法填入值"两个现象
    （v1.5.123/126 的键盘转发其实早已生效，字符确实到达了输入框，只是在
    responder 递归里炸掉）。
    【修复】改用真正的 **`m_94202_(int)` = setTextColor**（字节码实证：纯字段
    `f_94103_` 写入，不触发 responder）→ 递归消失，输入即刻生效。两处
    responder（NumRow + TextRow）都已替换；全库扫描确认无其他误用。
  - 【`AiMemoryExtractor$Extraction` NoClassDefFoundError】19:55 崩溃日志实证
    旧 jar 缺内部类（`parse()` 引用 `$Extraction` 找不到）。当前构建（v1.5.127/128
    jar）已验证 `AiMemoryExtractor$Extraction.class` 在包内、且外类引用完好。
    注意：19:53 部署新 jar 时游戏仍在运行——模组在启动时已从旧 jar 加载，
    崩溃来自旧 jar；**重启游戏后新 jar 生效**。

- **promaid 1.5.127：挖矿"愣 2~3 秒"根治（直接导航 + 扫描重建提速）+ 整理任务大升级**
  - 【挖矿停顿根因分析——两个独立来源】
    1. **扫描缓存耗尽后干等 5 秒**：`findOre` 的全量扫描每 5 秒（ORE_CACHE_TTL）才
       重建一次，期间只做"廉价缓存轮"（校验已记录的矿）。挖完缓存里最后一块矿后
       `pickFromCache` 返回 null，但重建要等 TTL 到期 → 女仆原地站着发呆 2~5 秒
       （矿越稀疏越频繁）——"愣 2~3 秒"的主要来源。
       【修复】缓存轮空 → **立即全量重建**（不等 TTL），顺带把"框内无矿 → 锚点
       迁移"从等 5 秒提速到即时。
    2. **MoveToTargetSink 的周期性"刹车"**：TLM 用原版 `MoveToTargetSink`（CORE@2）
       搬运 WALK_TARGET。反编译确认它构造为 minDuration 150 / maxDuration 250
       tick——跑满 7.5~12.5 秒就强制 doStop：**清掉 WALK_TARGET + 停导航**，
       导航卡死时还会 random(40)（0~2 秒）冷却。长距离走路每 7.5~12.5 秒被
       "刹车"一次重新寻路（第二来源）。
       【修复】挖矿移动改**直接导航**（借鉴 TLM 农务/伐木）：`setWalkTarget` 不再
       写 WALK_TARGET 记忆，改调 `navigation.moveTo`（超过寻路范围 14 格时先朝
       目标走 12 格中间点接力）→ sink 永不启动 → 走路全程无中断；搭高站桩由
       "清 WALK_TARGET"改为"停导航"；扫描节流 10→5 tick。块间衔接：挖完一块
       → 5 tick 内重新评估 → 立即走下一块，几乎无缝。
  - 【整理任务大升级（原版"倒背包进箱子"鸡肋）】
    1. **整理前先合并背包同类堆叠**（腾格子，配置 misc.organizeSelfMerge）
    2. **分类入库**：优先放进"已经装有同类物品"的箱子——多个箱子会自然分工成
       矿物箱/建材箱/食物箱（配置 misc.organizeSmartChest）
    3. **垃圾销毁**：黑名单物品（默认腐肉）直接销毁，不丢地上（丢地上会被拾取
       任务捡回死循环；配置 misc.organizeJunkItems）
    4. **保留物品**：配置列表（默认火把/面包/牛排/熟猪排/水桶）+ 手部物品 +
       工具/武器/装备永不搬动（配置 misc.organizeKeepItems）
    5. 箱子内部同类合并照旧；反馈气泡报告"合并 X 组、存入 Y 组、销毁 Z 件"。
    6. 走路同步改直接导航（不写 WALK_TARGET）。
    - 配置面板新增：整理小节（2 开关 + 2 文本行）；新增 TextRow 行类型
      （字符串输入，与数字框同款延迟提交交互）。

- **promaid 1.5.126：主人防窒息保护 + 材料提示中文名 + 输入框根治（照搬原版搜索框）**
  - 【主人保护①：确认建造地点后把主人传出施工区】`BuildPlan.setPlan` 在确认建造
    地点（建造原点 = 主人所在方块）后，若主人站在施工区包围盒内，用
    `findSpotOutsidePlan`（8 方向 × 3~16 格 × 宽 Y 范围找可呼吸站立点）把主人
    传送到施工区外的安全位置并提示"建造区域已确认……我把你传送到了施工区外的
    安全位置～"，再开始工作——避免施工方块从头顶落下把主人埋住窒息。
    v1.5.117 曾整体停用清场传送（"随机传送"反复出现）：本次只传主人不传女仆
    （旧版"主人 + 执行女仆一起传"是反复拽的根源——女仆被传走 → 站桩逻辑走回
    施工区 → 再被传）；续建/重下达同一计划时主人已在区外，`isInsidePlanBox`
    判 false 不会反复拽。
  - 【主人保护②：恢复防窒息救援（只救玩家）】`suffocateCheck` 从 if(false) 恢复
    启用，但**只扫玩家**：被实心方块埋住（连续 2 次检查消抖 + 每实体 10 秒冷却）
    的玩家传送出施工区并提示"施工区内请勿停留"。旧版的女仆救援循环整体移除
    （非建仆女仆被埋由 TLM 自带传送/防窒息处理，promaid 不再动女仆）——女仆
    救援就是"随机传送"投诉的另一来源。
  - 【材料提示中文名】新增 `BlueprintLib.cnName(blockId)`：精确表（蓝图用到的
    全部 80 种方块 + 矿物/矿石/食物/工具约 200 条）+ 规则兜底（颜色前缀
    white→白、木头名 oak→橡木、词干+后缀 planks→木板/stairs→楼梯/ore→矿石
    /pickaxe→镐 等，最长后缀优先）。接入全部玩家可见输出：两个 formatShortfall、
    needBubbleText 气泡、queryStatus 进度报告、describe 材料 top5、
    findObstacles 障碍物名、建仆跳过气泡（5 处）、挖矿"镐子挖不动"气泡、
    手册物品名兜底。以后缺料提示是"橡木木板 x64"，不再是一串英文 id。
  - 【输入框根治：1.20.1 输入链根本不调用 m_96583_】字节码实证：
    `KeyboardHandler.charTyped → screen.m_5534_(char,int)`（GuiEventListener
    接口方法，ContainerEventHandler 默认实现 = 转发 getFocused()）；
    `Screen.m_96583_(String,char,int)` 在 1.20.1 是**无人调用的死代码**（方法体
    是字符合法性过滤器）。v1.5.123 的"强制键盘转发"重写在了这个死方法上 →
    "点进输入框但打不了字"。IMBlocker 的 `AbstractContainerEventHandlerMixin`
    @Overwrite setFocused 经反编译确认与原版等价（良性）。本次照搬原版创造搜索框
    交互（CreativeModeInventoryScreen 重写 m_5534_/m_7933_ 直接转发给自己持有的
    searchBox 字段，不依赖 getFocused()）：新增 `activeBox` 字段（点击输入框时
    记录，m_7856_ 分页重建时清空），重写真正被调用的 `m_5534_(char,int)` +
    `m_7933_` 直接转发给 activeBox（super 兜底 getFocused() 链），m_96583_ 保留
    为兼容兜底。配合 v1.5.124 的延迟提交（输入只校验不写配置），输入应完全
    流畅可用（全局放置配额/强制加载区块上限等数字框）。

- **promaid 1.5.125：创造模式"缺 21 亿材料"整数溢出根治（不建造链路之一）**
  - 【根因】创造模式 `countPlayerMaterial` 返回 `Integer.MAX_VALUE`（=无限，设计如此）；
    但 `combinedShortfall`/partial 截断/`estimateMissing` 里把它**加上女仆背包持有数**
    → `MAX_VALUE + N` 溢出成**负数** → `have < need` 误判缺料 → 缺口 =
    `need - (负数)` ≈ **2147483647（21 亿）** → 手册点击走 partial 截断时第一步
    `have(负) > used(0)` 不成立 → buildable 空 → **"背包里没有任何建造材料，无法开始。
    缺少：xxx x2147483647" → 创造模式下达蓝图不建造**。女仆背包恰好为空时不溢出
    （18:45 日志那次"好！我来建瞭望塔啦"成功的原因），背包有料（挖矿攒的圆石等）
    就触发——与暂停残留是两个独立的"不建造"链路。
  - 【修复】新增 `BlueprintLib.combinedHave(owner, maid, blockId)`：任一侧 ≥ MAX/2
    视为无限直接返回 MAX_VALUE，否则才相加；替换 3 处溢出点
    （BlueprintBuildExecutor 的 combinedShortfall + partial 截断、
    MaidBuildBehavior.estimateMissing）。放置时 consumeBlock 的创造豁免本就正确
    （主人创造 → 直接返回物品不扣背包），修复后创造模式全链路：预检充足 →
    不转交材料 → 放置不扣料，正常建造。
- **promaid 1.5.124：建造根治（暂停改内存态）+ 行为不再 3 秒重启 + 挖矿节流减半 + 输入延迟提交**
  - 【建造·真根治】per-maid 暂停从 persistentData（随实体永久存档）改为【会话内存态】
    （静态 Map<UUID,Boolean>）——1.5.123 的 setPlan 清除在日志里仍被旧值盖回
    （18:45 新存档实测：下达后 build tick 仍 `maidPaused=true`，女仆数据带着旧世界的
    残留标记），两次都栽在这个持久标记上。内存态：新女仆/重启恒为 false，手册女仆
    面板暂停/恢复照常工作，无残留。
  - 【行为 60 tick 重启·根因】Behavior(Map) 默认 maxDuration=60 tick——5 个工作行为
    （build/mine/cook/brew/organize）每 3 秒自动停止再启动（日志实证 "build behavior
    start" 每 3 秒一条）。全部改为 `super(emptyMap, Integer.MAX_VALUE, MAX_VALUE)`
    无限运行（这些是任务期间常驻行为，本就不该自动重启）。顺带消除日志刷屏与
    实例状态反复重置。
  - 【挖矿"愣几秒"】①目标重设节流 30 → 10 tick（撞上 MoveToTargetSink 失败冷却
    时原地愣 2~3 秒的主因；setWalkTarget 内部对"目标未变且导航行进中"本就跳过，
    节流只是兜底）；②找矿扫描节流 20 → 10 tick（挖完一块更快接下一块）。
  - 【输入延迟提交（照 MC 内部搜索框交互）】NumRow 输入时【只做格式校验
    （非法红字）不写配置】——旧版每按键调 ForgeConfigSpec.set()（已验证 set 本身
    只是内存写入，但输入路径上不再有任何配置写入/异常可能，输入过程零副作用）；
    保存并返回/完成时统一写入 + SPEC.save()。输入文本按"板块:行标签"暂存，
    翻页重建不丢（挖矿页/战斗页同名行带板块前缀防串值）。
  - 顺带：上次 m_6375_ 命中日志（宽度整除 200 才打）已随重构移除。
- **promaid 1.5.123：三 bug 根治（挖矿"一步一停"根因 / 配置面板输入 / 下达后不建造根因）**
  - 【挖矿"走一步停一下"·真根因（v1.5.116-122 多次"修复"无效的原因）】TLM 的 WORK
    activity 优先级 20 有 `getLookAndRandomWalk`（MaidRunOne 内含 `RandomStroll.create(0.3f, 5, 3)`）——
    启用条件 = `IMaidTask.enableLookAndRandomWalk`（**默认 true**，MaidRunOne.tryStart 首判）。
    挖矿任务没重写它 → 随机散步行为每 60~120 tick 用【5 格内随机目标、速度 0.3】覆盖我们
    设向矿的 WALK_TARGET → 女仆被反复拉去随机散步："不管离矿近还是远都一步一停、效率极低"。
    之前的修复全在 setWalkTarget 重设逻辑上打转，没碰这个并发写 WALK_TARGET 的行为。
    **修复：5 个任务（mine/build/cook/brew/organize）全部重写 `enableLookAndRandomWalk` → false**
    ——MaidRunOne 整体不启动，散步/看向/DoNothing 全部消失。TLM 伐木不受影响（它走
    直接导航 navigation.moveTo，不经过 WALK_TARGET）。
  - 【配置面板输入框】①矿表搜索框每敲一个字就遍历全部注册物品 + 本地化查名
    （上万次 getString）＝"输入了会卡一下"；改为缓存候选表（id+中文名，懒构建 1 分钟
    有效），按键只做内存过滤。②新增 charTyped（m_96583_）/keyPressed（m_7933_）重写：
    先把输入强制发给聚焦的 EditBox 再走 super——装了 IMBlocker（输入法冲突修复）等
    外部输入处理 mod 后事件链可能被改写，"点进输入框但打不了字"有兜底。
  - 【下达后不建造·根因（日志实证）】女仆单独暂停标记 `maid_smart_paused` 存在
    persistentData 随实体永久存档，一旦被手册"暂停"过就永远 true；`setPlan` 只重置了
    全局暂停（arch.paused）没重置它 → 下达新蓝图：女仆回应"好！我来建XX啦！"→ 行为
    启动 → doTick 在 `isMaidPaused` 检查处直接 return → 永不建造。运行日志实证：
    `build tick: plan=36 origin=253,-60,34 paused=false maidPaused=true cooldown=1`。
    **修复：setPlan 时同步 `setMaidPaused(maid, false)`**（新计划 = 新的明确意图，
    与全局暂停语义一致）；手册"恢复"按钮照常可手动解除。
  - 顺带核实：今日 6 次崩溃均为旧 jar 残留（08:02 缺 BuildPlan$Progress 内部类 /
    08:19 ByteTag→CompoundTag / 09:31-10:16 PromaidPromptMixin @ModifyArg / 12:54
    heartfelt 缺 ApiQuotaBridge）——当前源码与 17:34 构建均已修复，本次重新编译打包
    并验证 jar 完整性（131 类全含）。
- **promaid 1.5.110：配置面板每项注释 + 全部文本"偏左超出屏幕"修复**
  - 【每项注释】配置面板（模组详细设置）每个项目行下方新增灰色注释——内容自动
    取自 MaidSmartConfig 里已有的 .comment()（脚本注入 128 条，始终与配置同步）；
    行高 32→44 防注释与下行重叠。
  - 【注释防裁切】新增 drawComment：自动换行（面板宽度内）+ 右缘钳制；注释起点
    对齐控件区（比旧 x=20 更靠右，符合"向右移动"要求）。
  - 【矿表注释居中】目标矿物/障碍物/珍稀矿物底部注释（chkHint）旧版以 left
    （≈cx-270）为圆心居中，窄屏左半裁出屏幕；改为屏幕中心居中 + clampCenterX
    钳制，悬停物品名/页码同样钳制。
  - 【记忆文本偏左】蓝图手册记忆面板文本旧版用 drawCenteredString 以 x=20 为
    圆心 → 左半裁出屏幕；改 m_280614_ 左对齐 + fitText 截断；空列表提示
    （maidEmptyText，x=10 圆心几乎全裁）同样改左对齐。
  - 教训固化：新增文本统一走 clampCenterX/clampLeftX 或自动换行，禁止再以
    小 x 为圆心 drawCenteredString。
- **promaid 1.5.109：上一版 4 个问题真正修复（换镐/搭高漂移/不停传送/安全区传送）**
  - 【换镐·真凶】MaidToolAutoEquipBehavior 是独立 core 行为，**每 tick** 调
    ensureForTask 无条件换背包最高级镐——1.5.107 只改了 MaidMineBehavior 的调用点，
    被这个行为每 tick 覆盖，所以"没修复"。现在 ensureForTask 对挖矿任务只做
    ensurePickaxeIfEmpty：手中已是镐就零开销不换，空手/非镐才装一把；按目标矿升级
    由 MaidMineBehavior.ensureForTarget（1.5.107 已实现：手中够用不换、不够换能挖的）。
  - 【搭高漂移·重新设计】删除 pullTowardTarget（setDeltaMovement 直接注入速度——
    与导航互相覆盖、搭高时把女仆从柱子上推走，是"疯狂漂移"根因）；搭高成功后
    改为站桩（不再"垫一步走一步"设 WalkTarget，靠 pillarGuard 钳位）；发现矿物
    移动速度默认 1.35→1.0（已同步改你的 promaid-common.toml）。移动完全交给导航。
  - 【不停传送】suffocateCheck 从"只要进施工区就传"（v1.5.84 主动清场）改回
    "身体中心被实心块埋住才传"（isBuriedInRegion）——开始建造后站在施工区旁的
    玩家/女仆不再被反复拽走；只有真被建造方块埋住才救援。
  - 【主人没被传安全区】findSpotOutsidePlan 从 4 方向×3~12 格×y-2~+6 扩为
    8 方向×3~16 格×y-6~+10——开阔地形几乎必中；极端地形返回 null 时由
    suffocateCheck"被埋才传"兜底，不会把主人困死。
  - 保留 1.5.108 的 SESSION_PLAN 守卫（存档静默恢复的旧计划不触发传送）。
- **promaid 1.5.108：建造"没规划就提前传送"修复（根因：存档静默恢复旧计划）**
  - 【根因】getPlan 会从 BuildArchive（世界档案）静默恢复上次没清掉的旧计划
    （游戏中途退出/崩溃时 cancel/clear 不会执行，档案残留）并写回 GLOBAL_PLAN →
    ensureChunks 算出施工区包围盒 → suffocateCheck 每 20 tick 看到"有计划"就
    开始传送玩家/女仆——用户本会话什么都没规划，女仆就被提前传送。
  - 【修复】BuildPlan 新增 SESSION_PLAN 标记（按维度，内存态）：
    - setPlan（开始建造/续建/全员加入都走它）→ 置位
    - cancel / clear（取消/完成）→ 清除
    - 静默恢复（getPlan → restoreFromArchive）→ 不置位
    - suffocateCheck 先检查 SESSION_PLAN：没有标记不传送
  - 效果：存档恢复的旧计划照常续建（方块继续放），但**不再无规划传送**；
    用户本会话点"开始建造"后传送保护恢复正常。
- **promaid 1.5.107：挖矿按需换镐（不再无条件切最高级）+ 耐久核实**
  - 【换镐按需】旧逻辑：进入挖矿任务即无条件换背包最高级镐；findOre 又按"当前
    手镐"过滤矿——手拿铁镐时钻石矿直接跳过报"需要钻石镐"，完全无视背包里能换的
    好镐，两处互相矛盾。
    新逻辑（用户设计）：先判周围是否有矿 → 再按手中镐等级决定是否切换——
    - findOre 只跳过"手+背包都挖不动"的矿（真挖不动才播报）
    - 选中矿后 MaidToolAutoEquip.ensureForTarget：手中镐能挖 → 零开销不换；
      挖不动 → 换背包里【能挖目标】的最高级镐；手+背包都没有 → 播报
    - 中途镐断/换低镐：先试从背包换能挖的，换不到才弃目标（进度保留）
    - 删除了 m_6114_/m_6735_ 里的无条件 ensureForTask 换镐调用
  - 【耐久核实】部署版字节码确认扣耐久代码正确且在运行：主手 = TLM 的
    EntityHandsInvWrapper（即原版装备槽，getMainHandItem 同一实时栈），每挖完
    一块 m_41622_(1, maid) 直接扣主手，仅软方块（硬度≤1 开路）豁免。
    "看起来不掉耐久"最可能是【Mending 附魔 + 矿石经验球】：矿石掉落经验，女仆
    自动拾取 XP，TLM 的 pickupXPOrb 含 Mending 修复逻辑 → 每挖一块扣 1 点又
    被经验立即修回（净耐久≈不变，原版正确行为）。非 Mending 镐会正常磨损。
- **heartfelt v1.1.1 + promaid 1.5.106：挖矿"鬼畜移动"修复（三方叠加根因）**
  - 【主因·heartfelt】爱憎分明 HungerManager.applySpeedEffect 每 20 tick（1 秒）直接
    setBaseValue 把移动速度基础值强改为 0.35/0.55/0.85/0.65 四档（随饥饿跨档跳变），
    女仆边吃边饿 → 速度每秒猛跳（再叠挖矿 1.35 倍率）＝移动一卡一卡。
    新增 HungerSpeedTierCacheMixin：按女仆缓存"饥饿档位"，档位没变就跳过本次重复
    clobber（保留饥饿减速功能，只去掉同档位每秒重复写）；超过 1 分钟同档位重新施加
    一次兜底外部改基础值；10 分钟旧条目惰性清理防泄漏。
  - 【次因·promaid】pullTowardTarget 直接 setDeltaMovement 与导航 moveControl 互相
    覆盖 → 拖拽至少隔 10 tick 一次（pullCooldown），压掉残余抖动。
  - 【次因·promaid】approachOre 纯"走过去"时每 8 tick 才重设一次 WalkTarget
    （walkRetargetCooldown）——旧版每 tick 重设让 TLM 每 tick 重寻路（移动顿挫）；
    搭高/斜坡成功后的即时重设不受影响。
- **promaid 1.5.105：记忆系统 ×（心契誓约 + 爱憎分明）联动全修（优雅降级）**
  - 【修复·死代码】RelationshipMemoryAdapter 信任/恐惧读取改用正确 API
    `EmotionData.get(maid, ownerUuid)` → `EmotionValues.trust()/fear()`——
    旧反射 `getMaidEmotion` 在原版/部署版均不存在（返回 null），"信任崩塌/恐惧
    上升写记忆"从未触发过，本次修好
  - 【数值不陈旧】信任/恐惧改事件式描述（"大幅下降/正在恢复"），恢复时同
    fact key 覆盖——不再残留"信任降到了 40"这类过期绝对数
  - 【新增记忆】怀孕/分娩、母亲身份（生过孩子/女儿在身边）、丧子
    （mood_data.childLossGrief）、背叛（IsBetraying NBT，失主女仆补检）、
    淡忘（forgetCountdown）、主人死亡哀悼（heartfelt_mourning_until）
  - 【离婚撤销】married true→false：作废旧永久"婚姻"记忆（AiMemoryStore 新增
    markDeletedByTag）+ 停用"妻子"关系三元组（新增 deactivateRelationsByPredicate，
    inactive 自动从注入/检索过滤）+ 写"我们结束了婚姻"（可被再婚覆盖）
  - 【优雅降级】所有新逻辑按 mmLoaded/llLoaded 门控；未装 maidmarriage/
    callresponse 时完全短路，不影响原运行；TaskDataKey + record accessor 缓存
    （键=声明类#方法名）
  - 版本号沿用 1.5.x 序列（jar 名不变 promaid-1.0.0.jar）
- **heartfelt v1.1.0：三模联动考究修复（家庭保护真正生效 / 溺爱偷窃 / 背叛仇恨 / 孕期保护 / 家庭互动·纯爱版）**
  - 【修复·家庭保护死代码】爱憎分明 MixinEntityMaid 把"女仆打女仆"近战用中性伤害源重放
    （hurt HEAD + super.m_6469_ + setReturnValue），LivingHurtEvent 里直接实体恒为 null——
    FamilyProtectionManager 从未触发。新增 EntityMaidDoHurtProtectMixin 拦截
    doHurtTarget（m_7327_，所有女仆近战唯一汇聚点）：受害者是关系女仆→取消攻击；
    同主人且攻击者好感≥64→取消；背叛女仆豁免（放行，配合背叛仇恨设计）。
  - 【修复·恋人偷东西】EmotionDotingSelectActionMixin：冻结层女仆（恋人/妻子/女儿/深爱）
    的溺爱 selectAction 强制 FLOWER（送花）——告白恋人（好感128~191）不再 40% 偷主人
    东西/30% 翻主人箱子（callresponse 自己的 isPartner 不认告白，原逻辑违和）；
    "被宠坏偷东西"只保留给未确认关系的普通溺爱女仆。
  - 【修复·背叛仇恨死代码】triggerBetrayal 瞬间 callresponse 清空 owner（m_21816_(null)），
    旧 selectTarget 改写（要求 getOwner 非空）永不触发。现在 triggerBetrayal HEAD 把
    "被恨的玩家"UUID 写入 ForgeData（heartfelt_hated_player），selectTarget 改写改读它——
    "背叛专挑主人珍视的人（妻子/女儿）下手"的戏剧性真正生效（主人永远最高优先）。
  - 【性能】RelationshipExemption 全部 TaskDataKey 与 record accessor（Method）缓存——
    旧版每 tick 情感扫描/伤害事件现 getMethod；StoryMemoryManager 复用同一 key 缓存。
  - 【孕期保护】PregnancyProtectionManager：怀孕中/产后 3 天内女仆饥饿钳制在 25~85
    （低于饿死线 9、高于吃撑线 91）——孕妇不再饿到濒死/吃撑死。
  - 【家庭互动·纯爱版】FamilyInteractionManager（每 2 分钟，每目标每天 1 次，纳入全局
    API 配额）：① 母女互动——女儿在场时母亲/女儿交替对主人说家庭向的话；② 暗恋者感慨——
    主人有妻子后，深爱未告白女仆偶尔温柔祝福/释然（绝不嫉妒破坏，遵循一夫一妻纯爱）。
  - 【主人死亡哀悼】FamilyMourningManager：主人倒下时关系女仆进入 1 游戏日哀悼期
    （heartfelt_mourning_until）——触发悲痛 LLM 对话（妻子/女儿台词不同）+ smart_intimate
    工具哀悼期内拒绝亲密互动。
- **heartfelt v1.0.2：heartfelt_connection 审查修复（7 项）**
  - 家庭保护：背叛女仆豁免——受害者是关系女仆时，背叛女仆的攻击不再被归零
    （背叛仇恨优先关系女仆，原逻辑自相矛盾，背叛攻击形同虚设）
  - smart_intimate 工具：initDone 只在全部反射加载成功后置位 + 每个动作调用前
    判空——maidmarriage 版本部分不匹配时不再抛 NPE（只影响对应动作）
  - 恐惧豁免：只拦正向恐惧增量，负增量（自然衰减/安抚）放行——冻结女仆带高恐惧
    进入冻结层后恐惧能正常下降（原双向冻结→恐惧对话无限触发）
  - 生育记忆：只在生育后 30 个游戏日内宣称"刚生过宝宝不久"（原永久宣称）
  - 背叛选目标扫描锚定改为背叛女仆自身（原锚定主人，可能选到另一维度/42 格外
    的女仆，背叛女仆根本走不到）
  - 反击背叛行为优先级 200 → 199（避开与 promaid 自动换装同优先级互抢槽位）
  - ApiQuotaBridge 反射失败结果缓存（promaid 未装时不再每 tick 反复 Class.forName）
- **1.5.122：挖矿"完全不动"修复 + 面板输入修复 + 建造诊断日志**
  - 【挖矿完全不动（v1.5.121 回归修复）】setWalkTarget 的 ±1 容差去掉 !isDone 条件后，
    女仆到达站立点不再重设导航 → 永久卡死。恢复"导航行进中（!isDone）才跳过"：
    容差防相邻格抖动重设，到达/寻路失败时重设重新激活导航。若"走一步停一下"仍
    存在，日志可定位（无新增日志）。
  - 【面板输入】①所有 NumRow 输入框去掉数字过滤（m_94153_）——旧版 filter 对
    非数字初始值（如"建造速度档位"的 "x1.5"）会把输入框锁死（任何输入都被拒）
    = "无法输入数字"；改为不限输入、onChange 校验（非法红字）。②"建造速度档位"
    从输入框改为【循环按钮】（x1 → x1.5 → x3 点击切换）——非数字配置不该用数字
    输入框。③点击 EditBox 显式 setFocused 保留 + 命中日志（200 tick 限频）。
  - 【建造诊断日志】"下达后不建造"至今无法定位 → 加日志：行为启动（m_6735_ 记录
    计划块数）、每 100 tick 记录 plan/origin/暂停/冷却状态。下次复现后看日志即可
    确认卡在哪一步（行为没启动 / 计划为空 / 原点丢失 / 暂停 / 冷却）。
- **1.5.121：建造恢复 home 模式（回归修复）+ 挖矿移动容差 + 面板输入聚焦**
  - 【建造不执行（回归修复）】v1.5.117 撤销强制 home 模式后，TLM MaidFollowOwnerTask
    持续启动把女仆拉向跟随状态，WORK activity 的建造行为不再运行 → 下达蓝图后
    只在系统消息回应、不建造。恢复"进入建造 → 强制 home 模式"（m_6735_/m_6732_）。
    注意：home 模式本身不产生传送（它恰是阻止 TLM 跟随/瞬移回主人的机制）；"随机
    传送"根源（setPlan 清场 / suffocateCheck 救援）在 v1.5.117 已全部禁用——恢复
    home 后 promaid 侧 0 传送 + TLM 不瞬移 = 彻底无随机传送，且建造恢复正常。
  - 【挖矿"走一步停一下"】setWalkTarget 目标比较加水平 ±1 格容差——女仆移动中
    "矿 6 方向最近站立格"会在相邻格间抖动，精确比较每节流重设 → 重新寻路 →
    走走停停；±1 容差内视为同目标、导航不中断。重设节流 12 → 30 tick（兜底下限）。
  - 【面板输入框】主类 m_6375_ 点击时显式对命中的 EditBox setFocused（兜底防
    children 事件顺序/命中差异导致"点不进输入框"）。
- **1.5.120：感知播报"分时宜"——危险时停止不合时宜的非紧急感知**
  - PerceptionManager：附近有怪物（12 格内 hostiles>0，遭到威胁）时——
    ①"主人一直在看着我…"（长时间注视）不再播报，且注视计时不累计（解除威胁后
    重新计时）；②"天气变化"不再播报。紧急类照常：敌对出现/靠近/消失、
    主人受伤/低血（危险中主人受伤更要报）。
  - 逻辑：威胁抑制在事件生成层（scan），不是显示限频层——危险中这些感知
    根本不算"发生"，比仅降频更彻底。
- **1.5.119：敌对感知显示降频（感知检测照常，仅显示频率降低）**
  - 新增配置 perception.hostileShowCooldown（默认 300 秒 = 5 分钟，范围 60~3600，
    面板"感知"页新增行）："发现怪物/怪物靠近/都清掉了"类感知气泡的显示间隔
    从原 30 秒（同类事件限频）独立出来，大大降低。
  - 感知检测逻辑完全不动——快照扫描/事件生成照常（女仆照常感知并记住怪物，
    "实际解锁次数不减少"），只是气泡显示频率降低；主人受伤/注视/天气仍走原
    30 秒限频。
- **1.5.118：挖矿移速对齐 TLM 伐木（0.6）+ 部署 116/117**
  - 【移速对齐伐木】TLM 伐木（farm）任务移动速度实测 = 0.6f（IFarmTask.createBrainTasks
    反汇编：ldc 0.6f → MaidFarmMoveTask）。挖矿基础移速 0.4 → 0.6（走路观感与伐木
    一致、自然不少；两档保留：远矿 >8 格 1.5×=0.9 赶路、近矿 ≤3 格 0.8×=0.48 防漂移；
    滑动迁移 0.6 = 伐木同款）。代码默认值 + config/promaid-common.toml 同步改。
  - 本版为累积部署：含 v1.5.116（挖矿 setWalkTarget 目标不变不重设，治"一走一停"）
    + v1.5.117（禁用 setPlan 清场传送 / 禁用 suffocateCheck 救援传送 / 撤销强制
    home 模式，建造侧 0 传送，允许 TLM 自带传送）。
- **1.5.117：建造侧传送从根源停用（用户决策）+ 撤销强制 home 模式**
  - 背景："随机传送"多次修复仍未根治，用户决定从根源绕开：如果站桩足够强力，
    promaid 侧传送措施就不需要。
  - 【禁用 setPlan 清场传送】确认建造后不再把施工区内的主人/女仆传出（v1.5.115
    的整块实现移除，含 resolveOwner 联动）。A4"切换蓝图提示"保留。
  - 【禁用 suffocateCheck 救援传送】女仆/玩家被埋不再由 promaid 传送救援——
    原实现整体保留在 if(false) 内（含 v1.5.116 连续 2 次被埋消抖），恢复时去掉
    if(false) 包裹即可。玩家被埋时需自行脱困（原版窒息扣血）。
  - 【撤销强制 home 模式】MaidBuildBehavior 进入/退出建造不再 setHomeModeEnable——
    建造期间女仆仍【强站桩】（setStill + 每 tick 清 WALK_TARGET + 停导航），
    但允许车万女仆自带传送（离主人过远由 TLM 瞬移回主人身边，隔空建造不受影响）。
  - 预期效果：promaid 侧 0 传送 → "随机传送"代码路径彻底关闭；女仆建到一半被
    TLM 瞬移回主人身边是正常现象（站桩继续隔空建）。
- **1.5.116：挖矿"一走一停"修复 + 建造被埋判定消抖（诊断预备）**
  - 【挖矿移动鬼畜】walkToOreBase/walkToStep/walkToBlockFace/slideAnchor 统一走
    新 setWalkTarget：目标站立点没变 且 导航仍在行进（!isDone）→ 不重设 WalkTarget。
    旧版每 8 tick 无条件重设 → 导航每次收新目标都停下重新寻路（recomputePath）→
    "走一下顿一下"。重设节流 8 → 12 tick（兜底下限）。
  - 【建造被埋消抖】suffocateCheck 的救援条件从"检查时点被埋"改为"连续 2 次检查
    （≈2 秒）仍被埋"——建造中的方块是瞬态（放置/拆除反复出现），单次捕捉到
    "头顶正好有方块"就传 = 玩家/女仆在施工区附近被反复拽（"随机传送"最可能来源，
    由 v1.5.114 头顶格判定放大）。传送均写日志（suffocateCheck/setPlan LOGGER）——
    此前 promaid 传送不写日志，无法从运行日志定位。
  - （本条目改动在 v1.5.117 禁用传送后保留代码备用，恢复传送时生效）
- **1.5.115：确认建造 → 施工区【一次性清场传送】（按用户预期修订）**
  - 正式确认建造（下达蓝图）→ 画好施工区（蓝图包围盒 = 强制加载区域）→ **立刻把
    施工区内的主人 + 所有女仆一起传送到区外安全点**（每个实体错开 0.8 格防重叠）。
    此传送【仅触发一次】：resumeSame 续建不重复传；之后 suffocateCheck 只救"被埋"
    的（身体中心/头顶格实心），不再无差别传送 → 既满足"开建前清场防被埋"，
    又不会"不停传送"。
  - 修订说明：v1.5.114 曾把女仆传送整体去掉（"只传玩家"）——但女仆站在施工区内
    （原点常在她脚下）会被第一层地板埋住，"隔空建造"≠"不需要出区"。按用户预期
    恢复"主人 + 女仆一起清场"；找不到区外安全点（封闭洞穴）时不传送、主人若在区内
    明确提示留意脚下（被埋由 suffocateCheck 兜底）。
- **1.5.114：建造逻辑深度优化（A 正确性 + B 性能 + C 手感 + D 清理 全组实施）**
  - 【A1 延后补建死锁（真 bug）】主循环对延后步骤只滑过（containsKey）、游标又只在
    放置成功时推进——缺料/支撑缺失步骤进 deferred 后游标钉在它上面，deferred 轮询
    旧判 `idx >= cursor` 也跳过它 → 补料/支撑就绪后【永远无人补建】（只有某轮窗口
    内碰巧一块没放、游标滑过才偶然解除）="缺料补料后有时不续建"的根因。改为轮询只
    跳过 `idx > cursor`：游标所指的延后条目由轮询补建，成功后主循环下轮自然推进游标。
  - 【A2 防窒息漏判"头被埋"】isBuriedInRegion 只查身体中心（站立时=站立格，几乎恒为
    空气/支撑）→ 头顶被蓝图方块盖住（隔空建造最常见的被埋形态）判定"未埋"→ 玩家
    窒息没人救。改为身体中心格 + 头顶格任一实心满块即算埋（原版窒息即头部格非空气）。
  - 【A3 不下达即闪女仆】setPlan 每次下达新蓝图都无条件把女仆闪出施工区——但建仆是
    隔空建造（站桩+home+强制加载），本就不需要安全点，且与 suffocateCheck 跳过建仆
    的设计矛盾。去掉女仆传送，只保留"玩家站在施工区内"时的传送；女仆被埋由自保
    antiSuffocate（每 tick 顶出）兜底。（**1.5.115 已按用户预期推翻本项**：恢复
    "主人+女仆一起清场"，见下条目）
  - 【A4 切换蓝图明确提示】同维度下达【不同】蓝图会直接覆盖旧计划（旧建筑女仆全体
    转向新蓝图、玩家无感知）→ 现在气泡提示"我把之前的「旧计划」切换成「新蓝图」啦"。
  - 【B1 扫描收窄】suffocateCheck 玩家救援循环里嵌套全维度 AABB(-3E7…) 扫女仆 →
    收窄到施工区+8 格 scanBox（多玩家被埋时不再无谓放大）。
  - 【B2 完成扫描条件化】完成时 scanGaps 全表 O(N) 扫描（55 万步蓝图完成瞬间 tick
    尖峰）→ 只有"有跳过/未放满"（skipped>0 或 placedCount<total）才扫描，正常完成
    直接收尾（放置成功方块已过 canSurvive+掉落双重验证，不会自行消失）。
  - 【C1 缺料带数量】缺料气泡"还缺 xxx" → "还缺 xxx ×N"（计划剩余需求 − 女仆背包 −
    主人背包，创造模式视为充足）。
  - 【C2 注册表查询缓存】主循环/deferred 每步 ForgeRegistries.BLOCKS.getValue(parse(id))
    （每 tick 几百次）→ Progress 新增计划级 blockCache（blockId→Block），同种方块
    大量重复，缓存后每 tick 少几百次注册表查询。
  - 【D1 清理】MaidBuildBehavior.planName 私有重复实现 → 复用 BuildPlan.planName。
- **1.5.113：挖矿机制深度优化（A 性能 + B 正确性 + C 手感 + D 清理 全组实施）**
  - 【A1 扫描降频 + 矿位置缓存】旧版空闲时每 3 tick 全量扫 49×49×37 ≈ 8.9 万格
    （每女仆约 3 万次方块查询/秒）；改为全量重建每 5 秒一次（ORE_CACHE_TTL），
    期间每 20 tick 只做廉价校验（存在性/框内/可挖性），服务器负载降一个量级。
  - 【A2 进度 NBT 节流】每 tick 写持久化 NBT → 每 20 tick 一次（挖完/弃置/失效
    关键点仍立即写），消除实体常驻 dirty 的存档压力。
  - 【A3 挡路/镐能力缓存】挡路预算按矿坐标缓存（缓存轮复用，不再重算视线）；
    镐能力先查主手（零开销）、不够才查背包最高级镐（一次遍历）。
  - 【B1/B2 滑动锚点迁移】框内挖空 → 锚点朝主人方向滑动一个搜索环宽（新框与
    旧框错开），女仆不用走路、马上能挖新框的矿；每 5 秒最多滑一次。旧版"朝主人
    走、走出旧框才重埋"被墙堵住时会原地重埋 → 新框重合 → 死循环，且迁移期纯
    走路零产出——两个问题一起根治。
  - 【B3 搭高柱不坠落】清理搭方块时若女仆正站在上面（脚下/所在格）→ 延后清理，
    走开即清——垂直矿脉搭高不再被自己搭的柱子摔下去。
  - 【B4 弃置矿短时排除】够不着的矿弃置后 30 秒内不再反复选中（挖掉任一方块
    即解除），框内只剩一块够不着的矿时不再来回折腾。
  - 【B5 六方向站立点】walkToOreBase 改为找矿 6 方向最近的可站空气格（距矿
    近、脚下有支撑）；旧版只向上找"矿正上方空气格"，岩壁深处矿会让女仆走到
    够不着的地方空转。
  - 【B6 冷却只在成功放置后设】搭高/台阶/搭桥失败不再消耗 pillarCooldown（旧版
    失败也吃冷却 → 搭高被错误阻塞）。
  - 【搭路走一步（用户新反馈）】搭桥/搭台阶成功后设移动目标走到刚垫的格子上——
    旧版搭一格就站着不动（case 3 搭桥后无移动目标）；现在"垫一块走一步再评估"。
  - 【C1 两档接近速度】矿远（>8 格）1.5× 快走赶路、近（≤3 格）0.8× 慢走（准备
    搭高/挖掘，防冲过头漂移）。
  - 【C2 缺搭块播报】背包没有可搭方块（圆石/泥土等）时气泡提示一次（30 秒限频），
    不再反复尝试搭高失败。
  - 【C3 无镐立即播报】镐碎裂/换不到能挖的镐 → 立即记录并播报"需要更高镐"
    （不用等 15 秒目标超时）。
  - 【挖矿行为常驻】canStart/canContinue 在挖矿任务期间持续为真（与建筑行为
    一致）——目标挖完不停止，下一 tick 继续扫描/迁移；旧版无矿即停 → 女仆原地
    发呆、永不迁移（"矿挖完就站住不动"根因之一）。
  - 【D 清理】删除死变量 pillarBlocks；删除已失效的"迁移停滞判定"配置项（滑动
    迁移无停滞概念）——面板/配置文件同步移除。
- **1.5.112：自保传送节奏 + 死亡传送重定位 + 换工具评分分离 + 手册注释全面重写**
  - 【自保传送判定】威胁消失后判定传送的间隔 5 秒 → 20 秒（threatGoneExit 100→400
    tick）：威胁消失后女仆在安全位置多观察一会儿才结束自保/传回主人身边，不再刚松口气
    就被拽回家。传送安全判定半径从写死 6 格改为可配置【传送安全判定半径】（默认 4 格，
    范围 2~8）：自己与主人身边此半径内无可见怪物才传送——6 格太宽松（远处有怪也算
    不安全导致一直不传），4 格更贴身合理。
  - 【死亡传送】目标从【死亡地点】改为【玩家重生点】（床/重生锚）——旧版女仆赶去守尸体
    没意义（主人复活在别处）；重生锚可能在其他维度（下界床/末地重生锚）→ 目标维度一起
    解析，女仆跨维度传送（m_264318_）；无重生点/维度异常退主世界出生点兜底。
  - 【换工具评分分离】修复"选战斗却优先掏镐"：旧版 attack 共用镐的 toolScore（只有镐
    有分、剑一律 0 分），且 isMeleeWeapon 把带攻击力属性的镐也算近战 → 背包有钻石镐时
    战斗也优先掏镐。新增近战评分 meleeScore（按主手攻击力：剑/斧 > 镐/铲），挖矿仍按
    镐等级——两类任务彻底分离，剑永远优先；实在没剑才退用镐当武器（比空手强）。
  - 【手册注释】渲染区分度：注释从灰色（与标签相近"做了跟没做一样"）改为【浅蓝 +
    » 前缀】（首行带前缀、续行缩进对齐）；内容全面重写：82 处"跟标签一样/太短"的
    注释改为详细功能描述（讲清调大调小的效果），并补上新增的"传送安全判定半径"行。
  - 配置：promaid-common.toml threatGoneExit 100→400，新增 teleportSafeRadius = 4.0。
- **1.5.111：标题居中模板 + 挖矿漂移根治 + 建造传送修复（3 大项）**
  - 【标题类文本统一模板】修复"所有标题偏左超出屏幕"的根因：drawCenteredString 以 x 为
    圆心居中，旧版到处写 (屏宽-文本宽)/2 再喂给它 → 圆心整体又左移半个文本宽，长标题
    偏出屏幕左缘。新增 drawCentered 模板（fitText 截断到屏宽-20 后以屏幕中心为圆心），
    手册/记忆面板全部标题、提示、页码、材料格内文本统一走模板，任何窗口宽度完整居中。
  - 【挖矿漂移根治】①搭高站桩清空旧 WalkTarget——walkToOreBase 设的移动目标残留在
    记忆里，垫一块后女仆继续高速朝旧目标跑 → 支柱散成一路方块"永远搭不高"；现在垫完
    原地站定（pillarGuard 钳位），下一 tick 重评：够得着就挖、不够再垫，垂直列干净成型。
    ②移动速度倍率 1.35 → 0.4（爱憎分明饥饿档基础速度最高 0.85 × 1.35 = 每秒 17+ 格
    狂奔 → 0.4 相当正常步行）；迁移换框/走向目标全部跟随该速度。③珍稀掉落物回收子系统
    整体删除（女仆挖矿途中被掉落物吸引、满场飞奔去捡的根源）——挖出的掉落物就地收进
    背包，捡不到的留给拾取任务，不再打断挖矿流程；配置面板"珍稀标记矿物"名单随之移除
    （矿表剩目标矿物/障碍物两张）。
  - 【建造传送修复】①玩家传送从未生效的根因：主人解析只依赖 getOwner()（同维度实体
    解析，跨维度/瞬态返回 null）→ 新增 resolveOwner：失败时回退 ownerUUID → 全局
    PlayerList 查，任何维度在线玩家都能找到；且只在主人站在施工区内时才传送（已在
    区外 = 已安全，不拽）。②"进入建造模式不停传送"的根因：建仆被自己放置的方块埋住 →
    suffocateCheck 反复"被埋→拽出→站回施工区→再埋"（每 10 秒一次像不停）→ 建仆不再
    参与救援传送（被方块挤压由自保 antiSuffocate 顶出处理），救援只服务非建仆与玩家。
    ③续建同一计划不再重复传送（反复点手册"建造此图纸"/强制续建不再拽人）。
  - 配置：promaid-common.toml moveSpeed 1.35 → 0.4（默认/范围同步改 0.4，0.2~1.5）；
    rareOres 条目删除。
- **1.5.104：审查建议实施——确认弹窗 + 建造完成通知 + 优化**
  - 重要操作统一加 MC 删除世界样式的确认弹窗（原版 ConfirmScreen）：
    清空记忆 / 删除蓝图 / 取消建造（取消建造=清计划+停女仆，需确认）
  - 建造完成通知：建好时工头在主人聊天栏发「【建造完成】xx 建好了『蓝图名』」；
    完成时 placeQuota 归零（防陈旧配额泄洪到下一个计划）
  - 配置面板数值输入即时反馈：非数字/越界时输入框文字变红（改回合法值恢复）
  - 手册记忆页新增「清空记忆」按钮（确认后服务端清空该女仆全部记忆并刷新）
  - suffocateCheck 防窒息扫描从全维度收窄到施工区包围盒 + 8 格边距（
    实体扫描成本与区域大小成正比）；超大区域不加载时直接跳过
- **1.5.103：全模组审查修复（1 高 + 4 中）+ 优化**
  - 【高】AiMemoryStore.prune 结构合并：同桶 ≥3 条相似段落时引用已被移除对象，
    indexOf(keep)=-1 → set(-1) 抛 IOOBE → 关系记忆适配写档时崩服。已加
    "仍在列表中"守卫 + 合并内容（drop 细节并入 keep，不再丢失）
  - 【中】感知天气判定：m_46749_=isLoaded（区块加载，恒 true）误当下雨 →
    天气永远不是 clear/雷雨检测失效。改 m_46470_=isThundering + m_46758_=isRainingAt
  - 【中】ChatBubbleLimitMixin 的 `maid` 字段从未赋值（恒 null）→"非工头建造
    女仆气泡静默"与"气泡同步主人聊天框"一直是死代码。加 @Shadow 取回目标类字段
  - 【中】WorkStatusReporter getTask() 未判空 → 可能 NPE 崩服。已加判空
  - 【中】挖矿静态 per-maid Map（锚点/超时/播报限频等 9 张）只增不清，长时运行
    泄漏。新增 purgeStaleMaids（getEntity O(1) 查存活），每 30 秒清理
  - 【优化】挖掘裂纹包只在阶段变化时发（省包）；核实 SimpleChannel 目录包
    数百 KB 在上限 2MB 内，无需改（回退最初加包大小的尝试）
- **1.5.102h：修复 heartfelt SmartPromptMixin（与 102g 同源的第二个崩溃点）**
  - 修好 PromaidPromptMixin 后崩溃转移到 heartfelt_connection 的 SmartPromptMixin——
    同样是 @ModifyArg(at=@At("HEAD")) 注入 LLMMessage::systemChat 的非法组合
  - 修复：改用 MixinExtras @WrapMethod 包裹静态方法 systemChat——直接拿 (maid, setting)，
    追加关系准则段后 original.call 原样调用；按类型匹配参数，与原版/SMART 字节码无关
  - 编译路径：从 mixinextras-forge 抽出内嵌 MixinExtras-0.5.4.jar 加入 compile_addon.txt
    的 classpath（javac 看不到 jarjar 内嵌类）；heartfelt_connection-1.0.0.jar 已重建
- **1.5.102g：修复 PromaidPromptMixin 注入非法导致女仆对话崩服**
  - 现象：进游戏后女仆一触发对话（主动对话/爱憎分明播报）即崩服，
    InvalidInjectionException: @ModifyArg ... targetting a non-method insn
  - 根因：PromaidPromptMixin 用 @ModifyArg(at=@At("HEAD")) 是非法组合——
    @ModifyArg 必须指向方法调用指令，HEAD 是方法开头不是调用。mixin 配置
    required:true + defaultRequire:1，任何注入失败都致命。之前没崩只是因为
    LLMMessage 类一直没被加载（没触发女仆对话），触发即崩
  - 修复：改为 @ModifyVariable(argsOnly=true) 在方法开头改写 String 参数——
    按类型匹配参数，与原版/SMART 字节码内容无关，稳健生效
- **1.5.102f：模组镐评分兜底（模组武器/附魔兼容确认）**
  - 非 TieredItem 的模组镐（自定义实现）给保底 1 分（石镐级）——旧版一律 0 分，
    背包同时有原版石镐时会永远不装备 mod 镐
  - 审计确认：挖矿附魔 效率/时运/精准采集/耐久/经验修补 全部已兼容
    （效率=公式手工加成；时运/精准=dropResources 六参版；耐久=hurtAndBreak
    自动防爆；经验修补=onOrbJoin 收经验球 + TLM pickupXPOrb 自动修复）；
    模组武器=isPickaxe/isMeleeWeapon(攻击力属性)/Bow/Crossbow/Trident 判定全覆盖
- **1.5.102e：修复耐久扣错对象 + 方块秒破（旧版残留问题）**
  - 耐久：旧版扣耐久的代码在背包里找「同类型的镐」扣/清除——但 v1.5.90 起镐
    在手部栏（getHandsInvWrapper），背包刚好有同款镐时会扣背包那把、主手那把
    永远不磨损（看起来耐久不掉/背包镐莫名消失）。现在直接扣主手镐，碎裂清主手槽
  - 秒破：单 tick 挖掘进度封顶 0.25——高等级镐+效率附魔会让石头/泥土 1~2 tick
    挖完（Efficiency V 下 delta≈0.93）。封顶后任何方块至少 4 tick（0.2 秒）
  - 秒破观感：女仆不是玩家，原版不会发破坏进度包 → 方块全程无裂纹、到点瞬间
    消失。新增 broadcastCrack：每 tick 向周围玩家广播 ClientboundBlockDestructionPacket
    （0~9 裂纹阶段，挖完发 10 移除），方块有真实渐进裂纹动画
- **1.5.102d：矿表面板/搜索/障碍物预勾选 + 换镐升级 + 拖拽削弱 + 无矿挡路上报**
  - 配置面板矿表子页：主标题「Promaid 模组详细配置」隐去（否则与名单标题重叠）；
    搜索照搬创造输入——注册名之外同时匹配物品中文名（输入法可直接搜中文）；
    "← 返回参数 / 保存并返回"上方按当前名单加一行对勾说明（三名单各一句）
  - 障碍物名单：自然生成的方块（OPEN_BREAKABLE 全表）默认已勾选 ✓（内置，
    不再需要手动加），面板名单只负责额外添加；基岩/屏障禁止加入（防误加傻挖）
  - 换镐升级：主手是低阶镐但背包有更高级镐时自动换装——遇到钻石矿不再直接
    报"需要钻石镐"，先检查背包换最好的镐（MaidToolAutoEquip 旧版主手有镐就 return）
  - 拖拽削弱：pullTowardTarget 力度 0.1 → 0.05 块/tick（用户反馈"快得跟疯狗"）
  - 无矿挡路上报：场上没找到矿但此前有被硬物挡住的矿时，仍上报一次
    「附近有矿物被硬方块挡住」（30 秒限频），让主人知道没矿是被挡而不是偷懒
- **1.5.102c：进度文字右对齐 + 报点中文名 + 石质方块开路**
  - 手册进度行（含【全局暂停中】/速度：极速等字样）改为整行右对齐——
    结尾的状态字样从中间跳到屏幕右侧（用户选定方案），进度条仍居中；
    进度文字带换行，每行都右对齐到屏幕右缘（drawWrapped 增加 right 对齐模式）
  - 挖矿报点全部改中文方块名（石头/圆石/深板岩/基岩/箱子/各类矿石等 70+ 常见
    方块；未知回退英文 id）：硬挡路报点（前方有X挡住）、珍稀掉落被挡提示、
    镐子不够跳过矿播报三处统一走 blockCnName
  - 石质方块族批量进开路白名单：石砖/平滑石头/磨制花岗岩/深板岩砖/黑石砖/
    砂岩切制/石英/海晶石/末地石/下界砖/泥砖等 40+ 种——地表/建筑常见的石头
    变体不再当硬挡路报点弃置（基岩仍是硬挡路——不可挖掘，但报中文名）
- **1.5.102b：修复建造中进度条/进度文本贴出屏幕底边**
  - 现象：建造中打开手册，进度条与百分比文本被屏幕底边裁掉（"突出屏幕"）。
  - 原因：home 页进度文本画在 h-28 → 进度条画在 h-6、百分比文字（字号 8px）
    实际画到 h+1，超出屏幕底边；maids 无翻页时 h-34 同样贴底太近。
  - 修复：新增 PROGRESS_BOTTOM_GAP=40——home 与 maids 无翻页页统一用
    h-40（进度条 h-18、百分比 h-19，完整可见不贴边）；建造页（h-84）本就
    在控制按钮上方、无溢出，保持不变。
- **1.5.102：全模组数值面板化——所有剩余硬编码数值全部纳入配置面板**
  - 此前只有珍珠/血量/距离等少量数值可调；本次把整个模组其余所有数值常量
    全部接入 MaidSmartConfig + 配置面板（共新增 58 项，按 9 个板块分页展示）：
  - **建造**：防窒息传送冷却（秒）、恢复缓存保留（毫秒）、卡住重试间隔（tick）、
    单轮扫描步数上限、延后步骤轮询上限、结构文件体积上限（宽×高×长）；
    强制加载区块上限改接已有配置项（BuildPlan 不再用自带常量）
  - **挖矿**：挖掘距离（格，原手长 4.5）、目标超时、锚点出框超时、重定位节流、
    迁移停滞判定、搭方块冷却、废石清理间隔、播报限频（均 tick）、创造面板默认价值
  - **AI 记忆**：扫描间隔（秒）、投影字符上限、提取超时（分钟）、检索融合参数 RRF k、
    衰减周期（天）、衰减保留重要度、关系感知扫描（秒）、信任/恐惧变化阈值
  - **感知**：快照扫描间隔（tick）、同类事件限频（秒）、主人低血阈值（%）、
    持续注视判定（秒）、看向进入/退出角度（度）
  - **情绪**：静默恢复间隔（秒）
  - **对话提示**：播报检查间隔（tick）、主动对话扫描间隔（秒）、主动关心低血阈值（%）、
    事件驱动冷却（秒）、自主检查间隔（秒）、自主触发主人范围、自主工作开始/结束时刻
  - **战斗自保**：安全回归血量、贴身距离、近战/远程搭高上限、治疗冷却、搭方块冷却（近战/远程）、
    威胁扫描间隔、威胁消失退出、逃跑速度、卡住窗口/位移阈值、警示粒子间隔、策略播报间隔、
    传送回家冷却（均 tick 或数值）、落地水保持/下探格数、施工区避让扫描间隔
  - **杂项**：整理/烹饪/酿造垂直搜索范围（三任务共用）
  - 面板内所有数值改动即时热生效（ForgeConfigSpec.set），关闭面板保存落盘；
    原硬编码常量全部删除/改读配置，行为逻辑零变化（默认值与原常量一致）
- **1.5.101f 末影珍珠逃生 CD 缩短 + 数值全部进配置面板**：
  - **珍珠逃生冷却 60 秒 → 5 秒**（1200 → 100 tick），玩家可调
  - **珍珠三数值进配置面板**（战斗自保页，即时热更新）：冷却（tick，默认 100）、
    触发血量（0-1，默认 0.30）、威胁距离（格，默认 8.0）——玩家自己控制，
    不再硬编码
- **1.5.101e 珍稀掉落物捡不到提示（气泡 + 主人聊天栏双提示）**：
  - 珍稀标记矿物的掉落物处于**捡不到**状态（被硬挡路如基岩/箱子挡住、掉进深处
    或被推远）放弃回收时：女仆**头顶气泡** + **主人聊天栏**同时提示（含原因与
    坐标；每 30 秒限频，不刷屏）
- **1.5.101d 珍稀标记矿物（掉落物回收改为"标记格"机制 + 手册第三个创造物品栏名单）**：
  - **标记格机制（按你的设计重构）**：打破**珍稀标记矿物**名单内的矿物后，标记
    其**所在格**——此后所有判定都以标记格为准（**即使该格已变空气**）：
    标记格附近找掉落物 → 够得着就捡；隔墙（可挖穿的）**挖穿推进**（引用挖矿的
    视线连线+挡路判定，流程跟挖矿一样）；**完成判定 = 掉落物被拾起**（标记格
    附近无存活掉落物 → 标记消失）。普通矿物/方块掉落物不标记（即时回收 1.5 格，
    不投入隔墙回收成本）
  - **手册调试项**：挖矿板块新增**"珍稀标记矿物"**名单（第三个创造物品栏名单，
    与目标矿物/障碍物同款交互：点击 toggle、已加入金框+✓、输入添加、列表删除）；
    切换按钮三态：目标矿物（绿）/ 障碍物（青）/ **珍稀标记矿物（金）**
  - 配置存 `mine.rareOres`（完整注册名如 `minecraft:diamond_ore`）
- **1.5.101c 挖矿掉落物隔墙回收 + 拾取优先级按价值（原版低价值 0.5）**：
  - **掉落物回收（修复"挖完矿捡不到"）**：挖矿打破矿物后，没被即时回收的
    掉落物（隔墙/超出 1.5 格）记入**待回收队列**——女仆对掉落物引用挖矿的
    **视线连线+挡路判定**：可挖穿的（泥土/圆石/石头/原木等）挖穿推进，无挡路
    直接走过去，够得着就捡；硬挡路/掉虚空/超 12 格才放弃。解决"两格高泥土
    只挖穿一行→矿物挖完→泥土没清完→走不过去捡→不了了之"
  - **拾取优先级改造**：不再"挖矿×N"放大——TLM 拾取任务返回的可见物品列表
    按**价值重排**：原版低价值物品（废石/沙/粘土等 23 种，仅 minecraft 命名空间）
    权重 **0.5** 排后，其余（矿石/工具/**模组物品权重 1**）排前；同权保持原顺序
    （距离近优先）。模组物品天然权重 1，不会被原版垃圾挤掉
- **1.5.101b 挖矿黑白名单升级（目标矿物 + 障碍物 双名单，创造面板 toggle + 输入添加恢复）**：
  - **双名单**：挖矿-名单管理子页顶部「目标矿物 / 障碍物」切换按钮——矿物名单
    （id=价值，决定挖什么矿）与障碍物名单（path 名，决定哪些方块可被挖穿而非
    当硬挡路报点弃置）分开管理
  - **创造面板 toggle**：点击方块图标**加入当前名单**；已在名单的**再点一次取消**
    （原来"已添加跳过"改为可逆切换）
  - **已加入标记**：网格中已加入当前名单的方块显示**彩色框 + ✓ 角标**
    （矿物=绿框，障碍物=青框），一目了然
  - **输入添加恢复**：名单子页恢复"输入 id 添加"框（矿物格式 `id=价值`、
    障碍物格式 `id`，障碍物自动去 namespace 存 path），留在原位
  - **障碍物名单生效**：`MaidMineBehavior` 的挡路可挖判定（isOpenStone/
    findBlockingBlock/穿透预算）统一走 `isBreakable`——内置 OPEN_BREAKABLE
    + 面板障碍物名单合并判定；配置存 `mine.extraBreakables`
- **1.5.101 修复 AI 记忆开关崩溃（TaskData ByteTag 强转 CompoundTag）**：
  - **现象**：手册记忆页点女仆的「记忆:开/关」按钮 → 几秒后游戏崩溃
    `ClassCastException: ByteTag cannot be cast to CompoundTag`（TLM
    TaskDataRegister.writeSyncData）
  - **根因**：per-maid 记忆开关（v1.5.86）注册成 TLM TaskData `Codec.BOOL`——
    编码结果是 ByteTag，而 TLM 的 TaskData 同步（每 tick getUpdateTag）把编码
    结果强制 `(CompoundTag)` 强转 → 一写就崩（开关从未在游戏里被点过，今天
    第一次点即触发）
  - **修复**：开关迁移到 **Forge persistentData**（`maid_smart_memory`，Byte，
    随女仆实体存档持久化），不再经过 TLM TaskData 同步机制——从根上消除崩溃；
    不再注册该 TaskData key（服务端读写，无需客户端同步，手册列表/详情由
    服务端 isEnabled 取值不受影响）
- **1.5.100d 修复女仆莫名传送（施工区防接近传送无冷却反复拽）**：
  - **现象**：女仆们莫名其妙地自己传送（玩家/女仆站在激活的施工区内被反复瞬移）
  - **根因**：`BuildPlan.suffocateCheck`（每 20 tick 跑一次）的"只要进入施工区块就
    传送"（v1.5.84）**没有任何冷却**——v1.5.92 把原 20 秒传送冷却删了。只要存在
    激活的建造计划，施工区（蓝图包围盒）内的任意女仆和玩家本人每 1 秒就被传送
    一次（比如玩家站在施工区看建造、或蓝图包围盒覆盖了基地）
  - **修复**：加回每实体 **10 秒传送冷却**（regionTeleportReady）——传一次出去后
    10 秒内不重复拽，再走进施工区才重新计时（防原地闪烁）；原"防窒息"意图不变
- **1.5.100c 开路白名单批量扩容（原木等自然方块）+ 矿物扫描加速**：
  - **原木不挖修复**：`OPEN_BREAKABLE` 批量加入自然生成、硬度相近的方块——
    全部原木/菌柄（橡木~樱花、绯红/诡异菌柄）+ 去皮变种 + 竹块 + 蘑菇方块 +
    南瓜/西瓜 + 冰/浮冰 + 苔石/苔石砖 + 珊瑚块（含死珊瑚）。女仆挖矿遇到
    这些不再当"硬挡路"报点弃置，直接挖穿（只进开路白名单，不进废石丢弃——
    挖出的原木保留）
  - **扫描加速**：矿物检索间隔 10 tick → **3 tick**（0.15 秒）——发现矿物更
    及时；汇报频率独立不受影响（reportSkippedOre/reportBlockedOre 原有 30 秒限频）
- **1.5.100b 挖矿目标拖拽 + 矿表创造面板 + 记忆页全面增强 + 面板细节修正**：
  - **挖矿目标拖拽**：有目标（矿/挡路块）但够不着时，若女仆水平速度过低（闲逛/
    发呆/寻路失败）就朝矿物方向施加缓慢拉力（0.1 块/tick ≈2 块/秒）；正常寻路
    不干预；撞墙由 MC 移动碰撞天然阻隔，不会穿墙。解决"一边散步一边挖、不朝
    矿物走"的消极
  - **矿表子页创造物品面板**：废弃"手输 ID 添加"——上方搜索框 + 物品网格
    （全部可放置方块，8 列分页），**点击方块图标即添加（默认价值 300）**，
    已添加的格子绿底高亮，悬停显示方块 id；下方已添加矿表列表（删除）
  - **女仆记忆页**：列表从"128 格内建造女仆"改为**全部女仆**（不限距离不限任务，
    按名字排序）；每行新增**记忆开关按钮**（记忆:开/关，点击即切换，服务端写
    per-maid TaskData）；"（也可在女仆配置界面切换）"提示改为**居中**显示在
    记忆详情页标题下（旧版塞进记忆行左对齐不居中）
  - **女仆管理页**：无翻页按钮时"当前没有进行中的建造计划"等状态文本移到页面
    底部（旧版 h-84 偏上，下方空一大片）
  - **配置面板**：页码"第 x/y 页"上移（h-66 → h-92，不再与"下一页"按钮重叠）；
    开关按钮加宽 60→130（CycleButton 显示"标签：值"，窄按钮长文本会被截断滚动）
  - **手册改名**：蓝图手册 → **Promaid 功能手册**（界面标题）
- **1.5.100 配置面板重构（手册式跳转+翻页）+ 手册目录按钮调小**：
  - **配置面板弃用横排 Tab + 滚轮**（横排在窄屏/GUI 缩放大时超出屏幕、滚轮有 bug）：
    - 目录页：两列竖排 9 个板块按钮，点击跳转（手册同款交互）
    - 板块页：内容行按页翻页——"< 上一页 / 下一页 >"按钮 + "第 x/y 页"页码，
      无滚轮；每页行数按窗口高度自适应
    - 挖矿板块：第一行"可挖掘方块表 →"入口，点进**矿表子页**整页管理
      （矿表列表内自带滚动，添加框 + 删除），"← 返回参数"返回
    - 所有板块页底部："← 返回目录" + "保存并返回" + "完成"
  - **手册目录页按钮调小**：200×40 → 170×28、间距 70→36——新增"模组详细配置"
    后原 4 按钮排到 y=290 与贴底进度条重叠；调小后底部给进度条和未来新入口留空间
- **1.5.99c 挖矿挡路判定根因修复（SRG 映射错误）+ 自动装备全面激活**：
  - **根因**：`ResourceLocation.m_135827_()` 是 **getNamespace**（返回 "minecraft"），
    不是 getPath！javap 字节码实证：`ResourceLocation(String,String)` 构造器把
    参数 1（namespace）存入 `f_135804_`，`m_135827_()` 返回 `f_135804_`；
    `m_135815_()` 返回 `f_135805_`（path）才是 getPath
  - **修复 6 处**：`MaidMineBehavior` 的挡路可挖判定（`findBlockingBlock`）、
    可开路废石判定（`isOpenStone`）、穿透预算计数、废石丢弃白名单、镐等级
    播报（tag path），`MaidToolAutoEquip` 的任务 UID 匹配（ns/path 颠倒）
  - **效果**：泥土/圆石/石头/安山岩等挡路方块现在会被**挖穿推进**到矿物
    （旧版全被当"硬挡路"→ 报点弃置，被泥土包裹的矿永不挖掘）；穿透预算
    不再把软方块算阻挡 → 埋地矿可选；**自动装备镐/剑/弓/弩首次真正生效**
    （旧版 ns/path 拿反，任何任务都匹配不上）
  - 顺带修复：`requiredTool` 播报能正确说出"钻石镐/铁镐/石镐"而非笼统"镐子"
- **1.5.99b 感知播报全局限频 + Blocker 崩溃排查**：
  - **多女仆刷屏修复**：`PerceptionManager` 限频从【按女仆各自计】改为【全局限频】——
    17 只女仆同秒不再各喊一遍"天气变了/主人看我"（同一事件全服 30 秒只报一次）
  - **Blocker 崩溃排查结论**：`NoClassDefFoundError: MaidMineBehavior$Blocker` 的日志
    是旧版 jar（06:33，v1.5.98b）产生的——当前 jar 已确认含 Blocker.class 且完整一致；
    该错误是旧 jar 与运行时状态不一致所致，**完全关闭游戏+PCL 重启后加载新 jar 即可**
  - 附带说明：日志中"主人不看我了…"为 v1.5.98c 已删的旧逻辑残留，重启后消失
- **1.5.99 配置面板 UI 重构（可滚动 + 加大行距 + 标签对齐）**：
  - **可滚动内容区**：9 个 Tab 内容超出屏幕时**鼠标滚轮上下滚动**（右下角显示
    "↑↓ 滚轮滚动"提示）；编辑框聚焦时不滚（防重建丢输入）；挖矿页矿表列表内
    滚轮仍滚矿表（互不干扰）
  - **标签不再重叠**：行标签改为按【每行自身坐标】绘制（旧版所有 section 标题
    画在固定 y=56，与第一行按钮重叠）；滚动后超出内容区的行自动跳过不画
  - **行距加大**：28 → 32，按钮高度 20 → 22——文字不拥挤，可读性提升
  - **挖矿页布局**：矿表（100px）+ 添加框并入滚动流（作为一整行），后续 8 个
    数值项跟随滚动，不再固定死在屏幕底部
- **1.5.98c 修复站桩女仆溺水 + 主人注视感知刷屏**：
  - **女仆一直受伤（溺水）修复**：`MaidStationaryMixin` 旧版把站桩女仆的 `m_7023_`
    （travel）整个 `ci.cancel()`——连垂直运动也掐死。站桩女仆在水里（河边整理/水边
    工作）无法上浮呼吸 → 持续溺水伤害。改为【只清水平速度 x/z、保留垂直 y】且不
    cancel——水平不动（站桩），垂直照常（水上浮/下落/落地），杜绝溺水
  - **"主人一直在看我"刷屏修复**（`PerceptionManager`）：
    - 删掉「主人不看我了…」事件（不看是常态，无信息量）
    - 加**滞回状态机**：进入"看向"需夹角 <35°，退出需 >55°，中间区间保持原状态
      ——消除玩家稍微转头导致的"看/不看"临界抖动
    - 加**持续注视计数**：连续注视 ≥3 秒（60 tick）才播报一次「主人一直在看着我…」
  - 附：`_read_log.py` 等排查脚本已清理；Xaero 世界地图缓存清理见上条
- **1.5.98b 启动崩溃修复（换回原版 TLM 后 mixin apply 失败）**：
  - **崩溃根因**：`MaidConfigMemoryMixin` 的 `@Shadow m_142416_`（Screen.addRenderableWidget
    的 SRG 名）在【原版 TLM】的 `MaidConfigContainerGui` 上定位失败 → mixin apply 崩溃。
    之前正常是因为 SMART.jar 是 SRG 重映射版（m_142416_ 存在）；换回原版 TLM
    （未混淆，开发名）后方法名不匹配。无 refmap 环境下 @Shadow 无法定位【继承】
    成员（Screen/ContainerScreen 的方法与字段）。
  - **修复**：两个受影响 mixin 全部绕开继承 @Shadow——
    - `MaidConfigMemoryMixin`：按钮添加改反射调用 `Screen.m_142416_`（protected，
      泛型擦除参数 GuiEventListener）；按钮定位改 public 宽高 f_96543_（屏幕右上角，
      不依赖 leftPos/topPos）
    - `MaidDebugPanelMixin`（同因的定时炸弹）：`@Shadow m_142416_` → 反射；
      `@Inject(method = "m_7856_")` → 开发名 `init`（maidmarriage 未混淆类）
    - `ChatBubbleLimitMixin`：去掉误用的 `@Shadow`（私有静态方法不需要 shadow）
  - 保留：`MaidDebugPanelMixin.maidUuid`（maidmarriage 类【自身声明】字段，开发名
    @Shadow 可定位 ✓）
- **1.5.98 关系记忆适配层（模组特色联动：记忆系统软感知心契誓约×爱憎分明）**：
  - **maidmarriage（心契誓约）状态 → 记忆**：结婚 → 写关系三元组「主人-是-妻子」+
    永久记忆段「主人和我是夫妻了」（salience 10）；告白完成 → 「恋人」；父女 → 「女儿」。
    心契誓约的相恋直接影响记忆系统的关系条目——气泡告知（"主人和我是夫妻了"）
  - **Love Loathe（爱憎分明）信任/恐惧 → 记忆**：信任显著下降（-15）→ 情绪记忆
    「主人最近让我有些不安」；恐惧显著上升 → 「我对主人产生了恐惧」
  - **TLM 好感等级 → 记忆**：等级提升写关系段（原生无依赖）
  - **与 LLM 对话不冲突**：mod 状态用专用 fact key（fact:marriage_state 等），
    玩家 remember 工具写自由 key——两者共存，注入按 salience 排序，冲突覆盖只
    在【同 key】时生效，互不覆盖
  - **软依赖**（复用 heartfelt 反射模式）：ModList 检测 + TaskDataRegister 读数据 +
    反射 record accessor——不引用 maidmarriage/Love Loathe 任何类，未装完全静默
  - **对话注入**：关系段前置「关系状态：主人是我的妻子/恋人/女儿」标签
  - **开关**：配置面板记忆页「关系感知适配」（默认开）
- **1.5.97 Soul 灵魂系统（跨存档/换女仆迁移记忆）**：
  - **灵魂核心物品**（`maid_smart:soul_core`，创造物品栏可用 / 外观=末影之眼）：
    - 右键【未绑定】女仆 → 自动创建新灵魂绑定（soulId 自动生成，写女仆 NBT +
      全局档案 `config/maid_smart/souls/<id>/soul.json`）
    - 右键【已绑定】女仆 → 显示绑定信息（灵魂名/最后世界/最后女仆/是否已迁移）
    - 潜行右键【已绑定】女仆 → 解绑（档案与记忆保留，不再自动迁移）
  - **跨存档迁移**（`SoulMigrationHandler`）：女仆生成/加载时检测绑定 → 档案
    lastMaidUuid/lastWorldId 与当前不符 → **复制 `promaid_memory/<旧uuid>` →
    <新uuid>**（含段落/关系/画像/情绪/工作笔记全量记忆）→ 气泡+聊天告知
    "灵魂的记忆跟着我过来了"
  - **幂等安全**：迁移后档案更新为当前女仆（不重复触发）；目标已有记忆不覆盖；
    旧目录改名 `.soul_backup` 备份；无记忆可迁则静默
  - 权限：仅女仆主人或 OP 可操作
- **1.5.96 全子系统精准开关体系（防未来 agent 冲突）+ Soul 系统评估**：
  - **配置面板扩为 9 个 Tab**（新增感知/情绪/AI 工具 3 段，顶部两排 Tab）：
    - **AI 记忆页新增子功能开关**：关系注入 / 冲突覆盖 / 摘要折叠 / 工作笔记注入
      （关掉任意一个即回退旧行为——接更强 agent 时可按需关闭让位）
    - **感知页**（新）：感知总开关 + 敌对检测 / 主人检测 / 天气检测 独立开关
    - **情绪页**（新）：PAD 情绪层总开关 + 注入对话开关
    - **AI 工具页**（新）：remember（主动写记忆）/ working_note（工作笔记）开关
  - **各子系统已接入开关**：AiMemoryContext（关系/折叠/笔记）、AiMemoryStore（冲突覆盖）、
    PerceptionManager（总/敌对/主人/天气）、AffectEventHooks（情绪总）、AffectContext（注入）、
    RememberTool / WorkingNoteTool（工具级）
  - **Soul 系统评估结论**：制作成本中等（存储/绑定逻辑简单，大头在跨存档迁移时机 +
    绑定 UI），玩家易控制（灵魂核心右键女仆即可）。**暂缓实施**——记忆文件本身
    （promaid_memory/<uuid>/）已可手动复制迁移，Soul 只是自动化；优先保障开关体系
- **1.5.95 女仆大脑全面加强（记忆 6 项增强 + maidsoulcore 4 个子系统借鉴）**：
  - **记忆增强**：
    - **关系三元组接入**：`relations.jsonl`（主人-喜欢-红茶）不再闲置——对话注入新增
      「关系：」段（高置信度在前），`query_memory` 检索新增关系路（命中权重更高）
    - **冲突覆盖规则**：同一 `fact:key` 的新记忆若重要度更高/永久 → 旧段落标记作废
      （"主人喜欢红茶" → "主人戒了红茶"正确覆盖，旧画像证据同步清理）
    - **检索增强**：uni+bi-gram 混合（中文单字召回更好）+ 长度加权 + 关系路
    - **摘要折叠**：核心记忆（重要度≥8 或永久）常驻；扩展记忆按需；超限截断时
      先丢扩展、核心保留（不再硬截断丢核心）
    - **FACT 置信度排序**：画像段按更新时间降序（内含置信度证据）
    - **记忆可视化**：蓝图手册大目录新增「♡ 女仆记忆」入口——列出 128 格内女仆 →
      点选 → 显示该女仆的记忆段落（重要度排序/永久标记）/关系/画像/开关状态（分页）
  - **借鉴 maidsoulcore 子系统（除记忆外）**：
    - **感知变化检测**（`PerceptionManager`）：每 1 秒采集世界快照对比上一快照 →
      敌对出现/接近/消失、主人受伤/血量低、主人看向/不再看向女仆、天气变化 →
      **纯规则气泡即时播报**（零 LLM token，30 秒同类限频）
    - **LLM 主动写记忆**（`remember` 工具）：玩家说"记住…/别忘了我讨厌…"时 LLM
      当场写记忆（走写入策略分层，关系/承诺自动抬重要度）——与被动提取（攒 12 条
      后台批量）互补
    - **PAD 情绪层**（`AffectManager`）：独立第四套数值（愉悦/唤醒/支配 + 亲密/冲突/
      思念 + 受伤债/修复债），事件驱动（主人互动/被打/静默恢复），注入对话上下文
      （`ai_affect`）——不修改 TLM 好感/心契/爱憎任何数值，完全兼容
    - **工作笔记**（`working_note` 工具）：跨对话任务状态（"帮我修桥"进行中），
      临时可覆盖可清除，注入对话上下文
- **1.5.94 AI 画蓝图（子 Agent 设计器）+ 蓝图手册删除**：
  - **AI 画蓝图（新工具 `smart_design`）**：玩家正常对话说"帮我设计一栋中式庭院" →
    主 LLM 调用 smart_design → 工具返回带**建筑设计师专属提示词**的子 Agent callback
    （TLM 原生子 Agent 分发：`LLMCallback` subagents 构造器 + 官方 "dispatched to a
    dedicated sub-agent"）→ 子 LLM 以建筑师角色用专门 JSON 蓝图格式设计 → 本工具
    解析校验 → `saveJsonBlueprint` 落盘（自动注册进蓝图手册）→ 走【正常建造流程】
    （`BlueprintBuildExecutor.execute`，非强制，材料不足 partial、障碍物拒绝）→
    建造结果（能建/缺料/障碍）由子 LLM 转述给玩家
  - **AI 设计尺寸上限**：配置面板「建造」页新增「AI 设计蓝图上限」（默认 500 块，
    范围 100~20000，想设计多大填多少），提示词自动引用
  - **蓝图手册删除按钮**：建造目录中【外部蓝图】（`maid_smart_ext:`，含 AI 生成
    的 .json）每行右侧出现 ✖ 删除按钮 → 点击弹出【确认警告界面】（显示蓝图名 +
    "文件也会被移除，无法恢复"）→ 确认后服务端删文件 + 清缓存 + 重发目录（被删
    蓝图从手册消失）；内置蓝图（小木屋等）不可删、不显示按钮
- **1.5.93 站桩锁死位移（治"坐着/站桩女仆诡异地动"）**：
  - **根因**：TLM 女仆用自定义 `MaidMoveControl`——当 `getSwimManager().wantToSwim()`
    （想游泳）时，它用 `setDeltaMovement`（m_20256_）**直接施加速度矢量**，绕过
    `WALK_TARGET` 和 `MoveToTargetSink`；且 TLM 多行为直接调 `PathNavigation.moveTo`
    （m_26519_）也会绕过。这些源让"坐着的/站桩的女仆"（尤其在水里/下雨积水）被推走，
    原版（v1.5.24 的 MoveToTargetSink 抑制）拦不住 —— 只堵住了移动源之一。
  - **修复**（新 `MaidStationaryMixin`）：在实体每 tick 的 `m_7023_`（travel）入口拦截，
    站桩标记存在时**把速度矢量归零（Vec3.ZERO）并取消本次位移**——游泳推力、moveTo
    漂移、move 控制推力全部失效。站桩工作是地面系（建筑/烹饪/酿造/整理均站地面），
    不涉及空中垫高/下落，取消位移不会卡半空。与既有的 MoveToTargetSink 交互拦截双层
    锁死。
- **1.5.92 建造期传送抖动根治（建仆强制 home 模式，从 TLM 源头断跟随）**：
  - **问题**：建仆被 `setPlan`/`suffocateCheck` 传送到施工区外后，TLM 本体跟随行为
    （`MaidFollowOwnerTask`）在离主人过远时仍会瞬移回主人身边（主人常在施工区附近）
    → 建仆回施工区 → 又被踢出去 → **反复抖动**。原因是建造站的死标记（站桩）只压制
    `MoveToTargetSink`，而跟随的 `teleportToOwner` 瞬移不受站桩标记控制。
  - **修复（按「建造开始强制 home、完工切回」的本质方案）**：建造行为的
    `m_6735_`（进入）强制 `maid.setHomeModeEnable(true)`——TLM 跟随的
    `maidStateConditions()` 要求 `!isHomeModeEnable()`，home 模式开启后跟随行为
    **从源头不启动**、永不瞬移回主人；`m_6732_`（行为停止/完工/切任务）自动
    `setHomeModeEnable(false)` 切回。区块划定由既有 `BuildPlan.clear`→`releaseChunks` 释放。
  - **移除无效的 20 秒冷却机制**：原 `SAFE_TELEPORT_COOLDOWN`/`isTeleportSuppressed`/
    配置 `suffocateCooldown` 整体删除——它只对"非建仆（路人）"是"被踢出后短暂别
    瞬移回"的临时窗口，而非建仆进施工区本就靠 `BuildAreaAvoidBehavior` **走路避让**
    （不瞬移），该窗口无实际意义。建仆的防回传已由 home 模式从源头保证（更本质、零开销）
- **1.5.91 自保传送加固（双判定 + 珍珠落点安全）**：
  - **传送回主人 = 双判定**：同时判定【自己身边】与【主人身边】两个**圆半径 6
    （直径 12）**范围内都**无可见怪物**（raycast 隔墙不算，与威胁判定一致）才传送。
    两处任一不安全都放行不传、下一轮再评估（不再卡死），双条件满足即传——
    **保证主人身边区域是安全的**（传送前已验证）
  - **传送成功即脱离自保**：双判定通过后传回主人身边 → 立即结束自保（清标记 +
    canContinue 退出），不再干等 5 秒无威胁；主人身边安全由传送前判定保证
  - **珍珠逃生落点安全判定**：落点除危险方块外，再加：脚下实心可站（防落岩浆/
    深渊）+ 站立位 + 头顶 2 格都空旷（防活埋/卡墙，治老版"珍珠瞬移进墙里"）
  - 隐患 3（两套传送互斥）按约定保留不修正
- **1.5.90 挖矿穿墙 + 任务工具自动装备（修复"主手有镐仍不挖挡路方块"）**：
  - **挖矿穿透挖掘重构（"非矿物挡路只报不挖"根因）**：旧版只有"矿在伸手范围内"
    才检查挡路方块——矿被厚土墙/岩壁隔着时女仆永远够不着矿 → 走到墙边站着不动
    （或 15 秒超时弃置），绝不挖开挡路方块。现在**只要挡路块在伸手范围内就开挖**
    （挖穿一层再评估下一层，逐层推进到矿）；挡路块还够不着时朝挡路块脚下推进
    （新 `walkToBlockFace`，不朝矿——矿在岩壁深处走不到）。深埋矿/厚墙矿全部可挖穿
  - **自动装备修复（v1.5.89 写错位置的根治）**：女仆的**主手不是 getMaidInv() 的
    slot 0**——getMaidInv() 是 36 格背包（MaidBackpackHandler），主手/副手是独立的
    `getHandsInvWrapper()`（slot 0 = 主手）。旧版把镐塞进背包 slot 0、真正的主手
    依然空着 → 镐放背包时挖矿判定全部失败。新 `MaidToolAutoEquip` 统一走
    getHandsInvWrapper() 换装，主手原物品 1:1 换回背包
  - **自动装备推广到原 mod 全部战斗任务**（新 core 行为 `MaidToolAutoEquipBehavior`，
    优先级 200，canUse 里换完即停不占行为槽）：
    - 挖矿 `maid_smart:mine` → 镐（自动挑等级最高的一把：钻石 > 铁 > 石 > 木/金）
    - 攻击 `touhou_little_maid:attack` → 近战武器（带攻击力属性：剑/斧/三叉戟）
    - 弓 `touhou_little_maid:ranged_attack` → 弓
    - 弩 `touhou_little_maid:crossbow_attack` → 弩
    - 三叉戟 `touhou_little_maid:trident_attack` → 三叉戟
    - 主手已是合适工具时零开销；弹幕/待命/工作类任务不换工具
- **1.5.25 落地水（被动生存技能）**——女仆自动落地水：
  - **独立 core 行为（`WaterClutchBehavior`，优先级 240，低于自保 250）**：
    不进自保状态、不触发逃跑/搭高，与自保完全互不干扰
  - **触发条件**（全部满足才放水）：背包有**水桶**（没水桶不触发）+ 正在下落
    + 未落地 + 脚下 4 格内即将触地 + 不在水里
  - **流程**：即将落地瞬间在脚下放一滩水 → 落地缓冲 → **1 秒（20 tick）后自动收回**
    （水方块移除 + 空桶→水桶，水桶循环使用不凭空消耗）
  - **播报**：放水时气泡"落地水！"（让主人看见）
- **1.5.25 续建修复（生存分批建造闭环）**——超大型建筑（上万块）背包放不下，
  分批建造的 3 个断裂点全部打通：
  - **续建不再被"障碍物"挡住**：`findObstacles` 预检时把**已建好的目标方块
    （或等价族替代品）排除**——补料后重新下达蓝图，已建部分不再被误判为
    "建造区域内有障碍物"而拒绝续建
  - **续建不再错位（原点锁定）**：`execute`/`startBuild` 下达时**沿用已有同名
    计划的 origin**（女仆脚下仅用于首次建造）——分批补料期间女仆走动/被挪位，
    已建判定也不会错位，不会重复建造
  - **续建不再重复吃材料**：新增 `BlueprintLib.filterBuilt()` 先过滤已建格，
    材料预检/障碍预检/partial 截断**全部基于未建部分**——补料后新料 100%
    用于未建部分，不再被已建部分消耗配额（旧版 LLM partial 截断会把已建
    部分算进材料，续建永远不够料）
  - 缺料气泡文案更新：提示"放进主人背包里，再让我建一次"（材料以主人背包
    为准，v1.5.24 起自动交付）
- **1.5.9**（BALANCED 补丁）：修复"第 6 格误判"——爱憎分明原版逻辑把女仆背包第 6 格
  出现物品都当成"主人放礼物"（信任+1/恐惧-2 + 台词"主人把东西放在你头上"）。
  女仆自己捡起物品也会先进第 6 格 → 误触发。现在扫描时先对比女仆周围掉落物
  （消失 = 刚捡起），第 6 格变化若是自己刚捡的同一物品则**豁免**，不再误判
- **1.5.25**（当前）：**搭方块/挖矿搭高/酿造全自动/整理增强（测试反馈）**——
  - **搭方块（自保）**：
    - **放置格修复（"搭高成功率低/只会平搭"的根因）**：`buildUp`/`pillarUpStep`
      旧版要求"所在格必须空气"——女仆站在平地上脚部格是地面方块 → 搭高永远失败，
      只剩悬空等罕见场景能触发。现在：**脚下悬空 → 垫所在格站上去；脚下实心（平地）
      → 垫身体格**（方块放置的实体挤压机制把她顶起一格，不碰地面方块）
    - **每 tick 窒息保险**（`antiSuffocate`）：身体中心（半身位置）所在格为实心
      满块 → 立即 setPos 移到方块顶面上方——搭方块位移滞后/放偏时的兜底，
      宁可瞬移半步也不让她被自己搭的方块闷住
    - **防乱垫浪费**：`bridgeStep` 前方 2 格必须"实心满块"才算墙（树叶/高草/水
      不再触发垫台阶）→ 不再原地平搭
  - **挖矿搭高（取钻石成功率）**：
    - **三选一搭路决策**（`approachOre`，每 tick 重算直到够得着）：
      ① 上方矿 + 水平近（dy≥2 且 hDist≤2.5）→ 脚下搭高；
      ② 斜上方矿（dy≥1 且 2.5<hDist≤4.5）→ 向前垫斜坡台阶（水平+垂直同时逼近）；
      ③ 其他 → 目标方向前方脚下悬空先搭桥，否则走过去
    - **搭高不再误判障碍**：头顶检查放宽——目标矿在正上方时不把它当"障碍"
      直接放弃（旧版把要挖的矿当障碍 → 目标作废 → 成功率低）
    - **只挖自己的**：`placedBlocks` 记录自己搭过的位置（搭高/斜坡/搭桥），
      下柱只挖这些，绝不破坏自然地形
  - **酿造全自动（修复"乱塞材料不酿药"）**：
    - **两阶段状态机**：槽空→补水瓶；水瓶(water)→槽3只放下界疣（唯一打底材料）；
      粗药(awkward)→槽3放二次材料；成品（water/awkward 之外）→收进背包；
      一次只推进一个阶段
    - 根因：金萝卜/烈焰粉/糖等材料**不能直接作用于水瓶**——必须先
      水瓶+下界疣=粗药，再粗药+材料=真药水。旧版不管基底直接塞槽3 →
      酿造台永不反应（"乱塞金萝卜/烈焰粉/水瓶却不会酿"）
    - **材料白名单收紧**：只留正向材料（治疗/夜视/力量/迅捷/跳跃/防火/水肺/
      再生/缓降），删除会酿出中毒(spider_eye)/虚弱(fermented_spider_eye)的
      负面材料，以及对粗药无效的 redstone/glowstone_dust/gunpowder
  - **整理增强（不再"聊胜于无"）**：
    - **一次批量清空背包**（旧版每 40 tick 只搬 1 组，30 格背包要 2 分钟才清完）
    - **工具/武器/装备不倒进箱子**（镐/剑/盔甲留在背包，不会整理一次全扔）
    - **箱子内部同类合并**：把箱子里分散的小堆叠合并成大堆叠（腾出格子）
    - **收纳反馈**：整理完成气泡"收进了箱子 N 组（M 个物品）"，背包清空后
      不再重复播报
  - **蓝图手册材料显示（v1.5.25）**：
    - **展开显示全部材料**：旧版展开最多 6 行（18 种）就截断为"…还有 N 种"，
      大蓝图（如宫殿/海景房 20+ 种材料）看不全。现在展开**显示全部**，
      条目高度按展开行数动态伸缩，分页改为【按高度分页】——展开高的条目
      自动占多行空间，不与下一条重叠
    - **创造模式 ∞**：旧版创造模式材料"已有"直接用 `Integer.MAX_VALUE`
      （21 亿 4748 万）拼进文本 → 显示 `2147483647/64`。现在显示 **∞**（无限符号），
      缺口判定不变（∞ ≥ 需求 → 绿色"材料充足"）
  - **坐下也能干活（"不站桩"的终极保险）**：根因 = TLM 坐下/坐垫/骑乘时把
    activity 从 WORK 切到 **RIDE_WORK**，而 RIDE_WORK 的任务行为来自
    `IMaidTask.createRideBrainTasks()`（默认空）——我们的任务只实现了
    `createBrainTasks()` → 坐下后任务行为全部消失。现在 5 个任务
    （mine/organize/cook/brew/build）全部补上 `createRideBrainTasks`
    （返回与 createBrainTasks 相同的行为）：**坐下 = 物理不动 + 任务照常执行**——
    站桩任务（烹饪/酿造/整理/建筑）坐在地上干活，MoveToTargetSink 抑制
    双保险下彻底站得住；自保（CORE，任何 activity 都运行）不受影响
  - **新增 30 个建筑蓝图（三批：生存实用 12 + 高端宫殿别墅 10 + 高科技 8）**：
    程序化生成（`blueprint_gen_lib.py` 框架 + `gen_survival/gen_luxury/gen_tech.py`），
    全部走结构文件黑名单通道（任意方块 + 精确状态），放进
    `config/maid_smart/blueprints/`（重进游戏即用）与 jar 内置目录。
    - **生存实用快速屋（12 个，97~416 块，便宜材料+实用家具）**：
      `survival_hut` 生存小屋（床/熔炉/工作台/箱）· `miner_shack` 矿工棚（双熔炉大箱）·
      `farm_cabin` 农家小屋（栅栏院+干草堆+小麦南瓜）· `fisherman_hut` 渔夫小屋（木桶+钓鱼台）·
      `lumberjack_cabin` 伐木小屋（全原木墙）· `desert_hut` 沙漠小屋（砂岩）·
      `snow_cabin` 雪地小屋（圆石+烟囱）· `watchtower` 瞭望塔（5x5 高 14，梯子+城垛）·
      `root_cellar` 地窖入口（地下储物间）· `nomad_tent` 游牧帐篷（羊毛+原木，97 块最快）·
      `forge_shed` 锻造棚（三熔炉阵列）· `cliff_camp` 崖边营地（背崖防御）
    - **高端宫殿别墅（10 个，442~1173 块，石英/金/深板岩/混凝土/海晶灯）**：
      `royal_palace` 皇家宫殿（柱廊+金塔+四角塔楼）· `luxury_villa` 豪华别墅（玻璃幕墙+泳池）·
      `golden_temple` 金顶神庙（柱廊+金穹顶）· `crystal_palace` 水晶宫（玻璃+海晶灯）·
      `renaissance_mansion` 文艺复兴庄园（红陶瓦斜顶对称双翼）· `oriental_palace` 东方宫殿
      （红墙金瓦翘檐+四角塔）· `marble_villa` 大理石别墅（石英+花园）·
      `castle_keep` 城堡主堡（17x15 四角塔城垛）· `enchanted_manor` 附魔庄园（紫水晶+书架墙）·
      `jade_garden_house` 翡翠花园宅邸（池塘+藤架回廊）
    - **高科技功能建筑（8 个，330~655 块，玻璃幕墙/红石灯/海晶灯/铁块+功能方块）**：
      `high_tech_lab` 高科技实验室（附魔/酿造/熔炉组/书架墙）· `solar_energy_tower` 太阳能塔
      （发光层塔）· `data_center` 数据中心（熔炉阵列+漏斗+红石灯）· `reactor_core` 反应堆核心
      （铁块核心+玻璃外壳）· `command_center` 指挥中心（瞭望台+信标）· `sci_fi_bunker`
      科幻基地（双层+铁门+熔炉组）· `observatory` 天文台（石英圆顶+黑曜石望远镜座）·
      `teleporter_hub` 传送枢纽（黑曜石门框+紫珀大厅）
    - 注：水/岩浆在黑名单不能放，泳池/池塘用**海晶灯"水"底**表现；床/门/漏斗等带
      精确方块状态（facing/part），女仆放置后立即可用
    - **高难度宏大建筑（5 个，方块上万，≤32768 上限）**：
      - `giant_cat_statue` 大猫猫雕塑（44x52x35，14153 块）：坐姿橘猫——椭球身体+
        白肚皮+大圆头+三角耳+黑眼睛+粉鼻嘴+白胡须+双前爪+上翘尾巴+深橘背斑
      - `oriental_dragon` 东方神龙（59x71x56，12288 块）：双圈盘旋腾空——螺旋龙身+
        白肚皮+金背鳞+红腹纹+龙头（金角/发光眼/白须）+4 龙爪+渐细龙尾尾鳍
      - `colossal_knight` 巨像骑士（45x73x45，10883 块）：持剑人形巨像——三层基座+
        双腿（金腿甲）+胸甲椭球+金肩甲+竖立巨剑（白剑身/金剑柄）+头盔+红披风
      - `space_rocket` 太空火箭（64x65x53，11017 块）：发射台+火箭主体（红白条纹+
        舷窗+舱门）+锥形整流罩+4 助推器+橙黄尾焰+铁框玻璃发射塔架+塔顶吊臂
      - `ancient_tree` 远古神树（53x76x45，19610 块）：放射树根+渐细苔石树干+树洞+
        4 粗枝+7 层巨冠（深浅树叶）+24 处海晶灯/萤石"萤火"点缀+树顶灯
      - 提示：单蓝图上万块，单女仆建造约 2~4 小时；用**创造模式**（材料 ∞）或
        **多女仆共同建造**（速度 ×N）实测更高效；材料不足会自动续建
  - **模组联动平衡（关系豁免 + 对话频率 + 记忆可视化）**：
    - **关系恐惧豁免（"妻子/女儿还会怕你"的违和感根治）**：新增 `RelationshipExemption`
      实时判定关系（结婚 `marriage_data.married` / 恋人 = 已婚 或 好感≥192 / 父女 =
      `child_state_data.child` 且父亲==主人，全部反射 maidmarriage，软依赖）；
      mixin 拦截 Love Loathe 恐惧值唯一写入入口 `EmotionData.addFear/addFearFloat`：
      关系成立时恐惧值**不再上涨**（恐惧对话/背叛/逃跑从源头断供），恐惧增量按
      **2:1 换算为好感度扣除**（她不再"怕"你，但你的伤害让她"心凉"）；信任照常。
      **好感跌破 192 → 豁免解除 → 恐惧机制自动恢复**（判定实时，天然实现"跌回
      陌生人线重新触发恐惧话题"）
    - **对话频率收敛**：maid_smart 主动对话 **20 → 4 次/天/女仆**（ProactiveDialogueManager
      DAILY_LIMIT）；新增全局 **API 配额 `ApiQuotaManager`：所有女仆单一天合计主动
      LLM 调用 ≤10 次**（按游戏日重置），覆盖 maid_smart 全部 6 处主动调用 +
      Love Loathe 的主动对话（`EmotionActiveDialogue.triggerAIDialogue` /
      `EmotionPassiveManager.triggerWitnessDialogue` 用 @Pseudo mixin 拦截，未装
      Love Loathe 静默跳过）。**被动对话（玩家发起聊天/广播）不经过配额，不受限制**
    - **长期记忆可视化**：`MaidMemoryManager` 写入改 `setAndSyncData`（记忆同步到
      客户端 TASK_DATA_SYNC）；mixin TLM 女仆配置界面 `MaidConfigContainerGui` 的
      `renderAddition`，在界面右侧空白区绘制**"长期记忆"面板**——关系状态（妻子/
      女儿/恋人）+ 4 条记忆事实 + maidmarriage 剧情状态（结婚/怀孕/心情/告白/女儿），
      只读展示
    - 称呼问题说明：关系已注入 AI 记忆（"我是主人的女儿/妻子"），LLM 应据此称呼；
      若仍叫"主人"可后续 patch TLM 系统提示词兜底
    - **爱憎分明 × 心契誓约隔离（关系女仆专属，v1.5.26）**：
      - **关系女仆永不进入 4 种情绪状态**（背叛/淡忘/溺爱/死忠之外的负面）：
        拦截 `EmotionBetrayalManager.triggerBetrayal`（不背叛：不攻击主人/破坏/爆炸）、
        `EmotionForgettingManager.triggerForgetting`（不淡忘：不传送走/解除契约）、
        `EmotionDotingManager.attackNearbyMaids`（不争风吃醋攻击其他女仆）；
        **死忠（Devoted）的增益/保护保留**——关系女仆始终以"忠诚"响应：
        攻击加成/减伤/治疗主人/攻击背叛者照常生效
      - **争风吃醋整体隔离**：主人已拥有任一关系女仆（`ownerHasRelatedMaid`）后，
        **所有女仆不再争风吃醋**（包括女仆之间互相吃醋）；好感跌破恋人线 192 →
        隔离解除、恢复正常
      - **正常女仆不把关系女仆当行动对象**：`FamilyProtectionManager` 扩展——
        受害者是关系女仆（妻子/女儿/恋人）→ 无条件取消伤害（不依赖攻击者好感）；
        背叛女仆除外——背叛女仆的目标优先级改为 **主人 > 妻子/女儿 > 普通女仆**
        （`EmotionBetrayalIsolationMixin` 改写 selectTarget 返回值）
      - **关系传播**：新增 `RelationBroadcastManager`——女仆与主人结成关系后，
        其他女仆**得知此事**并主动调用 LLM 对话（祝福/感慨/调侃，按关系类型给
        提示词）；每女仆每游戏日最多广播 1 次，纳入全局 API 配额；好感跌破阈值
        关系消失后不反复广播，恢复后重新广播
      - **战斗女仆反击背叛女仆（v1.5.26）**：新增 `AttackBetrayerBehavior`
        （core 行为优先级 200，低于自保 250）——**战斗状态**的女仆（当前任务是
        攻击类任务：近战/弓/弩/三叉戟/弹幕/枪械）主动攻击附近 24 格内最近的
        背叛女仆（反射 `EmotionBetrayalManager.isBetraying` 判定，写
        ATTACK_TARGET 由 TLM 攻击行为执行）；**其余状态（跟随/待命/工作等）
        不做处理**，**自保逃跑中（血量低、PRESERVE_TAG 置位）不整改**——
        行为自动让位
      - **结婚豁免语义（用户确认）**：**结婚永久**——maidmarriage 的正式婚姻状态
        不随好感跌破而解除（好感波动不"自动离婚"，解除由 maidmarriage 自己的
        离婚/剧情机制管）；好感跌破 192 只影响**恋人**线（重新恐惧/争风吃醋），
        但妻子永远是妻子（即使吵架也不背叛）
- **1.5.24**：**五项测试反馈修复 + 海景房蓝图 + 材料展示重构 + 6 种风格建筑**——
  - **6 种新风格建筑**（新增，共 12 个内置蓝图，全部含内部家具/装饰）：
    - `chinese_courtyard` 中式庭院（13x8x11，549 块）：白墙+深色橡木柱+红色陶瓦阶梯瓦顶+庭院石灯花草
    - `gothic_chapel` 哥特教堂（13x13x9，415 块）：中殿+彩色玻璃窗+玫瑰窗+中央尖塔+祭坛长椅
    - `modern_villa` 现代别墅（11x8x10，451 块）：白色混凝土+玻璃幕墙+二层露台+平屋顶天窗
    - `wizard_tower` 魔法师塔（9x14x9，347 块）：石砖高塔+品红装饰带+螺旋书架+锥形尖顶
    - `treehouse` 树屋（13x13x11，331 块）：橡木大树上架木屋+梯子+橡木台阶屋顶（树叶距树干≤6 格防衰减）
    - `lighthouse` 灯塔（7x13x7，358 块）：白塔红条纹+玻璃灯室+塔顶灯+基座入口
    说明：网上图纸有版权/来源不可靠问题，不直接打包；玩家可自行把下载的
    `.litematic/.schem` 丢进 `config/maid_smart/blueprints/`（或存档 `schematics/`）自动可用
  - **材料展示重构（蓝图手册）**：材料多时不再挤一行截断——默认每张图一行摘要
    （前 3 种缺口 + "等 N 种"），**点击材料行展开全部材料清单**，按宽度自动换行铺开，
    缺口黄色、充足绿色；每页条目 6→5 给展开区留位（比单纯缩小字号更好的方案：
    不牺牲可读性、信息完整）
  - **创造模式豁免（蓝图建造）**：主人处于创造模式时，材料流程整体跳过——
    预检默认齐全（手册 GUI / 手册点击 / LLM smart_build 都不再检测背包）、
    不扣玩家材料、不转交女仆背包，女仆放置时 `consumeBlock` 直接返回物品不消耗
    （创造模式 = 材料默认备齐，点图纸即可让女仆直接建造）
  - **海景房蓝图**（`maid_smart_ext:seaside_house`，11x8x9 共 445 块）：全新设计的
    双层海边小屋——圆石地基、石砖墙体、一层 4 窗 + 二层 7 连窗的海景大玻璃、
    正门（橡木门+门灯）、二层露台（橡木栅栏护栏+挂灯）、山墙斜屋顶（深色橡木台阶+
    石砖屋脊+三角山墙）、石砖烟囱，内部带箱子（海产）、熔炉、工作台、书架、花盆与花。
    生成后已同步到 config/maid_smart/blueprints/（打开手册/LLM 列表立即可见）
    ——比原 5 个内置小建筑（20~100 块）规模大 4~20 倍，材料需求也更真实
  - **挖矿对齐玩家公式**：旧版所有镐一律 30 除数 → 石镐也能"秒破"钻石矿。
    现在：正确工具判定（`DiggerItem.isCorrectToolForDrops`，石镐挖需铁镐的矿
    用 100 除数慢 3.3 倍）+ 效率附魔加成（与玩家 getDigSpeed 一致）+ 整体 ×1.2
    （人机智商不够，用数值补，比玩家稍快）
  - **自保道具可见**：`useHealingPotion` 此前写了但只在旧快照里被调用——已接入
    `m_6725_`；并扩展支持喷溅/滞留治疗药水 + 治疗 II（strong_healing），喝药有播报。
    搭高触发放宽：被近身 2 秒（40 tick）→ 1 秒（20 tick），更容易看到搭方块
  - **烹饪站桩修复**：`processFurnace` 旧逻辑"无事可做就放弃熔炉"→ 熔炉正在烧时
    女仆解除工作状态开始游荡。现在**持续站桩**：只要还在烹饪任务且熔炉仍在就站桩，
    任务切走才让位
  - **建造垫脚下**：`MaidBuildBehavior` 旧逻辑跳过女仆所在格 → 凉亭自己站的那块
    永远建不了。现在不跳过：先放自己所在格把自己垫上去，再继续建造
  - **材料以主人背包为准（自动交付）**：蓝图材料预检从"女仆背包"改为"主人背包"；
    确认后自动把女仆缺的材料从玩家背包转交给女仆（等价族感知，放不下的留在玩家
    原槽）；手册 GUI 的 0/N 也以玩家持有为准。避免周围多女仆/女仆背包空时误判
  - **第二轮测试反馈修复（5 项）**：
    - **【重大修复】MANIFEST 打包 bug——三个自保 mixin 从未生效**：`build_1511.py`
      用 Python **文本模式**写 MANIFEST.MF 的 `\r\n`，Windows 再把 `\n` 转成 `\r\n`
      → 每行变成 `\r\r\n` → **Java 解析 manifest 失败（invalid manifest format）**
      → Mixin 读不到 `MixinConfigs` → `FollowPreserveMixin`（跟随/传送豁免）、
      `MaidTeleportPreserveMixin`（传送拦截）、`HostileSensorPreserveMixin`
      （攻击断供）**从 1.5.x 早期起从未加载**。这解释了"逃跑时离主人太远仍被
      TLM 传回主人身边"（`MaidFollowOwnerTask.start` 的 `teleportToOwner` 从未被拦）、
      以及历史反馈里的"补刀""跟随失效"。已改为**二进制模式写 `\r\n`**，并用
      `JarFile.getManifest()` 验证 manifest 有效（`MixinConfigs` 可读）。修复后
      三个 mixin 首次真正生效，自保期间：不跟随、不传送、不补刀
    - **【mixin 方法名 SRG 修正（崩溃修复）**：manifest 修复后 mixin 首次加载，
      但 `HostileSensorPreserveMixin` 注入 `doTick` 崩溃（`InvalidInjectionException`）
      ——TLM 发布 jar 是**混合命名**：自有方法保留开发名（`teleportToOwner`、
      `checkExtraStartConditions` 经 javap 确认存在），但**覆盖 vanilla 抽象方法**
      的方法直接使用 SRG 名（`Sensor.doTick` = `m_5578_`，类里没有 `doTick`）。
      已把注入目标改为 `m_5578_`，handler 参数类型同步为 `LivingEntity`。
      三个 mixin 目标方法均经 javap 逐一验证存在，不会再注入失败
    - **自保三优化（防窒息 / 技能优先级链 / 解除阈值）**：
      - **防窒息**：搭方块节奏 2 tick → **4 tick**（远程 3）；`buildUp` 新增**实际头顶
        检查**（bounding box 顶面上方必须空旷——实体位移滞后时按真实位置判定，
        确保女仆永远在最上层方块之上）；**爆发搭高同一 tick 只放 1 块**起步
        （旧版连放 3 层，实体来不及被顶起 → 自己窒息）
      - **完整技能优先级链**：① 珍珠逃生（血 <30% 且威胁 <8 格，脱身最高优先）
        → ② 连续近身爆发（击退+强制搭高）→ ③ 强制搭高（持续上高、不逃跑）
        → ④ 被近身搭高（垫到安全高度：近战 2 块/远程 4 块后站桩，不再无脑一直搭）
        → ⑤ 逃跑（4~12 格，隐身/迅捷药水 + 反方向撤离）→ ⑥ 回血（只在无威胁 /
        威胁 8 格外进食——不再"被围殴还一直吃东西"）→ ⑦ 卡住兜底垫台阶。
        珍珠从"血 20%"放宽到"血 30%"，真正脱身手段优先于拖延手段
      - **解除自保阈值**：EXIT_RATIO 0.70 → **0.60**（血回 60% 即解除，回血道具
        不用凑满 70%）
    - **挖矿进阶：感知扩大 + 搭高挖矿 + 下柱回收**：
      - **感知范围扩大**：`findOre` 垂直范围 ±4 → **下方 8 格 / 上方 16 格**
        （悬崖上/矿洞顶的高处矿物也能发现）；挖掘接触距离 2 格 → **4.5 格**
        （对齐玩家手长 reach）
      - **搭高挖矿**：感知到高处矿物但够不着（水平已近 + 头顶 ≥2 格 + 背包有
        方块）→ 往脚下搭方块（4 tick/块，头顶空气检查防窒息，记录 pillarBlocks），
        把自己垫到够得着为止
      - **下柱回收**：挖完高处矿物后进入「下柱」——每 10 tick 挖掉脚下一块
        （自己搭的柱子顶，玩家同款破坏音效 + 掉落物回收），逐格降回地面，
        再继续找下一个矿；不挖自然地形（只挖自己搭的）
      - 挖矿扫描改为节流（扩大范围后全量查询变贵）
    - **酿造与整理修复（测试反馈）**：
      - **酿造只放烈焰粉**：根因是 `processStand` 的 **if-else 链每 40 tick 只做一步**
        且燃料优先（放完烈焰粉后下次才轮到材料）→ 观感"只放烈焰粉"。已改为
        **补水瓶/补燃料/补材料独立 if，一次处理全部**；水瓶判定从不确定的
        `Potions.f_43596_` 改为**动态 key 判定（"minecraft:water"）**；材料白名单
        从 static `Item` 引用改为**存 id 字符串 + 运行时动态匹配**（不依赖静态
        初始化时机）
      - **整理识别不到箱子**：`findChest` 搜索范围 **16 → 32 格**（"识别不到"主要
        是距离问题；行为本身已是任务判定启动）
    - **工作站桩统一（cook/brew/organize）**：旧逻辑"附近找不到工作方块行为就不启动"、
      "无事可做就放弃工作方块" → 熔炉/酿造台/箱子在烧/在用时女仆解除工作状态乱跑。
      现在**全部改为任务判定站桩（参考建筑行为）**：只要任务还是 cook/brew/organize
      就持续站桩，找不到工作方块也站着等（节流扫描），任务切走才让位
    - **喝药留空瓶**：治疗/隐身/迅捷药水消耗后**返还空玻璃瓶**（与玩家喝药一致，不再凭空消失）
    - **建造被阻挡不停止**：修复"一轮循环全被障碍跳过 → 误判全部完成 → 清空计划"的 bug——
      有格被玩家/生物/非自然方块挡住时，本轮跳过、**不结束计划**，障碍移除后自动续建
      （不再白白停止+感觉浪费材料）
    - **搭方块优化（自保）**：被近身**立即搭高**（不再等 1 秒近身 + 1 秒绕路）；
      搭方块节奏 15 tick → **2 tick（0.1 秒/块）**，配得上女仆超快速度，僵尸来不及追
    - **传送回家守卫**：`teleportHome` 前检查**女仆身边 16 格**仍有怪（哪怕被墙挡住）
      则不传送——修复"被大量敌人围攻时还传送回主人身边"
    - **连续近身爆发搭高（自保小加强）**：被连续两次近身（贴脸）后——
      ① 击退周围 5 格所有敌人（无伤害，仅击退，从女仆位置向外推）；
      ② 立即快速搭高 3 块（手动逐层垫，不等实体位移）；
      ③ **进入强制搭高状态**：持续往上垫、期间【不逃跑】——防"搭高了又自己
      掉下去"（正常逻辑搭高后一旦 flee 就会走下柱子摔落）；到顶（近战 10/远程 30）
      停在最高处等威胁消失；头顶被堵/无材料才结束强制；血 <20% 仍可珍珠逃生优先
    - **搭方块动态取料（修复"没圆石就完全不搭"）**：旧 `takeBuildBlock` 限定 8 种
      白名单（圆石/泥土/石头/橡木木板/深板岩/安山岩/花岗岩/闪长岩）——背包里没有
      这些就完全拿不到材料、不搭方块。现在**动态扫描**：背包里任何 BlockItem 对应的
      实心完整方块（`isCollisionShapeFullBlock`，排除下落沙砾/铁砧、TNT、仙人掌/
      岩浆块/营火等伤害方块）都能搭高，数量最多优先（保留原规则）。石砖/木板/原木/
      混凝土/黑曜石……什么多用什么
  - **建造系统五项完善（测试反馈第二轮）**：
    - **蓝图手册名称行去材料描述**：主按钮只显示「名称 + 尺寸/块数」，材料信息
      全部移到下方可展开的材料行——不再出现"长建筑名 + 一长串材料挤在一行"
    - **材料网格显示（横竖结合）**：展开的材料清单按宽度**每行 3 个**排布
      （竖排到底 + 横向铺开），最多显示 6 行，超出显示"…还有 N 种"；
      缺口黄色、充足绿色保留
    - **续建真正可用**：① 材料预检改为 `combinedShortfall`（玩家 + 女仆背包
      总量都算，不再因女仆背包空就误判缺料）；② partial 截断跳过已建块
      （parseStep 坐标 + 原点 + state==目标方块判定，续建时不重复计算已建部分
      的材料）；③ 放置循环本来就会跳过已建格 → 中途退出/补料/重进建筑任务
      自动从断点继续
    - **多女仆共同建造**：`BuildPlan` 从"每女仆 TaskDataKey"**全局化**——
      改为服务端静态 `Map<维度, 计划>`，同一维度任意建筑任务女仆共享同一计划
      （每 tick 重读 + 已建格跳过 → 天然协调）：N 个女仆同时建 = 摆放速度 ×N；
      计划随维度存在，任务切走不丢，任意女仆可接手续建。仍写入女仆 TaskData
      做存档兼容（重进游戏仍可续建）
    - **站桩根治（Mixin 从源头抑制 MoveToTargetSink）**：新 `MaidMoveSuppressMixin`
      注入 `MoveToTargetSink.doTick`（SRG `m_6725_`）HEAD 取消。1.20.1 Brain
      同一 activity 行为并发，工作行为每 tick 清 WALK_TARGET + 停导航仍会被
      MoveToTargetSink 竞态盖掉（"时灵时不灵"根因）。现在四个工作行为
      （建筑/烹饪/酿造/整理）在站桩时打 `WORK_STILL_TAG`（`MaidWorkTags`，
      persistentData + 当前任务双校验防残留冻结），mixin 检测到即：
      清 WALK_TARGET + 停导航 + 取消本 tick → 完全静止；自保逃跑期间同样抑制
      （只清 WALK_TARGET、不碰导航——逃跑用直连寻路，拦掉 MoveToTargetSink
      防止跟随行为塞目标把逃跑路径盖掉）。建筑任务 m_6725_ 无条件站桩，
      cook/brew/organize 走远时解除标记、到位后打标；cook 的 `m_6737_` 振荡
      （furnacePos==null 每 tick 启停）一并修复为任务判定持续站桩
- **1.5.23**：**自保"可见性"修复（测试反馈反思）**——
  - **反思结论**：v1.5.20-21 的新自保逻辑（搭高/珍珠/药水）全部依赖女仆背包物品——
    空背包测试时只有 v1.5.1 就有的逃跑 → 观感"跟上次一样"。这是设计缺陷：
    新逻辑没有兜底、没有反馈
  - **修复**：
    - **策略播报**：进入自保气泡带策略说明；开始搭高 → "它们贴得太近了，我搭方块垫高躲开！"；
      珍珠逃生 → "用末影珍珠瞬移逃走了！"；无材料 → "背包里没有搭方块的材料，我先想办法突围！"
      ——**空背包测试也能看见新逻辑在跑**
    - **无材料突围兜底**：被围殴且搭不了方块时，扫描 8 个方向、朝**怪物最少**的
      可行走方向逃跑（比单纯反威胁方向更能脱出包围圈）
- **1.5.22**：**挖矿回归修复（测试反馈）**——
  - **根因**：v1.5.19 用 FakePlayer 调 `BlockState.getDestroyProgress` 算每 tick 进度，
    但假玩家的"正确工具判定/装备状态"复杂，实测 delta 可能为 0 或行为频繁重启
    重置进度 → 有挥镐动作但永远挖不完（"连一块煤都挖不下来"）
  - **修复**：
    - **直接公式，不依赖 FakePlayer**：每 tick 进度 =
      `工具挖掘速度(ItemStack.getDestroySpeed) / 方块硬度(Block.getDestroySpeed) / 30`
      ——与玩家同公式，工具加速（石/铁/钻石镐）由工具速度直接决定
    - **进度持久化**（女仆 persistentData）：行为被抢占/重启时进度不丢，
      不会从头挖——这是"挖不完"的另一半根因
  - 预期速度（vs 玩家相同公式）：石镐挖煤约 1.1 秒、铁镐 0.75 秒、钻石 0.55 秒
- **1.5.21**：**自保优化（保命道具/威胁感知/智能逃生）**——
  - **保命道具**：
    - **末影珍珠逃生**：血 <20% 且威胁贴身（<10 格）→ 消耗 1 颗末影珍珠
      瞬移到反威胁方向 16 格的安全落点（落点检查岩浆等危险方块），播放传送音效；
      60 秒冷却
    - **治疗药水**：血 <50% → 喝 Instant Health 直接回血
    - **隐身药水**：逃跑中喝隐身（30 秒，潜行逃离）；**迅捷药水**：喝迅捷 II
      加速逃跑（2 秒尝试间隔）
  - **威胁感知**：
    - **视线检查**（raycast）：隔墙/被遮挡的怪物不算威胁，不再隔墙恐慌
    - **蛇形走位**：逃跑目标点每 10 tick 随机换侧偏方向（±3 格），降低被远程命中率
    - **危险方块避让**：逃跑目标点/珍珠落点有岩浆/火/仙人掌/营火 → 反向侧偏/不传
    - **头顶警示粒子**：自保期间每 3 秒头顶冒愤怒红雾粒子（主人一眼发现她在危险中）
  - **智能逃生**：
    - **卡住先绕路**：难逃时先带侧偏逃跑 20 tick（1 秒），绕不开再搭高
    - **传送冷却**：传送回家 60 秒内不重复传（防"传回家又被打→又传"循环）
    - **远程垫高提速**：远程怪模式搭方块 0.5 秒/块（近战仍 0.75 秒）
  - **按你的声明跳过**：苦力怕专项（TLM 自带苦力怕怕女仆）、盾牌格挡
    （TLM 自带技能）、逃跑方向偏主人（不做）
- **1.5.20**：**自保状态机重构（新逻辑）**——
  - **状态机**：血量过低 → 停止一切攻击进程（sensor 断供 + 清目标）→ 优先逃跑 →
    敌人持续近身难以逃脱 → **搭方块垫高自己** → 威胁消失（持续 5 秒）→ **传送回主人身边**
  - **按威胁类型决定搭高上限**：近身怪（僵尸/苦力怕等）**最高 10 格**；
    远程怪（骷髅/幻术师/烈焰人等，`RangedAttackMob` 判定）**最高 30 格**
    （远程怪在低处也能射，必须垫到射程外）
  - **"真搭高"修复**：旧版 buildUp 只在脚下悬空时垫一块（地面战斗永不触发——
    这就是"看不出搭方块效果"的根因）。重写为往**女仆所在格**放方块、被顶起一格，
    可连续垫高；头顶 2 格有方块 → 不得不停止
  - **僵尸式搭方块**（借鉴 EpicSiege/史诗战斗"僵尸搭方块追人"的逆向逃生版）：
    逃跑方向前方 2 格有墙 → 垫自己与墙之间脚下，形成**连续上升台阶翻墙**；
    前方脚下悬空（断崖）→ 垫前方脚下搭桥
  - **搭方块材料优先取背包数量最多的**（跨槽位合计，不浪费稀有方块；
    白名单扩到 8 种，沙/砾石等下坠方块不用）
  - **传送回家**：威胁消失 5 秒后，确认主人身边 4 格无怪物（防送死）才
    传送回主人身边；主人在 5 格内不传；**主人主动 TP 女仆不受影响**
    （mixin 只拦跟随任务的 teleportToOwner，不拦 /tp 指令）
  - 难以逃脱判定：卡住 20 tick 或 持续近身（<4 格）40 tick
- **1.5.19**：**挖矿渐进化（对齐 maid_useful_task 伐木逻辑）**——
  - 反编译 maid_useful_task 的 `DestoryBlockBehavior`/`MaidUtils`：伐木的渐进挖掘
    核心是 `destroyProgress += blockState.getDestroyProgress(假玩家, level, pos)` 每 tick，
    进度满 1 才破坏——**与玩家挖矿同公式**（挖掘速度/硬度/30）
  - **挖矿不再秒破**：每 tick 累计破坏进度，进度满才掉块（石镐挖煤矿约需数秒）
  - **工具加速**：假玩家主手同步女仆手中的镐 → 石/铁/钻石镐、效率附魔
    自动加快挖掘（空手几乎挖不动）——与伐木同机制
  - **每 tick 挥镐动画**（挖掘动作）
  - **玩家同款破坏音效**：挖完用 `levelEvent 2001`（按方块类型播放 break 音+粒子，
    与玩家挖矿完全一致）；移除旧的实体音效播放方式
- **1.5.18**：**蓝图卷轴移除 + 触发方式重述 + 手册材料缺口**——
  - **蓝图卷轴整体删除**（物品/配方/模型/赠送/右键交互）——手册成为唯一非 LLM 入口，
    开局只送《蓝图手册》1 本（`/give @p maid_smart:blueprint_book`）
  - **建造触发方式重述（三条件）**：
    1. 女仆**先进入建造状态**（任务=建筑）→ **立即站立不动**（无计划也站桩，等下达蓝图）
    2. 玩家在手册中选中建筑 → **仅当女仆已处于建筑任务才响应**（否则气泡提示先切任务，无效）
    3. 材料充足 → 开始建造（不足 → 先建材料够的部分/提示补料）
    校验统一在 `BlueprintBuildExecutor`（手册/LLM 两入口一致）；
    LLM 的 smart_build **不再自动切任务**，需先 switch_work_task 切建筑
  - **手册材料缺口（0/N 中文）**：每个蓝图条目下方显示材料行——
    用最近女仆背包实算 `中文名 已有/需要`（如 `橡木木板 0/64`、`玻璃 8/12`），
    缺口标黄、材料充足标绿；物品名走客户端本地化（不再是英文 id）
- **1.5.17**：**四问题修复（测试反馈）**——
  - **建造真正触发**：修复 `BuildPlan.getOrigin` 致命 bug——计划第一行是
    `O,x,y,z,蓝图名` 共 **5 段**，getOrigin 却按 4 段校验（`length != 4`）→ 永远返回 null
    → 行为立即清空计划 → 只有对话气泡、从不放方块。这是 v1.5.3 起建造从未真正工作的根因
  - **自保彻底不攻击（攻击源头断供）**：反编译确认 TLM 攻击行为（MaidMeleeAttack
    等 declarative OneShot）依赖 Brain memory ATTACK_TARGET，由 MaidHostilesSensor
    写入。新增 mixin `HostileSensorPreserveMixin`：自保标记存在时**跳过传感**——
    目标根本不写入 → 攻击行为永远拿不到目标 → 不再"逃跑间隙补刀"（比事后清
    memory 更可靠）
  - **自保搭方块**：垫高触发距离 5→7 格（威胁逼近即垫）；新增**逃跑路径预判**
    （pathBlocked）：逃跑方向前方 2 格有墙/坡 → 提前垫路翻越，不再等卡住 1 秒
  - **传送豁免双保险**：反编译确认传送动作在 `EntityMaid.teleportToOwner`（start 内
    调用）。新增 mixin `MaidTeleportPreserveMixin` 直接拦截该方法（自保标记且
    血<70% 时 return false）——无论跟随行为处于什么状态都瞬移不回主人身边；
    与原有 checkExtraStartConditions 前置拦截构成两道防线
  - **站桩强化（完全不动）**：烹饪/酿造/整理/建造四个行为，绑定工作方块后
    **每 tick** 清 WALK_TARGET + `navigation.stop()`（之前只在就位瞬间清一次，
    冷却间隙漫游/跟随会重新设目标）
- **1.5.16**：**三入口建造闭环（非 LLM 全流程 + LLM 状态查询）**——
  - **蓝图手册**（`maid_smart:blueprint_book`，开局赠送 1 本 + 合成：书+纸）：
    手持右键打开 GUI 列表——**内置 5 种 + 全部外部图纸 + 5 套预制**（含尺寸/块数/材料摘要），
    翻页浏览，点击即让 32 格内最近的女仆建造。**这是外部图纸不依赖 LLM 的完整触发方式**
    （SimpleChannel 双包：目录下发 + 选择执行；纯 Screen 无 Menu 容器，简单可靠）
  - **卷轴材料二次确认（非 LLM）**：右键卷轴时**开始前材料预检**——
    不足 → 气泡列出完整缺失清单 + "再右键一次=先建材料够的部分"；
    120 秒内二次右键同一卷轴 → 先建材料够的前缀（partial 截断）；超时/换卷轴 → 取消。
    建造中途缺料照旧气泡提示，补料自动续建
  - **smart_build 工具加 action=status（LLM）**：查询当前建造计划——
    蓝图名、已建 n/N 块（%）、剩余还缺的材料清单（按背包实算），
    LLM 可回答"建到哪了 / 还缺什么"，缺料时提示玩家补料后自动续建
  - 卷轴/手册/LLM 三条入口统一走 `BlueprintBuildExecutor`（解析→居中→障碍预检→材料决策）
- **1.5.15**：**预制精美建筑（5 套，首次启动自动安装）**——
  - **核查结论**：numen **本身没有任何预制建筑**（jar 内无 .nbt/.schem/.litematic 资源、
    反编译源码无硬编码方块数据、BlueprintStore 无默认示例生成）——它的复杂建筑
    完全依赖玩家放入 schematics/ 的图纸文件
  - 因此**自建预制库**：程序化生成 5 套中等复杂度的精美建筑（SNBT 结构文件，
    jar 内 `assets/maid_smart/builtin_blueprints/`，首次启动自动复制到
    `config/maid_smart/blueprints/`，玩家可自由修改/删除）：
    | 蓝图 | 建筑 | 规模 |
    |---|---|---|
    | `maid_smart_ext:cottage` | 乡间两层小屋（斜屋顶/烟囱/双开门/壁炉/阳台窗/门廊） | 10x7x8，457 块 |
    | `maid_smart_ext:round_tower` | 中世纪圆塔（程序化圆环塔身/每层 4 窗/城垛/锥形屋顶） | 9x20x9，570 块 |
    | `maid_smart_ext:gatehouse` | 城堡门楼（拱形门洞/双开门/四角塔/城垛/灯笼） | 12x10x9，405 块 |
    | `maid_smart_ext:barn` | 大谷仓（原木框架/大双开门/干草堆/阁楼/山墙屋顶） | 14x8x9，600 块 |
    | `maid_smart_ext:chapel` | 小教堂（彩色玻璃窗/长椅/祭坛/金字塔屋顶/十字架尖塔） | 11x14x11，712 块 |
  - **门上半自动补全**（MaidBuildBehavior）：放置 door 下半后若上方是空气，
    自动补 upper half——解决 setBlock 不自动补导致"半扇门"
  - 与外部蓝图完全同管线：站桩隔空建造/材料消耗/障碍物预检/续建/旋转全部适用
- **1.5.14**：**复杂精美建筑能力（迁移 numen BlueprintFormats）**——
  - **新增 .litematic（Litematica）与 .schem（WorldEdit/Schematica）格式支持**：
    网上分享的复杂精美建筑图纸绝大多数是这两种格式，现在下载后直接放进
    `config/maid_smart/blueprints/` 或存档 `schematics/` 即自动可用
  - 迁移 numen 的格式转换器：Litematica 变长位打包解包（bit-packed long[]）、
    多区域合并/坐标归一、方块实体（TileEntities）绑定；Schematic 的 Palette
    + 变长 varint 编码 BlockData、`state[string]` 属性字符串解析
    （`minecraft:oak_stairs[facing=north,...]` → 精确 BlockState）
  - **上限对齐 numen：8192 → 32768 格**（复杂精美建筑可达上万格）
  - 旋转 0/90/180/270 对 .litematic/.schem 同样生效（转换→旋转→重建）
  - 转换后统一走标准结构管线：精确状态/方块实体/黑名单/障碍物预检全部沿用
- **1.5.13**：**建造体验优化 + 蓝图获取生存化**——
  - **立刻停走**：进入建造模式瞬间停止移动（清 WALK_TARGET），建造中每 tick 清移动目标，
    不被漫游/跟随带走
  - **站桩隔空建造**：不再走到方块旁——女仆原地不动，隔空放置（服务端直接 setBlock，
    每 0.5 秒一块）；**每放一块挥臂 + 播放方块放置音效**（"放置动作"可感知）
  - **以女仆自身为中心**：建造原点 = 女仆脚下，蓝图平面自动居中在女仆位置；
    女仆所在格自动跳过（不会把自己埋进建筑里）
  - **障碍物预检**：开始建造前扫描整个建造区域——空气/自然地形（草/泥土/石头/沙等
    20 种）可覆盖，树/建筑/箱子等其他方块 → 气泡"这里不太合适……"并**拒绝建造**
  - **蓝图获取生存化**：**首次进世界自动赠送 5 张蓝图卷轴**（每玩家一次，绿色系统消息）；
    **廉价合成配方** ×5（纸 + 对应方块 1 个 = 蓝图卷轴 1 张，配方书可见）：
    小木屋=纸+橡木木板、凉亭/喷泉/瞭望塔=纸+石砖、水井=纸+圆石
  - 运行时放置兜底：目标格被障碍物占用时跳过（预检之外的动态变化）
- **1.5.12**：**numen 式结构文件蓝图迁移**——
  - 蓝图来源扩展到**标准结构文件**：`.nbt`（原版结构方块导出）、`.snbt`（文本 NBT），
    放进 `config/maid_smart/blueprints/` **或存档 `schematics/` 文件夹**即自动可用
    （增量扫描：文件变更自动重新解析，无需重启——放文件即用，同 numen）
  - 支持 **旋转**（smart_build 工具新增 rotation 0/90/180/270，顺时钟，
    坐标与方块朝向（楼梯/门等）同时旋转，同 numen）
  - **精确 BlockState**（台阶/楼梯朝向/门/床等原样放置）与**方块实体数据**
    （箱子内容/告示牌文字等）随步骤携带并恢复
  - 结构蓝图走**黑名单通道**（基岩/命令方块/液体等禁止，其余放行——玩家自己的文件
    不受 LLM 白名单限制），上限 8192 块（numen 32768 的保守版）；无物品方块/
    液体/门上半/床头自动跳过（对齐 numen）
  - 工具描述/目录/汇报升级：smart_build_list 与 smart_build 显示外部蓝图的
    尺寸/块数/材料 top5 摘要（对齐 numen blueprint_read 的"先报价再开工"）
  - 存档 schematics/ 扫描通过服务端启动事件注入（ServerStarted/Stopping）
- **1.5.11**：**不依赖 LLM 的建造触发方式 + 建造种类/数量扩展**——
  - **蓝图卷轴物品**：新增 5 种物品
    （`maid_smart:blueprint_hut/gazebo/fountain/tower/well`，
    用 `/give @p maid_smart:blueprint_hut` 获取），
    手持卷轴**右键女仆**即触发建造（无需 AI 对话、无需 LLM），
    消耗 1 张卷轴（创造模式不消耗），女仆气泡回应"好！我来建XX啦！"
  - **外部蓝图扩展**：玩家把 JSON 蓝图文件放进
    `config/maid_smart/blueprints/` 文件夹即自动注册为新蓝图
    （格式与 LLM JSON 蓝图相同，见文末示例），重启游戏生效；
    `smart_build_list` 目录、蓝图卷轴 tooltip、LLM 的 smart_build
    统一显示内置+外部全部蓝图——**造多少种、造多大，玩家自己决定**
  - 蓝图查找统一（smart_build 工具改调 `BlueprintLib.getBlueprint`，
    内置/外部/LLM JSON 三条路径合并；未知蓝图返回完整目录清单）
  - 物品模型 + 中英文名（`item.maid_smart.blueprint_*`）
- **1.5.10**：建筑链路重构（借鉴 numen 蓝图设计）——
  新增 `smart_build_list` 目录工具（玩家问"你能建什么"时 LLM 展示全部蓝图清单）；
  内置蓝图扩充到 5 个（新增瞭望塔 tower/水井 well）；
  smart_build 描述重写为玩家语言（中文名+尺寸+材料，明确续建流程：
  中途缺料暂停报告，补料后同一调用继续，已建方块自动跳过）；
  女仆"没有蓝图"提示改为引导话术（告诉玩家怎么说）
- **1.5.9**：
  - **已婚女仆完全关闭争宠吃醋**（BALANCED）：不再驱逐/种花/吃醋对话（婚姻豁免从
    "只种花"升级为"全部关闭"）；未结婚的高好感女仆仍按原规则（好感≥192 只种花）
  - 亲密动作好感门槛（smart_intimate：摸头/拥抱 ≥64、膝枕 ≥128、亲吻 ≥192，
    LLM 自行判断 + 服务端兜底拒绝）
  - 心契动作 LLM 工具 `smart_intimate`（hug 拥抱 / kiss 亲吻 / lap 膝枕 / pet 摸头，
    反射软依赖，未装心契誓约时明确提示）
  - 信任与好感强相关（BALANCED）：好感 L2+（≥192）信任向 70 爬升、L0（<64）向 30 回落，
    每 20 tick 缓慢漂移
  - 提示词"关系优先于情绪系统"：已婚/父女/告白等已确认关系覆盖情绪倾向
  - 酿造完整流程：自动从背包放水瓶（无需主人准备）+ 处理间隔 2 秒
  - 熔炉一次补食物+燃料（不再只放燃料）+ 间隔 2 秒
  - 烹饪/酿造/整理**先寻找后站桩**：搜索范围 16 格、0.5 秒重扫，
    与工作方块建立联系后才站桩工作
  - 挖矿音效改 STONE_BREAK（矿石破坏音）
  - 自保：垫方块触发距离 5 格、头顶不空时垫路兜底；
    **威胁消失 5 秒即退出自保**（没有回血食物也能回到主人身边）
- **1.5.8**：小女仆与父亲的剧情记忆——child_state/child_lineage 的
  father == 主人时注入"我是主人的女儿，主人是我的父亲（爸爸）"，
  提示词同步指示（叫主人爸爸、像女儿一样撒娇依赖）
- **1.5.7**：自保不再"抽空打怪"（每 tick 清攻击目标，Brain 各优先级组
  并行导致的攻击行为间隙反打已消除）；**重伤/死亡事件驱动**（玩家受伤 <30% 或
  死亡时立即触发 LLM 反馈，30 秒冷却，不受 20 秒扫描/4 分钟冷却限制）；
  烹饪/酿造"找不到方块"加**真实扫描诊断**（气泡报告实际扫描到的熔炉/酿造台数量）；
  提示词新增 **Relationship Memories 章节**（结婚/怀孕/丧子/孩子身份必须自然体现）
- **1.5.6**：工作状态播报——女仆选了 maid_smart 任务却不干活时，每 10 秒
  气泡解释原因（坐着/被骑乘/没镐/附近没矿/没蓝图…），不再"无声发呆"；
  smart_build 切任务后验证结果，失败明确报错而非静默
- **1.5.5**：记忆写入崩溃修复——TLM 的 TaskData 用 unboundedMap codec 解码
  返回不可变 Map（ImmutableMap），直接 put 会 UnsupportedOperationException
  （喂食/好感升级/玩家死亡事件触发）；读取时包 HashMap 保证可写
- **1.5.4**：借鉴史诗攻城 + numen AI——
  挖矿价值优先级（score = 距离 + 深度惩罚 − 矿石价值，钻石>铁>煤，不再碰到哪个挖哪个）、
  自保搭方块升级（头顶 2 格防窒息、逃跑方向垫路铺台阶、卡住检测脱困、0.75 秒/块节奏）、
  smart_build 提示词加材料风格建议（5 种风格）
- **1.5.3**：任务切换修复（createBrainTasks 必须返回**可变列表**——TLM 的
  registerWorkGoals 会往里追加行为，不可变列表导致 UnsupportedOperationException，
  任务永远切不上 = 之前"发呆"的真根因）、建造计划 codec 修复（TLM 的 TaskData
  同步要求编码结果为 CompoundTag 对象，Codec.list 会编成 ListTag 导致每 tick
  ClassCastException 崩溃 = 建筑崩溃的真根因）
- **1.5.2**：任务修复（行为有目标才启动，不再空转发呆，无目标时正常漫游）、
  建筑材料等价族（要橡木木板时任意木板都认）、自保传送豁免（跟随补丁：自保期间
  不被传送回主人身边）、建筑计划空指针防御
- **1.5.1**：自保升级——覆盖一切状态（不限战斗）、优先级 250 全局最高（压过跟随/任务/睡觉/恐慌）、双退出条件恢复原状态、直接寻路逃跑
- **1.5.0**：战斗自保（低血逃跑/进食/搭方块，纯规则零 LLM）
- **1.4.1**：建筑材料判定升级——缺料时先问玩家"建一半还是放弃"（mode=full/partial）
- **1.4.0**：建筑功能（smart_build 工具 + 3 个内置蓝图 + LLM 生成蓝图 + 材料判定）
- **1.3.0**：心契誓约剧情记忆（结婚/怀孕/心情/丧子/告白 实时注入 AI 对话）
- **1.2.0**：情绪价值动作（跑过来/递食物/看向/撒爱心）+ 家庭保护（防同主女仆互殴）
- **1.1.0**：新增烹饪/酿造任务、自主决策（空闲自判任务）、长期记忆（事件写入+context 注入）
- **1.0.0**：P1 四工具 + P2 挖矿/整理 + P3 主动对话

## 后续可做（P5）

- 自主规划的深化：多步目标规划（"去挖10个铁矿石再回来"），意图队列 + 配额
- 记忆的深化：基地坐标、纪念日、跨会话事件归档
- 任务深化：烹饪支持烟熏炉/高炉、酿造自动补水瓶
