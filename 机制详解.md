# Promaid 机制详解（全量）

> 版本：v1.0.2（内部迭代至 v1.5.387+）｜ 适用：Minecraft 1.20.1 + Forge 47.4.21 ｜ 前置：TLM ≥1.5.0（推荐 1.5.3）
> 本文档覆盖模组全部 137 个类（含 41 个 Mixin，其中 1 个已废弃保留空壳）的机制，按包分组，不遗漏任何机制。
> 命名空间说明：所有持久化标识（物品/网络/TaskData/蓝图路径）保留 `maid_smart` 以兼容旧档，仅 modId 为 `promaid`。关系联动（心契誓约×爱憎分明）已拆分为独立 Heartfelt-connection 补丁，本模组零依赖可单独运行。

---

## 〇、总览

Promaid 是《车万女仆》(TLM) 的 Forge 扩展，让女仆真正"活"起来：自主建造（外部蓝图/工头/全局进度）、LLM AI 对话（20+ 工具/主动对话/长期记忆）、战斗自保（自保行为/落地水/自动装备）、任务（挖矿/烹饪/酿造）、农场与杂项优化。**全部功能零依赖，只要求原版 TLM**；与 maidmarriage/callresponse/heartfelt 均为软联动。

扩展注册（ProMaidExtension，被 TLM `@LittleMaidExtension` 自动发现）：
- AI 工具 8 个：smart_move_to / smart_give_item / smart_set_pickup / smart_report / smart_attack / smart_craft / smart_place / smart_build / smart_build_list / smart_design / perception_query / work_list / remember / working_note / query_memory / query_memory_index / smart_owner_inventory；
- 任务 4 个：`maid_smart:build` / `mine` / `cook` / `brew`；
- AI context 3 个：ai_memory（长期记忆投影）、ai_affect（PAD 情绪）、ai_worklist（工作清单）；
- 事件管理器 11 个 + 自保/战术/落地水/插火把/盾牌共享等 core 行为。

---

## 一、入口（根目录 3 类）

### ProMaidMod
`@Mod("promaid")` 入口（原 maid_smart 改名重组）。构造时：`DeferredRegister`（命名空间保持 maid_smart，兼容旧存档物品）注册蓝图书 `blueprint_book`（右键打开蓝图列表 GUI）；注册网络通道（BlueprintBookNetworking）；注册 COMMON 配置（MaidSmartConfig.SPEC，客户端/服务端都可读，热更新）；客户端注册配置面板扩展点（PromaidConfigScreen）。

### ProMaidExtension
`@LittleMaidExtension` / `ILittleMaid`，通过官方扩展机制被 TLM 自动发现，注册全部功能：AI 工具、任务（addMaidTask）、TaskData（BuildPlan）、AI context（registerAIMaidContext：ai_memory/ai_affect/ai_worklist）、ExtraMaidBrain（自保/战术等 core 行为）。
事件注册：ProactiveDialogueManager、ReplyFeedbackTracker、AutonomousTaskManager、PerceptionManager、AffectEventHooks、RelationshipMemoryAdapter、SelfPreservationBehavior、AiMemoryManager、WorkStatusReporter、MasterDeathTeleportHandler、PlayerWaterLog。
tick 节流：挖矿搭方块 10 秒清理、自适应建造速度 TPS 测量、活跃建造女仆统计（40 tick）、挖矿 per-maid 清理（600 tick）、跨维度跟随扫描（100 tick）、建造 HUD 广播（20 tick）、钓鱼走位维持（3 tick）、幼儿武器禁持轮询（20 tick）。ServerStartedEvent 注入 server 并应用建造默认档位；ServerStoppingEvent 清场（残留块→掉落物、计划/冻结清空）。

### CreativeTabHandler
创造栏注入：蓝图书加入原版「建筑方块」与「工具与实用品」两个标签页（v1.5.284 修 modid——旧版事件订阅写 maid_smart ≠ modId promaid，注入从未生效）。

---

## 二、情绪价值动作（action 包，1 类）

**EmotionalActionExecutor**：主动对话触发时配合说话执行游戏内动作（全服务端、无瞬移、无凭空物品）：walkToOwner（走到主人身边，上限 32 格）、lookAtOwner（看向主人）、heartParticles（心形粒子）、giveFoodToOwner（主人饥饿<15 且背包有熟食时递上）。食物白名单 21 种安全有益食物，按 `nutrition × saturationModifier × 2.0` 选最优（金胡萝卜 14.4 最高、熟牛排/猪排 12.8 次之）；腐肉/蜘蛛眼/毒马铃薯/河豚/紫颂果/可疑炖菜及一切生食不喂；金苹果/附魔金苹果走即时增益道具路径；牛奶桶/蜂蜜瓶走负面效果解除路径（v1.5.288 蜂蜜瓶恢复为投喂食物双效）；「缺吃的」播报 60 秒限频。

---

## 三、PAD 情绪层（affect 包，3 类）

独立于 TLM 好感/心契心情/爱憎信任恐惧的**第四套数值**——只做「情绪状态+注入文本」，不改任何现有系统数值。

- **AffectManager**：维度 = PAD（valence 愉悦 -1~1 / arousal 唤醒 / dominance 支配）+ 关系（intimacy/conflict/longing）+ 修复债（hurtDebt/repairDebt）。事件驱动：主人对话→intimacy+/conflict-；被打→valence-/conflict+/hurtDebt+；世界伤害→conflict+/hurtDebt+；静默恢复→情绪回落。注入为 `ai_affect` 上下文；落盘 `<世界>/promaid_memory/<uuid>/affect.json`（独立文件零依赖）。
- **AffectEventHooks**：把游戏事件喂给 AffectManager（对话/右键交互、主人打女仆、世界伤害、每 20 秒静默恢复，间隔 affect.recoverInterval）。开关 affect.enable。
- **AffectSnapshot**：写入记忆段落 metadata 的紧凑 JSON（`{"affect":{...}}` 两位小数控 token），让每条记忆带"当时的心情"。

---

## 四、建造系统（build 包，19 类）

### 核心链路
- **BlueprintLib**（蓝图库）：格式 `List<String>`（`x,y,z,blockid` 或 `|stateSnbt|beSnbt`）。来源：外部文件（v1.5.11 JSON、v1.5.12 起 .nbt/.snbt 结构文件，放 `config/maid_smart/blueprints/` 与存档 `schematics/` 增量扫描即用）+ LLM 现场生成 + AI 设计器落盘。**v1.5.387 内置预设全部移除**（建筑授权）——手册建造目录只显示外部/生成蓝图。安全：LLM JSON 白名单约 45 种方块+平面 ±12+高度 ≤8+≤200 块；结构文件黑名单（基岩/命令方块/液体/jigsaw/end_portal 等）+ 上限 build.structureMaxBlocks。材料预检统计背包缺口。旧档已下达的 maid_smart: 内置 id 仍可解析（BuiltinHouses 保留生成器）。
- **BlueprintBuildExecutor**（执行器）：创建区块不再依赖女仆在场（以玩家脚下为原点），唯一硬性要求=不与已有区块重叠；区域内障碍物只警告不阻止。流程：解析→重叠检查→障碍物警告→已建感知→材料预检（需求−已建−背包）→创建/续建。Outcome：OK/SHORTFALL/OBSTACLE/UNKNOWN/OVERLAP。红石机器专属搭建顺序（结构→机构→活动件→传感→动力源→TNT，动力源最后落位），开关 build.machineSmart。
- **BuildPlan**（计划）：格式 `["O,x,y,z,name,id", "x,y,z,blockid", ...]`。v1.5.180 多区块共存：PlanState=一区块一计划，planId 全局唯一跨维度；女仆绑定存 persistentData（`maid_smart_bound_plan`，重启恢复）；创建不依赖女仆在场。CODEC 必须包成 `{"steps":[...]}` 对象（TLM TaskData 同步强制 CompoundTag，直接 list 会每 tick 崩溃）。
- **BuildArchive**（档案）：世界级「女仆工作记忆」，按维度持久化 `{origin, blueprintId, name, cursor, paused, foremanUuid}`（steps 不存，重启后由蓝图 id 重新解析）；游标从进度处继续。v1.5.180 升级为 SavedPlan 列表（多区块），旧单计划自动迁移。SavedData 名 `maid_smart_build`。

### 执行与行为
- **MaidBuildBehavior**：按计划逐块放置。规则：已是目标方块→跳过；被占用→跳过（不破坏玩家建筑）；缺料→自动从主人背包取料，都没有气泡提示。速度：默认每 3 tick 一块；档位 ×1/×1.5/×3 = 1/1.5/3 块每秒；极速模式（TURBO）吃满服务器上限（TPS 保护兜底）。v1.5.142 建造强制坐下标记（每 tick 按压坐下）。缺料/替代播报 30 秒冷却。
- **MaidBuildTask**：`maid_smart:build` 任务。createBrainTasks 返回可变列表（TLM 会追加行为），优先级 5；createRideBrainTasks 保证坐下照常运行；关闭 TLM 随机散步（RandomStroll 干扰站桩）。
- **BlueprintAreaPreview**：建造范围预览——金色框（半透明填充+线框+悬浮文字）随玩家移动；红色固定框（实际建造区域，多区块每区块一框+名称+坐标）；确认流程（previewSeen 标记→确认弹窗→区块创建成功 resetSeen）。
- **BlueprintBookItem**：手册物品（书+纸合成，/give 兜底）——非 LLM 的完整建造入口：右键打开蓝图列表 GUI（外部+生成全部图纸），点击即让附近女仆建造。材料缺口以主人背包为准（0/N），确认后由服务端交付女仆。区块内右击→该区块详情页；区块外→大目录。始终显示附魔光效（v1.5.285 修 isFoil 错写方法）。
- **BlueprintBookScreen**：手册 GUI（纯客户端 Screen，数据由 OpenBlueprintBookPacket 下发）。大目录页两入口（建造/女仆管理）+ 顶部实时进度行；建造面板/材料详情页（2 列×8 行自动翻页）；女仆管理面板（128 格内建造女仆，行内暂停/继续+设工头）。客户端每 2 秒轮询进度，进度/速度/女仆状态自动刷新。v1.5.71 自适应行数（小窗口不溢出）。
- **BlueprintBookNetworking**：网络通道（`maid_smart:blueprint_book`，协议版本 2）。报文：OpenBlueprintBookPacket / SelectBlueprintPacket / BuildControlPacket（全员加入/暂停/速度/强制续建/进度/取消/逐只暂停）/ ProgressUpdatePacket / AiMemoryTogglePacket / DeleteBlueprintPacket / MemoryViewRequest/Response / ClearMemoryPacket / MaidDebugRequest/Response/Action / VoicePackImport/Query / 手册「导入建筑」。
- **BuildHudRenderer / BuildHudTracker**：建造 HUD——左上角实时显示进行中区块（名称+进度+跳过数+速度+ETA，最多 3 区块，超出折叠）；服务端每 20 tick 采样 placedCount 增量算速度（EMA 平滑），按剩余块数估 ETA；首秒显示 "--"；打开任何界面/F3 不渲染。
- **ChunkFreeze**：建造区块冻结——强制加载保持 + randomTick 拦截（树叶不衰减、草不传播、作物不生长）+ 红石不激活（静默放置保证）。v1.5.180 按 planId 分桶冻结；PROTECTED_LEAVES 蓝图内树叶永久豁免随机刻度衰减（装饰树"打印"后不消失，重启随计划恢复）。
- **BuildTntGuard**：TNT 建造期防自燃护栏（v1.5.328/331）——线程级标记 + 保护时间窗（suppressUntilTick）。根因：LevelChunk.setBlockState 无条件调 onPlace（flag 2 静默放置不阻止），且「观察者→活塞→推 TNT」电路推动的点火链发生在 doPlace 之外。完工时 settleTntIgnition 点火结算（只点燃当前邻接带电的 TNT，静止 TNT 保持惰性）；宽限期满后按正常红石逻辑点火。
- **BuiltinHouses**：内置蓝图生成器（v1.5.366 改版：3 生存小屋 + 12 结构建筑 + 3 别墅，程序化生成）。**v1.5.387 起不再向手册/目录/LLM 工具注册**（建筑授权）；保留生成器仅供旧档已下达任务解析。
- **FarmSweepCache**：农场冷却表普通工具类（v1.5.386 从 mixin 抽出）——连锁收割冷却 HARVEST_CD（10 tick）、批量种植冷却 PLANT_CD（40 tick）。**根因**：mixin 类被 Mixin 系统标记 invalid，普通代码引用即 NoClassDefFoundError；mixin 只委托、业务代码只引用本类。

### LLM 建造工具
- **SmartBuildTool**（`smart_build`）：blueprint 参数两格式——外部蓝图 id（maid_smart_ext:xxx，可带 rotation 0/90/180/270）或 JSON 蓝图（LLM 现场生成，白名单/块数/平面/高度限制）。mode：full（材料不足不开始，返回缺失清单由 LLM 询问）；partial（只把材料够的前缀加入计划，顺序截断不悬空）。流程：解析校验→材料预检→按 mode 决策→记录计划→自动切建筑任务；中途缺料暂停提示补料，补后自动续建。
- **SmartBuildListTool**（`smart_build_list`）：展示女仆可建造的全部蓝图目录（玩家问"你能建什么"）。
- **SmartDesignTool**（`smart_design`）：AI 画蓝图（子 Agent 设计器）——主 LLM 把设计任务外包给子 Agent（TLM 原生子 Agent 分发），子 LLM 以建筑师角色用专门 JSON 蓝图格式设计→解析校验→saveJsonBlueprint 落盘自动注册进手册→走正常建造流程。与 smart_build 区别：子 LLM 只干设计、回复不污染主对话。尺寸上限 build.designMaxBlocks（默认 500）。

---

## 五、战斗与自保（combat 包，6 类）

- **SelfPreservationBehavior**：自保行为（纯规则零 LLM）——任何状态血量过低且附近有威胁即停当前一切转入自保。注册 core 优先级 250（高于 TLM core 最高 99）。状态机（滞回防抖）：进入 HP<30% 且 12 格内存在威胁；三策略：进食（金苹果/熟食真实回血，40 tick 冷却）、逃跑（朝威胁反方向 1.4 倍速直连导航）、搭方块（威胁贴身<4 格垫圆石/泥土/木板，1 秒一层，头顶有方块不垫）；退出 HP≥70% 或威胁消失且 HP≥45%。PRESERVE_TAG（`maid_smart_preserving`）写 persistentData，TLM 跟随读到会拒绝启动（防低血逃跑被传送回主人身边送死）。v1.5.135 记录 5 秒内攻击者（覆盖非 Monster 敌对生物/中立反咬/PVP），主人误伤不记；v1.5.213 坐下受伤立即站起。
- **MaidCombatTacticsBehavior**：单兵作战战术（PVP 式）——core 优先级 230，只对 TLM 原生战斗任务生效，只接管**移动**不碰攻击。三套操作：近战弧形接近+贴脸绕圈（半径 2.2）+打退拉扯（攻击完成瞬间后撤 14 tick）+跳劈接近（原版暴击 1.5 倍，大跳冷却 10 秒，触发区间 4.5~6.5 格）；远程距离管理（最大射程×0.6）+横移绕圈放风筝；时机举盾（配合 MaidShieldTimingMixin）。v1.5.282 单敌跳劈 100%。开关 combat.tactics + 子开关。
- **MaidAidOwnerBehavior**：自动投喂/治疗主人（被动技能，任何任务都运行，每 3 秒检查命中即止）。优先级链：血量<30%→喷溅治疗→滞留治疗→金苹果→再生药水→普通治疗塞背包→喂高饱食物；着火→抗火药水；负面效果→牛奶桶（全解）→蜂蜜瓶（解中毒+回饱食）；饱食度<12→按饱和度喂最优食物。药水识别统一用药水注册名；投掷 CD 按女仆记；效果判定为情境药水正门；药水种类 CD 按效果种类 key。
- **MaidShieldShareBehavior**：共享盾牌——主人拿盾且耐久低时从女仆背包取盾（剩余耐久最高者，绝不拿自己副手正在用的盾）；给盾后冷却 5 秒。v1.5.250 修：主人没拿盾不补；剩余耐久<30% 烂盾不给。v1.5.228 修 canStillUse 必须重写 true（原版默认 false 导致行为从诞生起就没 tick 过）。
- **MaidTorchPlacerBehavior**：被动插火把——主人周围 3×3 黑暗格（方块亮度<阈值默认 7）自动插火把（背包有 torch 且脚下实心），冷却 1.5 秒。主人不在身边（>8 格）不管。v1.5.228 同修 canStillUse。
- **WaterClutchBehavior**：落地水（被动生存技能）——自由落体即将触地瞬间放水缓冲，落地后 1 秒自动收回。core 优先级 240。触发（全部满足）：背包有水桶（不消耗）、正在下落、未落地、脚下 4 格内实心地面、脚下不是水。v1.5.25e 本次坠落只用一次；v1.5.27 传送突变检测（两 tick 位移>6 格视为传送）；v1.5.27b 落地立即重置。

---

## 六、指令（command 包，1 类）

**MaidArmyCommand**：`/maid_smart summon_builders <数量>`——N 只已驯服+已切建筑任务的女仆围成一圈生成（1~64，OP 权限防刷）；`/maid_smart memory on|off|status`——切换/查看 32 格内最近女仆的 AI 记忆开关（面板门控无法打开时的命令兜底）。

---

## 七、配置（config 包，2 类）

- **MaidSmartConfig**：全模组配置（COMMON，客户端/服务端都可读）——6 个 section：build / mine / memory / dialogue / combat / misc。面板保存写 `config/promaid-common.toml`，运行时 .set() 热更新。关键项：build（speedTier/turbo/globalQuota/maxForceChunks/maxBlocks/maxRange/maxHeight/designMaxBlocks/structureMaxBlocks/maxMaids/machineSmart/TNT_IGNITION_GRACE/缺料替代三张表+宽高替代/wides/noclips）；mine（oreValues 自定义矿表/breakables/searchRadius/chainMining/autoCollect/chainLimit/junkKeep/pillarGuard 等）；memory（enable/extractThreshold/maxEntries/promptTopN/relInject/conflictOverride/coreFold/workingNote/relationshipAdapter/scanInterval/extractTimeoutMin/rrfK/decayDays/lazySave/maintenanceMin/relationDecayDays/MEMORY_API_URL/KEY/MODEL/indexEnable 等）；dialogue/combat/misc 同理全量可调。
- **PromaidConfigScreen**：配置面板（手册式跳转+翻页，修窄屏溢出与滚轮 bug）。挖矿板块第一行「可挖掘方块表 →」进矿表子页；所有控件即时写入 ForgeConfigSpec（内存热更新），关闭时保存；NumRow 延迟提交（只校验格式，保存/完成才生效）；记忆 API 字段允许空值（清空=回退 TLM）。行高 44 防注释重叠。

---

## 八、对话与智能（dialogue 包，13 类）

- **ProactiveDialogueManager**：女仆主动对话（v1.5.191 重写为 7 阶段状态机）——LIGHT_FOLLOWUP→TOPIC_PUSH→WORLD_OBSERVE→RELATION_CANDIDATE→FINAL_NOTICE→LONG_SILENCE_CHECK→IDLE；阶段推进=无主人互动且到点，主人互动重置回起点。每轮最多 maxReplies（默认 4）；沉默计时用真实「主人聊天时间」；配额只在真正开火前获取（失败不烧冷却）；repairMode（被嫌话多）追加克制后缀、被拉黑话题不选。好感升级/击杀/重伤/死亡走统一 fireEvent()（计入日上限与全局配额，不吃每轮预算）。
- **ProactiveStage**：7 阶段状态机枚举（各阶段相对开火时刻的 tick 延迟：90s/300s/600s/900s/1200s/1800s/停）。
- **AutonomousTaskManager**：自主决策——女仆空闲时让 LLM 自己判断该做什么（走主模组 chat() 注入"自我指令"，系统提示词 Action First 规则驱动 LLM 调 switch_work_task 切任务）。平衡：仅任务为空闲（v1.5.287 UID 比较修复）+ 白天 1000~13000 tick + 主人在 16 格内；每 10 分钟最多一次、每天最多 10 次；LLM 未启用不触发。
- **ReplyFeedbackTracker**：聊天观察+回复反馈学习——每 5 秒扫聊天历史（watermark 只处理新消息）：①真实沉默计时（主人最后一次聊天 gameTime）；②主人意图检测（提问→LIGHT_FOLLOWUP 用、道歉→AffectManager.onOwnerApology）；③回复反馈学习（负面「别说了/好烦」→repairMode+拉黑话题到当日结束+写 error_mark 记忆，每小时最多 1 条；修复「我说的是…」→repairMode；正面「谢谢/说得对」→解除 repairMode+强化记忆）。模式串分四组+提问起始词 12 个。
- **PerceptionManager**：感知变化检测——每 20 tick 采集世界快照对比检测变化并气泡即时播报（零 LLM token）：敌对生物出现/接近/消失、主人受伤/低血、主人看向女仆、天气变化。每只女仆同类事件 30 秒限频。v1.5.193 dangerActive 判定缓存（0.5 秒 TTL，被气泡/主动对话/自主决策共用）。
- **PerceptionQueryTool**：`perception_query`——单工具聚合 6 种世界查询（LLM「先查后做」）：look_around（ASCII 空间感知网格）、terrain（地形平坦度）、build_site（建造场地分析：障碍物分类+清理建议）、inspect（单方块详情）、scanblock（按 ID 扫描）、scanentity（按类型扫描，hostile/passive/player/all+血量/距离）。超 16 次工具上限防超时。
- **WorldProbe**：世界探查引擎——实现上述六种查询（2.5D ASCII 网格、地形分析、可建性建议、方块/实体扫描），全部只读无副作用。
- **ApiQuotaManager**：全局 LLM API 配额——**所有女仆单一天合计**主动调用 ≤ 上限次（只约束女仆自主发起的 LLM 调用；被动对话不经过）。按游戏日重置；服务端单例静态；dailyApiLimit()=0 → 不限（v1.5.191 修旧 bug）；配额只在真正开火前获取；上限运行中可调高。heartfelt 经反射共享本配额。
- **ChatInfoUtil**：对话语言强制——配置 dialogue.outputLanguage 非空时强制该语言；留空=默认强制中文（zh_cn）（根治「突然全是日语」）。
- **MaidWorkList / WorkListContext / WorkListTool**：工作清单（LLM 跨轮规划）——work_list 工具写入/更新「当前任务计划」（[{content,status,priority}]），之后每轮对话/主动会话都注入上下文（WorkListContext，注册名 ai_worklist）；set 只允许一个 in_progress；query_todo（计划+建造进度）、build_need（当前绑定计划的材料缺口=剩余需求 vs 背包持有，补料后自动续建，不必重新下达）。
- **WorkStatusReporter**：工作状态播报——女仆选了 maid_smart 任务却不干活时气泡解释原因（被骑乘/没镐/附近没矿/没蓝图等）。v1.5.252f 降频（脱离工作状态持续满 20 秒才播报一次，之后每 20 秒最多一次）。reasonOf 抽给手册链路调试面板复用。

---

## 九、钓鱼（fishing 包，2 类）

- **FishingChairService**：钓鱼自动坐垫【普通服务类】（v1.5.252r 全部逻辑从 mixin 迁出——mixin 类被标记 invalid，普通代码调用即进世界第一 tick 崩溃）：①原逻辑已找到椅子/船→走原流程；②没有→扫 8 格内空坐垫/船→走过去骑；③还是没有→在最近水域岸边生成带标记 TLM 坐垫（`promaid_auto_chair` + 女仆 UUID），直接坐上→TLM 骑乘脑自动找水抛竿；④清理（每秒 sweep）：任务解除/脱离超 2 秒/女仆不在→删；⑤标记坐垫被摧毁只移除无掉落；⑥no_sit 气泡在马上生成坐垫时压掉。关键参数：生成坐垫冷却防循环、找不到水域 30 秒限频、远处水域自动走向（PENDING 表）、找椅宽限 7 秒、tickKeepSeatWalk 每 3 tick 补寻路（修"一步一停"）。
- **PlayerWaterLog**：玩家水行为日志（latest.log 搜 "player water"）——记录玩家放置/收走水（及岩浆）的位置与方块名（定位"女仆识别不到水"是距离还是判定问题）。v1.5.259 修 isSolid 误当 isLiquid 的 bug（改 FluidTags 判定）。

---

## 十、跨维度跟随（follow 包，1 类）

**MaidDimensionFollow**：女仆跟随主人跨维度传送——每 5 秒扫描全服女仆：跟随模式（非在家/非坐下/非骑乘/存活）且与主人不同维度→手动跨维度移动（setRemoved(CHANGED_DIMENSION)→setPos 主人身边落点→addFreshEntity）；落点取第一个「脚下实心、站立格空气」位置（向下最多 16 格），找不到本次不传；坐着的女仆不拉（建造强制坐下=玩家要她留在原地）；传送后清摔落距离+停导航+末影人传送音效。

---

## 十一、手册（guide 包，2 类）

- **GuideScreen**：详细介绍界面（与手册「建筑页面」同构）：章节目录→点章节→正文分页阅读。布局全自适应防重叠/出屏（标题居中截断、内容区动态、目录按钮行高自适应、正文自动换行分页、段间空行换行延续 § 颜色）。
- **GuideContent**：内容定义（v1.5.304 保姆式教学重写）——每章先讲「是什么」再手把手教「怎么用」+【举例】；25 章含更新日志（读 assets/promaid/guide/changelog.txt 按行分页）。

---

## 十二、记忆系统（memory 包，18 类）

### 管理/提取/存储
- **AiMemoryManager**：事件源（喂食→EVENT salience 7；好感升级→RELATION salience 9 永久；玩家死亡→EVENT salience 10）+ tick 调度（每 400 tick：maybeExtract 后台异步提取、游戏日切换生成「今日回顾」、维护周期固化/衰减/关系置信度衰减/error_mark 传播）。per-maid 开关：persistentData `maid_smart_memory` + 磁盘备份 `promaid_memory/toggles.json` + 客户端缓存广播。
- **AiMemoryExtractor**：后台异步提取——扫女仆聊天历史（TLM getHistory 公共 API），攒满 threshold 条新对话→**自建 HTTP 请求 LLM**（复用 TLM 站点配置，不经 TLM 的 LLMCallback：不烧玩家 token 配额、不污染女仆历史、无气泡/TTS 副作用）→异步回调切回服务器线程写记忆。失败/超时静默推进位置（不重试风暴）；进行中不重复触发；超时 5 分钟视为卡死允许重试。v1.5.198 记忆 API 绑定（独立站点，留空跟随 TLM；仅 OpenAI 兼容）。记忆根目录 `<世界>/promaid_memory`（v1.5.250 修 SRG——旧数据误写 advancements/promaid_memory，自动搬迁）。
- **AiMemoryStore**：五张 jsonl + meta.json（paragraphs/entities/relations/episodes/profiles/meta）。生命周期：精确去重（同 hash 合并 salience 取 max）；prune()（同 tag 桶 ngram-Jaccard 结构合并 + 非永久 30 天未访问衰减删除 + 超上限淘汰）；IO 异常静默；v1.5.190 防抖写盘（500ms 批量 flush）；v1.5.251 世界标注（world: 维度名）+ 灵魂目录路由。
- **AiMemoryModels**：数据模型（Gson 序列化 jsonl 单行）。时间约定：createdAt/updatedAt/lastAccessed=系统毫秒；eventTimeStart/End=游戏 tick。Paragraph（hash 去重键、content 截断 300 字、salience/accessCount/permanent/deleted/protectedUntil）。
- **AiMemorySearch**：多路召回 + RRF 融合（k=60 可调）——段落关键词（uni+bi-gram 混合）、最近段落、画像证据、关系三元组、多级记忆索引（第五路）；被否定的段落（error_mark/error_affected）不参与检索。
- **AiMemorySkipList**：跳表时间索引（游戏 tick→段落 hash，O(log n) 范围查询），供多级索引生成与按时间检索，取代线性扫描。
- **AiMemoryType**：8 种类型（EVENT/PREFERENCE/PROMISE/RELATION/EMOTION/WORLD/SUMMARY/DIALOGUE）。
- **AiMemoryWriteStrategy**：写入策略——按类型+tags 决策分层（short_context→user_profile/relationship_event/world_fact/self_memory/repair_debt/summary）/salience 下限（关系承诺≥8、偏好边界情绪≥7、世界事实≥6）/保护（关系偏好承诺情绪类不参与衰减淘汰）/永久（salience≥9 且边界承诺关系自省类）。
- **AiMemoryArchiver**：记忆自动归档——索引生成只在真实睡眠收尾（SleepFinishedTimeEvent，熬夜不触发）；短期层段落年龄≥shortTermDays 且重要度≥线→打 long_term 标记（豁免衰减遗忘）；日/3日/周/月四级日记式摘要（跨日/周/月边界或睡醒生成）；状态存 archiver_state.json。
- **AiMemoryIndexStore**：多级记忆索引库——四级压缩（日详细/3日中等/周压缩/月高压缩 top N），全部永久归档（memory_index.jsonl），每条记录标关键事件段落 hash 供回查；±1200 tick 容差判重。
- **CarePointGenerator**：每日关心点（纯规则零 LLM）——从情绪+记忆画像推导「下次该怎么对主人」的行动建议（最多 3 条），附加到每日回顾注入对话；主动会话自动把关心点当话题素材。
- **LlmEnableManager**：per-maid LLM 开关（默认开）——关闭后 TLM 原版 AI 聊天被 MaidChatLlmGateMixin 拦截；heartfelt 主动对话检查同一 NBT 标记降级为固定文本气泡（零跨 mod 依赖）。存储与记忆开关同构（persistentData + llm_toggles.json + 客户端缓存）。
- **RelationshipMemoryAdapter**：对心契誓约/爱憎分明的软适配（不依赖，装了就感知）——信任/恐惧改事件式描述、同 fact key 覆盖；新增状态：怀孕/分娩/母亲身份/丧子/背叛/淡忘/主人死亡哀悼/告白被拒/心碎/悔改/女儿成长/特殊奶计数；离婚撤销（married true→false 作废旧永久记忆+停用「妻子」三元组）。轮询检测（每 400 tick）只在状态变化时写；mmLoaded/llLoaded 门控优雅降级。
- **QueryMemoryTool**（`query_memory`）：长期记忆检索工具（多路 RRF 融合，带重要度）；记忆关闭时拒绝检索；命中段落按内容去重 touch 一次。
- **QueryMemoryIndexTool**（`query_memory_index`）：多级记忆索引查询——先不带范围调用返回可用跨度列表，选跨度带范围二次调用返回日记摘要+关键事件原文（粗定位→细查询）。
- **RememberTool**（`remember`）：LLM 当场决定记什么（content/type/importance/tags）——与被动提取互补；关系/承诺/偏好自动抬高重要度。
- **WorkingNoteTool**（`working_note`）：跨对话工作笔记（临时的、可覆盖的）——set/get/clear，落盘 `<世界>/promaid_memory/<uuid>/working_note.txt`；注入对话上下文。

---

## 十三、角色包（persona 包，2 类）

- **PersonaPackage**：每女仆一份（persona.properties 人格种子 + traits.properties 人格参数 0~1 + core_memories.jsonl 宪法级核心记忆，上限 20 条）。设计：与聊天记忆**分离**（聊天/提取/维护永不改写 persona）；注入为**只读投影**（LLM 可依据但不反向改人格）；首启自动生成默认模板；mtime 缓存（改文件即时生效）。
- **PersonaSyncManager**：人设同步——玩家中途修改 TLM 人设提示词 = 一次人格转折：指纹检测变化时 a 沉淀演化记忆（宪法级 salience 8）；b 身份性核心记忆降级（salience>3→3，新设定不被旧宪法压制）；c 同一天只处理一次；清空人设同样触发。指纹文件 persona_sync.properties。

---

## 十四、提示词补充（prompt 包，1 类）

**PromaidPromptAppender**：坐姿工作说明——女仆可坐着执行任务，但 LLM 看到 `<context>` "sitting: yes" 以为要站起来→言行矛盾。本段在 system 提示词末尾追加（PromaidPromptMixin 注入）：明确「坐着也能正常干活、不要宣布站起来、不是被控制」。MARKER="## Work Posture (Promaid)" 幂等。

---

## 十五、保护（protect 包，1 类）

**MasterDeathTeleportHandler**：主人死亡强制传送——主人死亡瞬间所有属于该主人的女仆无条件立即传送到主人**重生点**（床/重生锚，可能跨维度；无重生点退主世界出生点）。v1.5.231 修：远处未加载女仆静默失效→维护女仆追踪表（EntityJoinLevelEvent 注册/EntityLeaveWorldEvent 标记卸载/每 5 秒刷新），已加载直接传、未加载强制加载区块再传；实体永久移除彻底删追踪条目。与自保 teleportHome 区别：本处理器不受任何判定影响（不走冷却、不查威胁/距离/范围）——主人死亡是最高优先级事件。

---

## 十六、灵魂路由（soul 包，1 类）

**SoulBindingService**：v1.5.251e 灵魂核心功能移除（灵魂核心物品/开局赠送/绑定/迁移/双分身防护全删），**只保留路由兼容**：旧版本已绑定灵魂的女仆（`maid_smart_soul_id`）记忆继续读写全局灵魂目录 `config/maid_smart/souls/<soulId>/memory/`（已写入数据不丢）；未绑定/新女仆走世界目录。`storeFor(maid, level)` 为记忆系统统一入口（顺便注入 world: 来源世界标注）。

---

## 十七、任务（task 包，10 类）

- **MaidWorkTags**：工作站桩标记（`maid_smart_work_still`）——isStill 双保险（标记+任务确为工作 build/cook/brew）；isNonCombatWork（UID path 不在战斗集合即非战斗干活，排除 idle——待机不算干活）用于「干活不被打断」闸门；isCombatTask（攻击/弓/弩/三叉戟/弹幕）供副手盾牌判定。
- **MaidMineBehavior / MaidMineTask**：挖矿——找最近最有价值矿石→走过去→手持镐渐进开采（与玩家同公式不秒破；工具加速；女仆稍快 ×1.2）。目标选择=距离平方+深度惩罚(3.0/格)-矿石价值（钻石 500/绿宝石 450>金/铁 250>煤/石英 100）。扩展：自定义矿表（mine.oreValues 适配其他 mod 矿石）、连锁采集/自动收集（默认关）、搭方块 10 秒自动清理、换镐改发现矿石后判定。v1.5.123 关闭 TLM 随机散步（修"走一步停一下"）；RIDE_WORK 坐下照常挖。
- **MaidCookBehavior / MaidCookTask**：烹饪——给附近炉子补燃料/生食、收取成品（槽位 0 食材/1 燃料/2 成品；只操作熔炉类方块；每次处理 1 轮；食材白名单 14 种；燃料凡是原版 isFuel 都可用，优先数量最多；处理间隔 100 tick；绑定炉子到达后坐下不动）。v1.5.252 重写燃料（v1.5.252e 去掉冶炼回归纯烹饪）。
- **MaidBrewBehavior / MaidBrewTask**：酿造——给酿造台补烈焰粉与材料、收取药水（槽位 0-2 药水瓶/3 材料/4 烈焰粉；**正向材料白名单** 10 种：净疣+治疗/夜视/力量/迅捷/跳跃/防火/水肺/再生/缓降——删负面与喷溅化材料；处理间隔 100 tick；三瓶齐且燃料材料耗尽时收取）。
- **MaidToolAutoEquip**：任务工具自动装备——mine→镐（等级最高）、attack→近战武器、ranged→弓、crossbow→弩、trident→三叉戟、danmaku→御币。关键修复：真正主手是 getHandsInvWrapper slot 0（旧写背包 slot 0 从未生效）；武器评分统一（DPS>耐久>附魔词条）；v1.5.172 挖矿不再任务级自动换镐（用户嫌心急）；战斗自动副手盾（空手才换、爆盾自动补）。
- **MaidToolAutoEquipBehavior**：core 行为任何 activity 都运行；canUse 里直接执行换工具并返回 false（永不占用运行槽位）；行为幂等；还负责建造模式强制坐下（tickBuildSit 防跟随拉走）。
- **MaidWeaponGuard**：幼儿女儿武器禁持——婴儿/幼年阶段女儿手上出现武器时删除并原地丢一个一样的到地上（主手/副手，每 1 秒轮询覆盖一切入手机制）；工具（挖掘类）不受影响。

---

## 十八、AI 工具（tool 包，8 类）

- **SmartAttackTool**（`smart_attack`）：指挥战斗——三种目标模式：nearest（12 格 Monster 扫描+TLM canAttack 完整过滤防误伤）、owner_target（主人当前攻击目标，护主集火）、attacker（最近攻击过女仆的生物，5 秒窗口记录）。执行：切 TLM 原生 attack 任务→写 ATTACK_TARGET+setTarget 双重锁定→播报确认；单兵战术自动接管走位、自保兜底保命。
- **SmartCraftTool**（`smart_craft`）：按配方合成（像玩家一样合成）——LLM 传 item：从 RecipeManager 找输出匹配配方→从背包逐个抽材料（不拆堆不卡槽）→结果交主人背包（放不下退回）→材料不足报缺什么。不做凭空生成、物品守恒。
- **SmartGiveItemTool**（`smart_give_item`）：把背包指定物品交给主人（最多一整组）；主人背包满自动退回；无 owner/未知 id/未持有均有明确报错。
- **SmartMoveToTool**（`smart_move_to`）：让女仆走到主人身边（一次走过去，不改变跟随模式）。
- **SmartPickupTool**（`smart_set_pickup`）：切换自动拾取模式；已处于目标状态幂等返回。
- **SmartPlaceTool**（`smart_place`）：从背包取方块放到指定位置（默认女仆前方 2 格；replace 参数控制是否覆盖；只放实际有的方块、放不下报原因、每次最多放 8 个）。
- **SmartReportTool**（`smart_report`）：汇报自身状态（Health/Favorability 四档/Task/Schedule/Pickup）；v1.5.190 修 getTask() null NPE。
- **OwnerInventoryTool**（`smart_owner_inventory`）：查看主人物品栏（只读遍历背包 0-8 快捷栏/9-35 主区/36-39 护甲/40 副手，合并计数+槽位区，可传 item 过滤，限行数防 token 爆表）。

---

## 十九、语音（voice 包，2 类）

- **SystemTTSManager**：系统消息 TTS——所有系统气泡（感知/工作/自保/建好啦等规则消息，由 ChatBubbleLimitMixin 汇入）朗读：①系统语音包命中→直接播放（免 TTS）；②语音缓存命中（`config/maid_smart/voice_cache/<sha256>.ogg`，训练一次保存）→直接播放；③未命中→调 TLM TTS 站点合成，字节落盘缓存后播放。门禁：总开关+TLM TTS 开关+站点存在+有主人+文本可朗读+per-maid 冷却；fire-and-forget 全异常静默。
- **SystemVoicePack**：系统语音包——`config/maid_smart/system_voice/` 下 manifest.json+ogg 音频映射（`{"entries":[{"text":"建好啦！","file":"build_done.ogg","mode":"contains"}]}`，mode 省略=exact）；音频相对 manifest 目录；reload/entryCount 面板状态。

---

## 二十、Mixin 层（41 类）

### 提示词/对话（5）
- **PromaidPromptMixin**：拦截 TLM `LLMMessage.systemChat(EntityMaid, String)`（两条设定路径唯一汇聚点）@ModifyVariable 追加坐姿工作说明（MARKER 幂等）。v1.5.102g 修 @ModifyArg(at=HEAD) 非法组合崩溃。
- **MaidChatLlmGateMixin**：拦截 `MaidAIChatManager.chat(...)`（AI 聊天唯一入口）——per-maid LLM 开关关闭时 cancel 不发请求+系统提示。
- **MaidChatLanguageMixin**：拦截 tryToChat @ModifyVariable 直接替换 ChatClientInfo.language（覆盖所有调用方）——配置非空强制该语言，留空强制中文；内嵌 MaidLangCheckMixin（LLMCallback.onSuccess 输出语言二次检测，目标中文时汉字占比过低→内嵌翻译，翻译仍非中文放行原文不循环）。
- **ChatBubbleLimitMixin**：气泡限频——每女仆 5 秒最多 1 个对话气泡（思念/状态播报/缺料全受约束）；通过限频的气泡同步到主人聊天框；「建好啦」放行；敌袭红色 §c[警示] 前缀、全系统对话统一青色 §b；通过限频的真实气泡汇入 SystemTTSManager。
- **CallResponseChatSpamMixin**：**已废弃**（v1.5.386）——从 mixins.promaid.json 移除，保留空壳（无 @Mixin 注解）仅作历史记录。历史：屏蔽爱憎分明 2.0.2 女仆背包界面「饱食度」调试聊天消息；2.0.4 移除 onMaidGuiRender 后 @Redirect 找不到目标→InvalidInjectionException→模组 broken→渲染线程 NPE 崩溃。类已标 @Deprecated。

### 移动/站桩（3）
- **MaidMoveSuppressMixin**：拦截 MoveToTargetSink.doTick——三种标记两档强度：WORK_STILL_TAG（彻底静止：清 WALK_TARGET+停导航+取消）；PRESERVE_TAG（只清+取消、不停导航——逃跑路径是直连导航）；战术接管（同自保）。根治「站桩时灵时不灵」。
- **MaidStationaryMixin**：拦截 EntityMaid.travel——只清水平速度（setDeltaMovement(0,y,0)）不 cancel（v1.5.98c 修旧版掐死垂直运动导致站桩溺水）；TLM 自定义移动控制用 setDeltaMovement 直加速度绕过 WALK_TARGET。
- **MoveToTargetSinkDurationMixin**：@ModifyConstant 时长 150→600、250→1200（30~60 秒才刹车一次，走路连续）——原生任务呆滞修复③。

### 跟随/传送（3）
- **FollowPreserveMixin**：拦截 MaidFollowOwnerTask.checkExtraStartConditions——自保标记存在且血量未恢复到 70% 返回 false（防低血逃跑被传送回主人身边送死）。
- **MaidTeleportPreserveMixin**：拦截 EntityMaid.teleportToOwner——自保传送豁免第二道防线（跟随已 RUNNING 时也拦）；v1.5.138 建造女仆任何时刻禁止 TLM 瞬移（根治「下达建造后被传送走」）；v1.5.252q 修 path 判定。
- **MaidDimensionFollow 相关**：跨维度跟随见 follow 包（非 mixin）。

### 建造/冻结（6）
- **BuildActivityForceMixin**：拦截 MaidUpdateActivityFromSchedule——建造任务强制 WORK activity（修「下达后不建造」：TLM 按时间表切 REST/HOME/IDLE 导致任务行为不被评估）；暂停=解除绑定；v1.5.176 方法带完整 descriptor（SMART 版 TLM 有同名重载，按名字匹配会命中错误重载→崩溃）。
- **GravityFreezeMixin**：拦截 FallingBlock.tick——建造区重力冻结（沙子/沙砾/混凝土/铁砧放置后悬空不落）；完成时悬空重力方块落下由完成气泡提示。
- **ChunkFreezeMixin**：拦截 ServerLevel.tickChunk——冻结区块取消整个随机刻（树叶不衰减/草不传播/作物不生长）；注解目标必须写 SRG 名（手工编译无 refmap）。
- **LeavesBlockMixin**：拦截 LeavesBlock.randomTick——蓝图内树叶位置永久豁免（装饰树"打印"后不消失）。
- **MixinTntBuildGuard**：拦截 TntBlock.onPlace/neighborChanged——建造期防自燃（BuildTntGuard.suppressing 判定）。
- **ChairNoDropMixin**：拦截 AbstractEntityFromItem.killEntity——自动生成钓鱼坐垫被摧毁不掉落物品（防刷物品）。

### 工作打断/原生任务呆滞（7）
- **MealTaskGatingMixin**：拦截 MaidWorkMealTask——非战斗任务进行中跳过工作餐（饥饿值是惰性数据，工作餐本质是刷好感）。
- **StealEdibleGatingMixin**：拦截 MaidStealEdibleMoveBlockTask/UseTask——工作中禁止「偷吃」（拆/吃可食用方块）。
- **PanicGatingMixin**：拦截 MaidPanicTask.start——非战斗任务进行中+血量≥30% 不恐慌（血量<30% 仍恐慌，自保行为兜底）。
- **SchedulePullbackMixin**：拦截 MaidUpdateActivityFromSchedule.start——@Redirect 跳过 restricTo 与 walk（切班不再被拽回工位/回家）。
- **SchedulePosTickMixin**：拦截 SchedulePos.tick——非战斗干活中跳过本 tick 拉回（补 SchedulePullback 漏网）。
- **MaidRunOneYieldMixin**：拦截 MaidRunOne.tryStart——任务正走向目标时 RunOne 让位（随机散步不覆盖任务目标）。
- **MaidCheckRateHalveMixin**：拦截 MaidCheckRateTask——检查节流间隔整体减半（干活衔接更紧凑）。

### 原生任务流畅化（2）
- **NativeTaskSmoothMixin**：拦截 MaidBrain.registerWorkGoals/registerRideWorkGoals——@Redirect 把除 TLM 基建行为外的任务行为时长改为无限（Integer.MAX_VALUE，反射写 final 字段）——根治「干一下活→发呆→再干」。
- **MaidSweepMixin 相关**：见 FarmSweepMixin。

### 农场（1）
- **FarmSweepMixin**：拦截 MaidFarmPlantTask.start——连收连种（3×3 收割+补种，来回跑次数降约 1/8）+ 收获物自动收集 + 连锁收获（BFS 蔓延，冷却 10 tick）+ 批量种植（BFS，冷却 40 tick）+ 区块加载检查（防 Server thread 自锁卡死）。总开关 misc.produceTaskEnhance。

### 战斗相关（6）
- **TaskAttackWeaponMixin**：拦截 TaskAttack.isWeapon——锄头不作为武器（1.20.1 锄头带攻击伤害属性→女仆拿锄头攻击）。
- **MaidPlayerDamageMixin**：拦截 EntityMaid.hurt——玩家伤害五模式：0 跟随 TLM（÷5 封顶 2）/1 完全免疫/2 无限制/3 有上限（单次=最大生命×cap，默认 10%）/4 仅一点。HEAD 处理全免疫；@Redirect 拦截 Mth.clamp 压制调用还原再封顶。
- **MaidShieldTimingMixin**：拦截 MaidUseShieldTask——时机举盾（攻击冷却中+目标贴身才举盾，冷却满放盾攻击）替代原版「8 格内一直举盾」。
- **MaidBaubleTotemMixin**：拦截 checkTotemDeathProtection——饰品栏不死图腾触发复活 + 共享不死图腾（主人致命伤时女仆背包/饰品栏图腾优先救主人）；图腾必须真实消耗；背包里的图腾也触发；主人自己手里有图腾让原版处理。
- **HomingPotionMixin**：拦截 ThrowableProjectile.tick——投掷药水追踪弹（`maid_smart_homing`=目标 UUID，每 tick 修正速度方向；距离<1.2 直接挪到目标身边）；v1.5.239 目标从 ThrownPotion 改 ThrowableProjectile（tick 继承自父类，注入子类会崩溃）。
- **MaidHoneyIgniteMixin**：拦截 MaidCollectHoneyTask.start——采蜜后给蜂箱下方放营火（防蜜蜂蛰）；再采蜜自动补上。

### 杂项（7）
- **MaidConfigMemoryMixin**：拦截 TLM MaidConfigContainerGui renderAddition——绘制「长期记忆」面板（per-maid 开关+记忆条目投影；纯只读）；关系状态由 heartfelt 反射软调用（未装为 null）；无 refmap 时 @Shadow 无法定位继承成员→按钮用反射调 Screen.m_142416_、字体用 Minecraft.getInstance().font。
- **MaidDebugPanelMixin**：注入 maidmarriage 调试面板——底部加「AI记忆：开/关」按钮（C2S AiMemoryTogglePacket，服务端校验主人/OP）；v1.5.387 红字免责声明。
- **MaidTtsVolumeMixin**：拦截 MaidAISoundInstance 构造器——@ModifyArg 音量参数按配置倍率放大；v1.5.215 必须 static（super() 调用处不能访问 this）。
- **MaidCheckRateHalveMixin**：见上。
- **FishingAutoChairMixin**：拦截 MaidFindSitTask——钓鱼自动坐垫注入壳（逻辑全在 FishingChairService 普通类）；start TAIL 生成坐垫、lambda$start$2 HEAD 压掉 no_sit 气泡。
- **MaidPickupPriorityMixin**：拦截 MaidPickupEntitiesTask——挖矿时拾取不启动（矿掉物由挖矿流程即时回收）；v1.5.101c 价值重排（低价值物品权重 0 排后，模组物品权重 1 排前）。
- **MaidCakeEdibleMixin**：拦截 Item——蛋糕变成可吃物品（注册名 minecraft:cake 返回食物属性 营养 2/饱和度 0.6；自动获得进食动作/音效）；玩家右键蛋糕仍是放方块。
- **LoveLoatheHungerGateMixin**：拦截爱憎分明 HungerManager——饥饿系统测试开关（misc.loveLoatheHungerOff 默认 true=关闭）：HEAD cancel onServerTick（饿死/撑死/自动进食/速度惩罚全禁）与 onMaidDeath；onMaidEat 保留。v1.5.252o 修目标包名（tartaricacid→JumDa5he）。
- **MaidShearDropMixin**：拦截 MaidShearTask.start——剪羊毛掉落直进女仆背包。
- **MixinInteractionSittingAllow**：注入 maidmarriage MaidHugManager/ChildInteractionManager.handleInteractionToggle——HEAD 把目标坐姿女仆站起来（Alt+J 对坐着女仆可进交互面板）。
- **MaidChatLlmGateMixin**：见提示词/对话。

---

## 附：统计核对

137 个类全覆盖——根 3 + action 1 + affect 3 + build 19 + combat 6 + command 1 + config 2 + dialogue 13 + fishing 2 + follow 1 + guide 2 + memory 18 + mixin 41（含 1 个废弃空壳 CallResponseChatSpamMixin）+ persona 2 + prompt 1 + protect 1 + soul 1 + task 10 + tool 8 + voice 2。

跨包关键联动链：ChatBubbleLimitMixin（气泡）→ SystemTTSManager/SystemVoicePack（朗读）；FishingAutoChairMixin→FishingChairService；FarmSweepMixin→FarmSweepCache；SelfPreservationBehavior/MaidCombatTacticsBehavior→MaidMoveSuppressMixin；PromaidPromptAppender→PromaidPromptMixin；SoulBindingService→AiMemoryStore；BlueprintLib/BlueprintBuildExecutor/BuildPlan/BuildArchive 构成建造系统核心。
