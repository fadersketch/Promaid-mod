# 仿创造飞行 · 独立更新日志

> 按作者的要求，本分支**不改动主 CHANGELOG.md**（避免合并冲突），自己的实测记录单独放在这里。
> 编号沿用「实测」系列，接续上游 main 当时的最新一条（六百七十二）。

## 实测六百七十八【仿创造飞行 · 智能待命（落地贴着你）+ per-maid 开关（配置界面一行 + 快捷键）】

### ① 反馈

> "女仆飞行状态下无论如何都会一直和主人保持一定距离和高度差。但是有一些交互(TLM和原版)可能需要
> 玩家和女仆之间站得比较近（如喂食金苹果/药水）。"

根因很清楚：她常驻「主人身后 3.5 格 + 高 2 格」⇒ 3D 距离 ≈4 格，而**原版实体交互距离只有 3 格**
（`Player#entityInteractionRange`），所以喂食/摸头/右键交互全部够不到。

### ② 智能待命（`freeFlightIdle`，默认开）

**"会飞" ≠ "一直悬着"**：改成「赶路时飞、你停下来时落到你脚边待命」。

| 起飞（任一） | 落地待命（全部） |
| --- | --- |
| 主人在移动（水平速度 > 0.03 格/tick） | 主人静止 ≥ `freeFlightIdleSeconds`（默认 3 秒） |
| 水平距离 > 4 格 | 已贴近到 `freeFlightNearDist` + 1.5 格内（默认 2 格） |
| 高差 > 2 格 | 主人不在滑翔/创造飞行 |
| 她在半空（没落地，别让她掉） | 她与主人脚下都是「实心地面 + 上方两格空」（不往岩浆/水/虚空落） |
| 主人正在滑翔 / 创造飞行 | |
| 坐标档命令（`freeflight_goto`） | |

落地走**软着陆**（保持无重力 + 恒速 0.12 格/tick 下降 + 每 tick 清零坠落距离），下降途中还会朝主人
轻微漂移（≤0.15 格/tick）。落地后进入「待命」状态：重力交还、**不再抢移动**（完全交给 TLM 跟随），
她就是个站在你身边的女仆——喂食、摸头、抱抱全部可用。你再一动，她自动重新起飞。

关掉这一项 = 始终悬停（旧行为）。

### ③ per-maid 开关：全局总闸 + 每只女仆可单独关掉

- **全局 `freeFlight`** = 总闸（默认关）；**per-maid 默认跟随全局**，玩家可单独关掉某一只；
- **存法**：per-maid `persistentData` + 磁盘备份（`promaid_free_flight_toggles.json`）——
  persistentData 只随实体 NBT 保存，女仆被魂符收走/实体重建时会丢，丢了就会回落成"跟随全局"，
  于是"我明明关了她"变成"她又飞了"（AiMemoryManager 踩过同一个坑，照抄它的双保险）；
  **绝不用 TLM 的 TaskData 存**：它的 `writeSyncData` 会把编码结果强转 CompoundTag，一点开关就崩服（实测教训）；
- **界面一行**：右键女仆 → 配置界面 → 「创造飞行：开/关」，与 TLM 自己的「显示背包 / 能否开门」
  同格式同列（放在原生开关列上方，不遮挡任何 TLM 控件）。
  **TLM 那个界面确实可以扩展**：那 8 行是硬编码的、没有注册表，但
  `AbstractMaidContainerGui.initAdditionWidgets()` 是 protected 扩展点、`MaidConfigButton` 是公开控件
  ——追加自己的一行**不需要改 TLM 本体**（我们仓库的「AI 记忆」开关用的就是这套）；
- **快捷键**：Promaid 分类下新增「切换女仆仿创造飞行」（默认**不绑定**，玩家自己设）——
  准星对准女仆（或站到她 5 格内）按一下即可；服务端校验：主人或 OP + 4 格内；
- 客户端显示靠 S2C 状态包（`persistentData` 客户端读不到，否则重开界面会把"关"显示成"开"，
  一点反而又打开）。

### ④ 待实测

无头服没有真玩家 ⇒ 智能待命与界面行都需要**实机**验证：① 主人停下 3 秒后她是否落地贴到身边、
喂食是否可用；② 一动她是否重新起飞；③ 配置界面里那一行是否显示正确、点击是否生效（含重启后是否保持）。

## 实测六百七十七【仿创造飞行 · 朝向：倒退着飞 + 延迟转身 + 平滑转体】

### ① 反馈（用户实测后的 QoL）

> "女仆飞行时总是会朝向自己飞行的方向……当女仆站在玩家前面、面朝后面对玩家的时候，玩家如果
> 向前走，女仆会向前移动，这时候她就会**立刻从后面转向前面**，而且因为玩家的移动可能有一些
> 随机干扰，这个 180° 向后向前转可能看起来比较突兀。我可能预期的行为是：女仆一开始是
> **倒退着飞行**，在一段时间之后转 180°（这个转体过程如果有动画最好，如果因为一些结构性
> 问题没法有动画也能接受）转向前方朝前飞行。"

（用户补充：原本以为模型包里有"过渡动画/向后飞行动画"，实测发现**并不存在**——记错的是这部分。
所以转体只能靠朝向本身平滑，没有动画可用。）

### ② 根因（两个，都在旧 steer 里）

1. **朝向永远等于速度方向**：她站在主人前面、主人向前走时，跟随点也向前挪，她的速度方向
   指向"背对主人"，于是瞬间翻面；主人移动的随机抖动让这个 180° 来回翻；
2. **旧代码连 `yRotO` 一起拍成新值**：等于把客户端的逐 tick 插值也掐死了——就算朝向变化
   平滑，渲染出来也永远是硬切。

### ③ 新朝向规则（跟随主人的"社交档"）

| 状态 | 朝向 |
| --- | --- |
| 悬停 / 慢速（< 0.055 格/tick） | **面向主人**（陪伴感） |
| 迎着主人飞（速度与"指向主人"同向） | 面朝前进方向 |
| **退着飞**（主人在她身后推进） | **先面向主人倒退**，持续 60 tick（3 秒）后才**转身朝前** |
| 坐标档（freeflight_goto）/ 软着陆 | 面朝前进方向；停着保持 |

转体本身：每 tick 最多转 **12°**（180° 用 15 tick = 0.75 秒），走最短弧；**不再写 yRotO/yBodyRotO**，
让原版逐 tick 同步 + 客户端插值接手。另把"期望点"一并交给 `LookControl`（实测五百二十九 同款坑：
它的 tick 在大脑之后跑，残留目标会把头拧回去）。

### ④ 实测（无头服，坐标档）

飞向 A 点后朝向 −100.1°；改令飞向反方向 B 点，采样逐帧：
`-136 → -160 → +163.9（走最短弧穿过 ±180）→ 139.9 → 115.9 → 92.7`——每 tick 约 12°，
整个 180° 约 0.7 秒转完，**无瞬切** ✓。（社交档的倒退/延迟转身需要在有主人的实机里验证，
机制与坐标档同一条 rotateToward 路径。）

## 实测六百七十六【仿创造飞行 · 资格表支持"数据组件"（神化那类命令挂上去的飞行）】

### ① 反馈（用户实测三条）

1. **效果路成功**：给女仆施加 Apothic Attributes 的 `apothic_attributes:flying` → **飞行动画播出来了** ✓
   （缺"静止↔前进/后退"的过渡动画，看着略卡顿——属 QoL，先推迟）；
2. **物品路之外还自动认出了一件**：`modern_industrialization:quantum_chestplate`（tooltip"启用 创造模式飞行"）
   **没加进列表**却被认出来了 ✓——那正是 **"重力归零"通用启发式**在起作用（它把佩戴者的重力抹平），
   这条通用路不需要点名模组；
3. **神化（Apotheosis）的组件飞行没命中** ✗：
   `/apoth add_bonus_modifier neoforge:creative_flight ADD_VALUE 1 apothic_attributes:armor` 挂上去之后，
   加 `#neoforge:creative_flight` 也没用——因为那是**修饰符 id**，不是**物品标签**，
   而当时的匹配只看物品 id / 物品标签。

### ② 新增：资格表支持数据组件（两种写法）

| 写法 | 含义 |
| --- | --- |
| `@命名空间:组件` | 物品**有**这个数据组件就算格（宽） |
| `@命名空间:组件~文本` | 该组件的值里**包含这段文本**才算格（准） |
| `@*~文本` | 任意组件里包含这段文本（最宽，谨慎用） |

对上面第 3 条，配置里写：
```
@apothic_attributes:bonus_stack_attribute_modifiers~neoforge:creative_flight
```
实现口径：按 id 从**数据组件注册表**取类型 → `stack.has(type)` 判存在 → 需要文本时把**该组件自己的值**
转成字符串再找子串（只取该组件自己的值，避免"别的组件里恰好出现同一个词"造成误判）。

### ③ 实测（无头服，用原版组件当替身）

给女仆一件改名为 `FlyToy` 的钻石（= `minecraft:custom_name` 组件），配置写
`@minecraft:custom_name~FlyToy` → 日志 `物品/组件=true` → 她起飞、飞到目标、软着陆
（−42.8 → −47.7 → −52.5，恒速 0.12 格/tick）✓

### ④ 更新后的资格表全语法

物品 id · `#物品标签` · `@组件` · `@组件~文本` · `@*~文本`（另有三路探测：物品表 / 效果表 /
重力属性 ≈ 0）。面板那一行的说明也同步了。

## 实测六百七十五【滑翔动画二选一：游泳动作 / 模型自己的鞘翅动画】

### ① 诉求

"如果这个问题和鞘翅的动画差不多，我们能不能做个配置项，让玩家选择女仆滑翔的时候是使用游泳动画还是鞘翅动画？"

### ② 机制（顺着六百七十四 那条线摸清楚的）

作者给滑翔套的是**游泳动作**，不是模型自带的：TLM 的 `swim` 状态（priority 0）只认
`isVisuallySwimming()`，而 `EntityMaid.isVisuallySwimming()` 直接返回 `isSwimming()`
（共享标志位第 4 位，且被 `MaidSwimManager` 每 tick 清掉）——所以作者另写了一个 mixin
（`MaidSwimGlideMixin`）在**滑翔时把它的返回值顶成 true**，游泳动作就跟着来了。
这是兼容性最好的选择：官方包与第三方包普遍都有 `swim`，而**几乎没有 `elytra_fly`**。

而模型包（如圣女酒狐）里明明做了 `elytra_fly`（与 YSM 玩家版同名），在女仆身上是死数据
（见实测六百七十四）。

### ③ 做法：新增「滑翔时用鞘翅动画」（默认关）

- 关（默认）：一字不改，游泳动作照旧；
- 开：`MaidSwimGlideMixin` **不再顶游泳位**，改由客户端注册的 `elytra_fly` 状态接管
  （priority 1：0 档是死亡/睡觉/游泳，1 档排在 `jump`(2) 之前——她滑翔时正是"离地且不在水里"）；
  条件只读**已同步的滑翔标志位第 7 位**，多人下客户端也认得出。
- 与六百七十四 的 `fly` 状态同为 priority 1、两种状态互斥（滑翔 vs 自由飞行），
  同优先级内取先注册者，所以 `elytra_fly` 注册在前。

⚠️ 模型包**没做** `elytra_fly` 时，打开这一档会落到站立/待机姿态——所以默认关，
确认你的包有这条动画再开（官方包与绝大多数第三方包都没有）。

## 实测六百七十四【仿创造飞行 · 让模型包里那条 fly 动画活过来】

### ① 调查结论（读模型包 + TLM jar 反汇编）

- TLM 的动画"状态机"是 **Java 硬编码**的：`AnimationRegister.register(name, priority, predicate)`，
  按 priority 0..4 顺序取**第一个命中**的状态；全部状态只有死亡/睡觉/游泳/梯子/棋局/船/椅/坐/
  水中站/受击/跳跃/跑/走/待机——**没有 fly、也没有 elytra_fly**；
- 但模型包（如圣女酒狐）里**明明做了**这两条动画（`fly` loop、`elytra_fly` loop，与 YSM 玩家版同名）：
  它们在女仆身上是**死数据**。YSM 玩家侧能播，是因为 YSM 本体硬编码了
  `Player#getAbilities().flying → fly`、`Pose.FALL_FLYING && isFallFlying → elytra_fly`
  （玩家专属，女仆没有那套 Abilities）；
- 女仆在空中的实际回退是 `jump`（离地且不在水里），其余时候 `idle`。

### ② 做法：注册一个同名状态

`AnimationRegister.register` 是 private static（第三方无公开入口），用 `@Invoker` 暴露
+ 客户端 mixin 在 `registerAnimationState()` 尾部挂上我们的状态：

- 名字 **`fly`**——与模型包/YSM 同名，**模型包一行都不用改**；
- **priority 1**：0 档是死亡/睡觉/游泳（该优先），1 档排在 `jump`(2) 之前——她悬停时正是
  "离地且不在水里"，不抢就会被动画顶掉；
- 条件只用**客户端可见的同步数据**：在场资格物品 / 身上效果 / 重力属性 ≈ 0，
  外加"离地、不在水里、不是乘客、没坐下、没在挨打"。**刻意不去读服务端那份"是否在飞"**——
  原版 `Entity.noGravity` 不在同步数据里，为它单开一条同步通道不值当；
- 模型包没做 `fly` 动画时**毫无影响**（TLM 找不到该动画名就照常回退）。
- 注意：TLM 的类不参与 remap，所以这条客户端 mixin 在我们本地手搓构建里也能生效。

### ③ 待实测

客户端侧改动无法在无头服验证（渲染层），需要**进游戏**确认：
① 不崩；② 圣女酒狐这类**做了 fly 动画**的模型，在女仆悬浮时播的是 fly 而不是 jump。

## 实测六百七十三【仿创造飞行：让女仆用上"创造飞行类物品"】

### ① 诉求

整合包里给"创造飞行/悬浮"的手段五花八门——饰品、护甲套装、药水效果、重力归零类法术……
但它们**几乎全部只对玩家生效**（逻辑写死 `instanceof Player` / 读 `Abilities.mayfly`），
女仆装着它们一字不动。问：能不能让女仆也飞（悬浮 + 自由升降）？

### ② 关键调研结论（决定了实现形态）

- **不存在"通用飞行标记"**：1.21.1 里玩家的 `mayfly` 在 `Player#getAbilities()` 里，
  **女仆没有这套 Abilities**（TLM 全树除一处玩家判定外从未碰过 `mayfly`/`Abilities`）。
  所以只能"**仿**创造飞行"：由我们给她悬浮与升降，物品只是**资格凭证**。
- **ATM10 实地扫描（528 个 jar）** 显示给飞行的实现至少六种形态：curio 饰品、护甲套装、
  药水效果、仪式/法术、自带推进的装备（喷气背包）、重力归零型。前四种里除药水效果外，
  对女仆都无效。
- **扫帚那条链路无法直接复用**：它的"浮空"来自**载具**（TLM 的 `EntityBroom` 构造里就
  `setNoGravity(true)`）+ mixin 把载具的 `travel` 整段 cancel。女仆本人不是乘客时，重力照旧。

### ③ 实现（三路探测 + 一阶驱动）

- **探测三路**（`MaidFreeFlightKit`）：①`freeFlightItems` 物品表（双手/护甲/背包/饰品栏/额外容器，
  支持 `#标签`）②`freeFlightEffects` 效果表 ③`freeFlightGravity`：她的**重力属性 ≈ 0 就算资格**
  ——通用启发式，覆盖任何"重力归零"型来源，不需要点名模组；
- **驱动**（`MaidFreeFlightController`，事件驱动）：`setNoGravity(true)` + 每 tick 直接给速度。
  转向**照抄扫帚模式那套已实测的一阶公式**（`speed = min(1, dist/1.5)`、分量限速 0.35/0.22、
  `dist < 0.35` 零速悬停）——实测六百五十五 的教训：接管 travel 后没有 0.91 阻力，
  增量式推进就是无阻尼谐振子（表现为空中上下摆动），一阶系统才不振荡；
- **目标点四档**：调试坐标 → 主人身后的跟随圈（3.5 格 / +2 格高，同扫帚）→ 原地悬停；
- **抢移动**：她**不是乘客**，TLM 的寻路仍在跑 → 每 tick 清 `WALK_TARGET` + 停导航；
- **安全**：收工若在半空先**软着陆**（保持无重力、恒速 0.12 格/tick 下降、每 tick 清零坠落距离），
  落地才交还重力；异常也保持悬停（绝不因为一次异常摔死 20 血的她）；
- **命令**：`/maid_smart freeflight_goto <x> <y> <z> [女仆]`；
- 默认**关**（与飞行跟随/扫帚同档：属于观赏/可选的玩法增强）。

### ④ 实测

| 场景 | 结果 |
| --- | --- |
| 半空召唤（y=−40）+ 资格物品 | **稳稳悬停 12 秒不动**（y 始终 −40.0） |
| 命令飞向 (20, −30, 20) | 从 (0.5, −40, 0.5) 平滑飞到 (19.7, −32, 19.7)，到点自动软着陆 |
| 软着陆 | 恒速 0.12 格/tick（2.4 格/秒）——第一版漏了"软着陆期间保持无重力"，
  重力一回来 4 秒掉了 23 格（等于没做缓冲），已修 |

### ⑤ 两条工程记录（写下来给后来人）

1. **core 行为被实例化了却从不被咨询**：最初挂在 `getCoreBehaviors()`（priority 40，与其它
   附加行为同一条注册链），现场诊断显示构造函数**被调用**（大脑构建时确实取用了它），
   但它的 `checkExtraStartConditions` **一次都没有被调用**——而同一条链上 235 的重锤门
   却能正常被咨询（TLM 自己的 core 列表里也没有 40 这个号，排除撞号）。
   本版改用 `MaidTickEvent` 事件驱动绕过，原因待作者确认；
2. **`pkill -f` 会连自己的 shell 一起杀**：无头调试时 `pkill -f "…unix_args.txt"` 的匹配串
   同时命中了包装它的 bash 命令行，导致后续命令根本没执行（表现为"改了 jar 却没生效"）。
   改用 `fuser -k <world>/session.lock` 精确终止持锁进程。
